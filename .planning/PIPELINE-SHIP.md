# Ship Pipeline — 2026-04-14

## PR
- **Branch:** feat/v1.1-cohort-readiness-pr
- **Base:** main (origin/main @ `5cddbbf`)
- **Commits ahead:** 2 (single feature commit + Codex fix commit)
  - `d6a9114` feat(v1.1): Cohort Readiness System
  - `21187f9` fix: codex P1/P2 findings — idempotent migration + feature-gate PIN auth
- **Create PR at:** https://github.com/JesterCharles/mock-interview-assist/pull/new/feat/v1.1-cohort-readiness-pr
- **Codex review:** GATE FAIL (initial) → 2xP1 + 3xP2 addressed in `21187f9`

## Merge Gate: PENDING (human creates PR + approves merge)

### Summary of Codex resolutions

**P1** Baseline migration unsafe on existing DBs → `prisma/migrations/0000_baseline/migration.sql` now uses `CREATE TABLE IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`, and DO-block guarded FK adds trapping `duplicate_object`. `prisma migrate deploy` now no-ops cleanly over production DB state.

**P1** PIN limiter trusted client fingerprint → Feature-gated entire PIN auth surface behind `ENABLE_ASSOCIATE_AUTH` (default `false`, vitest sets `true`). All PIN routes return 404 when disabled. UI CTAs hidden. Defense-in-depth: limiter now keyed on composite server-IP + fingerprint with a separate IP-only bucket; `NLM_TRUSTED_PROXY=true` opts-in to `x-forwarded-for` parsing.

**P2** `Session.mode` not persisted → `persistSessionToDb` takes `options.mode`; authenticated associate + anonymous public completion routes pass `mode: 'automated'`.

**P2** Readiness sweep false success → `runReadinessPipeline` now returns `boolean`; sweep only closes sibling markers on `true`.

**P2** Setup wizard filter compounding → Separated `allTechs` (source) from `availableTechs` (visible); filter always reads from `allTechs`; `clearCurriculumFilter` restores the full list when associate unassigned.

## Deploy: PENDING (awaits PR merge)

## Canary: PENDING

## Documentation: Will run post-deploy via `/document-release` + `/gsd-docs-update`

## Milestone
- Summary: `.planning/reports/` (pending — will be generated by `/gsd-milestone-summary`)
- Audit: pending (`/gsd-audit-milestone`)
- Status: PENDING archive

## Ship stage blockers remaining

1. **Create PR manually** (no `gh` CLI installed) at the URL above
2. Paste PR body (see below) and submit
3. Review findings already addressed — ready for merge once CI green
4. Merge → post-merge: docs sync + milestone summary + archive

## PR Body

```markdown
## Summary

Ships v1.1 Cohort Readiness System: 8 phases, 22 plans. Cohort-first readiness
engine, curriculum-gated setup wizard, unified DESIGN.md system across every
route, single role-aware Navbar, unified /signin with Trainer | Associate
tabs, dark mode, PIN-only associate login (feature-gated until v1.2).

## Phases

- Phase 8: Schema migration (Cohort, CurriculumWeek, Session.mode)
- Phase 9: Associate PIN auth (dedicated ASSOCIATE_SESSION_SECRET)
- Phase 10: Automated interview pipeline (split endpoints, sweep)
- Phase 11: Cohort management
- Phase 12: Cohort dashboard views
- Phase 13: Curriculum schedule
- Phase 14: Design cohesion (new-route restyle, PublicShell, ReadinessSignal)
- Phase 15: Design cohesion sweep (legacy deletion, unified Navbar, /signin)

## Codex review (GATE PASS after fixes in `21187f9`)

P1 × 2 and P2 × 3 findings from codex structured review addressed:
- Baseline migration now idempotent for existing prod DBs
- PIN auth feature-gated behind ENABLE_ASSOCIATE_AUTH until v1.2
- Session.mode persisted as 'automated' for automated entries
- Readiness sweep respects explicit pipeline success signal
- Setup wizard filter no longer compounds

## Health

- typecheck: clean
- 239/239 vitest pass, 4 skipped
- next build: 35 routes
- Phase 15 Playwright: 24/24 green

## Feature flags

Production defaults:
- ENABLE_ASSOCIATE_AUTH=false (PIN auth hidden; enable in v1.2)
- NLM_TRUSTED_PROXY (optional; set 'true' behind edge LB for IP-keyed limiter)

## Test plan

- [x] Trainer signs in at /signin, lands on /trainer dashboard
- [x] Setup wizard at /interview/new, completes interview → /review → PDF
- [x] Anonymous / public automated flow runs end-to-end (rate-limited)
- [x] Dark mode toggle persists across all migrated routes
- [x] Legacy redirects intact: /dashboard → /interview/new, /login → /signin
- [x] PIN surfaces hidden when ENABLE_ASSOCIATE_AUTH=false

🤖 Generated with [Claude Code](https://claude.com/claude-code)
```
