---
phase: 14-design-cohesion
plan: 01
type: execute
wave: 1
depends_on: ["09", "10"]
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/layout/PublicShell.tsx
  - src/app/login/page.tsx
  - src/app/associate/[slug]/page.tsx
  - src/app/associate/login/page.tsx
  - src/app/associate/[slug]/interview/page.tsx
  - src/components/readiness/ReadinessSignal.tsx
  - tests/visual/phase-14/public-flow.spec.ts
autonomous: false
requirements: [DESIGN-01]
must_haves:
  truths:
    - "Page bg is warm parchment #F5F0E8 on NEW routes: trainer login, /associate/login, /associate/[slug], /associate/[slug]/interview"
    - "Headings on new routes render in Clash Display, body in DM Sans"
    - "Primary CTA on new routes uses flat burnt orange #C85A2E (via new .btn-accent-flat), not gradient"
    - "Associate profile readiness shown as '**82** ascending' typography pattern"
    - "LEGACY UTILITIES PRESERVED — .nlm-bg, .glass-card, .gradient-text, .gradient-text-static, glow classes, and motion keyframes remain in globals.css (Codex finding #8)"
    - "/interview and /review pages visually UNCHANGED — still consume legacy utilities"
    - "/ (anonymous automated-interview root) visually UNCHANGED — still consumes legacy utilities"
    - "No glass morphism, gradient backgrounds, or glow effects appear on NEW routes (grep scoped to new paths only)"
  artifacts:
    - path: src/app/globals.css
      provides: "DESIGN.md tokens ADDED alongside legacy --nlm-* tokens and utilities (both coexist)"
      contains: "--bg: #F5F0E8"
    - path: src/app/layout.tsx
      provides: "Clash Display + DM Sans font loading (additive)"
      contains: "Clash_Display|clash-display"
    - path: src/components/layout/PublicShell.tsx
      provides: "Shared shell for new public + associate + login pages"
      min_lines: 30
    - path: src/components/readiness/ReadinessSignal.tsx
      provides: "Typography readiness pattern per DESIGN.md"
      min_lines: 20
    - path: src/app/associate/[slug]/interview/page.tsx
      provides: "Authenticated automated-interview entry (Phase 9 D-26) styled with DESIGN.md tokens"
  key_links:
    - from: src/app/associate/[slug]/page.tsx
      to: src/components/readiness/ReadinessSignal.tsx
      via: "import + render"
      pattern: "ReadinessSignal"
    - from: src/app/associate/login/page.tsx
      to: src/components/layout/PublicShell.tsx
      via: "wrapping layout"
      pattern: "PublicShell"
    - from: src/app/login/page.tsx
      to: src/components/layout/PublicShell.tsx
      via: "wrapping layout"
      pattern: "PublicShell"
    - from: src/app/associate/[slug]/interview/page.tsx
      to: src/components/layout/PublicShell.tsx
      via: "wrapping layout (page shell only — interview UI component itself unchanged)"
      pattern: "PublicShell"
---

<objective>
Restyle NEW routes (trainer login, PIN entry, /associate/[slug], /associate/[slug]/interview) to DESIGN.md tokens. ADD DESIGN.md tokens to `globals.css` ALONGSIDE existing legacy `--nlm-*` tokens and utilities (Codex finding #8). Extract shared `PublicShell`. Implement typographic readiness signal on `/associate/[slug]`.

Purpose: Satisfy DESIGN-01 for NEW surfaces without breaking legacy pages. `/`, `/interview`, and `/review` remain on legacy utilities — those will be migrated (and utilities deleted) in a future milestone.
Output: NEW routes DESIGN.md-compliant; `/`, `/interview`, `/review` visually untouched; legacy utilities preserved in globals.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-design-cohesion/14-CONTEXT.md
@DESIGN.md
@src/app/globals.css
@src/app/layout.tsx
@src/app/associate/[slug]/page.tsx
@src/app/login/page.tsx
@src/app/page.tsx
@src/app/interview/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADD DESIGN.md tokens + new button classes to globals.css; preserve legacy utilities; load fonts additively</name>
  <files>src/app/globals.css, src/app/layout.tsx</files>
  <read_first>DESIGN.md, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, src/app/interview/page.tsx</read_first>
  <action>
    Per D-01/D-02/D-02a/D-03 (REVISED per Codex finding #8):

    1. In `src/app/globals.css`:
       - **KEEP** the existing `--nlm-*` token block and all legacy utilities (`.glass-card`, `.glass-card-strong`, `.glow-border`, `.glow-border-cyan`, `.gradient-text`, `.gradient-text-static`, `.nlm-bg`, `.progress-gradient`, `.recording-ring`) and all motion keyframes (`gradient-shift`, `float`, `pulse-glow`, `shimmer`, `recording-pulse`, `progress-glow`, `border-glow-pulse`, `slide-up`, `fade-in`) exactly as they are. DO NOT delete or rewrite them.
       - KEEP existing `.btn-primary` and `.btn-accent` definitions untouched (legacy pages depend on them).
       - **ADD** a new `:root` block (or merge into existing) with DESIGN.md tokens: `--bg: #F5F0E8; --surface: #FFFFFF; --surface-muted: #F0EBE2; --ink: #1A1A1A; --muted: #7A7267; --accent: #C85A2E; --accent-hover: #B04E27; --success: #2D6A4F; --warning: #B7791F; --danger: #B83B2E; --border: #DDD5C8; --border-subtle: #E8E2D9; --highlight: #FFF8F0;`
       - ADD `[data-theme="dark"]` block with DESIGN.md dark tokens (not wired).
       - EXTEND `@theme inline`: add `--color-bg`, `--color-surface`, `--color-ink`, `--color-accent`, `--color-muted`, `--color-border` mapped to the new CSS vars. Add `--font-display: var(--font-clash-display)`. ADD (do not replace) `--font-sans-new: var(--font-dm-sans)` if needed to avoid clobbering the legacy `--font-sans`; if the legacy `--font-sans` is only consumed by legacy pages, you may overwrite it, but VERIFY via grep first — if in doubt, add a distinct new var.
       - Do NOT change `body { ... }` if it currently uses legacy tokens — the `/`, `/interview`, `/review` pages depend on that body background. Instead, PublicShell explicitly sets `bg-[var(--bg)]` on its root div so new routes get the warm parchment bg without changing the global body.
       - **ADD NEW BUTTON CLASSES** (do not overwrite legacy):
         ```css
         .btn-accent-flat {
           background: var(--accent); color: white; font-weight: 600;
           border-radius: 0.5rem; padding: 0.625rem 1.25rem;
           transition: background 120ms;
         }
         .btn-accent-flat:hover { background: var(--accent-hover); }
         .btn-secondary-flat {
           background: var(--surface); color: var(--ink);
           border: 1px solid var(--border); font-weight: 600;
           border-radius: 0.5rem; padding: 0.625rem 1.25rem;
         }
         ```
       - Do NOT delete keyframes, do NOT delete utilities, do NOT rename `--nlm-*` tokens.

    2. In `src/app/layout.tsx`:
       - Import `DM_Sans` and `JetBrains_Mono` from `next/font/google` with subsets=["latin"] and CSS variables `--font-dm-sans` and `--font-jetbrains-mono`
       - Add Clash Display via `<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/clash-display@latest/index.css" />` in `<head>`
       - Set `--font-clash-display: 'Clash Display', 'DM Sans', system-ui`
       - APPLY the new font CSS variables via className on `<html>` or `<body>` ADDITIVELY — if Inter is currently loaded and used by legacy pages, keep its className too. Legacy pages with explicit font-family rules won't be affected.

    VERIFICATION GREP (scope-limited, do NOT scan legacy dirs):
    - `grep -c "glass-card" src/app/globals.css` → MUST be > 0 (legacy preserved)
    - `grep -c "nlm-bg" src/app/globals.css` → MUST be > 0 (legacy preserved)
    - `grep -c "F5F0E8" src/app/globals.css` → MUST be >= 1 (new token added)
    - `grep -c "btn-accent-flat" src/app/globals.css` → MUST be >= 1 (new class added)
  </action>
  <verify>npx tsc --noEmit && npm run lint && grep -q "glass-card" src/app/globals.css && grep -q "F5F0E8" src/app/globals.css && grep -q "btn-accent-flat" src/app/globals.css</verify>
  <acceptance_criteria>
    - globals.css contains BOTH `--nlm-` legacy tokens AND `--bg: #F5F0E8` new token
    - globals.css still contains `glass-card`, `gradient-text`, `nlm-bg`, and motion keyframes (legacy preserved)
    - globals.css contains new `.btn-accent-flat` and `.btn-secondary-flat` classes
    - layout.tsx imports DM_Sans and references clash-display (CDN link or variable)
    - Legacy `.btn-primary` / `.btn-accent` classes are unchanged
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>New tokens added, legacy preserved, fonts loading, no TS or lint errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build PublicShell + ReadinessSignal components</name>
  <files>src/components/layout/PublicShell.tsx, src/components/readiness/ReadinessSignal.tsx</files>
  <read_first>DESIGN.md, src/app/globals.css, src/app/associate/[slug]/page.tsx</read_first>
  <action>
    Per D-16 and DESIGN.md "Readiness Signal Pattern":

    1. `src/components/layout/PublicShell.tsx`:
       - Server component
       - Props: `{ children: React.ReactNode; title?: string }`
       - Root: `<div className="bg-[var(--bg)] min-h-screen text-[var(--ink)]">` — explicitly sets the warm parchment bg (body background left untouched for legacy compat)
       - Header: logo wordmark "Next Level Mock" in Clash Display 600 at 22px, centered max-width 1120px, `py-6 border-b border-[var(--border-subtle)]`
       - Main: `<main className="max-w-[1120px] mx-auto px-6 py-12">{children}</main>`
       - Footer: muted metadata `text-[var(--muted)] text-[12px]`
       - No nav, no theme toggle

    2. `src/components/readiness/ReadinessSignal.tsx`:
       - Props: `{ score: number; trend: "ascending" | "climbing" | "stalling"; size?: "lg" | "md" }`
       - Score: Clash Display 700 at 64px (lg) or 48px (md), `font-variant-numeric: tabular-nums`
       - Trend: DM Sans 600 at 11px, lowercase, tracking-wider (0.08em), color: `--success` ascending, `--accent` climbing, `--danger` stalling
       - Pure typography — no badge/pill/rounded bg

    Export both as default + named.
  </action>
  <verify>npx tsc --noEmit && grep -c "PublicShell" src/components/layout/PublicShell.tsx && grep -c "tabular-nums" src/components/readiness/ReadinessSignal.tsx</verify>
  <acceptance_criteria>
    - PublicShell contains `max-w-[1120px]` and `bg-[var(--bg)]`
    - ReadinessSignal contains `tabular-nums` and all three trend colors (`--success`, `--accent`, `--danger`)
    - Neither file contains `gradient-to`, `backdrop-blur`, `glass-card`, or hex colors outside DESIGN.md token set
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Shared shell and readiness components ready.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle NEW routes (trainer login, /associate/login, /associate/[slug], /associate/[slug]/interview)</name>
  <files>src/app/login/page.tsx, src/app/associate/[slug]/page.tsx, src/app/associate/login/page.tsx, src/app/associate/[slug]/interview/page.tsx</files>
  <read_first>src/app/login/page.tsx, src/app/associate/[slug]/page.tsx, DESIGN.md, src/components/layout/PublicShell.tsx, src/components/readiness/ReadinessSignal.tsx</read_first>
  <action>
    Per D-04/D-05/D-06a/D-07/D-11/D-18/D-19 (REVISED per Codex #8):

    1. `src/app/login/page.tsx`: wrap in `<PublicShell title="Trainer Sign In">`. Card: `bg-[var(--surface)] border border-[var(--border)] rounded-xl p-8 max-w-md mx-auto`. Heading "Sign in" Clash Display 28px. Input: `bg-[var(--bg)] border border-[var(--border)] rounded-lg px-4 py-3 focus:outline-none focus:border-[var(--accent)]`. Submit: `className="btn-accent-flat"` (NEW class — NOT legacy `.btn-primary`). Error: `text-[var(--danger)]`.

    2. `src/app/associate/login/page.tsx` (created by Phase 9 — restyle here): wrap in `<PublicShell>`. Heading: "Enter your PIN" Clash Display 48px. Subheading: DM Sans 18px `text-[var(--muted)]`. 6-digit input: `text-[32px] tracking-[0.4em] tabular-nums text-center font-mono w-48`. Submit: `btn-accent-flat`. Error: `text-[var(--danger)]`.

    3. `src/app/associate/[slug]/page.tsx`: wrap in `<PublicShell title="{displayName}">`. Replace readiness badge with `<ReadinessSignal score={...} trend={...} size="lg" />`. Session history list: `bg-[var(--surface)] border border-[var(--border-subtle)] rounded-xl divide-y divide-[var(--border-subtle)]`. Remove any `--nlm-*` / `gradient-text` / `glass-card` references WITHIN THIS FILE (they remain defined in globals.css for legacy pages). Keep server-rendered data fetching logic unchanged.

    4. `src/app/associate/[slug]/interview/page.tsx` (created by Phase 9 D-26 — restyle here): wrap in `<PublicShell title="Automated Interview">`. This page is the SHELL; the interview UI client component itself is shared with `/` and may still use legacy classes internally — that is acceptable because only the shell/chrome changes. The shell applies the warm parchment bg + header/footer via PublicShell; inner interview UI renders inside `<main>` as the children. Add a subtle "identity" tag at the top: `text-[var(--muted)] text-[11px] uppercase tracking-wider` reading "Signed in as {associateName}" — confirms to the user that identity is carried server-side (per Codex finding #2 / #3 intent).

    SCOPED hard constraints (D-18, D-19 revised):
    - `grep -rE "#[0-9A-Fa-f]{6}" src/app/login src/app/associate/login src/app/associate/[slug]/page.tsx src/app/associate/[slug]/interview/page.tsx` → zero
    - `grep -rE "glass-card|gradient-text|backdrop-blur|nlm-" src/app/login src/app/associate/login src/app/associate/[slug]/page.tsx src/app/associate/[slug]/interview/page.tsx` → zero
    - DO NOT scan `src/app/page.tsx`, `src/app/interview`, `src/app/review` — those legitimately use legacy classes (preserved).
  </action>
  <verify>npx tsc --noEmit && npm run lint && npm run build</verify>
  <acceptance_criteria>
    - All four new-route files import and use `PublicShell`
    - `src/app/associate/[slug]/page.tsx` imports and renders `ReadinessSignal`
    - Scoped grep for hex colors + legacy class references in NEW paths returns zero
    - Legacy pages (`/interview`, `/review`, `/`) still load and are NOT modified
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>New routes on DESIGN.md tokens; legacy pages untouched; legacy utilities still defined and consumed by legacy pages.</done>
</task>

<task type="auto">
  <name>Task 4: Playwright visual smoke tests + regression sanity on legacy pages + start dev server</name>
  <files>tests/visual/phase-14/public-flow.spec.ts</files>
  <read_first>playwright.config.ts, DESIGN.md</read_first>
  <action>
    Per D-20, D-21:

    1. Create `tests/visual/phase-14/public-flow.spec.ts`:
       - `trainer login renders DESIGN.md tokens`: `/login` — assert computed background-color of the PublicShell root resolves to `rgb(245, 240, 232)`; assert `.btn-accent-flat` background resolves to `rgb(200, 90, 46)`
       - `PIN entry renders`: `/associate/login` — heading contains "Enter your PIN", input has `inputmode="numeric"`
       - `associate profile renders readiness signal`: navigate to a seeded slug (or skip with TODO)
       - `authenticated interview entry renders`: `/associate/{slug}/interview` — asserts PublicShell bg applied, identity tag visible
       - **LEGACY REGRESSION** (REQUIRED per Codex #8): navigate to `/` and `/interview`; assert their root elements still have the legacy `.nlm-bg` class applied AND that no console errors fire. This proves legacy preservation.
       - Screenshots committed under `tests/visual/phase-14/screenshots/`

    2. Start dev server in background; wait for ready at `/login`.

    Advisory only — do not gate CI.
  </action>
  <verify>fetch http://localhost:3000/login returns 200 && npx playwright test tests/visual/phase-14 --reporter=list</verify>
  <acceptance_criteria>
    - `tests/visual/phase-14/public-flow.spec.ts` exists with at least 4 test blocks (new routes) PLUS a legacy regression block
    - Dev server responds 200 at `/login`, `/associate/login`, `/`, `/interview`
    - Legacy regression test passes (nlm-bg still applied on `/` and `/interview`)
    - At least one screenshot committed
  </acceptance_criteria>
  <done>Visual smoke tests exist including legacy regression; dev server running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>NEW public flow restyle complete (token-driven); legacy pages preserved — dev server at http://localhost:3000</what-built>
  <how-to-verify>
    1. http://localhost:3000/login
       - Warm parchment bg, Clash Display heading, DM Sans body, flat burnt orange submit
    2. http://localhost:3000/associate/login
       - Warm parchment, PIN entry in JetBrains Mono
    3. http://localhost:3000/associate/[any-existing-slug]
       - PublicShell header, readiness as "**82** ascending" typography
    4. http://localhost:3000/associate/[any-existing-slug]/interview
       - PublicShell chrome around the interview UI; identity tag "Signed in as {name}"
    5. REGRESSION CHECK (CRITICAL per Codex #8):
       - http://localhost:3000/ — anonymous automated-interview root MUST render with its pre-existing look (nlm-bg + glass-card + gradient text etc. still visible). NO visual change.
       - http://localhost:3000/interview — trainer-led interview mid-session surface UNCHANGED.
       - http://localhost:3000/review — UNCHANGED.
       - If any of /, /interview, /review look broken, that is a blocker — investigate whether legacy utilities were accidentally removed from globals.css.

    Confirm all five before approval.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run lint` passes
- [ ] `npm run build` succeeds
- [ ] `grep -rE "glass-card|gradient-text|nlm-|backdrop-blur" src/app/login src/app/associate/login src/app/associate/[slug]/page.tsx src/app/associate/[slug]/interview/page.tsx src/components/layout src/components/readiness` returns zero (scoped to NEW paths)
- [ ] `grep -c "glass-card" src/app/globals.css` > 0 (legacy preserved)
- [ ] `grep -c "F5F0E8" src/app/globals.css` >= 1 (new token added)
- [ ] `/`, `/interview`, `/review` render correctly with legacy utilities (human verified)
- [ ] Playwright tests pass or skip cleanly
</verification>

<success_criteria>
- All tasks completed
- DESIGN-01 satisfied for NEW public/associate/auth routes
- Legacy `/`, `/interview`, `/review` visually unchanged (Codex finding #8)
- Legacy utilities and tokens preserved in globals.css
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/14-design-cohesion/14-01-SUMMARY.md`
</output>
