---
phase: 14-design-cohesion
plan: 01
type: execute
wave: 1
depends_on: ["09", "10"]
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/layout/PublicShell.tsx
  - src/app/login/page.tsx
  - src/app/associate/[slug]/page.tsx
  - src/app/associate/login/page.tsx
  - src/components/readiness/ReadinessSignal.tsx
  - tests/visual/phase-14/public-flow.spec.ts
autonomous: false
requirements: [DESIGN-01]
must_haves:
  truths:
    - "Page bg is warm parchment #F5F0E8 on public landing, PIN entry, associate profile, trainer login"
    - "Headings render in Clash Display, body text in DM Sans"
    - "Primary CTA uses burnt orange #C85A2E, not indigo/cyan gradient"
    - "Associate profile readiness shown as '**82** ascending' typography pattern, not traffic-light badge"
    - "/interview and /review pages visually unchanged (no token regression on mid-session flow)"
    - "No glass morphism, gradient backgrounds, or glow effects appear on restyled pages"
  artifacts:
    - path: src/app/globals.css
      provides: "DESIGN.md tokens (--bg, --surface, --ink, --accent) replacing --nlm-* tokens"
      contains: "--bg: #F5F0E8"
    - path: src/app/layout.tsx
      provides: "Clash Display + DM Sans font loading"
      contains: "Clash_Display|clash-display"
    - path: src/components/layout/PublicShell.tsx
      provides: "Shared shell for public + associate + login pages"
      min_lines: 30
    - path: src/components/readiness/ReadinessSignal.tsx
      provides: "Typography readiness pattern per DESIGN.md"
      min_lines: 20
  key_links:
    - from: src/app/associate/[slug]/page.tsx
      to: src/components/readiness/ReadinessSignal.tsx
      via: "import + render"
      pattern: "ReadinessSignal"
    - from: src/app/associate/login/page.tsx
      to: src/components/layout/PublicShell.tsx
      via: "wrapping layout"
      pattern: "PublicShell"
    - from: src/app/login/page.tsx
      to: src/components/layout/PublicShell.tsx
      via: "wrapping layout"
      pattern: "PublicShell"
---

<objective>
Restyle the public interview flow, PIN entry, trainer login, and associate profile page to DESIGN.md tokens. Replace the indigo/cyan dark theme in `globals.css` with warm parchment + burnt orange tokens. Extract a shared `PublicShell` component. Implement the typographic readiness signal pattern on `/associate/[slug]`.

Purpose: Satisfy DESIGN-01. Kill visual debt from the v1.0 indigo theme before v1.1 features compound it.
Output: DESIGN.md-compliant public flow; `/interview` and `/review` visually untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-design-cohesion/14-CONTEXT.md
@DESIGN.md
@src/app/globals.css
@src/app/layout.tsx
@src/app/associate/[slug]/page.tsx
@src/app/login/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap globals.css tokens + load DESIGN.md fonts</name>
  <files>src/app/globals.css, src/app/layout.tsx</files>
  <read_first>DESIGN.md, src/app/globals.css, src/app/layout.tsx</read_first>
  <action>
    Per D-01, D-02, D-03:

    1. In `src/app/globals.css`:
       - Replace entire `:root` block with DESIGN.md light-mode tokens. Use these exact names: `--bg: #F5F0E8; --surface: #FFFFFF; --surface-muted: #F0EBE2; --ink: #1A1A1A; --muted: #7A7267; --accent: #C85A2E; --accent-hover: #B04E27; --success: #2D6A4F; --warning: #B7791F; --danger: #B83B2E; --border: #DDD5C8; --border-subtle: #E8E2D9; --highlight: #FFF8F0;`
       - Add `[data-theme="dark"]` block with DESIGN.md dark tokens (defined but not wired to toggle)
       - Update `@theme inline`: map `--color-bg`, `--color-surface`, `--color-ink`, `--color-accent`, `--color-muted`, `--color-border` to the CSS vars. Add `--font-display: var(--font-clash-display)` and update `--font-sans: var(--font-dm-sans)`.
       - Update `body { background: var(--bg); color: var(--ink); font-family: var(--font-dm-sans); }`
       - Delete keyframes: `gradient-shift`, `float`, `pulse-glow`, `shimmer`, `recording-pulse`, `progress-glow`, `border-glow-pulse`. Keep `slide-up` and `fade-in` only.
       - Delete utility classes: `.glass-card`, `.glass-card-strong`, `.glow-border`, `.glow-border-cyan`, `.gradient-text`, `.gradient-text-static`, `.nlm-bg`, `.progress-gradient`, `.recording-ring`. Rewrite `.btn-primary` as flat burnt orange: `background: var(--accent); color: white; font-weight: 600; border-radius: 0.5rem; padding: 0.625rem 1.25rem;` with `:hover { background: var(--accent-hover); }` — no gradient, no glow.
       - Rewrite `.btn-accent` as flat `var(--surface)` with `--border` 1px outline (secondary variant).
       - Replace scrollbar colors from indigo to `var(--border)` / `var(--muted)`.

    2. In `src/app/layout.tsx`:
       - Import `DM_Sans` and `JetBrains_Mono` from `next/font/google` with subsets=["latin"] and CSS variables `--font-dm-sans` and `--font-jetbrains-mono`
       - Add Clash Display via `<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/clash-display@latest/index.css" />` in `<head>` (or use the official `@fontsource-variable/clash-display` if installable via npm — prefer the CDN per DESIGN.md §Typography)
       - Set CSS variable `--font-clash-display: 'Clash Display', 'DM Sans', system-ui`
       - Remove any Inter / Geist font imports
       - Apply `className={\`\${dmSans.variable} \${jetbrainsMono.variable}\`}` to `<html>` or `<body>`

    Do NOT keep any `--nlm-*` tokens. Do NOT leave deprecated utility classes with empty bodies — delete them.
  </action>
  <verify>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; grep -c "nlm-" src/app/globals.css (expect 0) &amp;&amp; grep -c "F5F0E8" src/app/globals.css (expect &gt;=1)</verify>
  <acceptance_criteria>
    - `src/app/globals.css` contains `--bg: #F5F0E8` and `--accent: #C85A2E` exactly
    - `src/app/globals.css` contains zero occurrences of `--nlm-` and zero occurrences of `glass-card` / `gradient-text` / `glow-border`
    - `src/app/layout.tsx` imports `DM_Sans` from `next/font/google`
    - `src/app/layout.tsx` references `clash-display` (CDN link or variable)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Token swap complete, fonts loading, no TS or lint errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build PublicShell + ReadinessSignal components</name>
  <files>src/components/layout/PublicShell.tsx, src/components/readiness/ReadinessSignal.tsx</files>
  <read_first>DESIGN.md, src/app/globals.css, src/app/associate/[slug]/page.tsx</read_first>
  <action>
    Per D-16 and DESIGN.md "Readiness Signal Pattern":

    1. Create `src/components/layout/PublicShell.tsx`:
       - Server component by default
       - Props: `{ children: React.ReactNode; title?: string }`
       - Renders: `<div>` with `bg-[var(--bg)] min-h-screen text-[var(--ink)]`; a header with logo wordmark "Next Level Mock" in Clash Display 600 at 22px, centered max-width 1120px, `py-6 border-b border-[var(--border-subtle)]`; `<main className="max-w-[1120px] mx-auto px-6 py-12">{children}</main>`; a footer with muted metadata `text-[var(--muted)] text-[12px]`.
       - No nav links (public surface). No theme toggle.

    2. Create `src/components/readiness/ReadinessSignal.tsx`:
       - Client component not required — server-compatible
       - Props: `{ score: number; trend: "ascending" | "climbing" | "stalling"; size?: "lg" | "md" }`
       - Renders: score in Clash Display 700 at 64px (lg) or 48px (md) with `font-variant-numeric: tabular-nums`; trend word below in DM Sans 600 at 11px, lowercase, `tracking-wider` (0.08em), color: `--success` for ascending, `--accent` for climbing, `--danger` for stalling.
       - Do NOT wrap in a badge / pill / rounded background. Pure typography per DESIGN.md.

    Export both as default + named.
  </action>
  <verify>npx tsc --noEmit &amp;&amp; grep -c "PublicShell" src/components/layout/PublicShell.tsx &amp;&amp; grep -c "tabular-nums" src/components/readiness/ReadinessSignal.tsx</verify>
  <acceptance_criteria>
    - `src/components/layout/PublicShell.tsx` exists, contains `max-w-[1120px]` and `bg-[var(--bg)]`
    - `src/components/readiness/ReadinessSignal.tsx` contains `tabular-nums` and all three trend colors (`--success`, `--accent`, `--danger`)
    - Neither file contains `gradient-to`, `backdrop-blur`, `glass-card`, or hex colors outside DESIGN.md token set
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Shared shell and readiness components ready for consumption.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle login, associate profile, and PIN entry pages</name>
  <files>src/app/login/page.tsx, src/app/associate/[slug]/page.tsx, src/app/associate/login/page.tsx</files>
  <read_first>src/app/login/page.tsx, src/app/associate/[slug]/page.tsx, DESIGN.md, src/components/layout/PublicShell.tsx, src/components/readiness/ReadinessSignal.tsx</read_first>
  <action>
    Per D-04 through D-11, D-18, D-19:

    1. `src/app/login/page.tsx` (trainer login):
       - Wrap in `<PublicShell title="Trainer Sign In">`
       - Center a single card: `bg-[var(--surface)] border border-[var(--border)] rounded-xl p-8 max-w-md mx-auto`
       - Heading: "Sign in" in Clash Display 28px
       - Password input: `w-full bg-[var(--bg)] border border-[var(--border)] rounded-lg px-4 py-3 focus:outline-none focus:border-[var(--accent)]`
       - Submit button: `.btn-primary` utility (already flat burnt orange from Task 1)
       - Error state: `text-[var(--danger)] text-sm`
       - Remove any indigo/cyan/gradient references. No `glass-card`.

    2. `src/app/associate/login/page.tsx` (PIN entry — CREATE if Phase 9 has not yet created it; otherwise restyle):
       - If file does not exist, create a minimal scaffold that POSTs `{pin}` to `/api/associate/login` (Phase 9 endpoint — use the actual endpoint if it exists; else use placeholder + TODO comment linking to Phase 9)
       - Wrap in `<PublicShell>`
       - Heading: "Enter your PIN" Clash Display 48px
       - Subheading: "Your trainer provided a 6-digit code" DM Sans 18px `text-[var(--muted)]`
       - 6-digit input: single `<input inputMode="numeric" maxLength={6} pattern="[0-9]*">` with `text-[32px] tracking-[0.4em] tabular-nums text-center font-mono` (JetBrains Mono); width `w-48`
       - Submit: `.btn-primary`
       - Error: `text-[var(--danger)]`

    3. `src/app/associate/[slug]/page.tsx` (associate profile):
       - Wrap in `<PublicShell title="{displayName}">`
       - Replace any readiness badge/traffic-light display with `<ReadinessSignal score={...} trend={...} size="lg" />` per D-07 and DESIGN.md Readiness Signal Pattern
       - Session history list: `bg-[var(--surface)] border border-[var(--border-subtle)] rounded-xl divide-y divide-[var(--border-subtle)]` with each row `px-5 py-4`
       - Session row typography: date + overall score in DM Sans 14px tabular-nums; skill name in Clash Display 22px
       - Remove every `--nlm-*` reference, every `gradient-text`, every `glass-card`, every `backdrop-blur`, every arbitrary `#xxxxxx` color
       - Keep server-rendered data fetching logic unchanged — only touch JSX + classes

    Hard constraint (D-18, D-19): After edits, `grep -rE "#[0-9A-Fa-f]{6}" src/app/login src/app/associate` returns zero lines (all colors via tokens). `grep -rE "glass-card|gradient-text|backdrop-blur|nlm-" src/app/login src/app/associate` returns zero.
  </action>
  <verify>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build</verify>
  <acceptance_criteria>
    - All three files import and use `PublicShell`
    - `src/app/associate/[slug]/page.tsx` imports and renders `ReadinessSignal`
    - `grep -rE "#[0-9A-Fa-f]{6}" src/app/login src/app/associate` returns zero matches
    - `grep -rE "glass-card|gradient-text|backdrop-blur|nlm-|indigo-|cyan-|purple-500" src/app/login src/app/associate` returns zero matches
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Public flow pages render on warm parchment with Clash Display + DM Sans + burnt orange accents.</done>
</task>

<task type="auto">
  <name>Task 4: Write Playwright visual smoke tests + start dev server</name>
  <files>tests/visual/phase-14/public-flow.spec.ts</files>
  <read_first>playwright.config.ts, DESIGN.md</read_first>
  <action>
    Per D-20, D-21:

    1. Create `tests/visual/phase-14/public-flow.spec.ts` with three tests:
       - `trainer login renders DESIGN.md tokens`: navigates to `/login`, asserts computed `background-color` on `body` resolves to `rgb(245, 240, 232)` (#F5F0E8), asserts the submit button's background-color resolves to `rgb(200, 90, 46)` (#C85A2E)
       - `PIN entry renders`: navigates to `/associate/login`, asserts heading contains "Enter your PIN", asserts input has `inputmode="numeric"`
       - `associate profile renders readiness signal`: navigates to a seeded `/associate/[slug]` (use an existing test slug or skip with `test.skip()` if no seed fixture exists — add TODO)
       - Each test also captures a screenshot via `await page.screenshot({ path: ... })` for manual review, committed as baselines under `tests/visual/phase-14/screenshots/`
    2. Start dev server in background: `npm run dev` (port 3000), wait for ready on `GET http://localhost:3000/login` returning 200.

    Advisory only — do not gate CI. If Playwright is not yet installed, add it as a devDependency and install. Use `@playwright/test`.
  </action>
  <verify>fetch http://localhost:3000/login returns 200 &amp;&amp; npx playwright test tests/visual/phase-14 --reporter=list (expect 2-3 tests pass; associate profile may skip)</verify>
  <acceptance_criteria>
    - `tests/visual/phase-14/public-flow.spec.ts` exists with at least 2 test blocks
    - Dev server responds 200 at `/login` and `/associate/login`
    - At least one screenshot committed under `tests/visual/phase-14/screenshots/`
  </acceptance_criteria>
  <done>Visual smoke tests exist as advisory gate; dev server running for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Public flow restyle complete — dev server running at http://localhost:3000</what-built>
  <how-to-verify>
    Visit each URL and verify against DESIGN.md:

    1. http://localhost:3000/login
       - Background is warm parchment (cream/beige), NOT dark indigo
       - Heading "Sign in" renders in Clash Display (geometric, assertive)
       - Body text in DM Sans
       - Submit button is flat burnt orange (#C85A2E), no gradient, no glow
       - No glass morphism, no blur effects

    2. http://localhost:3000/associate/login
       - Same warm parchment bg
       - "Enter your PIN" heading in Clash Display, large
       - 6-digit input uses monospace (JetBrains Mono), wide letter-spacing, tabular numerals
       - Submit button matches login page (flat burnt orange)

    3. http://localhost:3000/associate/[any-existing-slug]
       - Warm parchment bg, shared header with "Next Level Mock" wordmark
       - Readiness score shows as "**82** ascending" style typography (large Clash Display number + small lowercase trend word), NOT as a colored traffic-light badge
       - Session history list has matte white cards with subtle warm borders
       - Zero purple, zero cyan, zero indigo, zero gradient backgrounds

    4. REGRESSION CHECK: http://localhost:3000/interview and http://localhost:3000/review
       - Should be visually UNCHANGED from before this plan
       - Token swap may affect incidental colors but layout/structure/key UI must be intact
       - If anything looks broken on /interview or /review, report it as a blocker

    Confirm all four checks before approval.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run lint` passes
- [ ] `npm run build` succeeds (standalone build for Docker)
- [ ] `grep -rE "glass-card|gradient-text|nlm-|backdrop-blur" src/app/login src/app/associate src/components/layout src/components/readiness` returns zero
- [ ] `grep -rE "#[0-9A-Fa-f]{6}" src/app/login src/app/associate src/components/layout src/components/readiness` returns zero (outside comments)
- [ ] `/interview` and `/review` pages load without visual regression (human verified)
- [ ] Playwright tests pass or skip cleanly
</verification>

<success_criteria>
- All 5 tasks completed
- DESIGN-01 requirement satisfied (public flow + associate page + auth pages on DESIGN.md tokens)
- Trainer-led interview flow (`/interview`, `/review`) visually unchanged
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/14-design-cohesion/14-01-SUMMARY.md`
</output>
