---
phase: 21-app-shell-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/shell/TopBar.tsx
  - src/components/shell/SectionSidebar.tsx
  - src/components/shell/AvatarMenu.tsx
  - src/components/shell/CohortSwitcher.tsx
  - src/components/shell/MobileSidebar.tsx
  - src/components/shell/sidebar-configs.ts
  - src/components/shell/types.ts
  - src/components/ConditionalNavbar.tsx
  - src/components/ClientLayout.tsx
  - src/components/shell/sidebar-configs.test.ts
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-04

must_haves:
  truths:
    - "Shell component files exist with correct exports"
    - "Sidebar configs define Dashboard and Settings groups with correct hrefs and icons"
    - "ConditionalNavbar suppresses Navbar on /trainer/* routes"
    - "ClientLayout uses ConditionalNavbar instead of Navbar"
    - "Radix deps installed (react-dialog, react-dropdown-menu)"
  artifacts:
    - path: "src/components/shell/TopBar.tsx"
      provides: "Global topbar with nav items, cohort switcher, avatar menu"
      exports: ["TopBar"]
    - path: "src/components/shell/SectionSidebar.tsx"
      provides: "Sidebar renderer from config arrays"
      exports: ["SectionSidebar"]
    - path: "src/components/shell/AvatarMenu.tsx"
      provides: "Radix DropdownMenu avatar with email, role badge, sign out"
      exports: ["AvatarMenu"]
    - path: "src/components/shell/CohortSwitcher.tsx"
      provides: "Radix DropdownMenu cohort picker with URL param + localStorage"
      exports: ["CohortSwitcher"]
    - path: "src/components/shell/MobileSidebar.tsx"
      provides: "Radix Dialog off-canvas sheet for mobile"
      exports: ["MobileSidebar"]
    - path: "src/components/shell/sidebar-configs.ts"
      provides: "Dashboard and Settings sidebar group configs"
      exports: ["dashboardSidebarGroups", "settingsSidebarGroups"]
    - path: "src/components/shell/types.ts"
      provides: "SidebarGroup and SidebarItem interfaces"
      exports: ["SidebarGroup", "SidebarItem"]
    - path: "src/components/ConditionalNavbar.tsx"
      provides: "Navbar wrapper that hides on /trainer/* routes"
      exports: ["ConditionalNavbar"]
  key_links:
    - from: "src/components/shell/TopBar.tsx"
      to: "src/components/shell/CohortSwitcher.tsx"
      via: "import + Suspense wrapper"
      pattern: "Suspense.*CohortSwitcher"
    - from: "src/components/shell/TopBar.tsx"
      to: "src/components/shell/AvatarMenu.tsx"
      via: "import"
      pattern: "import.*AvatarMenu"
    - from: "src/components/ClientLayout.tsx"
      to: "src/components/ConditionalNavbar.tsx"
      via: "import replacement"
      pattern: "ConditionalNavbar"
---

<objective>
Install Radix UI primitives and build all shell components (TopBar, SectionSidebar, AvatarMenu, CohortSwitcher, MobileSidebar) + sidebar configs + ConditionalNavbar. Wire ConditionalNavbar into ClientLayout to suppress Navbar on trainer routes.

Purpose: Create the complete component library for the two-level nav shell before route reorganization.
Output: All shell components at `src/components/shell/*`, ConditionalNavbar, updated ClientLayout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-app-shell-redesign/21-CONTEXT.md
@.planning/phases/21-app-shell-redesign/21-RESEARCH.md
@.planning/phases/21-app-shell-redesign/21-UI-SPEC.md
@DESIGN.md
@src/components/ClientLayout.tsx
@src/components/Navbar.tsx
@src/components/ThemeToggle.tsx
@src/lib/auth-context.tsx
@src/app/globals.css

<interfaces>
<!-- Executor needs these existing interfaces -->

From src/lib/auth-context.tsx:
```typescript
interface AuthContextType {
  isAuthenticated: boolean;
  isLoading: boolean;
  user: User | null;  // Supabase User — has .email, .user_metadata.role
  login: (email: string, password: string) => Promise<boolean>;
  logout: () => Promise<void>;
}
export function useAuth(): AuthContextType;
```

From src/components/ThemeToggle.tsx:
```typescript
export function ThemeToggle(): JSX.Element;  // Already a client component
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Radix deps + create shell types, configs, and ConditionalNavbar</name>
  <files>
    package.json,
    src/components/shell/types.ts,
    src/components/shell/sidebar-configs.ts,
    src/components/ConditionalNavbar.tsx,
    src/components/ClientLayout.tsx,
    src/components/shell/sidebar-configs.test.ts
  </files>
  <read_first>
    package.json,
    src/components/ClientLayout.tsx,
    src/components/Navbar.tsx,
    DESIGN.md,
    .planning/phases/21-app-shell-redesign/21-UI-SPEC.md,
    .planning/phases/21-app-shell-redesign/21-RESEARCH.md
  </read_first>
  <action>
1. Install Radix: `npm install @radix-ui/react-dialog @radix-ui/react-dropdown-menu`

2. Create `src/components/shell/types.ts`:
   - Export `SidebarItem` interface: `{ href: string; label: string; icon: React.ComponentType<{ className?: string }> }`
   - Export `SidebarGroup` interface: `{ label: string; items: SidebarItem[] }`

3. Create `src/components/shell/sidebar-configs.ts` (per D-08, D-09, D-10, UI-SPEC Sidebar Config Reference):
   - Import icons from lucide-react: `Users, BarChart2, Scale, PlayCircle, FileText, Sliders, Users2, BookOpen, UserCog, User`
   - Export `dashboardSidebarGroups: SidebarGroup[]` with:
     - Overview group: Roster (`/trainer`, Users icon), Gap Analysis (`/trainer/gap-analysis`, BarChart2), Calibration (`/trainer/calibration`, Scale)
     - Actions group: New Mock (`/interview/new`, PlayCircle), Reports (`/trainer/reports`, FileText)
   - Export `settingsSidebarGroups: SidebarGroup[]` with:
     - Settings group: Threshold (`/trainer/settings/threshold`, Sliders), Cohorts (`/trainer/settings/cohorts`, Users2), Curriculum (`/trainer/settings/curriculum`, BookOpen), Users (`/trainer/settings/users`, UserCog), Associates (`/trainer/settings/associates`, User)

4. Create `src/components/ConditionalNavbar.tsx` — `'use client'`, reads `usePathname()`, returns `null` if path starts with `/trainer`, otherwise renders `<Navbar />`. Per RESEARCH.md Pitfall 1.

5. Update `src/components/ClientLayout.tsx`: Replace `import Navbar` and `<Navbar />` with `import { ConditionalNavbar }` and `<ConditionalNavbar />`. Keep `AuthProvider` wrapping unchanged.

6. Create `src/components/shell/sidebar-configs.test.ts`:
   - Test dashboardSidebarGroups has 2 groups ("Overview" with 3 items, "Actions" with 2 items)
   - Test settingsSidebarGroups has 1 group ("Settings" with 5 items)
   - Test all hrefs are correct strings matching the UI-SPEC reference table
   - Test all items have icon property that is a function (React component)
  </action>
  <verify>
    <automated>cd /Users/jestercharles/mock-interview-assist && npm run test -- --run src/components/shell/sidebar-configs.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/@radix-ui/react-dialog` exists
    - `node_modules/@radix-ui/react-dropdown-menu` exists
    - `grep -c "SidebarItem\|SidebarGroup" src/components/shell/types.ts` returns 2+
    - `grep -c "dashboardSidebarGroups\|settingsSidebarGroups" src/components/shell/sidebar-configs.ts` returns 2+
    - `grep "ConditionalNavbar" src/components/ClientLayout.tsx` matches
    - `grep "pathname.startsWith.*trainer" src/components/ConditionalNavbar.tsx` matches
    - sidebar-configs test passes
  </acceptance_criteria>
  <done>Radix installed, types + sidebar configs exported, ConditionalNavbar wired into ClientLayout, config tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Build TopBar, AvatarMenu, CohortSwitcher, SectionSidebar, MobileSidebar</name>
  <files>
    src/components/shell/TopBar.tsx,
    src/components/shell/AvatarMenu.tsx,
    src/components/shell/CohortSwitcher.tsx,
    src/components/shell/SectionSidebar.tsx,
    src/components/shell/MobileSidebar.tsx
  </files>
  <read_first>
    src/components/shell/types.ts,
    src/components/shell/sidebar-configs.ts,
    src/lib/auth-context.tsx,
    src/components/ThemeToggle.tsx,
    DESIGN.md,
    .planning/phases/21-app-shell-redesign/21-UI-SPEC.md,
    .planning/phases/21-app-shell-redesign/21-RESEARCH.md
  </read_first>
  <action>
All components are `'use client'`. Follow UI-SPEC Component Inventory and Interaction States exactly.

**TopBar.tsx** (per D-01, D-02, D-03, D-04, D-15, UI-SPEC):
- Height 56px, `position: sticky; top: 0; z-index: 30`, background `var(--surface-muted)`, border-bottom `1px solid var(--border)`
- Left zone: NLM wordmark (Clash Display 500, `<Link href="/trainer">`) + hamburger button (`Menu` icon, `md:hidden`, opens MobileSidebar)
- Center zone: nav links array `[{ label: 'Dashboard', prefix: '/trainer', href: '/trainer' }, { label: 'Interviews', prefix: '/interview', href: '/interview/new' }, { label: 'Question Banks', prefix: '/question-banks', href: '/question-banks' }, { label: 'Settings', prefix: '/trainer/settings', href: '/trainer/settings' }]`. Hidden on mobile (`hidden md:flex`). Active detection: `usePathname()` — for Dashboard use exact `/trainer` match OR startsWith `/trainer/` but NOT `/trainer/settings`; for Settings use startsWith `/trainer/settings`; for others use startsWith prefix. Active item: `--accent` text + 2px bottom border. Hover: `--highlight` bg, border-radius 6px, 120ms ease-out.
- Right zone: `<Suspense fallback={null}><CohortSwitcher /></Suspense>` + `<ThemeToggle />` + `<AvatarMenu />`
- Accept optional `sidebarGroups` prop and render `<MobileSidebar groups={sidebarGroups} />` when groups provided (hamburger triggers it)

**AvatarMenu.tsx** (per D-03, UI-SPEC):
- Uses `@radix-ui/react-dropdown-menu` (`import * as DropdownMenu from '@radix-ui/react-dropdown-menu'`)
- Trigger: 32px circle with initial from `user?.email?.[0]?.toUpperCase() || '?'`, background `var(--surface-muted)`, border `1px solid var(--border)`, 13px DM Sans 500
- Content: `side="bottom" align="end" sideOffset={8}`, background `var(--surface)`, border `1px solid var(--border)`, border-radius 8px
- Items: user email (non-interactive label, 16px DM Sans 400, `--muted` color), role badge (12px DM Sans 500 — "trainer" in `--success`, "admin" in `--warning`), separator, "Sign Out" item (16px DM Sans 400, `--danger` text). Sign Out calls `logout()` from `useAuth()`.
- Role from `user?.user_metadata?.role || 'trainer'`

**CohortSwitcher.tsx** (per D-05, D-06, D-07, UI-SPEC):
- Uses `@radix-ui/react-dropdown-menu`
- `useSearchParams()` to read `?cohort` param. `useRouter()` + `usePathname()` for navigation.
- On mount: read URL param first; if absent, read `localStorage.getItem('nlm_cohort_id')` and push to URL via `router.replace`
- Fetch cohorts from `/api/cohorts` on mount (GET, returns `{ cohorts: Array<{ id, name, _count: { associates } }> }`)
- Trigger: cohort name or "All Cohorts" + `ChevronDown` icon, max-width 160px, text truncate with ellipsis
- Items: "All Cohorts" (top, always), then each cohort with name + associate count in muted text. Selected item shows `Check` icon in `--accent`.
- `handleSelect(cohortId: string | null)`: update URL param, update localStorage, update local state
- Error state: if fetch fails, trigger shows "All Cohorts", items show "Couldn't load cohorts. Refresh to try again."

**SectionSidebar.tsx** (per D-08, D-12, D-13, UI-SPEC):
- Props: `groups: SidebarGroup[]`
- Expanded width 200px, collapsed width 48px. CSS transition on width: 200ms ease-in-out.
- Read collapsed from `useState(false)`, hydrate from `localStorage.getItem('nlm_sidebar_collapsed')` in `useEffect`
- Toggle button at bottom: `ChevronLeft`/`ChevronRight`, aria-label "Collapse sidebar"/"Expand sidebar"
- Group labels: 12px DM Sans 500, uppercase, 0.06em tracking. Hidden when collapsed.
- Items: 13px DM Sans 500, active = 2px `--accent` left border + `--accent` text, hover = `--highlight` bg 150ms ease-out, border-radius 6px. Use `usePathname()` for active detection (exact match for `/trainer`, startsWith for others).
- When collapsed: icons only (16px), no labels. Tooltip on hover (title attribute is fine).
- `hidden md:flex` — sidebar not visible on mobile (MobileSidebar handles it)

**MobileSidebar.tsx** (per D-14, D-15, UI-SPEC):
- Uses `@radix-ui/react-dialog`
- Props: `groups: SidebarGroup[]`
- Trigger rendered externally (hamburger in TopBar controls open state via shared state or Dialog.Root wrapping)
- Actually: make MobileSidebar self-contained with its own `Dialog.Root` + `Dialog.Trigger` (hamburger button, `md:hidden`). TopBar renders `<MobileSidebar groups={sidebarGroups} />` and the hamburger is inside it.
- Overlay: `fixed inset-0 bg-black/30 z-40`
- Content: `fixed inset-y-0 left-0 z-50 w-64`, background `var(--surface)`, border-right `1px solid var(--border)`
- Close button: `X` icon top-right, aria-label "Close menu"
- Content renders same sidebar items as desktop, always expanded (no collapse in mobile sheet)
- Auto-close on navigation: use `usePathname()` in `useEffect` to close dialog when path changes
  </action>
  <verify>
    <automated>cd /Users/jestercharles/mock-interview-assist && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export.*TopBar" src/components/shell/TopBar.tsx` returns 1
    - `grep "DropdownMenu" src/components/shell/AvatarMenu.tsx` matches (Radix import)
    - `grep "DropdownMenu" src/components/shell/CohortSwitcher.tsx` matches
    - `grep "useSearchParams" src/components/shell/CohortSwitcher.tsx` matches
    - `grep "nlm_sidebar_collapsed" src/components/shell/SectionSidebar.tsx` matches
    - `grep "Dialog" src/components/shell/MobileSidebar.tsx` matches (Radix Dialog import)
    - `grep "Close menu" src/components/shell/MobileSidebar.tsx` matches
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All 5 shell components created with correct Radix usage, design tokens, localStorage persistence, URL param handling, and ARIA labels. TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/cohorts | CohortSwitcher fetches cohort list; middleware already guards trainer routes |
| URL param → component state | `?cohort=<id>` read from URL and stored in state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | CohortSwitcher URL param | accept | Param is a cohort ID string used only for client-side filtering in this phase; Dashboard pages (Phase 22) must validate against actual cohort list before passing to Prisma queries |
| T-21-02 | Elevation of Privilege | TopBar rendered on non-trainer route | mitigate | TopBar only rendered inside trainer layout.tsx (Plan 02); ConditionalNavbar suppresses global Navbar for /trainer/* routes; middleware AUTH-09 already blocks non-trainers |
| T-21-03 | Information Disclosure | AvatarMenu shows user email | accept | Email is the user's own email from their authenticated session; no cross-user data |
</threat_model>

<verification>
- `npm run test -- --run src/components/shell/sidebar-configs.test.ts` passes
- `npx tsc --noEmit` passes
- `npm run build` succeeds (no route conflicts yet — route moves happen in Plan 02)
</verification>

<success_criteria>
- All 5 shell components + types + configs + ConditionalNavbar exist and export correctly
- Radix packages installed in package.json
- ClientLayout uses ConditionalNavbar (Navbar hidden on /trainer/*)
- Sidebar config tests pass with correct group counts and hrefs
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/21-app-shell-redesign/21-01-SUMMARY.md`
</output>
