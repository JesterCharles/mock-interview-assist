---
phase: 21-app-shell-redesign
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/app/trainer/layout.tsx
  - src/app/trainer/(dashboard)/page.tsx
  - src/app/trainer/(dashboard)/layout.tsx
  - src/app/trainer/(dashboard)/loading.tsx
  - src/app/trainer/(dashboard)/[slug]/page.tsx
  - src/app/trainer/(dashboard)/[slug]/loading.tsx
  - src/app/trainer/(dashboard)/[slug]/AssociateCohortSelect.tsx
  - src/app/trainer/(dashboard)/gap-analysis/page.tsx
  - src/app/trainer/(dashboard)/calibration/page.tsx
  - src/app/trainer/(dashboard)/reports/page.tsx
  - src/app/trainer/(settings)/layout.tsx
  - src/app/trainer/(settings)/settings/page.tsx
  - src/app/trainer/(settings)/settings/threshold/page.tsx
  - src/app/trainer/(settings)/settings/cohorts/page.tsx
  - src/app/trainer/(settings)/settings/cohorts/CohortsClient.tsx
  - src/app/trainer/(settings)/settings/cohorts/page.test.tsx
  - src/app/trainer/(settings)/settings/cohorts/[id]/page.tsx
  - src/app/trainer/(settings)/settings/cohorts/[id]/curriculum/page.tsx
  - src/app/trainer/(settings)/settings/cohorts/[id]/curriculum/CurriculumManager.tsx
  - src/app/trainer/(settings)/settings/curriculum/page.tsx
  - src/app/trainer/(settings)/settings/users/page.tsx
  - src/app/trainer/(settings)/settings/associates/page.tsx
  - src/app/trainer/(settings)/settings/associates/AssociatesBackfillTable.tsx
  - src/app/trainer/(settings)/settings/associates/DryRunPreviewCard.tsx
  - src/app/trainer/nav-link.test.ts
  - src/app/trainer/trainer.css
autonomous: false
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-03
  - SHELL-04

must_haves:
  truths:
    - "Every /trainer/* route renders TopBar with Dashboard/Interviews/QB/Settings nav"
    - "Dashboard section shows sidebar with Overview + Actions groups"
    - "Settings section shows sidebar with 5 settings items"
    - "/trainer lands on Dashboard > Roster (existing roster page)"
    - "/trainer/[slug] works as child route under Dashboard"
    - "Cohort pages moved to /trainer/settings/cohorts with working links"
    - "Placeholder pages exist for Gap Analysis, Calibration, Reports"
    - "Settings landing, threshold, curriculum, users pages exist"
    - "Sidebar collapse persists to localStorage"
    - "Cohort switcher reads/writes ?cohort param"
  artifacts:
    - path: "src/app/trainer/layout.tsx"
      provides: "Trainer shell layout wrapping all /trainer/* routes with TopBar"
      contains: "TopBar"
    - path: "src/app/trainer/(dashboard)/layout.tsx"
      provides: "Dashboard section layout with SectionSidebar using dashboardSidebarGroups"
      contains: "dashboardSidebarGroups"
    - path: "src/app/trainer/(settings)/layout.tsx"
      provides: "Settings section layout with SectionSidebar using settingsSidebarGroups"
      contains: "settingsSidebarGroups"
    - path: "src/app/trainer/(dashboard)/page.tsx"
      provides: "Roster page (moved from trainer/page.tsx)"
    - path: "src/app/trainer/(dashboard)/gap-analysis/page.tsx"
      provides: "Gap Analysis placeholder"
      contains: "Gap Analysis"
    - path: "src/app/trainer/(dashboard)/calibration/page.tsx"
      provides: "Calibration placeholder"
      contains: "Calibration"
    - path: "src/app/trainer/(dashboard)/reports/page.tsx"
      provides: "Reports placeholder"
      contains: "Reports"
  key_links:
    - from: "src/app/trainer/layout.tsx"
      to: "src/components/shell/TopBar.tsx"
      via: "import and render"
      pattern: "import.*TopBar"
    - from: "src/app/trainer/(dashboard)/layout.tsx"
      to: "src/components/shell/SectionSidebar.tsx"
      via: "import with dashboardSidebarGroups"
      pattern: "dashboardSidebarGroups"
    - from: "src/app/trainer/(settings)/layout.tsx"
      to: "src/components/shell/SectionSidebar.tsx"
      via: "import with settingsSidebarGroups"
      pattern: "settingsSidebarGroups"
---

<objective>
Reorganize trainer routes into Next.js route groups `(dashboard)` and `(settings)`, wire shell components into layouts, move files, create placeholder and settings pages, and update all internal links.

Purpose: Complete the two-level nav shell by connecting shell components to the route structure.
Output: Working trainer shell with topbar + section-scoped sidebars on all trainer routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-app-shell-redesign/21-CONTEXT.md
@.planning/phases/21-app-shell-redesign/21-RESEARCH.md
@.planning/phases/21-app-shell-redesign/21-UI-SPEC.md
@.planning/phases/21-app-shell-redesign/21-01-SUMMARY.md
@DESIGN.md
@src/app/trainer/page.tsx
@src/app/trainer/[slug]/page.tsx
@src/app/trainer/cohorts/page.tsx
@src/app/trainer/cohorts/CohortsClient.tsx
@src/app/trainer/cohorts/[id]/page.tsx
@src/app/trainer/cohorts/[id]/curriculum/page.tsx
@src/app/trainer/settings/associates/page.tsx
@src/app/trainer/nav-link.test.ts
@src/app/trainer/loading.tsx
@src/app/trainer/trainer.css

<interfaces>
<!-- From Plan 01 outputs -->

From src/components/shell/types.ts:
```typescript
export interface SidebarItem {
  href: string;
  label: string;
  icon: React.ComponentType<{ className?: string }>;
}
export interface SidebarGroup {
  label: string;
  items: SidebarItem[];
}
```

From src/components/shell/sidebar-configs.ts:
```typescript
export const dashboardSidebarGroups: SidebarGroup[];
export const settingsSidebarGroups: SidebarGroup[];
```

From src/components/shell/TopBar.tsx:
```typescript
export function TopBar({ sidebarGroups }: { sidebarGroups?: SidebarGroup[] }): JSX.Element;
```

From src/components/shell/SectionSidebar.tsx:
```typescript
export function SectionSidebar({ groups }: { groups: SidebarGroup[] }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create trainer layout + route groups + move files + placeholder pages</name>
  <files>
    src/app/trainer/layout.tsx,
    src/app/trainer/(dashboard)/layout.tsx,
    src/app/trainer/(dashboard)/page.tsx,
    src/app/trainer/(dashboard)/loading.tsx,
    src/app/trainer/(dashboard)/[slug]/page.tsx,
    src/app/trainer/(dashboard)/[slug]/loading.tsx,
    src/app/trainer/(dashboard)/[slug]/AssociateCohortSelect.tsx,
    src/app/trainer/(dashboard)/gap-analysis/page.tsx,
    src/app/trainer/(dashboard)/calibration/page.tsx,
    src/app/trainer/(dashboard)/reports/page.tsx,
    src/app/trainer/(settings)/layout.tsx,
    src/app/trainer/(settings)/settings/page.tsx,
    src/app/trainer/(settings)/settings/threshold/page.tsx,
    src/app/trainer/(settings)/settings/cohorts/page.tsx,
    src/app/trainer/(settings)/settings/cohorts/CohortsClient.tsx,
    src/app/trainer/(settings)/settings/cohorts/page.test.tsx,
    src/app/trainer/(settings)/settings/cohorts/[id]/page.tsx,
    src/app/trainer/(settings)/settings/cohorts/[id]/curriculum/page.tsx,
    src/app/trainer/(settings)/settings/cohorts/[id]/curriculum/CurriculumManager.tsx,
    src/app/trainer/(settings)/settings/curriculum/page.tsx,
    src/app/trainer/(settings)/settings/users/page.tsx,
    src/app/trainer/(settings)/settings/associates/page.tsx,
    src/app/trainer/(settings)/settings/associates/AssociatesBackfillTable.tsx,
    src/app/trainer/(settings)/settings/associates/DryRunPreviewCard.tsx,
    src/app/trainer/trainer.css
  </files>
  <read_first>
    src/app/trainer/page.tsx,
    src/app/trainer/[slug]/page.tsx,
    src/app/trainer/[slug]/loading.tsx,
    src/app/trainer/[slug]/AssociateCohortSelect.tsx,
    src/app/trainer/loading.tsx,
    src/app/trainer/cohorts/page.tsx,
    src/app/trainer/cohorts/CohortsClient.tsx,
    src/app/trainer/cohorts/page.test.tsx,
    src/app/trainer/cohorts/[id]/page.tsx,
    src/app/trainer/cohorts/[id]/curriculum/page.tsx,
    src/app/trainer/cohorts/[id]/curriculum/CurriculumManager.tsx,
    src/app/trainer/settings/associates/page.tsx,
    src/app/trainer/settings/associates/AssociatesBackfillTable.tsx,
    src/app/trainer/settings/associates/DryRunPreviewCard.tsx,
    src/app/trainer/trainer.css,
    src/components/shell/TopBar.tsx,
    src/components/shell/SectionSidebar.tsx,
    src/components/shell/sidebar-configs.ts,
    DESIGN.md,
    .planning/phases/21-app-shell-redesign/21-RESEARCH.md
  </read_first>
  <action>
**Step 1 — Create `src/app/trainer/layout.tsx` (per D-04, D-17):**
- Server component (no `'use client'`). This is the shared trainer shell.
- Import `TopBar` from `@/components/shell/TopBar`
- Render: `<div className="flex flex-col min-h-screen" style={{ background: 'var(--bg)' }}><TopBar /><div className="flex flex-1 overflow-hidden">{children}</div></div>`
- NOTE: TopBar is a client component but can be imported in a server layout

**Step 2 — Create route group directories and move files:**

Move dashboard files (per D-17, D-18):
- `mkdir -p src/app/trainer/(dashboard)/[slug]`
- Move `src/app/trainer/page.tsx` → `src/app/trainer/(dashboard)/page.tsx`
- Move `src/app/trainer/[slug]/page.tsx` → `src/app/trainer/(dashboard)/[slug]/page.tsx`
- Move `src/app/trainer/[slug]/loading.tsx` → `src/app/trainer/(dashboard)/[slug]/loading.tsx` (if exists)
- Move `src/app/trainer/[slug]/AssociateCohortSelect.tsx` → `src/app/trainer/(dashboard)/[slug]/AssociateCohortSelect.tsx`
- Move `src/app/trainer/loading.tsx` → `src/app/trainer/(dashboard)/loading.tsx`
- Move `src/app/trainer/trainer.css` → `src/app/trainer/(dashboard)/trainer.css` (it's imported by the roster page)
- DELETE originals after move (per RESEARCH.md Pitfall 2 — prevent route conflicts)

Move cohorts under settings (per D-20):
- `mkdir -p src/app/trainer/(settings)/settings/cohorts/[id]/curriculum`
- Move entire `src/app/trainer/cohorts/` contents to `src/app/trainer/(settings)/settings/cohorts/`
- This includes: `page.tsx`, `CohortsClient.tsx`, `page.test.tsx`, `[id]/page.tsx`, `[id]/curriculum/page.tsx`, `[id]/curriculum/CurriculumManager.tsx`
- DELETE `src/app/trainer/cohorts/` directory after move

Move associates settings (per D-19):
- `mkdir -p src/app/trainer/(settings)/settings/associates`
- Move `src/app/trainer/settings/associates/` contents to `src/app/trainer/(settings)/settings/associates/`
- This includes: `page.tsx`, `AssociatesBackfillTable.tsx`, `DryRunPreviewCard.tsx`
- DELETE `src/app/trainer/settings/associates/` and `src/app/trainer/settings/` if empty

**Step 3 — Create dashboard section layout:**
`src/app/trainer/(dashboard)/layout.tsx` — `'use client'` component:
- Import `SectionSidebar` from `@/components/shell/SectionSidebar`
- Import `dashboardSidebarGroups` from `@/components/shell/sidebar-configs`
- Import `MobileSidebar` from `@/components/shell/MobileSidebar`
- Render: `<SectionSidebar groups={dashboardSidebarGroups} />` + `<main className="flex-1 overflow-y-auto">{children}</main>`
- Sidebar hidden on mobile (`hidden md:flex`), MobileSidebar handles it

**Step 4 — Create settings section layout:**
`src/app/trainer/(settings)/layout.tsx` — `'use client'` component:
- Same pattern as dashboard layout but with `settingsSidebarGroups`

**Step 5 — Create placeholder pages (per RESEARCH.md Open Question 2, UI-SPEC Copywriting Contract):**

Each placeholder is a server component with centered layout:
- `src/app/trainer/(dashboard)/gap-analysis/page.tsx`: heading "Gap Analysis", body "Skill gap aggregation across your cohort. Coming in the next release."
- `src/app/trainer/(dashboard)/calibration/page.tsx`: heading "Calibration", body "Trainer override frequency and AI-trainer variance. Coming in the next release."
- `src/app/trainer/(dashboard)/reports/page.tsx`: heading "Reports", body "Cohort and per-associate PDF exports. Coming in the next release."
- Style: centered column max-width 480px, heading 28px Clash Display 500 `--ink`, body 16px DM Sans 400 `--muted`

**Step 6 — Create settings sub-pages (per D-19):**

Each is a minimal page. Existing pages that were moved keep their content. New ones:
- `src/app/trainer/(settings)/settings/page.tsx`: redirect to `/trainer/settings/threshold` via `redirect()` from `next/navigation`
- `src/app/trainer/(settings)/settings/threshold/page.tsx`: Import existing threshold settings logic if it exists, or create placeholder "Readiness Threshold" page with a note that the existing `/api/settings` endpoint powers this
- `src/app/trainer/(settings)/settings/curriculum/page.tsx`: placeholder "Curriculum Management" — "Select a cohort to manage its curriculum."
- `src/app/trainer/(settings)/settings/users/page.tsx`: placeholder "Users" — "User management and role assignment. Coming in a future release." (per deferred admin-promote UI)

**Step 7 — Update internal links (per RESEARCH.md Pitfall 3):**
After moving cohorts pages, update all internal `Link` hrefs:
- In moved `CohortsClient.tsx`: update `href="/trainer/cohorts/${c.id}"` → `href="/trainer/settings/cohorts/${c.id}"`
- In moved `[id]/page.tsx`: update `href="/trainer/cohorts"` → `href="/trainer/settings/cohorts"` and `href="/trainer/cohorts/${cohort.id}/curriculum"` → `href="/trainer/settings/cohorts/${cohort.id}/curriculum"`
- In moved `[id]/curriculum/page.tsx`: update `href="/trainer/cohorts"` → `href="/trainer/settings/cohorts"` and `href="/trainer/cohorts/${cohort.id}"` → `href="/trainer/settings/cohorts/${cohort.id}"`
- In moved `(dashboard)/page.tsx` (roster): update `href="/trainer/cohorts"` → `href="/trainer/settings/cohorts"` (line 136 area). Also REMOVE the inline sub-nav (Dashboard/Cohorts tabs at lines ~120-145) since TopBar + sidebar now handle navigation.
- In moved `page.test.tsx`: update test assertions from `/trainer/cohorts` to `/trainer/settings/cohorts`
- Also update CSS import path in roster page if it references `./trainer.css` — verify the relative path is correct after move.

**Step 8 — Remove page-level auth guards from moved pages (per RESEARCH.md Anti-Patterns):**
The existing `trainer/page.tsx` does `useAuth()` redirect. Since middleware already guards `/trainer/*`, remove the client-side auth redirect from the roster page. Keep the `useAuth()` call only if it's used for data (e.g., user display), not for redirect.
  </action>
  <verify>
    <automated>cd /Users/jestercharles/mock-interview-assist && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/app/trainer/layout.tsx && grep "TopBar" src/app/trainer/layout.tsx` succeeds
    - `test -f src/app/trainer/(dashboard)/layout.tsx && grep "dashboardSidebarGroups" src/app/trainer/(dashboard)/layout.tsx` succeeds
    - `test -f src/app/trainer/(settings)/layout.tsx && grep "settingsSidebarGroups" src/app/trainer/(settings)/layout.tsx` succeeds
    - `test -f src/app/trainer/(dashboard)/page.tsx` (roster moved)
    - `test ! -f src/app/trainer/page.tsx` (original deleted — no route conflict)
    - `test -f src/app/trainer/(dashboard)/gap-analysis/page.tsx`
    - `test -f src/app/trainer/(dashboard)/calibration/page.tsx`
    - `test -f src/app/trainer/(dashboard)/reports/page.tsx`
    - `test -f src/app/trainer/(settings)/settings/cohorts/page.tsx` (cohorts moved)
    - `test ! -d src/app/trainer/cohorts` (original deleted)
    - `grep -r "/trainer/cohorts" src/app/trainer/ | grep -v "/trainer/settings/cohorts" | grep -v node_modules` returns empty (all old links updated)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>All files moved into route groups, layouts wire TopBar + SectionSidebar, placeholder pages created, settings routes created, internal links updated, build passes with no route conflicts</done>
</task>

<task type="auto">
  <name>Task 2: Update tests + verify full suite</name>
  <files>
    src/app/trainer/nav-link.test.ts
  </files>
  <read_first>
    src/app/trainer/nav-link.test.ts,
    src/app/trainer/(dashboard)/page.tsx,
    src/components/shell/sidebar-configs.ts
  </read_first>
  <action>
**Update `src/app/trainer/nav-link.test.ts`:**

The existing test checks that `src/app/trainer/page.tsx` contains a `<Link href="/trainer/cohorts">`. After the move:
- The roster page is at `src/app/trainer/(dashboard)/page.tsx`
- The cohorts link is now `/trainer/settings/cohorts`
- But the inline sub-nav was removed (TopBar + sidebar handle navigation now)

Rewrite the test to verify the sidebar config instead:
```typescript
import { describe, it, expect } from 'vitest'
import { settingsSidebarGroups } from '@/components/shell/sidebar-configs'

describe('trainer nav → cohorts link', () => {
  it('settings sidebar config includes a link to /trainer/settings/cohorts', () => {
    const allItems = settingsSidebarGroups.flatMap(g => g.items)
    const cohortsItem = allItems.find(item => item.href === '/trainer/settings/cohorts')
    expect(cohortsItem).toBeDefined()
    expect(cohortsItem!.label).toBe('Cohorts')
  })
})
```

Then run full test suite: `npm run test` — all tests must pass.
  </action>
  <verify>
    <automated>cd /Users/jestercharles/mock-interview-assist && npm run test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "settings/cohorts" src/app/trainer/nav-link.test.ts` matches
    - `npm run test` passes (all existing + updated tests)
  </acceptance_criteria>
  <done>Nav-link test updated to verify sidebar config instead of inline page link. Full test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of trainer shell</name>
  <what-built>Two-level trainer navigation shell: global topbar (Dashboard/Interviews/Question Banks/Settings + cohort switcher + avatar menu) and section-scoped sidebars (Dashboard sidebar with Overview+Actions, Settings sidebar with 5 items). Route reorganization preserving existing URLs. Mobile off-canvas sidebar via Radix Dialog.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/signin — sign in as trainer
    2. Verify TopBar appears with NLM wordmark, 4 nav items, cohort switcher, theme toggle, avatar menu
    3. Click avatar menu — verify email, role badge, Sign Out button appear
    4. Click "Dashboard" in topbar — verify sidebar shows Overview (Roster, Gap Analysis, Calibration) + Actions (New Mock, Reports)
    5. Visit /trainer — verify roster page loads with sidebar visible
    6. Visit /trainer/gap-analysis — verify placeholder "Gap Analysis" page with "Coming in the next release" text
    7. Visit /trainer/calibration — verify placeholder
    8. Visit /trainer/reports — verify placeholder
    9. Click "Settings" in topbar — verify sidebar changes to Threshold/Cohorts/Curriculum/Users/Associates
    10. Visit /trainer/settings/cohorts — verify existing cohorts page works
    11. Visit /trainer/settings/associates — verify existing backfill table works
    12. Click a cohort in the cohort switcher — verify URL gets `?cohort=<id>` param
    13. Resize browser to mobile width (<768px) — verify hamburger appears, center nav hides
    14. Tap hamburger — verify off-canvas sidebar opens from left with sidebar items
    15. Click sidebar collapse toggle (bottom of sidebar on desktop) — verify sidebar collapses to icons only
    16. Refresh page — verify sidebar stays collapsed (localStorage persistence)
    17. Visit /interview/new — verify NO old Navbar appears, TopBar highlights "Interviews"
    18. Visit / (home) — verify old Navbar still renders (ConditionalNavbar working)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → route resolution | Route groups must not expose trainer-only pages to unauthenticated users |
| URL param → state | `?cohort=<id>` param read into component state |
| client → layout | Trainer layout renders TopBar only for /trainer/* routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Elevation of Privilege | Route groups exposing pages | mitigate | Middleware AUTH-09 continues to guard all `/trainer/*` routes regardless of route group nesting; route groups affect layout only, not auth |
| T-21-05 | Tampering | Internal link updates after file move | mitigate | Grep verification in acceptance criteria ensures no stale `/trainer/cohorts` links remain; nav-link test updated to assert new path |
| T-21-06 | Denial of Service | Build failure from route conflicts | mitigate | Acceptance criteria verify old files deleted; `npm run build` is a verification step |
</threat_model>

<verification>
- `npm run build` succeeds (no route conflicts, all imports resolve)
- `npm run test` passes (updated nav-link test + all existing tests)
- No stale `/trainer/cohorts` links in source (grep verification)
- Human verification of full navigation flow
</verification>

<success_criteria>
- TopBar visible on all /trainer/* routes with correct active section highlighting
- Dashboard sidebar renders Overview + Actions groups
- Settings sidebar renders 5 settings items
- /trainer → Roster page (moved, working)
- /trainer/settings/cohorts → Cohorts page (moved, working, links updated)
- Placeholder pages at gap-analysis, calibration, reports
- Settings sub-pages exist (threshold, curriculum, users — placeholders where needed)
- Sidebar collapse persists to localStorage
- Cohort switcher writes ?cohort param to URL
- Mobile hamburger opens off-canvas sidebar
- Old Navbar still renders on non-trainer routes
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-app-shell-redesign/21-02-SUMMARY.md`
</output>
