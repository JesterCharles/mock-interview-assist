---
phase: 13-curriculum-schedule
plan: 01
type: execute
wave: 1
depends_on: ["11-01", "11-02"]
files_modified:
  - src/app/api/cohorts/[id]/curriculum/route.ts
  - src/app/api/cohorts/[id]/curriculum/[weekId]/route.ts
  - src/lib/curriculumService.ts
  - src/lib/curriculumService.test.ts
files_touched_external:
  - prisma/schema.prisma
autonomous: true
requirements: [CURRIC-01]
must_haves:
  truths:
    - "Trainer can create a curriculum week for a cohort via API"
    - "Trainer can list, update, and delete curriculum weeks for a cohort"
    - "Taught-weeks query returns only weeks where startDate <= today"
  artifacts:
    - path: "src/app/api/cohorts/[id]/curriculum/route.ts"
      provides: "GET list + POST create curriculum weeks, with ?taught=true filter"
      exports: ["GET", "POST"]
    - path: "src/app/api/cohorts/[id]/curriculum/[weekId]/route.ts"
      provides: "PATCH update + DELETE remove curriculum week"
      exports: ["PATCH", "DELETE"]
    - path: "src/lib/curriculumService.ts"
      provides: "Pure DB access functions for curriculum weeks"
      contains: "getTaughtWeeks"
  key_links:
    - from: "src/app/api/cohorts/[id]/curriculum/route.ts"
      to: "prisma.curriculumWeek"
      via: "Prisma query with cohortId scope"
      pattern: "prisma\\.curriculumWeek\\.(findMany|create)"
    - from: "src/lib/curriculumService.ts"
      to: "prisma.curriculumWeek"
      via: "taught-weeks query filters startDate <= now"
      pattern: "startDate.*lte.*new Date"
---

<objective>
Build the Curriculum CRUD API and service layer for per-cohort weekly schedules (CURRIC-01).

Purpose: Provides the data foundation downstream plans (trainer UI, wizard filter) consume. Without these endpoints, the trainer can't define curriculum and the wizard has nothing to fetch.
Output: Four REST endpoints under `/api/cohorts/[id]/curriculum`, a pure-function service for DB access, and a Vitest suite covering the taught-weeks filter semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-curriculum-schedule/13-CONTEXT.md
@prisma/schema.prisma
@src/lib/prisma.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create curriculumService with taught-weeks filter</name>
  <files>src/lib/curriculumService.ts, src/lib/curriculumService.test.ts</files>
  <read_first>prisma/schema.prisma, src/lib/prisma.ts, src/lib/gapService.ts</read_first>
  <behavior>
    - listWeeks(cohortId): returns all weeks for cohort ordered by weekNumber asc
    - getTaughtWeeks(cohortId, now=new Date()): returns only weeks where startDate <= now, ordered by weekNumber asc
    - createWeek(cohortId, input): validates and inserts week
    - updateWeek(weekId, input): updates fields, returns updated row
    - deleteWeek(weekId): deletes row, returns void
    - Test: getTaughtWeeks filters out future-dated weeks (startDate > now)
    - Test: getTaughtWeeks includes weeks with startDate == now (boundary)
  </behavior>
  <action>Create pure async service functions wrapping `prisma.curriculumWeek`. Export TypeScript types `CurriculumWeek`, `CurriculumWeekInput`. Per D-08 and D-09: listWeeks returns all, getTaughtWeeks filters `startDate: { lte: now }`. All queries order by `weekNumber: 'asc'`. Use `prisma` singleton from `src/lib/prisma.ts`. No auth concerns at this layer — that is route-level. Write Vitest tests with mocked prisma client covering boundary (startDate == now is taught) and future-dated exclusion.</action>
  <verify>
    <automated>npm run test -- src/lib/curriculumService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - curriculumService.ts exports: listWeeks, getTaughtWeeks, createWeek, updateWeek, deleteWeek
    - curriculumService.test.ts passes with boundary + future-exclusion cases
    - getTaughtWeeks uses `startDate: { lte: ... }` filter
  </acceptance_criteria>
  <done>All service functions typed, tested, and green.</done>
</task>

<task type="auto">
  <name>Task 2: Create curriculum collection API route (list + create + taught filter)</name>
  <files>src/app/api/cohorts/[id]/curriculum/route.ts</files>
  <read_first>src/lib/curriculumService.ts, src/app/api/settings/route.ts, src/lib/auth-server.ts</read_first>
  <action>Create `GET` and `POST` handlers at `src/app/api/cohorts/[id]/curriculum/route.ts`. GET reads `?taught=true` query param: when present call `getTaughtWeeks`, otherwise `listWeeks` (per D-08, D-09). POST validates body with Zod: `{ weekNumber: number (int, >=1), skillName: string (min 1), topicTags: string[] (default []), startDate: string (ISO date) }` then calls `createWeek`. Guard both with trainer auth via existing `getTrainerSessionFromCookies` helper (D-13) — return 401 if no valid session. Return 404 if cohort doesn't exist. Return 400 on Zod failure with `{ error, issues }`. Return 201 with created row on POST success. Use `NextResponse.json`.</action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint -- src/app/api/cohorts</automated>
  </verify>
  <acceptance_criteria>
    - route.ts exports GET and POST
    - GET checks `searchParams.get('taught') === 'true'` and branches to getTaughtWeeks
    - POST uses zod schema and calls createWeek
    - Both handlers check trainer auth before DB access
  </acceptance_criteria>
  <done>Type check passes, lint clean, auth guard present, both handlers exported.</done>
</task>

<task type="auto">
  <name>Task 3: Create per-week API route (update + delete)</name>
  <files>src/app/api/cohorts/[id]/curriculum/[weekId]/route.ts</files>
  <read_first>src/app/api/cohorts/[id]/curriculum/route.ts, src/lib/curriculumService.ts</read_first>
  <action>Create `PATCH` and `DELETE` handlers at `src/app/api/cohorts/[id]/curriculum/[weekId]/route.ts` (D-11, D-12). PATCH validates partial body with Zod (same fields as POST but all optional), calls `updateWeek`. DELETE calls `deleteWeek`. Both verify the week's `cohortId` matches the `[id]` route param before mutation (prevents cross-cohort tampering) — return 404 if mismatch or not found. Guard with trainer auth (D-13). PATCH returns 200 with updated row. DELETE returns 204 no content.</action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; curl -fsS -X DELETE http://localhost:3000/api/cohorts/0/curriculum/0 -o /dev/null -w "%{http_code}" | grep -qE "401|404"</automated>
  </verify>
  <acceptance_criteria>
    - route.ts exports PATCH and DELETE
    - Both verify week.cohortId matches params.id before mutation
    - Unauthenticated requests return 401
  </acceptance_criteria>
  <done>Type check passes, auth guard present, cross-cohort mismatch returns 404.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test -- curriculumService` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run lint` clean for modified files
- [ ] Manual smoke: `curl -X POST /api/cohorts/1/curriculum` with valid trainer cookie creates a week; `curl '/api/cohorts/1/curriculum?taught=true'` returns only taught rows
</verification>

<success_criteria>
- All 4 endpoints implemented and auth-guarded
- curriculumService has unit test coverage on taught-weeks filter
- CURRIC-01 API surface complete and consumable by Plan 02 (UI) and Plan 03 (wizard)
</success_criteria>

<output>
After completion, create `.planning/phases/13-curriculum-schedule/13-01-SUMMARY.md`
</output>
