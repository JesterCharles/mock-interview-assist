---
phase: 13-curriculum-schedule
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/lib/curriculumFilter.ts
  - src/lib/curriculumFilter.test.ts
  - src/app/dashboard/page.tsx
  - src/components/dashboard/CurriculumFilterBadge.tsx
  - tests/e2e/setup-wizard-curriculum.spec.ts
autonomous: false
requirements: [CURRIC-02]
must_haves:
  truths:
    - "Setup wizard fetches curriculum and GitHub techs in parallel via Promise.all"
    - "When associate has a cohort with curriculum, tech list is filtered to taught skills via exact (case-insensitive) skillSlug match — no substring matching (Codex finding #9)"
    - "Adaptive gap-based weights apply to the filtered tech list"
    - "If no cohort or no curriculum, wizard shows full tech list (v1.0 behavior, no regression)"
    - "Filter badge displays N taught skills when filter is active"
    - "Wizard load time is measured and logged; <400ms is a TARGET not a gate (Codex finding #7) — assertion ceiling is <2000ms as sanity check only"
  artifacts:
    - path: "src/lib/curriculumFilter.ts"
      provides: "Pure function filtering GitHubFile[] by taught skillSlugs via exact match"
      contains: "filterTechsByCurriculum"
    - path: "src/components/dashboard/CurriculumFilterBadge.tsx"
      provides: "Badge UI showing N taught skills with dropdown list (uses skillName for display)"
    - path: "tests/e2e/setup-wizard-curriculum.spec.ts"
      provides: "E2E test verifying filter behavior + advisory perf logging"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "/api/cohorts/[id]/curriculum?taught=true"
      via: "fetch in Promise.all alongside GitHub fetch"
      pattern: "Promise\\.all[\\s\\S]*curriculum\\?taught=true"
    - from: "src/app/dashboard/page.tsx"
      to: "src/lib/curriculumFilter.ts"
      via: "filter availableTechs via exact skillSlug match before setState"
      pattern: "filterTechsByCurriculum"
    - from: "src/app/dashboard/page.tsx"
      to: "src/lib/adaptiveSetup.ts"
      via: "mapGapScoresToWeights called on filtered set"
      pattern: "mapGapScoresToWeights"
---

<objective>
Wire the curriculum filter into the dashboard setup wizard. Matching is canonical `skillSlug` EXACT (case-insensitive) comparison — no substring matching (Codex finding #9). The `<400ms` wizard load is an advisory target, not a release gate (Codex finding #7).

Purpose: Closes the loop — trainer-defined curriculum shapes what questions associates get. This is the user-visible payoff of Phase 13. Matching is deterministic and auditable.
Output: Pure filter function + test using skillSlug exact match, wizard integration with parallel fetch, filter badge UI, and Playwright E2E asserting filter correctness + logging perf (advisory).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-curriculum-schedule/13-CONTEXT.md
@.planning/phases/13-curriculum-schedule/13-01-SUMMARY.md
@src/app/dashboard/page.tsx
@src/lib/adaptiveSetup.ts
@src/lib/github-service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build curriculumFilter pure function with exact skillSlug matching</name>
  <files>src/lib/curriculumFilter.ts, src/lib/curriculumFilter.test.ts</files>
  <read_first>src/lib/github-service.ts, src/lib/adaptiveSetup.ts, .planning/phases/13-curriculum-schedule/13-CONTEXT.md</read_first>
  <behavior>
    - filterTechsByCurriculum(techs, taughtSlugs): returns techs whose path's first segment equals (case-insensitive) any taughtSlug — EXACT match only (Codex #9)
    - Empty taughtSlugs → returns ALL techs (fallback per D-17)
    - No matches → empty array
    - filterGapScoresByCurriculum(scores, taughtSlugs): filters gap scores similarly (D-22) — assumes gap-score skill field is the same canonical slug OR add a `normalizeSkill` helper
    - Case-insensitive matching (D-15 revised)
    - Test: "react/question-bank-v1.md" with taughtSlugs=["react"] → included
    - Test: "react-native/..." with taughtSlugs=["react"] → EXCLUDED (substring no longer matches — Codex #9)
    - Test: "postgresql/..." with taughtSlugs=["sql"] → EXCLUDED
    - Test: "node/..." with taughtSlugs=["node"] → included; taughtSlugs=["nodejs"] → EXCLUDED
    - Test: Empty taughtSlugs returns full input
    - Test: Case-insensitive — "React/" with taughtSlugs=["react"] → included
  </behavior>
  <action>
    Create pure function `filterTechsByCurriculum(techs: GitHubFile[], taughtSlugs: string[]): GitHubFile[]`.

    Matching (REVISED per Codex #9):
    ```ts
    const normalizedTaught = new Set(taughtSlugs.map(s => s.toLowerCase()));
    return techs.filter(tech => {
      const firstSegment = tech.path.split('/')[0].toLowerCase();
      return normalizedTaught.has(firstSegment);  // EXACT match, no substring
    });
    ```

    If `taughtSlugs.length === 0` → return `techs` unchanged (fallback, D-17).

    Also export `filterGapScoresByCurriculum(scores: SkillGapScore[], taughtSlugs: string[]): SkillGapScore[]` using same exact-match rule.

    Vitest covers all boundary cases — critically the negative cases (`react-native` excluded when taught `react`; `postgresql` excluded when taught `sql`).
  </action>
  <verify>
    <automated>npm run test -- src/lib/curriculumFilter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - curriculumFilter.ts exports filterTechsByCurriculum and filterGapScoresByCurriculum
    - Matching uses a Set lookup or equivalent exact-match (no `.includes()` substring call)
    - Empty taughtSlugs returns all (grep: `taughtSlugs.length === 0` or equivalent)
    - All tests pass including negative substring-ambiguity cases
  </acceptance_criteria>
  <done>Tests green, exact-match semantics proven, Codex finding #9 closed at the filter layer.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate curriculum fetch + filter + badge into dashboard wizard</name>
  <files>src/app/dashboard/page.tsx, src/components/dashboard/CurriculumFilterBadge.tsx</files>
  <read_first>src/app/dashboard/page.tsx, src/lib/curriculumFilter.ts, src/lib/adaptiveSetup.ts, .planning/phases/13-curriculum-schedule/13-CONTEXT.md</read_first>
  <action>
    Modify the setup wizard flow in `src/app/dashboard/page.tsx`:

    1. Look up associate's `cohortId` after slug validation.

    2. Wrap existing tech-fetch in `Promise.all`: GitHub techs AND curriculum (`/api/cohorts/{cohortId}/curriculum?taught=true`) when `cohortId` present (D-14). If no cohortId, skip curriculum fetch (D-17).

    3. Extract taught slugs from the curriculum response: `taughtSlugs = weeks.map(w => w.skillSlug)` — NOT `w.skillName` (Codex #9). Call `filterTechsByCurriculum(githubTechs, taughtSlugs)`.

    4. Filter gap scores via `filterGapScoresByCurriculum(scores, taughtSlugs)` BEFORE calling `mapGapScoresToWeights` (D-16, D-22).

    5. Fallback: try/catch around curriculum fetch. On failure or empty taughtSlugs → log `console.warn`, do NOT filter.

    6. Create `CurriculumFilterBadge` component: shows "Filtered by cohort curriculum (N skills)" when `taughtWeeks.length > 0`. Dropdown lists week names (display-only: uses `skillName`) ordered by `weekNumber`. Hidden when filter inactive. DESIGN.md tokens.

    7. Render badge near tech list header.

    CRITICAL: do NOT alter v1.0 flow when no cohort. Preserve existing Promise.all pattern.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/page.tsx contains a Promise.all that includes curriculum fetch when cohortId present
    - filterTechsByCurriculum is called with `taughtSlugs` derived from `w.skillSlug` (not `w.skillName`)
    - filterGapScoresByCurriculum is called before mapGapScoresToWeights
    - CurriculumFilterBadge renders only when taughtWeeks.length > 0
    - try/catch around curriculum fetch with console.warn on failure
  </acceptance_criteria>
  <done>Build green, wizard works for associates with and without cohorts, badge visible when filter active, matching is canonical.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright E2E — filter correctness + advisory perf logging</name>
  <files>tests/e2e/setup-wizard-curriculum.spec.ts</files>
  <read_first>playwright.config.ts (if exists), tests/e2e/ (for existing patterns), .planning/phases/13-curriculum-schedule/13-CONTEXT.md</read_first>
  <action>
    Write Playwright specs (D-21 revised per Codex #7):

    1. "no cohort shows full tech list" — associate with no cohortId, assert full GitHub list visible, no badge.

    2. "cohort with curriculum filters to taught skillSlugs only" — seed cohort with 2 taught weeks (skillSlug: "react", "typescript") and 1 future week (skillSlug: "rust"). Assert visible techs first-segment in {"react", "typescript"}, exclude "rust". Assert badge shows "2".

    3. "substring-like slugs are NOT matched" (NEW per Codex #9) — seed cohort with taughtSlugs=["react"]. Include in GitHub response a tech with path starting "react-native/". Assert that tech is EXCLUDED. Proves exact-match semantics hold end-to-end.

    4. "wizard load time is logged" (REVISED per Codex #7) — measure wizard mount → tech list render via `performance.now()` + testid marker. LOG the elapsed ms to the test output. ASSERT `elapsed < 2000` as a sanity ceiling (not the <400ms perf gate — that's deferred until a cached manifest exists). Comment in the spec: "Per Codex finding #7, <400ms is a target not a gate. This assertion is a loose ceiling."

    Setup: `tests/e2e/fixtures/seed-curriculum.ts` helper seeds cohort + weeks + associate via Prisma and tears down after.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/setup-wizard-curriculum.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Four specs exist and pass
    - Perf spec asserts `< 2000` (not 400) and logs actual elapsed
    - Substring-exclusion spec (react-native excluded when react taught) passes
    - Fixture cleans up seeded data
  </acceptance_criteria>
  <done>All four specs green locally; canonical-matching and advisory-perf posture encoded in tests.</done>
</task>

<task type="auto">
  <name>Task 4: Start dev server for final verification</name>
  <files></files>
  <action>Run `npm run dev` in background, wait for "ready".</action>
  <verify>
    <automated>curl -fsS http://localhost:3000 -o /dev/null -w "%{http_code}" | grep -q 200</automated>
  </verify>
  <done>Dev server up.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Curriculum-filtered setup wizard using canonical skillSlug matching — dev server at http://localhost:3000</what-built>
  <how-to-verify>
    1. Log in as trainer. Visit `/dashboard`.
    2. Enter an associate slug with NO cohort — full tech list, no badge, v1.0 behavior preserved.
    3. Enter an associate slug in a cohort with curriculum weeks (taughtSlugs include "react"). Verify:
       - Tech list filtered to techs whose path first segment matches a taught slug EXACTLY
       - IF your question bank has both `react/` and `react-native/` folders, ONLY `react/` is visible (substring ambiguity gone)
       - Filter badge shows skill count; click expands dropdown with display names
       - Adaptive weights compose on filtered set
    4. Add a future-dated week; reload — NOT visible.
    5. Delete all curriculum weeks; reload — full list returns.
    6. DevTools Network: curriculum + GitHub fetches overlap (parallel).
    7. DevTools Performance: note the elapsed time from dashboard mount to tech list render. It MAY be > 400ms depending on GitHub latency — this is expected per Codex finding #7. The <400ms figure is a future target, not a current gate.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npm run test -- curriculumFilter` passes
- [ ] `npx playwright test tests/e2e/setup-wizard-curriculum.spec.ts` passes (including substring-exclusion spec)
- [ ] `npm run build` succeeds
- [ ] No cohort → full tech list (no regression)
- [ ] Cohort with curriculum → exact-slug filtered list + badge
- [ ] Adaptive weights compose correctly on filtered set
- [ ] Advisory perf log emitted; no hard <400ms assertion
</verification>

<success_criteria>
- All tasks complete and user-approved
- CURRIC-02 requirement met: setup wizard filters by taught curriculum via canonical skillSlug, composes with adaptive weights, fallback preserves v1.0 behavior
- Codex finding #7 addressed: <400ms downgraded to target; test is advisory
- Codex finding #9 addressed: canonical skillSlug exact-match replaces fragile substring matching
</success_criteria>

<output>
After completion, create `.planning/phases/13-curriculum-schedule/13-03-SUMMARY.md`
</output>
