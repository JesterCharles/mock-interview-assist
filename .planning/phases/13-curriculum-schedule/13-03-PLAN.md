---
phase: 13-curriculum-schedule
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/lib/curriculumFilter.ts
  - src/lib/curriculumFilter.test.ts
  - src/app/dashboard/page.tsx
  - src/components/dashboard/CurriculumFilterBadge.tsx
  - tests/e2e/setup-wizard-curriculum.spec.ts
autonomous: false
requirements: [CURRIC-02]
must_haves:
  truths:
    - "Setup wizard fetches curriculum and GitHub techs in parallel via Promise.all"
    - "When associate has a cohort with curriculum, tech list is filtered to taught skills only"
    - "Adaptive gap-based weights apply to the filtered tech list"
    - "If no cohort or no curriculum, wizard shows full tech list (v1.0 behavior, no regression)"
    - "Filter badge displays N taught skills when filter is active"
    - "Wizard renders in under 400ms with filter active"
  artifacts:
    - path: "src/lib/curriculumFilter.ts"
      provides: "Pure function filtering GitHubFile[] by taught skill names"
      contains: "filterTechsByCurriculum"
    - path: "src/components/dashboard/CurriculumFilterBadge.tsx"
      provides: "Badge UI showing N taught skills with dropdown list"
    - path: "tests/e2e/setup-wizard-curriculum.spec.ts"
      provides: "E2E test verifying filter behavior and <400ms perf budget"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "/api/cohorts/[id]/curriculum?taught=true"
      via: "fetch in Promise.all alongside GitHub fetch"
      pattern: "Promise\\.all[\\s\\S]*curriculum\\?taught=true"
    - from: "src/app/dashboard/page.tsx"
      to: "src/lib/curriculumFilter.ts"
      via: "filter availableTechs before setState"
      pattern: "filterTechsByCurriculum"
    - from: "src/app/dashboard/page.tsx"
      to: "src/lib/adaptiveSetup.ts"
      via: "mapGapScoresToWeights called on filtered set"
      pattern: "mapGapScoresToWeights"
---

<objective>
Wire the curriculum filter into the dashboard setup wizard, composing with the existing adaptive gap weights, and verify the <400ms perf budget (CURRIC-02).

Purpose: Closes the loop — trainer-defined curriculum now automatically shapes what questions associates get. This is the user-visible payoff of Phase 13.
Output: Pure filter function + test, wizard integration with parallel fetch, filter badge UI, and Playwright E2E asserting filter + performance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-curriculum-schedule/13-CONTEXT.md
@.planning/phases/13-curriculum-schedule/13-01-SUMMARY.md
@src/app/dashboard/page.tsx
@src/lib/adaptiveSetup.ts
@src/lib/github-service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build curriculumFilter pure function with tests</name>
  <files>src/lib/curriculumFilter.ts, src/lib/curriculumFilter.test.ts</files>
  <read_first>src/lib/github-service.ts, src/lib/adaptiveSetup.ts, .planning/phases/13-curriculum-schedule/13-CONTEXT.md</read_first>
  <behavior>
    - filterTechsByCurriculum(techs, taughtSkillNames): returns techs whose path's first segment (case-insensitive) matches any taughtSkillName
    - Empty taughtSkillNames → returns ALL techs (fallback behavior per D-17)
    - No matches → returns empty array
    - filterGapScoresByCurriculum(scores, taughtSkillNames): filters gap scores similarly (D-22)
    - Case-insensitive matching (D-15)
    - Test: "react/question-bank-v1.md" matches taught skill "React"
    - Test: Empty taughtSkillNames returns full input (fallback)
    - Test: No match returns []
  </behavior>
  <action>Create pure function `filterTechsByCurriculum(techs: GitHubFile[], taughtSkillNames: string[]): GitHubFile[]`. Matching: `tech.path.split('/')[0].toLowerCase()` compared to `taughtSkillNames.map(s => s.toLowerCase())` via `includes` or substring match (D-15). If `taughtSkillNames` is empty array, return all techs unchanged (fallback, D-17). Also export `filterGapScoresByCurriculum(scores: SkillGapScore[], taughtSkillNames: string[]): SkillGapScore[]` using same rule on `scores[].skill` (D-22). Vitest covers boundary cases above.</action>
  <verify>
    <automated>npm run test -- src/lib/curriculumFilter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - curriculumFilter.ts exports filterTechsByCurriculum and filterGapScoresByCurriculum
    - Empty taughtSkillNames → returns all (grep: "taughtSkillNames.length === 0" or equivalent)
    - Tests pass including case-insensitive match
  </acceptance_criteria>
  <done>Tests green, both functions pure and typed.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate curriculum fetch + filter + badge into dashboard wizard</name>
  <files>src/app/dashboard/page.tsx, src/components/dashboard/CurriculumFilterBadge.tsx</files>
  <read_first>src/app/dashboard/page.tsx, src/lib/curriculumFilter.ts, src/lib/adaptiveSetup.ts, .planning/phases/13-curriculum-schedule/13-CONTEXT.md</read_first>
  <action>
    Modify the setup wizard flow in `src/app/dashboard/page.tsx`:

    1. After associate slug is entered/validated, look up associate's `cohortId`. Extend the existing `/api/associates/[slug]/gap-scores` response OR add a parallel fetch to `/api/associates/[slug]` that returns `{ cohortId }`. Pick the simpler path: if gap-scores endpoint already returns associate metadata, extend it to include cohortId; otherwise add cohortId to its response (server-side one-line change in that route).

    2. Wrap the existing tech-fetch flow in `Promise.all`: fetch GitHub techs AND curriculum (`/api/cohorts/{cohortId}/curriculum?taught=true`) in parallel when `cohortId` is present (D-14, D-19). If no cohortId, skip curriculum fetch entirely — existing flow unchanged (D-17).

    3. After both resolve, call `filterTechsByCurriculum(githubTechs, taughtSkillNames)` to produce the displayed tech list. Store `taughtWeeks` in state for the badge.

    4. Adapt the existing gap-weight pre-population: filter gap scores via `filterGapScoresByCurriculum` BEFORE calling `mapGapScoresToWeights` (D-16, D-22). Order in code must be: curriculum filter → adaptive weights.

    5. Fallback (D-17): wrap curriculum fetch in try/catch. On fetch failure or empty taught weeks → log `console.warn`, do NOT filter, full list shown.

    6. Create `CurriculumFilterBadge` component: shows "Filtered by cohort curriculum (N skills)" when `taughtWeeks.length > 0`. Click expands a dropdown listing week names ordered by weekNumber (D-18). Hidden entirely when filter inactive. DESIGN.md tokens.

    7. Render the badge near the tech list header.

    CRITICAL: do NOT alter the v1.0 flow when no cohort — all existing behavior must persist. Read the existing file carefully before editing; preserve the Promise.all pattern already in use for gap scores + GitHub fetch.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/page.tsx contains a Promise.all that includes curriculum fetch when cohortId present
    - filterTechsByCurriculum is called before setting availableTechs state
    - filterGapScoresByCurriculum is called before mapGapScoresToWeights
    - CurriculumFilterBadge renders only when taughtWeeks.length > 0
    - try/catch around curriculum fetch with console.warn on failure
  </acceptance_criteria>
  <done>Build green, wizard works for associates with and without cohorts, badge visible when filter active.</done>
</task>

<task type="auto">
  <name>Task 3: Write Playwright E2E covering filter behavior and &lt;400ms perf budget</name>
  <files>tests/e2e/setup-wizard-curriculum.spec.ts</files>
  <read_first>playwright.config.ts (if exists), tests/e2e/ (for existing patterns), .planning/phases/13-curriculum-schedule/13-CONTEXT.md</read_first>
  <action>
    Write three Playwright specs (D-21):

    1. "no cohort shows full tech list" — log in as trainer, enter associate slug for associate with no cohortId, advance to tech selection, assert tech count matches full GitHub list (or > 5 as sanity).

    2. "cohort with curriculum filters to taught skills only" — seed a test cohort with 2 taught weeks (react, typescript) and 1 future week (rust). Enter associate slug assigned to this cohort. Assert visible techs include react + typescript, exclude rust. Assert CurriculumFilterBadge visible with text containing "2".

    3. "wizard loads in under 400ms with filter active" (D-19) — measure `performance.now()` from navigation to tech-list-render. Use Playwright's `page.evaluate` + a testid marker on the tech list container. Assert elapsed < 400. If seeded fixture not practical, mock the curriculum and github API responses via `page.route` to isolate client-side filter+render cost.

    Setup: create or reuse a `tests/e2e/fixtures/seed-curriculum.ts` helper that inserts a cohort + weeks + associate via Prisma, and a teardown that deletes them.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/setup-wizard-curriculum.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Three specs exist and pass
    - Perf spec asserts elapsed < 400ms
    - Fixture cleans up seeded data after run
  </acceptance_criteria>
  <done>All three specs green locally.</done>
</task>

<task type="auto">
  <name>Task 4: Start dev server for final verification</name>
  <files></files>
  <action>Run `npm run dev` in background, wait for "ready".</action>
  <verify>
    <automated>curl -fsS http://localhost:3000 -o /dev/null -w "%{http_code}" | grep -q 200</automated>
  </verify>
  <done>Dev server up.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Curriculum-filtered setup wizard — server at http://localhost:3000</what-built>
  <how-to-verify>
    1. Log in as trainer. Visit `/dashboard`.
    2. Enter an associate slug who has NO cohort — verify full tech list renders, no filter badge, wizard behaves exactly as before (v1.0 regression check).
    3. Enter an associate slug assigned to a cohort with curriculum weeks defined in Plan 02 — verify:
       - Tech list is filtered to only weeks where startDate <= today
       - Filter badge appears: "Filtered by cohort curriculum (N skills)"
       - Clicking badge expands dropdown listing week names
       - Adaptive weights (if associate has 3+ sessions) pre-populate only filtered techs
    4. Add a future-dated week to the cohort's curriculum, reload wizard — confirm it does NOT appear.
    5. Delete all curriculum weeks for the cohort, reload — confirm full list returns (fallback).
    6. DevTools Network tab: confirm curriculum and GitHub fetches overlap in time (parallel, not serial).
    7. DevTools Performance: time from dashboard mount to tech list render < 400ms.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npm run test -- curriculumFilter` passes
- [ ] `npx playwright test tests/e2e/setup-wizard-curriculum.spec.ts` passes (including perf assertion)
- [ ] `npm run build` succeeds
- [ ] No cohort → full tech list (no regression)
- [ ] Cohort with curriculum → filtered list + badge
- [ ] Adaptive weights compose correctly on filtered set
- [ ] Curriculum + GitHub fetches run in parallel (DevTools verified)
</verification>

<success_criteria>
- All tasks complete and user-approved
- CURRIC-02 requirement met: setup wizard filters by taught curriculum, composes with adaptive weights, fallback preserves v1.0 behavior, <400ms budget holds
</success_criteria>

<output>
After completion, create `.planning/phases/13-curriculum-schedule/13-03-SUMMARY.md`
</output>
