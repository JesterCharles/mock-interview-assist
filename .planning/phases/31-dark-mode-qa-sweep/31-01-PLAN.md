---
phase: 31-dark-mode-qa-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/trainer/GapTrendChart.tsx
  - src/components/trainer/CohortSummaryBar.tsx
  - src/components/trainer/SessionHistoryList.tsx
  - src/components/trainer/RosterTable.tsx
  - src/components/trainer/CurriculumWeekRow.tsx
  - src/components/trainer/AddCurriculumWeekForm.tsx
  - src/components/trainer/CalibrationView.tsx
  - src/components/trainer/SkillFilterDropdown.tsx
  - src/components/trainer/ReadinessDisplay.tsx
  - src/components/trainer/CohortFilterBar.tsx
  - src/components/trainer/CurriculumTable.tsx
  - src/components/interview/AuthenticatedInterviewClient.tsx
  - src/components/dashboard/CurriculumFilterBadge.tsx
  - src/app/page.tsx
  - src/app/interview/new/page.tsx
  - src/app/review/page.tsx
  - src/app/pdf/page.tsx
  - src/app/question-banks/page.tsx
  - src/app/trainer/(dashboard)/page.tsx
  - src/app/trainer/(dashboard)/loading.tsx
  - src/app/trainer/(dashboard)/[slug]/page.tsx
  - src/app/trainer/(dashboard)/[slug]/loading.tsx
  - src/app/trainer/(dashboard)/[slug]/AssociateCohortSelect.tsx
  - src/app/trainer/(dashboard)/reports/page.tsx
  - src/app/trainer/onboarding/EmailChipInput.tsx
  - src/app/trainer/onboarding/BulkPreviewTable.tsx
  - src/app/trainer/onboarding/BulkResultTable.tsx
  - src/app/trainer/onboarding/page.tsx
autonomous: false
requirements:
  - DARK-01
  - DARK-02

must_haves:
  truths:
    - "Switching to dark mode on any page produces no parchment-white backgrounds, no dark-on-dark text, and no invisible borders"
    - "All recharts components use CSS variable tokens for stroke, fill, and tooltip styling"
    - "No component file contains a hardcoded hex color that has a design token equivalent"
    - "Semantic badge backgrounds (success/warning/danger) switch to dark variants automatically"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Semantic badge background tokens (--success-bg, --warning-bg, --danger-bg) with light and dark pairs"
      contains: "--success-bg"
    - path: "src/components/trainer/GapTrendChart.tsx"
      provides: "Chart component using --chart-* and --accent tokens instead of TOPIC_COLORS hex array"
      min_lines: 30
  key_links:
    - from: "src/app/globals.css"
      to: "[data-theme='dark']"
      via: "CSS custom property overrides"
      pattern: "--success-bg|--warning-bg|--danger-bg"
    - from: "src/components/trainer/GapTrendChart.tsx"
      to: "src/app/globals.css"
      via: "var(--chart-*) token references"
      pattern: "var\\(--chart-"
---

<objective>
Replace every hardcoded hex color and light-only inline style across all component and page files with CSS variable token references. Add missing semantic badge background tokens to globals.css with dark mode pairs.

Purpose: Complete dark mode support so toggling theme produces correct colors on every surface. This is the final polish pass after Phases 26-30 build all new surfaces.
Output: All .tsx files use `var(--token)` references; globals.css has badge-bg tokens; `npm run build` passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@src/app/globals.css

IMPORTANT: PDFReport.tsx uses @react-pdf/renderer which cannot resolve CSS custom properties. It is the ONE acceptable exception — do not modify its hardcoded hex values.

The hardcoded hex values found map to existing tokens as follows:

| Hex | Token |
|-----|-------|
| `#F5F0E8` | `var(--bg)` |
| `#FFFFFF` | `var(--surface)` |
| `#F0EBE2` | `var(--surface-muted)` |
| `#1A1A1A` | `var(--ink)` |
| `#7A7267` | `var(--muted)` |
| `#C85A2E` | `var(--accent)` |
| `#B04E27` | `var(--accent-hover)` |
| `#2D6A4F` | `var(--success)` |
| `#B7791F` | `var(--warning)` |
| `#B83B2E` | `var(--danger)` |
| `#DDD5C8` | `var(--border)` |
| `#E8E2D9` | `var(--border-subtle)` |
| `#FFF8F0` | `var(--highlight)` |
| `#C9C2B8` | `var(--border)` (close enough) |
| `#E8F5EE` | `var(--success-bg)` (NEW — must create in Task 1) |
| `#FEF3E0` / `#FFF1E6` | `var(--warning-bg)` (NEW) |
| `#FDECEB` | `var(--danger-bg)` (NEW) |

For `#FFFFFF` used as button text on colored backgrounds (e.g., `color: '#FFFFFF'` on accent buttons), use the literal string `'white'` or `'#fff'` — these are intentionally always white regardless of theme. Same for `::selection` color in globals.css.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add semantic badge-background tokens to globals.css</name>
  <files>src/app/globals.css</files>
  <action>
Add three new semantic badge-background tokens to the `:root` block (after the existing `--highlight` line) and their dark-mode equivalents to the `[data-theme="dark"]` block. Also add corresponding Tailwind `@theme inline` mappings.

New tokens (per DESIGN.md §Semantic Badge Colors):

**Light (:root):**
- `--success-bg: #E8F5EE;`
- `--warning-bg: #FEF3E0;`
- `--danger-bg: #FDECEB;`

**Dark ([data-theme="dark"]):**
- `--success-bg: #1A2E24;`
- `--warning-bg: #2E2518;`
- `--danger-bg: #2E1C1A;`

**@theme inline additions:**
- `--color-success-bg: var(--success-bg);`
- `--color-warning-bg: var(--warning-bg);`
- `--color-danger-bg: var(--danger-bg);`

Do NOT modify any existing tokens. Only add the three new ones in each section.
  </action>
  <verify>
    <automated>grep -c "success-bg\|warning-bg\|danger-bg" src/app/globals.css | grep -q "9" && echo "PASS: 9 badge token references found (3 root + 3 dark + 3 theme)" || echo "FAIL"</automated>
  </verify>
  <done>globals.css contains --success-bg, --warning-bg, --danger-bg in :root, [data-theme="dark"], and @theme inline blocks. `npm run build` still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded hex in trainer components and chart components (per D-01, D-03, D-04)</name>
  <files>
    src/components/trainer/GapTrendChart.tsx,
    src/components/trainer/CohortSummaryBar.tsx,
    src/components/trainer/SessionHistoryList.tsx,
    src/components/trainer/RosterTable.tsx,
    src/components/trainer/CurriculumWeekRow.tsx,
    src/components/trainer/AddCurriculumWeekForm.tsx,
    src/components/trainer/CalibrationView.tsx,
    src/components/trainer/SkillFilterDropdown.tsx,
    src/components/trainer/ReadinessDisplay.tsx,
    src/components/trainer/CohortFilterBar.tsx,
    src/components/trainer/CurriculumTable.tsx
  </files>
  <action>
For each file listed, replace every hardcoded hex value in inline `style={{}}` objects with the corresponding `var(--token)` reference using the mapping table in the context section above.

**GapTrendChart.tsx (D-04 — known offender):**
- Replace `TOPIC_COLORS = ['#2D6A4F', '#B7791F', '#7A7267', '#C85A2E']` with `TOPIC_COLORS = ['var(--chart-1)', 'var(--chart-2)', 'var(--chart-4)', 'var(--accent)']`. Note: chart-1 maps to success/sage, chart-2 maps to warning/amber, chart-4 maps to warm taupe (replaces muted), accent stays accent.
- Replace `stroke="#DDD5C8"` on CartesianGrid with `stroke="var(--border)"` (per DESIGN.md axis convention, should actually be `var(--border-subtle)` for grid lines — use `var(--border-subtle)`).
- Replace `fill: '#7A7267'` on axis ticks with `fill: 'var(--muted)'`.
- Replace any remaining hardcoded `#1A1A1A` with `var(--ink)`, `#7A7267` with `var(--muted)`.
- Add tooltip `contentStyle` per DESIGN.md chart tooltip convention if not already present.

**All other trainer components:**
Apply the hex-to-token mapping mechanically:
- `'#1A1A1A'` → `'var(--ink)'`
- `'#7A7267'` → `'var(--muted)'`
- `'#FFFFFF'` (as backgroundColor) → `'var(--surface)'`
- `'#FFFFFF'` (as text color on accent bg) → leave as `'white'` (intentional)
- `'#DDD5C8'` → `'var(--border)'`
- `'#E8E2D9'` → `'var(--border-subtle)'`
- `'#F0EBE2'` → `'var(--surface-muted)'`
- `'#FFF8F0'` → `'var(--highlight)'`
- `'#C85A2E'` → `'var(--accent)'`
- `'#B83B2E'` → `'var(--danger)'`
- `'#2D6A4F'` → `'var(--success)'`
- `'#B7791F'` → `'var(--warning)'`
- `'#E8F5EE'` → `'var(--success-bg)'`
- `'#FEF3E0'` / `'#FFF1E6'` → `'var(--warning-bg)'`
- `'#FDECEB'` → `'var(--danger-bg)'`

For `CohortFilterBar.tsx` event handlers that set `style.borderColor` and `style.boxShadow` imperatively: replace `'#C85A2E'` with `'var(--accent)'`, `'#DDD5C8'` with `'var(--border)'`, and the rgba focus ring `rgba(200, 90, 46, 0.15)` with `'var(--chart-highlight)'`.

Do NOT touch `boxShadow: '0 1px 2px rgba(0,0,0,0.04)'` patterns — those are subtle neutral shadows that work in both themes. Only replace hex values that map to design tokens.
  </action>
  <verify>
    <automated>! grep -rn '#[0-9A-Fa-f]\{3,8\}' src/components/trainer/*.tsx 2>/dev/null | grep -v 'PDFReport\|// comment\|\.test\.' | grep -v "^Binary" | head -5 | grep -q '#' && echo "PASS: No hardcoded hex in trainer components" || echo "FAIL: Hardcoded hex still present"</automated>
  </verify>
  <done>All 11 trainer component files use var(--token) references. GapTrendChart.tsx TOPIC_COLORS uses --chart-* tokens. Zero hardcoded hex values remain in src/components/trainer/ (excluding comments).</done>
</task>

<task type="auto">
  <name>Task 3: Replace hardcoded hex in app routes, remaining components, and loading skeletons (per D-01, D-02)</name>
  <files>
    src/app/page.tsx,
    src/app/interview/new/page.tsx,
    src/app/review/page.tsx,
    src/app/pdf/page.tsx,
    src/app/question-banks/page.tsx,
    src/app/trainer/(dashboard)/page.tsx,
    src/app/trainer/(dashboard)/loading.tsx,
    src/app/trainer/(dashboard)/[slug]/page.tsx,
    src/app/trainer/(dashboard)/[slug]/loading.tsx,
    src/app/trainer/(dashboard)/[slug]/AssociateCohortSelect.tsx,
    src/app/trainer/(dashboard)/reports/page.tsx,
    src/app/trainer/onboarding/EmailChipInput.tsx,
    src/app/trainer/onboarding/BulkPreviewTable.tsx,
    src/app/trainer/onboarding/BulkResultTable.tsx,
    src/app/trainer/onboarding/page.tsx,
    src/components/interview/AuthenticatedInterviewClient.tsx,
    src/components/dashboard/CurriculumFilterBadge.tsx
  </files>
  <action>
Apply the same hex-to-token mapping from Task 2 to all listed files.

**Special cases:**

**AuthenticatedInterviewClient.tsx:** Has a `THEME` const object with hardcoded hex. Replace each value with `var(--token)`:
```
bg: 'var(--bg)', surface: 'var(--surface)', ink: 'var(--ink)',
muted: 'var(--muted)', accent: 'var(--accent)', border: 'var(--border)',
surfaceMuted: 'var(--surface-muted)'
```

**CurriculumFilterBadge.tsx:** Already uses `var(--token, #fallback)` pattern with hardcoded fallbacks. Remove the fallback hex values — the tokens are defined in globals.css and do not need fallbacks. Change `var(--surface-muted, #2E2A27)` → `var(--surface-muted)`, etc.

**Loading skeletons** (`loading.tsx` files): Replace all hardcoded skeleton colors with token refs. `#E8E2D9` (shimmer bg) → `var(--border-subtle)`, `#F0EBE2` (shimmer highlight) → `var(--surface-muted)`, `#DDD5C8` → `var(--border)`, `#FFFFFF` → `var(--surface)`.

**reports/page.tsx:** Already partially uses `var(--token, #fallback)`. Strip the fallback hex. For `color: '#fff'` on accent buttons, keep as `'white'`.

**Badge backgrounds across all files:** `#E8F5EE` → `var(--success-bg)`, `#FEF3E0` → `var(--warning-bg)`, `#FDECEB` → `var(--danger-bg)`.

**src/app/page.tsx line 1257:** `rgba(26,26,26,0.6)` is a modal overlay — replace with `rgba(0,0,0,0.6)` which works in both themes (dark scrim).

**Do NOT modify:** `PDFReport.tsx` (uses @react-pdf/renderer, cannot resolve CSS vars). Neutral shadow `rgba(0,0,0,*)` values (work in both themes). `MobileSidebar.tsx` overlay `rgba(0,0,0,0.30)` (works in both themes). `AvatarMenu.tsx` role badge `rgba()` backgrounds (subtle tint, works acceptably in both themes).

After all replacements, run `npm run build` to confirm no broken references.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All app route and remaining component files use var(--token) references. `npm run build` passes. The only file with hardcoded hex is PDFReport.tsx (acceptable exception). No light-only Tailwind classes bypass the token system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced — this phase is purely cosmetic (CSS token replacements). No user input processing, API changes, or data flow modifications.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | CSS custom properties | accept | CSS vars are visible in DevTools by design; no secrets in color tokens |
</threat_model>

<verification>
1. `npm run build` passes with zero errors
2. `grep -rn '#[0-9A-Fa-f]{3,8}' src/components/trainer/*.tsx src/app/**/*.tsx` returns only PDFReport.tsx hits and neutral rgba shadows
3. `grep -c 'var(--' src/components/trainer/GapTrendChart.tsx` shows chart tokens in use
4. Toggle dark mode on `/trainer`, `/trainer/[slug]`, `/interview/new`, `/`, `/question-banks` — no parchment-white backgrounds or invisible text
</verification>

<success_criteria>
- Zero hardcoded hex colors in any .tsx file except PDFReport.tsx
- All recharts components (GapTrendChart + any new Phase 29 charts) use var(--chart-*) tokens
- Dark mode toggle on every route shows correct theme-aware colors
- `npm run build` passes
- globals.css has --success-bg, --warning-bg, --danger-bg with light+dark pairs
</success_criteria>

<output>
After completion, create `.planning/phases/31-dark-mode-qa-sweep/31-01-SUMMARY.md`
</output>
