---
phase: 32-shell-overhaul
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - src/components/shell/AvatarMenu.tsx
  - src/components/shell/ProfileModal.tsx
  - src/app/profile/ProfileTabs.tsx
  - src/app/profile/page.tsx
  - src/components/shell/LandingHeader.tsx
  - src/app/page.tsx
  - src/middleware.ts
autonomous: true
requirements: [SHELL-32-04, SHELL-32-05, SHELL-32-06]

must_haves:
  truths:
    - "Clicking Profile in avatar menu opens a centered modal overlay with ProfileTabs content"
    - "Profile modal renders profile, security, and learning tabs with fetched data"
    - "Landing page shows a minimal header with NLM logo and Sign In button"
    - "/profile route redirects to home — profile is modal-only"
  artifacts:
    - path: "src/components/shell/ProfileModal.tsx"
      provides: "Radix Dialog wrapper that fetches profile data and renders ProfileTabs content"
      contains: "Dialog.Root"
    - path: "src/components/shell/LandingHeader.tsx"
      provides: "Minimal landing page header with logo and sign-in button"
      contains: "LandingHeader"
    - path: "src/app/profile/page.tsx"
      provides: "Redirect from /profile to appropriate dashboard"
      contains: "redirect"
  key_links:
    - from: "src/components/shell/AvatarMenu.tsx"
      to: "src/components/shell/ProfileModal.tsx"
      via: "onOpenProfile callback triggers modal open state"
      pattern: "setProfileOpen"
    - from: "src/components/shell/ProfileModal.tsx"
      to: "/api/profile"
      via: "fetch on modal open"
      pattern: "fetch.*api/profile"
    - from: "src/app/page.tsx"
      to: "src/components/shell/LandingHeader.tsx"
      via: "import and render at top of landing page"
      pattern: "LandingHeader"
---

<objective>
Add Profile modal from avatar menu, landing page minimal header, and redirect /profile route.

Purpose: Per D-08, D-09, D-12, D-13 — Profile becomes a modal overlay (not a standalone page). Avatar menu triggers it. Landing page gets a minimal header for unauthenticated users. The /profile route redirects instead of rendering.
Output: ProfileModal component, LandingHeader component, updated AvatarMenu, redirecting /profile page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-shell-overhaul/32-CONTEXT.md
@.planning/phases/32-shell-overhaul/32-RESEARCH.md
@.planning/phases/32-shell-overhaul/32-01-SUMMARY.md
@DESIGN.md

<interfaces>
<!-- Existing interfaces executor needs -->

From src/app/profile/ProfileTabs.tsx:
```typescript
interface ProfileTabsProps {
  profile: { displayName: string | null; githubUsername: string | null; bio: string | null; learningGoals: string | null };
  email: string;
  role: string;
  readiness: { status: string; recommendedArea: string | null; score: number | null; sessionCount: number } | null;
}
type Tab = 'profile' | 'security' | 'learning';
```

From src/app/api/profile/route.ts GET response:
```typescript
// Returns:
{ profile: ProfileData, role: string, email: string, readiness: ReadinessData | null }
```

From src/components/shell/AvatarMenu.tsx:
```typescript
// Currently links to /profile with <a href="/profile">
// Needs to become modal trigger
```

From @radix-ui/react-dialog (already installed):
```typescript
import * as Dialog from '@radix-ui/react-dialog';
// Used by MobileSidebar — pattern already established
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile modal + AvatarMenu trigger + /profile redirect</name>
  <files>src/components/shell/ProfileModal.tsx, src/components/shell/AvatarMenu.tsx, src/app/profile/ProfileTabs.tsx, src/app/profile/page.tsx</files>
  <action>
**ProfileModal.tsx** — Create new component at `src/components/shell/ProfileModal.tsx`. Per D-12 and RESEARCH:

1. `'use client'` component. Props: `open: boolean`, `onClose: () => void`, `initialTab?: 'profile' | 'security' | 'learning'`.
2. Import `* as Dialog from '@radix-ui/react-dialog'` and `X` from `lucide-react`.
3. On mount (when `open` becomes true), fetch `GET /api/profile`. Store in state. Show skeleton while loading.
4. Render `Dialog.Root` with `open` and `onOpenChange` mapped to `onClose`.
5. Dialog.Overlay: `position: fixed`, `inset: 0`, `background: rgba(0,0,0,0.4)`, `zIndex: 60`.
6. Dialog.Content: `position: fixed`, `top: 50%`, `left: 50%`, `transform: translate(-50%, -50%)`, `width: 90vw`, `maxWidth: 640`, `maxHeight: 90vh`, `overflowY: auto`, `background: var(--surface)`, `border: 1px solid var(--border)`, `borderRadius: 12`, `zIndex: 61`, `padding: 0` (ProfileTabs handles internal padding).
7. Dialog.Close as X button top-right: `position: absolute`, `top: 16`, `right: 16`, `zIndex: 1`.
8. When data is loaded, render `ProfileTabs` directly (importing from `@/app/profile/ProfileTabs`), passing fetched profile, email, role, readiness data AND `initialTab` prop.
9. The Profile modal's fetch fires every time `open` transitions from false to true (re-fetch on each open to get fresh data).

**ProfileTabs.tsx** — Two changes per D-12:
1. Remove the outer wrapper div that has `minHeight: 100vh`, `display: flex`, `alignItems: flex-start`, `justifyContent: center`, `backgroundColor: var(--bg)`, `padding: 48px 16px` (lines 203-213). The inner card div (`maxWidth: 600`, `backgroundColor: var(--surface)`) is now the root element.
2. Add optional `initialTab?: Tab` prop to `ProfileTabsProps`. In the component, use it to set initial `activeTab` state: `useState<Tab>(initialTab ?? 'profile')`.
3. Since ProfileTabs no longer has its full-page wrapper, the `/profile` page redirect is safe.

**AvatarMenu.tsx** — Per D-08:
1. Add `profileOpen` state (`useState(false)`) and `profileInitialTab` state (`useState<'profile' | 'security' | 'learning'>('profile')`).
2. Replace the Profile `<a href="/profile">` with a `DropdownMenu.Item` that uses `onSelect={() => { setProfileInitialTab('profile'); setProfileOpen(true); }}`. Use `onSelect` NOT `onClick` (Radix fires `onSelect` after dropdown closes — prevents z-index conflicts).
3. Add a Settings item between Profile and Sign Out (per D-08 order: Profile, Settings, separator, Sign Out). Settings navigates to `/trainer/settings` for trainers or opens profile modal security tab for associates. Use `onSelect` with `router.push('/trainer/settings')` for trainer, or `setProfileInitialTab('security'); setProfileOpen(true)` for associate. Detect role from `user?.user_metadata?.role`.
4. Render `<ProfileModal open={profileOpen} onClose={() => setProfileOpen(false)} initialTab={profileInitialTab} />` after the DropdownMenu.Root.
5. Import `ProfileModal` from `./ProfileModal` and `useRouter` from `next/navigation`.

**page.tsx (profile)** — Per D-13:
1. Replace the entire server component body with a redirect. Import `redirect` from `next/navigation`. Import `getCallerIdentity` from `@/lib/identity`.
2. If anonymous: `redirect('/signin')`. If trainer/admin: `redirect('/trainer')`. If associate: `redirect('/associate/${identity.associateSlug}/dashboard')` or `redirect('/')` if no slug.
3. Remove all Prisma imports, profile data fetching, readiness computation. The file shrinks to ~15 lines.
4. Keep the middleware guard for `/profile` in `src/middleware.ts` — it already handles unauthenticated redirect. No middleware changes needed.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Avatar menu "Profile" opens modal overlay with ProfileTabs. Modal fetches profile data from /api/profile on open. ProfileTabs renders without full-page wrapper. /profile redirects to appropriate dashboard. Avatar menu order: email/role, separator, Profile, Settings, separator, Sign Out (per D-08).</done>
</task>

<task type="auto">
  <name>Task 2: Landing page minimal header</name>
  <files>src/components/shell/LandingHeader.tsx, src/app/page.tsx</files>
  <action>
**LandingHeader.tsx** — Per D-09, create new component at `src/components/shell/LandingHeader.tsx`:

1. `import Link from 'next/link'`.
2. Render a `<header>` with:
   - `height: 56` (matches TopBar), `position: sticky`, `top: 0`, `zIndex: 30`
   - `background: var(--surface-muted)`, `borderBottom: 1px solid var(--border)`
   - `display: flex`, `alignItems: center`, `justifyContent: space-between`
   - `padding: 0 16px`
3. Left: `<Link href="/">` with NLM wordmark text styled identically to TopBar wordmark:
   - `fontFamily: var(--font-display), "Clash Display", sans-serif`
   - `fontWeight: 500`, `fontSize: 16`, `color: var(--ink)`, `textDecoration: none`, `letterSpacing: -0.01em`
4. Right: `<Link href="/signin" className="btn-accent-flat" style={{ fontSize: 13, padding: '6px 16px' }}>Sign In</Link>`
5. No sidebar, no full shell. This is a standalone lightweight header.

**page.tsx (landing)** — Add `LandingHeader` to the top of the landing page:
1. Import `LandingHeader` from `@/components/shell/LandingHeader`.
2. Wrap the existing page content. Add `<LandingHeader />` as the first child, before the existing content. The landing page currently starts with its own layout div — insert LandingHeader above it.
3. Do NOT modify any existing landing page content or functionality. Pure additive.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Landing page renders a sticky 56px header with NLM wordmark on the left and "Sign In" button on the right. No sidebar. No full shell. Existing landing page content unchanged below the header.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/profile | Profile modal fetches user data; endpoint already auth-guarded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02 | Information Disclosure | ProfileModal fetching /api/profile | accept | API endpoint already checks `getCallerIdentity()` and returns 401 for anonymous. No new surface. |
| T-32-03 | Elevation of Privilege | /profile redirect exposing associate slug | accept | Redirect uses `identity.associateSlug` from authenticated session — no exposure to other users |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run lint` passes
3. Manual: Click avatar menu -> Profile opens modal with tabs
4. Manual: Close modal, navigate to /profile in URL bar -> redirects to dashboard
5. Manual: Visit landing page (/) -> shows NLM logo + Sign In header
</verification>

<success_criteria>
- Profile modal opens from avatar menu with all 3 tabs functional
- Profile modal fetches data from /api/profile on each open
- /profile route redirects to appropriate role-based dashboard
- Landing page has minimal sticky header with NLM wordmark and Sign In link
- Avatar menu order matches D-08: Profile, Settings, separator, Sign Out
</success_criteria>

<output>
After completion, create `.planning/phases/32-shell-overhaul/32-02-SUMMARY.md`
</output>
