---
phase: 32-shell-overhaul
plan: 03
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - src/components/trainer/RosterTable.tsx
  - src/app/trainer/(dashboard)/[slug]/page.tsx
autonomous: true
requirements: [SHELL-32-07, SHELL-32-08]

must_haves:
  truths:
    - "Roster table has no slug column"
    - "Trainer clicking a roster row sees the same dashboard view as associates (SkillCardList, FocusHero, charts, radar)"
  artifacts:
    - path: "src/components/trainer/RosterTable.tsx"
      provides: "Roster table without slug column"
    - path: "src/app/trainer/(dashboard)/[slug]/page.tsx"
      provides: "Trainer detail page rendering AssociateDashboardClient"
      contains: "AssociateDashboardClient"
  key_links:
    - from: "src/app/trainer/(dashboard)/[slug]/page.tsx"
      to: "src/app/associate/[slug]/dashboard/AssociateDashboardClient.tsx"
      via: "import and render with mapped data"
      pattern: "AssociateDashboardClient"
    - from: "src/app/trainer/(dashboard)/[slug]/page.tsx"
      to: "/api/trainer/[slug]"
      via: "fetch AssociateDetail and map to AssociateDashboardClient props"
      pattern: "fetch.*api/trainer"
---

<objective>
Remove slug column from roster and replace trainer associate detail view with AssociateDashboardClient.

Purpose: Per D-10, the slug column is removed from the roster table. Per D-11, the trainer associate detail page renders the same AssociateDashboardClient component that associates see, giving trainers an identical dashboard view.
Output: Cleaned-up roster table, trainer detail page reusing associate dashboard component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-shell-overhaul/32-CONTEXT.md
@.planning/phases/32-shell-overhaul/32-RESEARCH.md
@DESIGN.md

<interfaces>
<!-- Key types and contracts -->

From src/app/associate/[slug]/dashboard/AssociateDashboardClient.tsx:
```typescript
interface AssociateDashboardClientProps {
  displayName: string;
  gapScores: GapScoreEntry[];
  sessions: SessionSummary[];
  readinessPercent: number;
  threshold: number;
  recommendedArea: string | null;
  lowestScore: number | null;
  lowestSkillSessionCount: number;
}
```

From src/lib/trainer-types.ts:
```typescript
export interface AssociateDetail {
  slug: string;
  displayName: string;
  readinessStatus: string;
  readinessScore: number | null;
  sessionCount: number;
  sessions: SessionSummary[];
  gapScores: GapScoreEntry[];
  cohortId: string | null;
  cohortName: string | null;
}
```

From src/components/trainer/RosterTable.tsx:
```typescript
// Current columns: Name, Slug, Readiness, Sessions, Last Session, Recommended Area, Trend, Top Gap, Sparkline
// Target columns: Name, Readiness, Sessions, Last Session, Recommended Area, Trend, Top Gap, Sparkline (slug removed)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove slug column from RosterTable</name>
  <files>src/components/trainer/RosterTable.tsx</files>
  <action>
Per D-10:

1. Remove the `<th>Slug</th>` header cell (currently line 111).
2. Remove the slug `<td>` cell in the body row (lines 167-171 — the `<td>` with `trainer-meta` class and JetBrains Mono font showing `associate.slug`).
3. Update the empty-state `colSpan` from 9 to 8 (one fewer column).
4. Do NOT change any other columns, sorting logic, or row click behavior. Row click still navigates to `/trainer/${associate.slug}`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Roster table renders 8 columns (Name, Readiness, Sessions, Last Session, Recommended Area, Trend, Top Gap, Sparkline). No slug column. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Replace trainer detail view body with AssociateDashboardClient</name>
  <files>src/app/trainer/(dashboard)/[slug]/page.tsx</files>
  <action>
Per D-11 — trainer sees the same view associates see:

1. Import `AssociateDashboardClient` from `@/app/associate/[slug]/dashboard/AssociateDashboardClient`.
2. Keep the existing data fetching from `/api/trainer/${slug}` (it already returns `AssociateDetail`).
3. Keep the "Back to roster" link and the header section (name, readiness display).
4. Add a compact trainer-only action strip above the dashboard: keep `AssociateCohortSelect` and "Export PDF" button. Remove the slug display `<p>` (slug column removed from roster, no need to show here either).
5. Replace the asymmetric grid layout (session history left / gap chart + calibration right) with `<AssociateDashboardClient>`. Map `AssociateDetail` fields to props:
   - `displayName` = `detail.displayName`
   - `gapScores` = `detail.gapScores` (already `GapScoreEntry[]` from shared types)
   - `sessions` = `detail.sessions` (already `SessionSummary[]`)
   - `readinessPercent` = `detail.readinessScore ?? 0` (readinessScore is 0-100 already)
   - `threshold` = 75 (default — or fetch from `/api/settings` if available in detail. If `AssociateDetail` does not include threshold, hardcode 75 for now and fetch from settings API via a separate call).
   - `recommendedArea` = check if `AssociateDetail` has this field. It does NOT in the current type — use `detail.gapScores` sorted ascending, take first skill. Or extract from the header. The detail page header already shows recommended area if present. Map from gap scores: `detail.gapScores.filter(g => !g.topic || g.topic === '').sort((a,b) => a.weightedScore - b.weightedScore)[0]?.skill ?? null`.
   - `lowestScore` = same lowest gap score entry's `weightedScore`.
   - `lowestSkillSessionCount` = number of sessions where the lowest skill appears. Approximate: `detail.sessions.length` (safe approximation since we can't easily compute per-skill session count from the summary data).
6. Remove old imports: `SessionHistoryList`, `EmptyGapState`, `GapTrendChart`, `CalibrationView`. Keep `ReadinessDisplay`, `AssociateCohortSelect`.
7. Remove `hasGapData`, `hasCalibrationData` variables (no longer used).
8. The page structure becomes: Back link -> Header (name + readiness + cohort select + export PDF) -> AssociateDashboardClient.
9. Fetch trainer settings threshold from `/api/settings` on mount (parallel with detail fetch). If fetch fails, default to 75.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Trainer detail page at /trainer/[slug] renders AssociateDashboardClient with SkillCardList, FocusHero, charts, and radar. Trainer-only actions (cohort select, PDF export) appear above the dashboard. Old trainer-specific views (GapTrendChart, CalibrationView, SessionHistoryList grid) removed. TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/trainer/[slug] | Already auth-guarded for trainer role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Elevation of Privilege | Trainer detail reusing associate component | accept | Component is data-display only. Data comes from trainer-guarded API. No new write surface. |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run lint` passes
3. Manual: Roster table has no slug column
4. Manual: Click roster row -> trainer detail shows SkillCardList, FocusHero, radar, trend chart (same as associate view)
5. Manual: Cohort select and Export PDF still functional above the dashboard
</verification>

<success_criteria>
- Roster table renders without slug column
- Trainer detail page shows identical dashboard view to what associates see
- Trainer-only actions (cohort assign, PDF export) preserved above the dashboard content
- Data flows correctly from /api/trainer/[slug] through AssociateDashboardClient
</success_criteria>

<output>
After completion, create `.planning/phases/32-shell-overhaul/32-03-SUMMARY.md`
</output>
