---
phase: 48-github-actions-ci-deploy-staging-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/pr-checks.yml
  - .github/RUNBOOK-BRANCH-PROTECTION.md
autonomous: false
requirements:
  - CI-01
user_setup:
  - service: github
    why: "Required-checks branch protection rule must be toggled manually in repo settings after first run of pr-checks.yml (gh CLI can set it but the settings page is where the user verifies)"
    env_vars: []
    dashboard_config:
      - task: "Enable required status checks: typecheck, lint, test, prisma-format on `main`"
        location: "GitHub → Settings → Branches → Branch protection rules → main"
must_haves:
  truths:
    - "Opening a PR triggers pr-checks.yml and runs 4 jobs (typecheck, lint, test, prisma-format) in parallel"
    - "A deliberate TypeScript error in a PR fails the typecheck job and marks the PR as failing checks"
    - "A deliberate ESLint error fails the lint job without blocking the other jobs"
    - "All 4 jobs finish within ~3 min on a cache-warm run"
    - "prisma-format check fails when the schema drifts from `prisma format` canonical output"
  artifacts:
    - path: ".github/workflows/pr-checks.yml"
      provides: "PR gating workflow with 4 parallel jobs"
      contains: "jobs:\n  typecheck:\n"
    - path: ".github/RUNBOOK-BRANCH-PROTECTION.md"
      provides: "Runbook for enabling required-checks branch protection"
      min_lines: 20
  key_links:
    - from: ".github/workflows/pr-checks.yml"
      to: "package.json scripts"
      via: "npm run test / npm run lint; npx tsc --noEmit; npx prisma format"
      pattern: "run: (npx tsc --noEmit|npm run lint|npm run test|npx prisma format)"
    - from: ".github/workflows/pr-checks.yml"
      to: "actions/setup-node@v4 npm cache"
      via: "cache: 'npm'"
      pattern: "cache: 'npm'"
---

<objective>
Create the PR-gating workflow (`pr-checks.yml`) that runs 4 parallel jobs — typecheck, lint, test, prisma-format — on every pull request targeting `main` (and on direct pushes to `main`). This is the first line of merge defence per CI-01. Runs entirely on GitHub-hosted runners with npm + Docker layer cache enabled per D-06.

Purpose: Every PR is gated by automated checks; a failing check blocks merge (success criterion 1 for Phase 48). Mitigates T-48-01 (CI bypasses).

Output: `.github/workflows/pr-checks.yml` + runbook for enabling required checks in branch protection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-github-actions-ci-deploy-staging-observability/48-CONTEXT.md
@package.json
@vitest.config.ts
@prisma/schema.prisma

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From package.json scripts:
```json
{
  "lint": "eslint",
  "test": "vitest run"
}
```

Node runtime: 22 (matches Dockerfile `node:22-alpine`). Use `actions/setup-node@v4` with `node-version: '22'`.

Repo slug: `JesterCharles/mock-interview-assist`. Branch: `main`.

Prisma version 7.7.0 supports `prisma format` (idempotent when schema is canonical). Drift detection = diff after format.

GitHub Actions versions (pinned per D-06 + RESEARCH defaults):
- `actions/checkout@v4`
- `actions/setup-node@v4`
- Do NOT use v5 variants (still beta at time of plan).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pr-checks.yml with 4 parallel jobs</name>
  <files>.github/workflows/pr-checks.yml</files>
  <action>
Create `.github/workflows/pr-checks.yml` with exactly this structure per D-01, D-06:

```yaml
name: PR Checks

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: pr-checks-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  typecheck:
    name: Typecheck
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci --prefer-offline --no-audit --no-fund
      - run: npx prisma generate
      - run: npx tsc --noEmit

  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci --prefer-offline --no-audit --no-fund
      - run: npm run lint

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci --prefer-offline --no-audit --no-fund
      - run: npx prisma generate
      - run: npm run test

  prisma-format:
    name: Prisma Schema Format
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci --prefer-offline --no-audit --no-fund
      - name: Format check
        run: |
          npx prisma format
          git diff --exit-code prisma/schema.prisma \
            || (echo "::error::prisma/schema.prisma is not canonically formatted. Run 'npx prisma format' locally and commit."; exit 1)
```

**Locked decisions enforced:**
- Per D-01: 4 parallel jobs (not merged into one matrix). Parallel = fastest feedback.
- Per D-06: `cache: 'npm'` on every job (populates ~/.npm cross-runner); `npm ci --prefer-offline` to prefer cache.
- Per D-01: typecheck runs `npx tsc --noEmit` exactly; lint runs `npm run lint` exactly; test runs `npm run test` exactly.
- `prisma generate` runs before typecheck/test because TS imports the generated client from `src/generated/prisma/` (per CLAUDE.md).
- `concurrency` cancels superseded runs on the same PR (saves minutes; convention).
- `permissions: contents: read` — no write access needed; least privilege (T-48-04 scope reduction).
- `timeout-minutes` on every job prevents runaway workflows.

**Do NOT:**
- Add Playwright / e2e tests (deferred per CONTEXT.md Deferred Ideas).
- Add Docker build to PR checks (too slow; deploy-staging handles it — belongs in Plan 02).
- Add `pull_request_target` (security risk — exposes secrets to fork PRs).
  </action>
  <verify>
    <automated>cd /Users/jestercharles/mock-interview-assist && npx --yes js-yaml .github/workflows/pr-checks.yml > /dev/null && grep -q "npx tsc --noEmit" .github/workflows/pr-checks.yml && grep -q "npm run lint" .github/workflows/pr-checks.yml && grep -q "npm run test" .github/workflows/pr-checks.yml && grep -q "git diff --exit-code prisma/schema.prisma" .github/workflows/pr-checks.yml && grep -c "cache: 'npm'" .github/workflows/pr-checks.yml | grep -q "^4$" && echo "OK"</automated>
  </verify>
  <done>File exists, YAML parses, contains all 4 required commands, npm cache on all 4 jobs, concurrency block present.</done>
</task>

<task type="auto">
  <name>Task 2: Write branch protection runbook</name>
  <files>.github/RUNBOOK-BRANCH-PROTECTION.md</files>
  <action>
Create `.github/RUNBOOK-BRANCH-PROTECTION.md` documenting the manual step to enable required status checks (mitigates T-48-01).

Content (exact):

```markdown
# Branch Protection Runbook — NLM v1.5

**Purpose:** Enable required status checks on `main` so the `pr-checks.yml` workflow actually blocks merges. Without this, the workflow runs but failures are advisory only.

## When

Run this **once** after the first successful run of `pr-checks.yml` (GitHub must see the check names at least once before they can be required).

## Steps

### Option A — via `gh` CLI (recommended)

```bash
gh api -X PUT repos/JesterCharles/mock-interview-assist/branches/main/protection \
  -f required_status_checks[strict]=true \
  -f required_status_checks[contexts][]='Typecheck' \
  -f required_status_checks[contexts][]='Lint' \
  -f required_status_checks[contexts][]='Unit Tests' \
  -f required_status_checks[contexts][]='Prisma Schema Format' \
  -F enforce_admins=false \
  -f required_pull_request_reviews[required_approving_review_count]=0 \
  -F restrictions=null
```

### Option B — via GitHub UI

1. Navigate to: https://github.com/JesterCharles/mock-interview-assist/settings/branches
2. Click **Add branch protection rule** (or edit existing `main` rule)
3. Branch name pattern: `main`
4. Check **Require status checks to pass before merging**
5. Check **Require branches to be up to date before merging**
6. Search for and select all 4 checks:
   - `Typecheck`
   - `Lint`
   - `Unit Tests`
   - `Prisma Schema Format`
7. Save changes

## Verification

```bash
gh api repos/JesterCharles/mock-interview-assist/branches/main/protection \
  --jq '.required_status_checks.contexts'
# Expected: ["Typecheck","Lint","Unit Tests","Prisma Schema Format"]
```

## Rollback

```bash
gh api -X DELETE repos/JesterCharles/mock-interview-assist/branches/main/protection
```

## Notes

- Solo developer: `required_approving_review_count=0` is intentional (no mandatory review; Codex review is the review gate per CLAUDE.md).
- `enforce_admins=false` so the user can push hotfixes directly if CI is broken.
- This runbook is manual-gated because GitHub API occasionally requires interactive 2FA re-auth for `PUT /branches/:branch/protection`.
```

This file is committed so future Claude instances can re-run the setup.
  </action>
  <verify>
    <automated>test -f /Users/jestercharles/mock-interview-assist/.github/RUNBOOK-BRANCH-PROTECTION.md && grep -q "required_status_checks" /Users/jestercharles/mock-interview-assist/.github/RUNBOOK-BRANCH-PROTECTION.md && grep -q "Typecheck" /Users/jestercharles/mock-interview-assist/.github/RUNBOOK-BRANCH-PROTECTION.md && echo "OK"</automated>
  </verify>
  <done>File exists with gh CLI command referencing all 4 check names.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (checkpoint): Verify pr-checks.yml gates merge</name>
  <files>n/a — verification only</files>
  <action>User-driven verification. See how-to-verify block for steps.</action>
  <what-built>`.github/workflows/pr-checks.yml` (4 parallel jobs) + `.github/RUNBOOK-BRANCH-PROTECTION.md`. No runs yet against GitHub (commit lands when user pushes branch).</what-built>
  <verify>Manual (human-verify checkpoint — see how-to-verify).</verify>
  <done>User types "approved".</done>
  <how-to-verify>
After execute-phase finishes and the commit is pushed to the feature branch:

1. Open a PR (or push a commit) — confirm `pr-checks.yml` triggers and all 4 jobs run in parallel on the Actions tab.
2. Deliberately introduce a TypeScript error in a scratch branch (e.g., `const x: number = 'bad'` in any `src/*.ts`) and open a PR — confirm the `Typecheck` job fails and the PR shows a blocking red check.
3. Revert the error; confirm checks pass on a clean run.
4. Apply the branch-protection runbook (`gh api` command) and confirm `gh api repos/JesterCharles/mock-interview-assist/branches/main/protection --jq '.required_status_checks.contexts'` returns all 4 names.

If any step fails, describe the failure and return to revision mode.
  </how-to-verify>
  <resume-signal>Type "approved" once all 4 jobs run green on a clean PR and the failing-typecheck rehearsal blocks merge.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author → CI | Untrusted code runs in GitHub-hosted runner; must NOT have write access to anything |
| Workflow → GitHub repo | Read-only permissions; no token write; no deploy action in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | `pr-checks.yml` merge gate | mitigate | Branch protection runbook (task 2) enforces required-check status on `main`; `enforce_admins=false` accepted as solo-dev tradeoff |
| T-48-01a | Elevation of Privilege | `pull_request_target` mis-use | mitigate | Use `pull_request` (not `_target`); no secrets exposed to fork PRs |
| T-48-04 | Information Disclosure | Workflow permissions scope | mitigate | `permissions: contents: read` — no write token issued to workflow |
</threat_model>

<verification>
- `gh workflow list | grep pr-checks.yml` → present
- PR with typecheck error → Typecheck job fails, PR merge blocked
- `gh api repos/JesterCharles/mock-interview-assist/branches/main/protection --jq '.required_status_checks.contexts | length'` → `4`
</verification>

<success_criteria>
- pr-checks.yml exists, runs 4 parallel jobs, all passing on clean `main`
- Branch-protection runbook committed; required checks enabled in GitHub settings
- CI-01 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/48-github-actions-ci-deploy-staging-observability/48-01-SUMMARY.md` with:
- Workflow file path + line count
- Branch protection rule JSON snapshot
- Any failing-typecheck rehearsal evidence (PR link or local test)
</output>
