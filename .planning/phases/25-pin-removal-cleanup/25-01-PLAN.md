---
phase: 25-pin-removal-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/pinService.ts
  - src/lib/pinService.test.ts
  - src/lib/pinAttemptLimiter.ts
  - src/lib/associateSession.ts
  - src/lib/featureFlags.ts
  - src/lib/auth-server.ts
  - src/lib/auth-server.test.ts
  - src/lib/associateService.ts
  - src/lib/githubManifestCache.ts
  - src/app/api/associate/pin/generate/route.ts
  - src/app/api/associate/pin/verify/route.ts
  - src/app/api/associate/pin/verify/route.test.ts
  - src/app/api/associate/me/route.ts
  - src/app/api/associate/status/route.ts
  - src/app/api/associate/logout/route.ts
  - src/app/api/associate/interview/complete/route.ts
  - src/app/api/public/interview/complete/route.test.ts
  - src/app/associate/login/page.tsx
  - src/app/associate/login/PinEntryForm.tsx
  - src/app/trainer/components/GeneratePinButton.tsx
  - src/app/trainer/(dashboard)/[slug]/page.tsx
  - src/app/signin/SignInTabs.tsx
  - src/app/api/auth/route.ts
  - vitest.config.ts
  - src/__tests__/pin-removal-gate.test.ts
autonomous: true
requirements:
  - CLEANUP-01
  - CLEANUP-02

must_haves:
  truths:
    - "No PIN-related source files exist in src/lib/ or src/app/api/associate/pin/"
    - "No file in src/ references ENABLE_ASSOCIATE_AUTH, pinHash, pinGeneratedAt, associate_session, verifyAssociateToken, or isAssociateAuthEnabled"
    - "auth-server.ts exports only isAuthenticatedSession (PIN functions removed)"
    - "Associate tab in SignInTabs renders unconditionally (no feature flag)"
    - "Grep-gate test passes when PIN code is absent and would fail if reintroduced"
    - "npm run test passes with all PIN tests removed"
  artifacts:
    - path: "src/__tests__/pin-removal-gate.test.ts"
      provides: "CI grep-gate preventing PIN resurrection"
      contains: "ENABLE_ASSOCIATE_AUTH"
  key_links:
    - from: "src/app/api/associate/interview/complete/route.ts"
      to: "src/lib/identity.ts"
      via: "getCallerIdentity replaces getAssociateSession"
      pattern: "getCallerIdentity"
    - from: "src/app/api/associate/me/route.ts"
      to: "src/lib/identity.ts"
      via: "getCallerIdentity replaces getAssociateIdentity"
      pattern: "getCallerIdentity"
---

<objective>
Delete all PIN auth code, update dependent files to use Supabase identity, and add a grep-gate test that prevents PIN resurrection.

Purpose: Remove dead PIN auth system (superseded by Supabase in Phase 18) and ensure it cannot be accidentally reintroduced.
Output: Clean codebase with zero PIN references in src/ (excluding generated Prisma client, cleaned in Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-pin-removal-cleanup/25-CONTEXT.md

<interfaces>
<!-- Key types the executor needs for rewriting dependent files -->

From src/lib/identity.ts:
```typescript
export type CallerIdentity =
  | { kind: 'admin'; userId: string; email: string }
  | { kind: 'trainer'; userId: string; email: string }
  | { kind: 'associate'; userId: string; email: string; associateId: number; associateSlug: string }
  | { kind: 'anonymous' };

export async function getCallerIdentity(): Promise<CallerIdentity>;
```

<!-- This is the Supabase-based identity resolver that replaces all PIN-based identity functions.
     Routes currently using getAssociateSession/getAssociateIdentity from auth-server.ts
     must switch to getCallerIdentity from identity.ts. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete PIN files and remove PIN imports from dependents</name>
  <files>
    src/lib/pinService.ts, src/lib/pinService.test.ts, src/lib/pinAttemptLimiter.ts,
    src/lib/associateSession.ts, src/lib/featureFlags.ts,
    src/app/api/associate/pin/generate/route.ts, src/app/api/associate/pin/verify/route.ts,
    src/app/api/associate/pin/verify/route.test.ts,
    src/app/associate/login/page.tsx, src/app/associate/login/PinEntryForm.tsx,
    src/app/trainer/components/GeneratePinButton.tsx,
    src/lib/auth-server.ts, src/lib/auth-server.test.ts, src/lib/associateService.ts,
    src/app/api/associate/me/route.ts, src/app/api/associate/status/route.ts,
    src/app/api/associate/logout/route.ts, src/app/api/associate/interview/complete/route.ts,
    src/app/api/public/interview/complete/route.test.ts,
    src/app/trainer/(dashboard)/[slug]/page.tsx, src/app/api/auth/route.ts,
    src/lib/githubManifestCache.ts, vitest.config.ts
  </files>
  <read_first>
    src/lib/auth-server.ts, src/lib/auth-server.test.ts, src/lib/associateService.ts,
    src/app/api/associate/me/route.ts, src/app/api/associate/status/route.ts,
    src/app/api/associate/logout/route.ts, src/app/api/associate/interview/complete/route.ts,
    src/app/api/public/interview/complete/route.test.ts,
    src/app/trainer/(dashboard)/[slug]/page.tsx, src/app/api/auth/route.ts,
    src/lib/githubManifestCache.ts, vitest.config.ts, src/lib/identity.ts
  </read_first>
  <action>
**DELETE these files entirely (per D-01, D-02, D-03, D-05):**
- `src/lib/pinService.ts`
- `src/lib/pinService.test.ts`
- `src/lib/pinAttemptLimiter.ts`
- `src/lib/associateSession.ts`
- `src/lib/featureFlags.ts`
- `src/app/api/associate/pin/generate/route.ts`
- `src/app/api/associate/pin/verify/route.ts`
- `src/app/api/associate/pin/verify/route.test.ts`
- `src/app/associate/login/page.tsx`
- `src/app/associate/login/PinEntryForm.tsx`
- `src/app/trainer/components/GeneratePinButton.tsx`

Delete the entire directories if empty after file removal:
- `src/app/api/associate/pin/generate/`
- `src/app/api/associate/pin/verify/`
- `src/app/api/associate/pin/`
- `src/app/associate/login/`

**REWRITE `src/lib/auth-server.ts`:**
Remove ALL PIN-related functions (`resolveAssociate`, `isAssociateAuthenticated`, `getAssociateIdentity`, `getAssociateSession`). Remove imports of `verifyAssociateToken` and `getAssociateById`. Keep ONLY `isAuthenticatedSession()` (still used by `/api/auth` GET and cache invalidate route). The file should be ~15 lines: import cookies, export isAuthenticatedSession.

**REWRITE `src/lib/auth-server.test.ts`:**
Remove ALL PIN-related test suites (`isAssociateAuthenticated`, `getAssociateIdentity`, `getAssociateSession`). Remove `signAssociateToken` import, `ASSOCIATE_SESSION_SECRET` env setup, associateService mock. Keep ONLY the `isAuthenticatedSession` describe block. Remove mock for prisma (no longer needed).

**REWRITE `src/lib/associateService.ts`:**
Remove `pinGeneratedAt` from the `getAssociateById` return type and select clause. The function should return `{ id: number; slug: string } | null`. Update the JSDoc to remove PIN references.

**REWRITE `src/app/api/associate/me/route.ts`:**
Remove `isAssociateAuthEnabled` import and the feature flag guard. Replace `getAssociateIdentity` with `getCallerIdentity` from `@/lib/identity`. Check `caller.kind === 'associate'`, use `caller.associateId` for the Prisma lookup.

**REWRITE `src/app/api/associate/status/route.ts`:**
This route existed solely to report `ENABLE_ASSOCIATE_AUTH` status. It is now dead code. Replace the body to always return `{ enabled: true }` (associate auth is always enabled with Supabase). Remove `featureFlags` import.

**REWRITE `src/app/api/associate/logout/route.ts`:**
Remove `associate_session` cookie clearing. Replace with Supabase sign-out: import `createSupabaseServerClient` from `@/lib/supabase/server`, call `supabase.auth.signOut()`, return `{ ok: true }`.

**REWRITE `src/app/api/associate/interview/complete/route.ts`:**
Remove `isAssociateAuthEnabled` import and feature flag guard. Replace `getAssociateSession` (from auth-server) with `getCallerIdentity` (from `@/lib/identity`). Check `caller.kind === 'associate'`, use `caller.associateSlug` and `caller.associateId`. Remove all JSDoc references to PIN cookies and `pinGeneratedAt`. Update the `SECURITY` doc comment to reference Supabase session instead of `associate_session` cookie.

**UPDATE `src/app/api/public/interview/complete/route.test.ts`:**
Line ~72 has `{ cookie: 'associate_session=anything' }`. Change the cookie name to something non-PIN (e.g., `sb-session=anything`) since the test proves the public route ignores ALL cookies. The test logic is unchanged; only the cookie name string changes to avoid the grep-gate.

**UPDATE `src/app/trainer/(dashboard)/[slug]/page.tsx`:**
Remove `GeneratePinButton` import. Remove the `associateAuthEnabled` state + useEffect that fetches `/api/associate/status`. Remove the `{associateAuthEnabled && ...}` conditional block rendering GeneratePinButton. No replacement needed -- PIN generation is dead with Supabase auth.

**UPDATE `src/app/api/auth/route.ts`:**
Remove the `POST` handler entirely (it uses `APP_PASSWORD` for legacy trainer password login, superseded by Supabase email/password in Phase 18). Keep `GET` (returns `isAuthenticatedSession()`) and `DELETE` (clears `nlm_session` cookie) -- these are still consumed by cache invalidate and other legacy paths.

**UPDATE `src/lib/githubManifestCache.ts`:**
Line ~11 has a comment referencing `pinAttemptLimiter.ts`. Change the comment to say "Mirrors module-state pattern: no class, top-level Map, `__resetAll` + `__setFetcher` test hooks." (remove the file reference).

**UPDATE `vitest.config.ts`:**
Remove `ENABLE_ASSOCIATE_AUTH: 'true'` from the `env` block.
  </action>
  <verify>
    <automated>grep -rn "pinService\|pinAttemptLimiter\|associateSession\|featureFlags\|GeneratePinButton\|PinEntryForm\|verifyAssociateToken\|isAssociateAuthEnabled\|signAssociateToken" src/lib/ src/app/ --include="*.ts" --include="*.tsx" | grep -v "node_modules\|generated" | head -20; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
- Zero matches for PIN imports in non-generated src/ files
- `src/lib/pinService.ts`, `src/lib/pinAttemptLimiter.ts`, `src/lib/associateSession.ts`, `src/lib/featureFlags.ts` do not exist
- `src/app/api/associate/pin/` directory does not exist
- `src/app/associate/login/` directory does not exist
- `src/app/trainer/components/GeneratePinButton.tsx` does not exist
- `src/lib/auth-server.ts` exports only `isAuthenticatedSession`
- `src/app/api/associate/interview/complete/route.ts` uses `getCallerIdentity`
- `vitest.config.ts` has no `ENABLE_ASSOCIATE_AUTH` in env
  </acceptance_criteria>
  <done>All PIN files deleted, all dependents updated to Supabase identity, no PIN imports remain in src/</done>
</task>

<task type="auto">
  <name>Task 2: Remove feature flag logic from SignInTabs</name>
  <files>src/app/signin/SignInTabs.tsx</files>
  <read_first>src/app/signin/SignInTabs.tsx</read_first>
  <action>
Per D-04: Remove any `ENABLE_ASSOCIATE_AUTH` conditional that hides/shows the Associate tab. The Associate tab should always be visible now. Remove any import of `isAssociateAuthEnabled` or fetch to `/api/associate/status`. If there is a state variable like `associateAuthEnabled` controlling tab visibility, remove it and render the Associate tab unconditionally.
  </action>
  <verify>
    <automated>grep -n "isAssociateAuthEnabled\|ENABLE_ASSOCIATE_AUTH\|associateAuthEnabled\|associate/status" src/app/signin/SignInTabs.tsx; test $? -eq 1 && echo "PASS: no feature flag references"</automated>
  </verify>
  <acceptance_criteria>
- `SignInTabs.tsx` has zero references to `ENABLE_ASSOCIATE_AUTH`, `isAssociateAuthEnabled`, `associateAuthEnabled`, or `/api/associate/status`
- Associate tab renders unconditionally
  </acceptance_criteria>
  <done>SignInTabs renders both Trainer and Associate tabs without feature flag gating</done>
</task>

<task type="auto">
  <name>Task 3: Create grep-gate Vitest test (per D-10, D-11)</name>
  <files>src/__tests__/pin-removal-gate.test.ts</files>
  <read_first>vitest.config.ts</read_first>
  <action>
Create `src/__tests__/pin-removal-gate.test.ts` — a Vitest test that runs `grep -rn` over `src/` (excluding `src/generated/` and `src/__tests__/pin-removal-gate.test.ts` itself) for the pattern: `ENABLE_ASSOCIATE_AUTH|pinHash|pinGeneratedAt|associate_session|verifyAssociateToken|isAssociateAuthEnabled`.

Use `child_process.execSync` to run:
```
grep -rn "ENABLE_ASSOCIATE_AUTH\|pinHash\|pinGeneratedAt\|associate_session\|verifyAssociateToken\|isAssociateAuthEnabled" src/ --include="*.ts" --include="*.tsx" | grep -v "src/generated/" | grep -v "pin-removal-gate.test.ts"
```

The test asserts the command returns exit code 1 (no matches). If matches are found, the test fails with the matched lines in the error message so the developer sees exactly what needs fixing.

Test structure:
```typescript
import { execSync } from 'child_process';
import { describe, it, expect } from 'vitest';

describe('PIN removal grep-gate (CLEANUP-01)', () => {
  it('src/ contains zero references to PIN auth patterns', () => {
    // grep exits 1 when no matches — that is the passing case
    // grep exits 0 when matches found — we capture stdout as the failure message
    try {
      const output = execSync(
        'grep -rn "ENABLE_ASSOCIATE_AUTH\\|pinHash\\|pinGeneratedAt\\|associate_session\\|verifyAssociateToken\\|isAssociateAuthEnabled" src/ --include="*.ts" --include="*.tsx" | grep -v "src/generated/" | grep -v "pin-removal-gate.test.ts"',
        { encoding: 'utf-8' }
      );
      // If we reach here, matches were found — fail
      expect.fail(`PIN auth patterns still present in src/:\n${output}`);
    } catch (e: unknown) {
      // grep exit code 1 = no matches = success
      const err = e as { status?: number };
      expect(err.status).toBe(1);
    }
  });
});
```
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/pin-removal-gate.test.ts --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `src/__tests__/pin-removal-gate.test.ts` exists
- Test passes (exit code 0) when no PIN patterns in src/ (excluding generated + self)
- Test would fail if any PIN pattern were reintroduced
  </acceptance_criteria>
  <done>Grep-gate test exists and passes, preventing PIN resurrection in CI</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| associate API routes | Previously gated by PIN feature flag; now rely on Supabase session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Spoofing | /api/associate/interview/complete | mitigate | Switch from PIN cookie to getCallerIdentity() — Supabase session is server-validated via getUser() |
| T-25-02 | Spoofing | /api/associate/me | mitigate | Switch from PIN identity to getCallerIdentity() — same Supabase validation |
| T-25-03 | Information Disclosure | /api/associate/status | accept | Route now returns static { enabled: true } — no sensitive info |
| T-25-04 | Repudiation | grep-gate test | mitigate | Vitest test catches any PIN pattern reintroduction before merge |
</threat_model>

<verification>
1. `npm run test` passes (all PIN tests removed, grep-gate passes)
2. `npx tsc --noEmit` passes (no broken imports)
3. `grep -rn` for PIN patterns in src/ (excluding generated/) returns zero matches
</verification>

<success_criteria>
- All PIN files deleted
- All dependent files updated to use Supabase identity
- SignInTabs renders Associate tab unconditionally
- Grep-gate test exists and passes
- TypeScript compiles without errors
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/25-pin-removal-cleanup/25-01-SUMMARY.md`
</output>
