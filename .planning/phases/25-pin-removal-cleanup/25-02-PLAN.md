---
phase: 25-pin-removal-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - prisma/schema.prisma
  - src/generated/prisma/index.d.ts
  - src/generated/prisma/index.js
  - src/generated/prisma/edge.js
  - src/generated/prisma/schema.prisma
  - src/generated/prisma/index-browser.js
  - .env.example
  - .env.docker
  - CLAUDE.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - CLEANUP-03

must_haves:
  truths:
    - "Prisma schema has no pinHash or pinGeneratedAt fields on Associate model"
    - "Generated Prisma client has no pinHash or pinGeneratedAt references"
    - ".env.example has no APP_PASSWORD, ASSOCIATE_SESSION_SECRET, or ENABLE_ASSOCIATE_AUTH"
    - ".env.docker has no APP_PASSWORD, ASSOCIATE_SESSION_SECRET, or ENABLE_ASSOCIATE_AUTH"
    - "CLAUDE.md has no PIN auth references"
    - "App boots and npm run build succeeds with only Supabase env vars"
  artifacts:
    - path: "prisma/migrations/0004_drop_pin_columns/migration.sql"
      provides: "Destructive migration dropping PIN columns"
      contains: "DROP COLUMN"
  key_links:
    - from: "prisma/schema.prisma"
      to: "src/generated/prisma/"
      via: "prisma generate"
      pattern: "prisma generate"
---

<objective>
Drop PIN schema columns, clean env files and docs, regenerate Prisma client, verify clean build.

Purpose: Complete the PIN removal by dropping database columns and removing all env/doc references.
Output: Clean schema, clean env files, clean docs, passing build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-pin-removal-cleanup/25-CONTEXT.md
@.planning/phases/25-pin-removal-cleanup/25-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma migration to drop PIN columns (per D-06, D-07)</name>
  <files>prisma/schema.prisma, prisma/migrations/0004_drop_pin_columns/migration.sql</files>
  <read_first>prisma/schema.prisma</read_first>
  <action>
**Edit `prisma/schema.prisma`:**
Remove these two lines from the `Associate` model:
```
pinHash         String? // bcrypt hash of 6-digit PIN; null until trainer generates
pinGeneratedAt  DateTime? // when current PIN was issued; also the token-version for cookie revocation
```

**Create migration manually** (do NOT use `prisma migrate dev` which requires a live DB):
Create `prisma/migrations/0004_drop_pin_columns/migration.sql` with:
```sql
-- Drop PIN auth columns (Phase 25: PIN removal after Supabase cutover)
ALTER TABLE "Associate" DROP COLUMN IF EXISTS "pinHash";
ALTER TABLE "Associate" DROP COLUMN IF EXISTS "pinGeneratedAt";
```

Using `IF EXISTS` for idempotency (safe to re-run).

**Regenerate Prisma client:**
Run `npx prisma generate` to regenerate `src/generated/prisma/` without PIN fields.
  </action>
  <verify>
    <automated>grep -n "pinHash\|pinGeneratedAt" prisma/schema.prisma; test $? -eq 1 && echo "PASS: schema clean" && grep -c "pinHash\|pinGeneratedAt" src/generated/prisma/index.d.ts 2>/dev/null; test $? -eq 1 && echo "PASS: generated types clean"</automated>
  </verify>
  <acceptance_criteria>
- `prisma/schema.prisma` has zero `pinHash` or `pinGeneratedAt` references
- `prisma/migrations/0004_drop_pin_columns/migration.sql` exists with DROP COLUMN statements
- `src/generated/prisma/index.d.ts` has zero `pinHash` or `pinGeneratedAt` references
- `npx prisma generate` succeeds
  </acceptance_criteria>
  <done>PIN columns removed from schema, migration created, Prisma client regenerated clean</done>
</task>

<task type="auto">
  <name>Task 2: Remove env vars from config files and update docs (per D-08, D-09)</name>
  <files>.env.example, .env.docker, CLAUDE.md, .planning/PROJECT.md</files>
  <read_first>.env.example, .env.docker, CLAUDE.md, .planning/PROJECT.md</read_first>
  <action>
**Edit `.env.example`:**
Remove the `ASSOCIATE_SESSION_SECRET` entry and its surrounding comment block. The APP_PASSWORD is NOT in .env.example (already confirmed), but if present remove it. Remove any ENABLE_ASSOCIATE_AUTH reference.

**Edit `.env.docker`:**
Remove `APP_PASSWORD=PlanoBois2026!` line. Remove `ASSOCIATE_SESSION_SECRET=PlanoTest26!` line. Remove any `ENABLE_ASSOCIATE_AUTH` line.

**Edit `CLAUDE.md`:**
1. Remove `APP_PASSWORD` from Environment Variables section
2. Remove `ASSOCIATE_SESSION_SECRET` from Environment Variables section (and its description)
3. Remove `ENABLE_ASSOCIATE_AUTH` from Environment Variables section (and its description)
4. Remove `NLM_TRUSTED_PROXY` from Environment Variables section (only used by PIN limiter, now deleted)
5. In Authentication section: Remove all PIN auth paragraphs. Remove references to `pinService`, `pinAttemptLimiter`, `associateSession`, `featureFlags.ts`, `GeneratePinButton`, `PinEntryForm`, PIN verification, `associate_session` cookie, `ASSOCIATE_SESSION_SECRET`. Keep the Supabase auth description.
6. Remove `/associate/login` from any route references
7. Remove `/api/associate/pin/*` routes from Key API Routes table
8. Remove `/api/associate/status` route (or update to note it returns static `{ enabled: true }`)
9. In Key services section: Remove `pinService`, `pinAttemptLimiter`, `associateSession`, `featureFlags` entries
10. Update the sign-in description: remove "The Associate tab is hidden when `ENABLE_ASSOCIATE_AUTH` is `false`"
11. Remove the `APP_PASSWORD` POST handler description from `/api/auth` route if documented

**Edit `.planning/PROJECT.md`:**
Apply the same removals as CLAUDE.md — remove PIN references, env vars, service descriptions, route entries. Keep Supabase auth documentation.

**Verify boot:** Run `npm run build` to confirm the app builds without PIN env vars. Run `npm run test` to confirm all tests pass (including the grep-gate from Plan 01).
  </action>
  <verify>
    <automated>grep -n "APP_PASSWORD\|ASSOCIATE_SESSION_SECRET\|ENABLE_ASSOCIATE_AUTH" .env.example .env.docker 2>/dev/null; test $? -eq 1 && echo "PASS: env files clean" && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
- `.env.example` has zero references to `APP_PASSWORD`, `ASSOCIATE_SESSION_SECRET`, `ENABLE_ASSOCIATE_AUTH`
- `.env.docker` has zero references to `APP_PASSWORD`, `ASSOCIATE_SESSION_SECRET`, `ENABLE_ASSOCIATE_AUTH`
- `CLAUDE.md` has zero PIN-related content (no pinService, featureFlags, associate_session, PIN routes)
- `.planning/PROJECT.md` has zero PIN-related content
- `npm run build` succeeds
- `npm run test` passes
  </acceptance_criteria>
  <done>All env vars removed, all docs updated, app builds and tests pass with Supabase-only auth</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema migration | Destructive column drop on production DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Denial of Service | migration.sql | accept | DROP COLUMN IF EXISTS is idempotent; PIN data was never populated in production per D-07 |
| T-25-06 | Information Disclosure | .env.docker | mitigate | Remove hardcoded secrets (APP_PASSWORD, ASSOCIATE_SESSION_SECRET) from committed env file |
</threat_model>

<verification>
1. `npx prisma generate` succeeds
2. `npm run build` succeeds
3. `npm run test` passes (including grep-gate)
4. `grep -rn "APP_PASSWORD\|ASSOCIATE_SESSION_SECRET\|ENABLE_ASSOCIATE_AUTH" .env.example .env.docker` returns no matches
</verification>

<success_criteria>
- PIN columns dropped from schema
- Prisma client regenerated clean
- Env files cleaned of all PIN-era variables
- CLAUDE.md and PROJECT.md updated to Supabase-only auth
- App builds and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/25-pin-removal-cleanup/25-02-SUMMARY.md`
</output>
