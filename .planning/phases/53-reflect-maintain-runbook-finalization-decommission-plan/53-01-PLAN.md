---
phase: 53-reflect-maintain-runbook-finalization-decommission-plan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PIPELINE-REFLECT.md
  - .planning/PIPELINE-MAINTAIN.md
  - .planning/milestones/v1.4-v1.5-RETROSPECTIVE.md
  - second-brain/projects/nlm/notes/retro-2026-04-18.md
  - second-brain/projects/nlm/notes/milestone-v1.5-summary.md
autonomous: true
requirements: [META-01, META-02]
must_haves:
  truths:
    - "v1.4 and v1.5 retrospectives are both captured in a single committed PIPELINE-REFLECT.md artifact"
    - "Retro narrative is saved to second-brain at the dated path per CLAUDE.md second-brain rule"
    - "Milestone-v1.5 summary is saved to second-brain"
    - "Codebase health is a computed numeric score (0-10), not a subjective adjective"
    - "PIPELINE-MAINTAIN.md records the computed health_score in frontmatter (machine-readable for Plan 04 gate)"
  artifacts:
    - path: .planning/PIPELINE-REFLECT.md
      provides: "v1.4 + v1.5 retrospective narrative"
    - path: .planning/PIPELINE-MAINTAIN.md
      provides: "Health sweep with numeric score 0-10 in frontmatter"
    - path: .planning/milestones/v1.4-v1.5-RETROSPECTIVE.md
      provides: "Archived copy of retrospective (per D-03)"
    - path: second-brain/projects/nlm/notes/retro-2026-04-18.md
      provides: "Second-brain retro note per CLAUDE.md table (absolute path uses $HOME)"
    - path: second-brain/projects/nlm/notes/milestone-v1.5-summary.md
      provides: "One-page milestone v1.5 narrative (absolute path uses $HOME)"
  key_links:
    - from: .planning/PIPELINE-MAINTAIN.md
      to: .planning/PIPELINE-REFLECT.md
      via: "health score feeds 'where we are now' in retro"
    - from: .planning/PIPELINE-REFLECT.md
      to: second-brain/projects/nlm/notes/retro-2026-04-18.md
      via: "same narrative, dual-destination per CLAUDE.md second-brain rule"
---

<objective>
Run the deferred v1.4 reflect + maintain sweep AND the v1.5 reflect + maintain sweep in a single session. Produce both pipeline artifacts AND second-brain notes, with a real computed numeric health score (not subjective).

Purpose: PIPELINE.md explicitly deferred v1.4 reflect/maintain ("will run at v1.5 ship"). Phase 53 discharges both. Locks in learnings for v1.6 and produces the machine-readable health_score that Plan 04's phase gate will verify.
Output: `.planning/PIPELINE-REFLECT.md`, `.planning/PIPELINE-MAINTAIN.md`, archived `.planning/milestones/v1.4-v1.5-RETROSPECTIVE.md`, second-brain `retro-2026-04-18.md` + `milestone-v1.5-summary.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PIPELINE.md
@.planning/REQUIREMENTS.md
@.planning/CHECKPOINT-v1.3-reflect.md
@.planning/phases/53-reflect-maintain-runbook-finalization-decommission-plan/53-CONTEXT.md
@.planning/PIPELINE-REFLECT.md
@.planning/PIPELINE-MAINTAIN.md
@.planning/RETROSPECTIVE.md
@./CLAUDE.md

<interfaces>
<!-- Template shape for PIPELINE-REFLECT.md based on existing artifact (v1.4 header) -->

Existing frontmatter shape of .planning/PIPELINE-REFLECT.md (overwrite with merged v1.4+v1.5):
```yaml
type: pipeline-reflect
milestone: v1.4-v1.5
date: 2026-04-18
pr: 7 (v1.4), TBD (v1.5)
branch: v1.5-cloudrun-migration
merge_commit: (deferred — no-auto-merge policy)
```

Existing PIPELINE-MAINTAIN.md pattern (Health Score 7.5/10 narrative from post-v1.4 snapshot).
New frontmatter to add (REQUIRED for Plan 04 phase gate to parse):
```yaml
type: pipeline-maintain
milestone: v1.5
date: 2026-04-18
health_score: <computed 0-10>
component_scores:
  tests: <0-10>
  typecheck: <0-10>
  lint: <0-10>
  audit: <0-10>
```

Health score weights per D-04 (this plan's CONTEXT.md):
- tests: 30%  (npm run test — pass rate)
- typecheck: 30%  (npx tsc --noEmit — error count; 0 errors = 10, each error -1 down to 0)
- lint: 20%  (npm run lint — project-only warning count; exclude src/generated/prisma/** and .obsidian/**)
- audit: 20%  (npm audit --json — severity-weighted; 0 high/critical = 10, each high -2, each critical -4)

Formula: `health_score = (tests*0.30 + typecheck*0.30 + lint*0.20 + audit*0.20)` rounded to 1 decimal.

CLAUDE.md Second-Brain Integration table (load-bearing):
| Trigger | Save To | How |
| `/retro` output | `projects/nlm/notes/retro-YYYY-MM-DD.md` | Obsidian plugin |
| Milestone completion | `projects/nlm/notes/milestone-N-summary.md` | After `/gsd-complete-milestone` |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run /pipeline-maintain sweep and compute health_score</name>
  <files>
    .planning/PIPELINE-MAINTAIN.md
  </files>
  <action>
    Execute the maintain sweep per D-04 and write a NEW `.planning/PIPELINE-MAINTAIN.md` (overwriting the post-v1.4 snapshot).

    Step 1 — Run the four health checks (capture raw output):
    1. `npm run test -- --run 2>&1 | tail -40` → extract "N passed / M total" and failure count
    2. `npx tsc --noEmit 2>&1 | tail -80` → count error lines (lines ending with "error TS")
    3. `npm run lint 2>&1 | tail -200` → count warnings + errors, then filter OUT lines matching `src/generated/prisma/` and `.obsidian/` to get PROJECT-ONLY count
    4. `npm audit --json 2>&1` → parse .metadata.vulnerabilities for high/critical counts

    Step 2 — Compute component scores (0-10 each):
    - `tests`: `pass_rate = passed / total`; score = `round(pass_rate * 10, 1)`. If any test FAILED → max score 7.
    - `typecheck`: `errors = 0` → 10, `errors 1-3` → 6, `errors 4-10` → 4, `errors > 10` → 2, `errors > 50` → 0.
    - `lint`: project-only-issues `0` → 10, `1-20` → 9, `21-50` → 8, `51-100` → 7, `101-200` → 6, `>200` → 4.
    - `audit`: critical=0 + high=0 → 10; each critical -4; each high -2; floor at 0.

    Step 3 — Compute `health_score = round((tests*0.30 + typecheck*0.30 + lint*0.20 + audit*0.20), 1)`.

    Step 4 — Write `.planning/PIPELINE-MAINTAIN.md` with REQUIRED frontmatter:
    ```yaml
    ---
    type: pipeline-maintain
    milestone: v1.5
    date: 2026-04-18
    health_score: <NUMBER, e.g., 8.2>
    component_scores:
      tests: <NUMBER>
      typecheck: <NUMBER>
      lint: <NUMBER>
      audit: <NUMBER>
    ---
    ```
    Then Markdown body with sections:
    - `## Health Score: **<N> / 10**` (human-facing table mirroring post-v1.4 shape)
    - `## Component Evidence` (raw counts for tests/typecheck/lint/audit with command excerpts)
    - `## Cleanup Actions — Executed` (if any — low-risk things like stale test-results/ removal)
    - `## Cleanup Actions — Deferred to v1.6` (per D-05, medium/low findings become v1.6 backlog; surface here but DO NOT write v1.6-seeds.md yet — that's Plan 04's job)
    - `## Red Flags` (anything blocking — e.g., typecheck failing in CI)

    CRITICAL: the frontmatter `health_score` MUST be a real computed number. Plan 04 phase gate will grep for `^health_score: [0-9]` — if missing or non-numeric the gate fails.

    DO NOT execute destructive cleanup actions (do not delete files from src/, do not run `npm audit fix --force`, do not delete node_modules). Surface the action; defer execution.
  </action>
  <verify>
    <automated>
      test -f .planning/PIPELINE-MAINTAIN.md &&
      grep -E '^health_score: [0-9]+(\.[0-9]+)?$' .planning/PIPELINE-MAINTAIN.md &&
      grep -E '^  tests: [0-9]' .planning/PIPELINE-MAINTAIN.md &&
      grep -E '^  typecheck: [0-9]' .planning/PIPELINE-MAINTAIN.md &&
      grep -E '^  lint: [0-9]' .planning/PIPELINE-MAINTAIN.md &&
      grep -E '^  audit: [0-9]' .planning/PIPELINE-MAINTAIN.md
    </automated>
  </verify>
  <done>PIPELINE-MAINTAIN.md exists with computed numeric health_score (0-10) + 4 component subscores in frontmatter; evidence sections populated; no destructive cleanup executed.</done>
</task>

<task type="auto">
  <name>Task 2: Write merged v1.4 + v1.5 retrospective (PIPELINE-REFLECT.md + second-brain notes)</name>
  <files>
    .planning/PIPELINE-REFLECT.md
    .planning/milestones/v1.4-v1.5-RETROSPECTIVE.md
    ~/second-brain/projects/nlm/notes/retro-2026-04-18.md"
    ~/second-brain/projects/nlm/notes/milestone-v1.5-summary.md"
  </files>
  <action>
    Write the combined v1.4+v1.5 retrospective per D-01, D-02, D-03, D-13, D-14. Four files, same-session:

    Step 1 — Overwrite `.planning/PIPELINE-REFLECT.md` with merged frontmatter + body:

    ```yaml
    ---
    type: pipeline-reflect
    milestone: v1.4-v1.5
    date: 2026-04-18
    v1_4_pr: 7
    v1_4_merge_commit: e14be93
    v1_5_branch: chore/v1.5-archive-v1.4
    v1_5_merge_commit: TBD
    ---
    ```

    Then sections (per D-02):
    - `# Reflection — v1.4 + v1.5 (2026-04-18)`
    - `## v1.4 Coding Challenges + Multi-Language Sandbox`
      - `### What Worked` (autonomous pipeline, MSA-from-day-1 choice, spike gate caught Judge0 issues early)
      - `### What Didn't` (typecheck red on test files, Judge0 spike partial-deferral)
      - `### Decisions We'd Change` (reference v1.5 context reframe — would have verified prod-was-on-v0.1-GCE BEFORE starting v1.4)
      - `### Surprises` (v1.4 shipped without ever being live on prod — whole milestone was pre-migration)
      - `### Stats` (9 phases 36-44, 28 plans, ~10h wall-clock, 963 tests)
    - `## v1.5 Production Migration`
      - `### What Worked` (phase split forced real migration discipline, SUNSET window explicit, 30-day warm rollback)
      - `### What Didn't` (gap-fill reflect: fill in from actual v1.5 execution; if phases 45-52 still not shipped at time of execute, note "v1.5 retro authored at phase-53 plan time — fill gaps with ground-truth from PIPELINE.md stage statuses and phase-N CONTEXT.md outcomes")
      - `### Decisions We'd Change`
      - `### Surprises`
      - `### Stats` (9 phases 45-53, plan count from ROADMAP §Phase-by-phase plans, timeline)
    - `## Action Items for v1.6` (these become Plan 04's seed inputs — keep them explicit and copy-pasteable)

    Step 2 — Copy `.planning/PIPELINE-REFLECT.md` to `.planning/milestones/v1.4-v1.5-RETROSPECTIVE.md` per D-03 (overwrite any existing file with same name).

    Step 3 — Write `~/second-brain/projects/nlm/notes/retro-2026-04-18.md` per D-13. Use tilde-expanded absolute path via `echo $HOME/second-brain/projects/nlm/notes/retro-2026-04-18.md`. Content: same as PIPELINE-REFLECT.md body (without the frontmatter) with an Obsidian-friendly header:
    ```markdown
    # Retro — v1.4 + v1.5 (2026-04-18)

    Project: [[nlm]]
    Milestones: v1.4 (Coding Challenges), v1.5 (Production Migration)
    Source: .planning/PIPELINE-REFLECT.md

    <body>
    ```
    If `~/second-brain/projects/nlm/notes/` does not exist, create it via `mkdir -p`.

    Step 4 — Write `~/second-brain/projects/nlm/notes/milestone-v1.5-summary.md` per D-14. One-page narrative (~400 words):
    - Goal: migrate NLM from v0.1 GCE to Cloud Run
    - Outcome: [fill from PIPELINE.md + Phase 52 ship status]
    - Key numbers: phases, plans, load-test baseline p95, cold-start time, cost/1000 requests
    - Lessons: top 3 from retro action items

    CRITICAL: Git commit all 4 files in the same commit at end of plan. The second-brain files are OUTSIDE the repo — commit only the repo files (PIPELINE-REFLECT.md + v1.4-v1.5-RETROSPECTIVE.md + PIPELINE-MAINTAIN.md from Task 1). Second-brain is a separate git repo managed by Obsidian plugin; DO NOT try to commit paths outside the repo from this repo. Mitigation of T-53-01: after writing both second-brain files, run `ls -la $HOME/second-brain/projects/nlm/notes/retro-2026-04-18.md $HOME/second-brain/projects/nlm/notes/milestone-v1.5-summary.md` to confirm both exist on disk; if the second-brain is its own git repo the user runs commit there separately.
  </action>
  <verify>
    <automated>
      test -f .planning/PIPELINE-REFLECT.md &&
      test -f .planning/milestones/v1.4-v1.5-RETROSPECTIVE.md &&
      test -f $HOME/second-brain/projects/nlm/notes/retro-2026-04-18.md &&
      test -f $HOME/second-brain/projects/nlm/notes/milestone-v1.5-summary.md &&
      grep -q "## v1.4" .planning/PIPELINE-REFLECT.md &&
      grep -q "## v1.5" .planning/PIPELINE-REFLECT.md &&
      grep -q "Action Items for v1.6" .planning/PIPELINE-REFLECT.md
    </automated>
  </verify>
  <done>Both retro + maintain artifacts exist in .planning/; both second-brain notes exist at exact CLAUDE.md-specified paths; PIPELINE-REFLECT.md contains both "## v1.4" and "## v1.5" sections plus "Action Items for v1.6" (feeds Plan 04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo ↔ second-brain | Second-brain lives at `~/second-brain/` — separate git repo. Retro must be saved to BOTH places. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Information Disclosure (loss) | Retro written only locally | mitigate | Task 2 writes to both `.planning/PIPELINE-REFLECT.md` (committed to repo) AND `~/second-brain/projects/nlm/notes/retro-2026-04-18.md` (committed in the separate vault repo). Verify block asserts both paths exist. |
</threat_model>

<verification>
- `.planning/PIPELINE-MAINTAIN.md` frontmatter contains `health_score:` with a numeric value
- `.planning/PIPELINE-REFLECT.md` covers BOTH v1.4 and v1.5 (dual H2 sections)
- Two second-brain notes exist at the CLAUDE.md-specified paths
- `.planning/milestones/v1.4-v1.5-RETROSPECTIVE.md` is a copy of the retro (archive)
</verification>

<success_criteria>
- META-01 satisfied: v1.4 reflect artifact exists in second-brain + `.planning/PIPELINE-REFLECT.md`
- META-02 satisfied: maintain sweep committed with numeric health_score (0-10) in frontmatter
- Plan 04 phase gate can parse health_score via grep
- Retro explicitly lists v1.6 action items (Plan 04 reads these to write seeds)
</success_criteria>

<output>
After completion, create `.planning/phases/53-reflect-maintain-runbook-finalization-decommission-plan/53-01-SUMMARY.md` listing the 5 files written (3 in repo + 2 in second-brain), the computed health_score, and the top 3 v1.6 action items.
</output>
