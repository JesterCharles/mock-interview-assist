---
phase: 53-reflect-maintain-runbook-finalization-decommission-plan
plan: 03
type: execute
wave: 2
depends_on: [53-01-PLAN]
files_modified:
  - CLAUDE.md
  - README.md
autonomous: true
requirements: [DOCS-03, DOCS-04]
must_haves:
  truths:
    - "CLAUDE.md Deploy section reflects Cloud Run reality (not GCE + docker compose)"
    - "CLAUDE.md no longer contains the substring 'docker compose up' (per D-10)"
    - "CLAUDE.md Tech Stack section mentions @faker-js/faker and k6 (per D-09)"
    - "README.md overview links to .planning/DEPLOY.md and mentions Cloud Run"
    - "README.md no longer lists Docker-Compose-up as the deploy story"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Claude Code project instructions — Deploy + Tech Stack sections updated to Cloud Run"
      must_not_contain: "docker compose up"
      must_contain: "Cloud Run"
    - path: "README.md"
      provides: "Project README — overview + deploy story updated to Cloud Run"
      must_contain_link: "[DEPLOY.md](.planning/DEPLOY.md)"
  key_links:
    - from: "README.md"
      to: ".planning/DEPLOY.md"
      via: "overview section markdown link"
    - from: "CLAUDE.md"
      to: ".planning/DEPLOY.md"
      via: "Deploy section reference"
---

<objective>
Rewrite the Deploy section of `CLAUDE.md` per D-08, update Tech Stack per D-09, strip the obsolete `docker compose up` line per D-10. Update `README.md` project-overview per D-11 + replace any docker-compose deploy story per D-12.

Purpose: CLAUDE.md is the first thing every Claude Code session reads — it must reflect Cloud Run reality, not v0.1 GCE docker-compose. README.md is the first thing humans read — same rule.
Output: Both files updated; grep confirms `docker compose up` is GONE from CLAUDE.md; both files link to .planning/DEPLOY.md; Cloud Run language replaces GCE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-reflect-maintain-runbook-finalization-decommission-plan/53-CONTEXT.md
@./CLAUDE.md
@./README.md

<interfaces>
<!-- Current state of the two files (load-bearing for the Edit surgery below) -->

CLAUDE.md relevant sections to modify:
- Line 20 (confirmed via grep): `Docker: \`docker compose up\` (uses \`.env.docker\`, maps port 80 → 3000).` — DELETE or REPLACE this line.
- `## Tech Stack` section (single line entries) — ADD entries for @faker-js/faker (dev), k6 (dev), google-github-actions (CI).
- No current explicit "## Deploy" section in CLAUDE.md (grep confirms). NEW section to add AFTER "## Tech Stack".

README.md relevant sections to modify:
- Line 1-3 overview: `# Next Level Mock\n\nAn adaptive technical skills development platform that gives associates repeated mock experiences with AI-scored feedback, tracks improvement over time, and surfaces readiness signals to trainers.` — APPEND "Deployed to Cloud Run on GCP" + link.
- Lines 62-66 "## Docker" section currently reads:
  ```
  ## Docker

  ```bash
  docker compose up    # Uses .env.docker, maps port 80 -> 3000
  ```
  ```
  This section remains valid for LOCAL DEV (docker compose is still the local Judge0 story per "Coding Challenges — Local Dev" §) — rename to "## Docker (Local Dev Only)" and add a note "Production runs on Cloud Run; see .planning/DEPLOY.md."

D-08 REQUIRED replacement text for CLAUDE.md Deploy section:
"Deployed to Cloud Run on GCP. See `.planning/DEPLOY.md`. Env vars live in Google Secret Manager per project. CI: `.github/workflows/pr-checks.yml` + `deploy-staging.yml` + `deploy-prod.yml` + `rollback-prod.yml`. Auth via WIF — no SA keys in repo."

D-11 REQUIRED overview addition: "Deployed to Cloud Run on GCP. See [DEPLOY.md](.planning/DEPLOY.md)."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md — remove docker-compose-up line, add Deploy section, update Tech Stack</name>
  <files>
    CLAUDE.md
  </files>
  <action>
    Three precise edits to `CLAUDE.md`. Use the Edit tool with exact-string match to avoid collateral damage.

    ### Edit 1 — DELETE the `docker compose up` line (D-10)

    Find the exact line:
    ```
    Docker: `docker compose up` (uses `.env.docker`, maps port 80 → 3000).
    ```
    (It appears once at line 20 per grep.) DELETE the entire line AND the blank line above it if that creates a double-blank.

    After this edit: `grep -c "docker compose up" CLAUDE.md` MUST equal 0. This is part of the verify block.

    ### Edit 2 — ADD a new `## Deploy` section right after `## Commands` block (before `## Architecture`)

    Find the anchor (the end of the Commands code block):
    ```
    npm run test:watch  # Run Vitest in watch mode
    ```
    ```

    After that closing triple-backtick (and any blank line), insert the new Deploy section (EXACT D-08 text, verbatim):

    ```markdown

    ## Deploy

    Deployed to Cloud Run on GCP. See `.planning/DEPLOY.md`. Env vars live in Google Secret Manager per project. CI: `.github/workflows/pr-checks.yml` + `deploy-staging.yml` + `deploy-prod.yml` + `rollback-prod.yml`. Auth via WIF — no SA keys in repo.

    ```

    ### Edit 3 — Tech Stack additions (D-09)

    Find the existing `## Tech Stack` block (currently ~12 entries). Add these entries at the END of the bulleted list (preserve existing entries exactly):

    ```markdown
    - **Seed data**: @faker-js/faker (devDep, staging seeder per Phase 46)
    - **Load testing**: k6 (loadtest/baseline.js per Phase 49)
    - **CI auth**: google-github-actions/auth + setup-gcloud via WIF (no SA keys in repo)
    ```

    Do NOT remove or reorder existing Tech Stack entries.

    T-53-03 mitigation: verify block asserts `docker compose up` is NOT in CLAUDE.md.
  </action>
  <verify>
    <automated>
      test $(grep -c "docker compose up" CLAUDE.md) -eq 0 &&
      grep -q "^## Deploy$" CLAUDE.md &&
      grep -q "Deployed to Cloud Run on GCP" CLAUDE.md &&
      grep -q "deploy-staging.yml" CLAUDE.md &&
      grep -q "rollback-prod.yml" CLAUDE.md &&
      grep -q "WIF — no SA keys in repo" CLAUDE.md &&
      grep -q "@faker-js/faker" CLAUDE.md &&
      grep -q "k6" CLAUDE.md &&
      grep -q "google-github-actions" CLAUDE.md
    </automated>
  </verify>
  <done>CLAUDE.md contains exactly 0 occurrences of "docker compose up", has a new `## Deploy` section with the exact D-08 phrasing, and Tech Stack lists @faker-js/faker + k6 + google-github-actions.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md — overview link + rename Docker section as local-dev-only</name>
  <files>
    README.md
  </files>
  <action>
    Two edits to `README.md`:

    ### Edit 1 — Add Cloud Run line to project overview (D-11)

    Find the opening paragraph:
    ```
    # Next Level Mock

    An adaptive technical skills development platform that gives associates repeated mock experiences with AI-scored feedback, tracks improvement over time, and surfaces readiness signals to trainers.
    ```

    After the closing period of that paragraph, add a new paragraph:
    ```markdown

    Deployed to Cloud Run on GCP. See [DEPLOY.md](.planning/DEPLOY.md).
    ```

    ### Edit 2 — Rename and contextualize the Docker section (D-12)

    Find the existing section:
    ```markdown
    ## Docker

    ```bash
    docker compose up    # Uses .env.docker, maps port 80 -> 3000
    ```
    ```

    Replace with:
    ```markdown
    ## Docker (Local Dev Only)

    For local development against Dockerized Judge0 (see "Coding Challenges — Local Dev" below). Production runs on Cloud Run; see [DEPLOY.md](.planning/DEPLOY.md).

    ```bash
    docker compose up    # Uses .env.docker, maps port 80 -> 3000
    ```
    ```

    Preserve the rest of the README (Features, Commands, Coding Challenges — Local Dev, Tech Stack). Do not reorder sections.

    Note: README.md retains `docker compose up` under "Coding Challenges — Local Dev" and "Docker (Local Dev Only)" — that's INTENTIONAL because docker-compose is still the local Judge0 dev story. The CLAUDE.md grep-removal rule applies ONLY to CLAUDE.md per D-10. README.md only needs the heading rename + overview link.
  </action>
  <verify>
    <automated>
      grep -q "Deployed to Cloud Run on GCP" README.md &&
      grep -q "\[DEPLOY.md\](.planning/DEPLOY.md)" README.md &&
      grep -q "^## Docker (Local Dev Only)" README.md &&
      grep -q "Production runs on Cloud Run" README.md
    </automated>
  </verify>
  <done>README.md overview mentions Cloud Run + links to .planning/DEPLOY.md; Docker section renamed to "Docker (Local Dev Only)" with a pointer to DEPLOY.md for prod; existing sections (Features, Commands, Coding Challenges Local Dev, Tech Stack) preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md ↔ Claude Code sessions | Every Claude session reads this file. Stale deploy instructions = wrong mental model for 100% of future work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-03 | Information Disclosure (stale docs) | CLAUDE.md retains `docker compose up` deploy line post-migration | mitigate | Verify block asserts `grep -c "docker compose up" CLAUDE.md -eq 0`. Task 1 Edit 1 explicitly removes the single line. Note: README.md intentionally keeps `docker compose up` in the LOCAL DEV section (different semantic context). |
</threat_model>

<verification>
- `grep -c "docker compose up" CLAUDE.md` equals 0
- CLAUDE.md has `## Deploy` section with verbatim D-08 text
- CLAUDE.md Tech Stack lists @faker-js/faker, k6, google-github-actions
- README.md overview links to `.planning/DEPLOY.md`
- README.md Docker section is now `## Docker (Local Dev Only)` with a Cloud Run pointer
</verification>

<success_criteria>
- DOCS-03 satisfied: CLAUDE.md Deploy section reflects Cloud Run (GCE + docker-compose references removed from that section)
- DOCS-04 satisfied: README.md overview says "Deployed to Cloud Run on GCP" with DEPLOY.md link
- T-53-03 mitigated: grep confirms no stale `docker compose up` line remains in CLAUDE.md
</success_criteria>

<output>
After completion, create `.planning/phases/53-reflect-maintain-runbook-finalization-decommission-plan/53-03-SUMMARY.md` listing the two files modified, the 3 edits to CLAUDE.md, and the 2 edits to README.md.
</output>
