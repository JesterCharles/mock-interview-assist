---
phase: 15-design-cohesion-sweep
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/app/page.tsx
  - src/app/interview/page.tsx
  - src/app/review/page.tsx
autonomous: false
requirements: [DESIGN-03]
must_haves:
  truths:
    - "/ (anonymous automated-interview root) renders on DESIGN.md tokens — warm parchment bg, Clash Display headings, flat burnt orange accent, no glass/gradient/glow"
    - "/interview (trainer-led) renders on DESIGN.md tokens with ZERO behavioral change to STT, scoring, timer, or question flow"
    - "/review (trainer validation) renders on DESIGN.md tokens with unchanged score-table / override logic"
    - "A full trainer-led interview smoke test (setup → interview → review → PDF) completes successfully without visual artifacts or functional regressions"
    - "Zero legacy utility / --nlm-* / kill-list animation references remain in these three files"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Anonymous automated-interview root wrapped in PublicShell, hero on surface card, flat accent CTAs"
    - path: "src/app/interview/page.tsx"
      provides: "Trainer-led session shell on var(--bg), token-driven cards, no gradient headings"
    - path: "src/app/review/page.tsx"
      provides: "Score validator UI on DESIGN tokens, preserved table + override logic"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/layout/PublicShell.tsx"
      via: "wrapper import"
      pattern: "PublicShell"
    - from: "src/app/interview/page.tsx"
      to: "src/components/QuestionCard.tsx, src/components/SpeechToText.tsx"
      via: "already migrated in Plan 01 — consume as-is"
      pattern: "from ['\"]@/components/(QuestionCard|SpeechToText)['\"]"
---

<objective>
Migrate the three highest-regression-risk legacy surfaces — `/`, `/interview`, `/review` — onto DESIGN.md tokens. These are the live interview flows. Token/utility swap ONLY; no logic changes. Manual trainer-led full-interview smoke is MANDATORY before plan close (D-06, D-20).

Purpose: Unblock the final deletion commit (Plan 04). These three pages are the largest remaining consumers of legacy utilities (`/` alone has 31 hits). Once migrated, every other consumer has already been handled by Plan 01 (shared components) or Plan 03 (low-risk pages).

Output: Three restyled pages, manual smoke-test approval, zero legacy references in touched files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@DESIGN.md
@.planning/phases/15-design-cohesion-sweep/15-CONTEXT.md
@.planning/phases/14-design-cohesion/14-01-SUMMARY.md
@.planning/phases/15-design-cohesion-sweep/15-01-SUMMARY.md
@src/app/globals.css
@src/components/layout/PublicShell.tsx
@src/app/page.tsx
@src/app/interview/page.tsx
@src/app/review/page.tsx

<interfaces>
<!-- PublicShell (from Phase 14 Plan 01) — server component, warm parchment, max-w-1120, Clash wordmark. -->
<!-- Accepts children + optional data-http-status / data-testid passthrough. -->
import PublicShell from '@/components/layout/PublicShell';

<!-- Already-migrated shared components from Plan 15-01 — import normally, they use DESIGN tokens now -->
import QuestionCard from '@/components/QuestionCard';
import SpeechToText from '@/components/SpeechToText';
import Navbar from '@/components/Navbar';
import ProgressBar from '@/components/ProgressBar';

<!-- DESIGN tokens — see 15-01-PLAN interfaces block. Same set. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate / (anonymous automated-interview root)</name>
  <files>src/app/page.tsx</files>
  <action>
  Highest-hit file (31 legacy references). This is the anonymous /  route — public automated interview. Behavior MUST NOT change; the underlying interview state machine (startInterview, handleResponse, scoring callbacks) is untouched.

  Step 1 (baseline):
    grep -cE "nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\\bbtn-primary\\b|\\bbtn-accent\\b|gradient-shift|float|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse" src/app/page.tsx
    (Capture number for commit body. Target post-migration: 0.)

  Step 2 (migrate):
    - Wrap the page's returned JSX in <PublicShell> (server or client component — match current file type; if client, PublicShell is already safe to use).
    - Hero section: replace .glass-card / .glass-card-strong + .gradient-text / .gradient-text-static with:
        Outer: <section style={{ background: 'var(--surface)', border: '1px solid var(--border)', borderRadius: 12, boxShadow: '0 1px 2px rgba(0,0,0,0.04)', padding: 'var(--space-xl, 32px)' }}>
        Heading: Clash Display 48px, color var(--ink), NO gradient.
        Subtext: DM Sans 18px var(--muted).
    - CTAs: .btn-primary / .btn-accent → .btn-accent-flat (primary) / .btn-secondary-flat (secondary).
    - Backgrounds: any .nlm-bg wrapper → remove (PublicShell + globals body already owns var(--bg)).
    - Glow borders / glow classes → plain `border border-[var(--border)]` or focus ring `ring-1 ring-[var(--accent)]/30`.
    - All --nlm-* token reads → DESIGN.md equivalents per D-01 mapping table in CONTEXT.
    - Remove any inline `animation:` using gradient-shift / float / pulse-glow / shimmer / border-glow-pulse. Allowed motion only: slide-up 200ms ease-out or fade-in 150ms ease-out.
    - Recording indicator inside the interview flow is owned by the already-migrated SpeechToText component (Plan 01) — do NOT reimplement.

  Step 3 (verify):
    - npx tsc --noEmit
    - npm run build (catches RSC / client boundary issues)
    - npm run test -- --run
    - Scoped grep gate MUST be 0:
        grep -cE "nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\\bbtn-primary\\b|\\bbtn-accent\\b|gradient-shift|\\bfloat\\b|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse" src/app/page.tsx

  Step 4 (atomic commit): `refactor(15-02): migrate / (anon automated-interview) to DESIGN tokens`
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npm run test -- --run &amp;&amp; test "$(grep -cE 'nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\bbtn-primary\b|\bbtn-accent\b|gradient-shift|\bfloat\b|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse' src/app/page.tsx)" = "0"</automated>
  </verify>
  <done>src/app/page.tsx: zero legacy references, build + typecheck + vitest green, atomic commit landed. Behavior preserved (no JSX restructuring beyond token/utility swap + PublicShell wrap).</done>
</task>

<task type="auto">
  <name>Task 2: Migrate /interview and /review (trainer-led flow)</name>
  <files>src/app/interview/page.tsx, src/app/review/page.tsx</files>
  <action>
  Mid-session surfaces. Token swap ONLY. Preserve: Zustand store subscriptions, STT wiring, per-question scoring calls, timer, keyword tracking, review/override handlers, PDF generation trigger.

  Step 1 (baseline):
    grep -cE "nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\\bbtn-primary\\b|\\bbtn-accent\\b|gradient-shift|\\bfloat\\b|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse" src/app/interview/page.tsx src/app/review/page.tsx

  Step 2 (/interview — 4 legacy hits):
    - Outer wrapper: bg-[var(--bg)] min-h-screen. Do NOT wrap in PublicShell (trainer-led has Navbar chrome from the root layout; PublicShell is for public surfaces per D-14 discretion).
    - Question panel: bg-[var(--surface)] border border-[var(--border)] rounded-xl shadow-[0_1px_2px_rgba(0,0,0,0.04)].
    - Section headings: Clash Display, var(--ink), no gradient-text.
    - CTA buttons (Next / End / Submit): .btn-accent-flat. Secondary: .btn-secondary-flat.
    - ProgressBar + SpeechToText + QuestionCard are already DESIGN-token components from Plan 01 — import + use, no local overrides.
    - Any stray --nlm-* color references → var(--accent) / var(--success) / var(--warning) / var(--danger).

  Step 3 (/review — 7 legacy hits):
    - Outer wrapper: bg-[var(--bg)] min-h-screen.
    - Score table: bg-[var(--surface)], row separator border-[var(--border-subtle)], hover bg-[var(--highlight)], tabular-nums on score cells (fontFamily: 'var(--font-jetbrains-mono)' or DM Sans tabular-nums).
    - Validator controls: .btn-accent-flat for "Approve", .btn-secondary-flat for "Override". Override textarea: bg-[var(--bg)] border border-[var(--border)] focus: border-[var(--accent)].
    - Soft-skill pills: bg-[var(--surface-muted)] text-[var(--muted)] rounded-full.
    - Status badge: semantic badge colors per DESIGN.md §Semantic Badge Colors.

  Step 4 (verify):
    - npx tsc --noEmit
    - npm run build
    - npm run test -- --run
    - Scoped grep gate (MUST be 0):
        grep -cE "nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\\bbtn-primary\\b|\\bbtn-accent\\b|gradient-shift|\\bfloat\\b|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse" src/app/interview/page.tsx src/app/review/page.tsx

  Step 5 (atomic commit): `refactor(15-02): migrate /interview + /review to DESIGN tokens`
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npm run test -- --run &amp;&amp; test "$(grep -cE 'nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\bbtn-primary\b|\bbtn-accent\b|gradient-shift|\bfloat\b|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse' src/app/interview/page.tsx src/app/review/page.tsx | awk -F: '{s+=$2} END {print s}')" = "0"</automated>
  </verify>
  <done>/interview + /review on DESIGN tokens, zero legacy references, build + tests green, atomic commit landed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual trainer-led full-interview smoke (D-20 hard gate)</name>
  <what-built>
  Migrated /, /interview, /review onto DESIGN.md tokens. No logic changes. All three pages now render on warm parchment bg with Clash Display headings, flat burnt orange CTAs, no glass/gradient/glow/decorative motion.
  </what-built>
  <how-to-verify>
  Run a FULL trainer-led interview end-to-end. This is the D-20 hard gate — Plan 04 (deletion) cannot start until this passes.

  1. `npm run dev` — open http://localhost:3000
  2. Log in as trainer.
  3. Go to /dashboard, complete setup wizard (pick a small repo + 2-3 questions).
  4. Start interview → verify /interview page:
     - Warm parchment bg (#F5F0E8), NOT dark blue.
     - Question card is matte white surface with subtle warm border.
     - Section heading is Clash Display, solid charcoal, NO gradient.
     - "Recording" indicator is a static flat dot + uppercase mono label (no pulse animation).
     - ProgressBar is flat burnt orange on parchment track.
     - CTAs are flat burnt orange buttons (.btn-accent-flat).
  5. Answer 2 questions via voice + text. Confirm STT works, scoring queues, timer advances.
  6. Finish interview → /review page:
     - Score table renders on warm surface, tabular-nums aligned.
     - Override textarea accepts edits, saves via existing handler.
     - Submit → PDF generates → email send UI works.
  7. Anonymous /: open incognito, hit /, verify hero card on warm parchment, no glass/gradient, start public interview, answer 1 question, confirm automated flow works.
  8. Regression check: /dashboard still loads (will be migrated in Plan 03 — legacy styling currently EXPECTED).

  Type "approved — full smoke passed" OR describe any functional regression or visual artifact.
  </how-to-verify>
  <resume-signal>Type "approved" to unblock Plan 04, or describe issue.</resume-signal>
</task>

</tasks>

<verification>
Plan-level gate:
```
grep -rE "nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\bbtn-primary\b|\bbtn-accent\b|gradient-shift|\bfloat\b|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse" \
  src/app/page.tsx src/app/interview/page.tsx src/app/review/page.tsx
# MUST return nothing.
```
Plus: manual smoke signed off.
</verification>

<success_criteria>
- /, /interview, /review fully migrated to DESIGN tokens.
- Zero legacy references in the three files.
- Typecheck + build + vitest green.
- Manual trainer-led full-interview smoke passed (D-20).
- Two atomic commits landed (Task 1, Task 2).
</success_criteria>

<output>
After completion, create `.planning/phases/15-design-cohesion-sweep/15-02-SUMMARY.md`. Include: baseline vs. post grep counts per file, commit SHAs, manual smoke approval quote, any deviations. This plan's SUMMARY is a hard prerequisite for Plan 04's deletion commit.
</output>
