---
phase: 07-adaptive-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/adaptiveSetup.ts
  - src/lib/__tests__/adaptiveSetup.test.ts
  - src/app/api/associates/[slug]/gap-scores/route.ts
autonomous: true
requirements:
  - GAP-05
user_setup: []

must_haves:
  truths:
    - "mapGapScoresToWeights returns weight 5 for lowest score and weight 1 for highest score"
    - "mapGapScoresToWeights returns weight 3 for all skills when scores are equal"
    - "GET /api/associates/[slug]/gap-scores returns gap scores for a known associate"
    - "GET /api/associates/[slug]/gap-scores returns found:false for unknown slug"
    - "GET /api/associates/[slug]/gap-scores returns 401 for unauthenticated requests"
    - "GET /api/associates/[slug]/gap-scores returns 400 for invalid slug format"
  artifacts:
    - path: "src/lib/adaptiveSetup.ts"
      provides: "Weight interpolation utility and types"
      exports: ["mapGapScoresToWeights", "SkillGapScore", "GapScoreResponse"]
    - path: "src/lib/__tests__/adaptiveSetup.test.ts"
      provides: "Unit tests for weight mapping"
      min_lines: 40
    - path: "src/app/api/associates/[slug]/gap-scores/route.ts"
      provides: "Gap scores API endpoint"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/associates/[slug]/gap-scores/route.ts"
      to: "prisma.associate"
      via: "Prisma query with gapScores relation"
      pattern: "prisma\\.associate\\.findUnique"
    - from: "src/app/api/associates/[slug]/gap-scores/route.ts"
      to: "src/lib/auth-server.ts"
      via: "isAuthenticatedSession guard"
      pattern: "isAuthenticatedSession"
    - from: "src/lib/adaptiveSetup.ts"
      to: "src/app/api/associates/[slug]/gap-scores/route.ts"
      via: "Shared GapScoreResponse type"
      pattern: "GapScoreResponse"
---

<objective>
Create the gap scores API endpoint and weight interpolation utility that powers adaptive mock setup.

Purpose: Provides the backend data and transformation logic for pre-populating the setup wizard with gap-informed technology weights. Plan 02 consumes these to wire the dashboard UI.

Output: GET endpoint at /api/associates/[slug]/gap-scores, pure utility function mapGapScoresToWeights, and unit tests for the weight mapping logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-adaptive-setup/07-CONTEXT.md
@.planning/phases/07-adaptive-setup/07-RESEARCH.md
@DESIGN.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/github-service.ts:
```typescript
export interface GitHubFile {
    name: string;
    path: string;
    sha: string;
    size: number;
    url: string;
    html_url: string;
    git_url: string;
    download_url: string;
    type: 'file' | 'dir';
}
```

From src/lib/auth-server.ts:
```typescript
export async function isAuthenticatedSession(): Promise<boolean>;
```

From src/store/interviewStore.ts (consumer in Plan 02, but shapes the types here):
```typescript
setSelectedTechs: (techs: GitHubFile[]) => void;
setTechWeight: (techPath: string, weight: number) => void;
techWeights: Record<string, number>; // Maps tech path to weight (1-5)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create weight interpolation utility with types and tests</name>
  <files>src/lib/adaptiveSetup.ts, src/lib/__tests__/adaptiveSetup.test.ts</files>
  <read_first>
    - src/lib/types.ts (existing type patterns)
    - .planning/phases/07-adaptive-setup/07-RESEARCH.md (code examples section)
    - .planning/phases/04-gap-service/04-CONTEXT.md (GapScore model: D-06)
  </read_first>
  <behavior>
    - Test 1: Given scores [0.2, 0.5, 0.8], returns {skill1: 5, skill2: 3, skill3: 1} — lowest score gets weight 5 (per D-02)
    - Test 2: Given scores [0.9, 0.1], returns {skill1: 1, skill2: 5} — two skills, inverted
    - Test 3: Given single skill [0.5], returns {skill1: 3} — equal scores get neutral weight
    - Test 4: Given all-equal scores [0.5, 0.5, 0.5], returns all weight 3
    - Test 5: Given all-zero scores [0.0, 0.0], returns all weight 3
    - Test 6: Given empty array, returns empty object
    - Test 7: Given scores [0.0, 1.0], returns {skill1: 5, skill2: 1} — full range boundary
  </behavior>
  <action>
    Create src/lib/adaptiveSetup.ts with:

    1. Export `SkillGapScore` interface: `{ skill: string; weightedScore: number }` — the shape returned from the gap scores API (skill is the tech file path matching GitHubFile.path, weightedScore is 0.0-1.0 range per Phase 4 GapScore model).

    2. Export `GapScoreResponse` interface: `{ found: boolean; sessionCount: number; scores: SkillGapScore[] }` — the full API response shape consumed by dashboard.

    3. Export `mapGapScoresToWeights(scores: SkillGapScore[]): Record<string, 1 | 2 | 3 | 4 | 5>` — per D-02: lowest gap score skill gets weight 5, highest gets weight 1, linear interpolation between. When all scores are equal (maxScore === minScore), return weight 3 for all. Formula: `normalized = (score - min) / (max - min)`, `weight = 5 - Math.round(normalized * 4)`, clamped to [1, 5].

    Create src/lib/__tests__/adaptiveSetup.test.ts with the behavior tests above. Use the project's test runner (check if vitest or jest is configured; if neither exists, write tests compatible with jest since 07-RESEARCH.md references jest). If no test runner is installed, add a note but still write the test file.
  </action>
  <acceptance_criteria>
    - grep "export function mapGapScoresToWeights" src/lib/adaptiveSetup.ts
    - grep "export interface SkillGapScore" src/lib/adaptiveSetup.ts
    - grep "export interface GapScoreResponse" src/lib/adaptiveSetup.ts
    - grep "weight 5 for lowest" src/lib/__tests__/adaptiveSetup.test.ts OR grep "lowest score" src/lib/__tests__/adaptiveSetup.test.ts
  </acceptance_criteria>
  <verify>
    <automated>npx jest --testPathPattern=adaptiveSetup --no-coverage 2>/dev/null || npx vitest run src/lib/__tests__/adaptiveSetup.test.ts 2>/dev/null || echo "Test runner not installed — verify tests manually"</automated>
  </verify>
  <done>
    - mapGapScoresToWeights correctly inverts gap scores to weights 1-5 per D-02
    - All 7 behavior tests pass (or test file exists with correct assertions if no runner)
    - SkillGapScore and GapScoreResponse types exported for use by API route and dashboard
  </done>
</task>

<task type="auto">
  <name>Task 2: Create gap scores API endpoint</name>
  <files>src/app/api/associates/[slug]/gap-scores/route.ts</files>
  <read_first>
    - src/app/api/history/route.ts (existing API route auth pattern)
    - src/lib/auth-server.ts (isAuthenticatedSession usage)
    - .planning/phases/04-gap-service/04-CONTEXT.md (GapScore model D-06, Associate model)
    - .planning/phases/03-associate-profiles/03-CONTEXT.md (Associate model D-03, slug validation D-05)
    - src/lib/adaptiveSetup.ts (SkillGapScore and GapScoreResponse types from Task 1)
  </read_first>
  <action>
    Create src/app/api/associates/[slug]/gap-scores/route.ts with a GET handler:

    1. Auth guard: Call `isAuthenticatedSession()` from `@/lib/auth-server`. Return 401 JSON if unauthenticated. Follow the exact pattern used in existing routes like /api/history.

    2. Slug validation: Use zod to validate `params.slug` matches `^[a-z0-9-]+$` (per Phase 3 D-05 slug format). Return 400 JSON `{ error: 'Invalid slug format' }` on failure. Import zod — if zod is not yet installed in the project, use a simple regex check as fallback with a TODO comment to switch to zod once installed.

    3. Query Prisma: `prisma.associate.findUnique({ where: { slug }, include: { gapScores: { where: { topic: null } } } })` — topic null = skill-level scores only (per Phase 4 D-06). Import prisma singleton from `@/lib/prisma`.

    4. If associate not found: Return `{ found: false, sessionCount: 0, scores: [] }` with 200 status. Per D-04, unknown slug = new associate, treated as manual mode by the client. Do NOT return 404 — this prevents slug enumeration (return same shape regardless).

    5. Count sessions: `prisma.session.count({ where: { associateId: associate.id, status: 'completed' } })`.

    6. Return GapScoreResponse: `{ found: true, sessionCount, scores: associate.gapScores.map(g => ({ skill: g.skill, weightedScore: g.weightedScore })) }`.

    Import `GapScoreResponse` type from `@/lib/adaptiveSetup` for type safety on the response shape. The actual runtime response is a plain object — the type import is for development-time checking only.

    NOTE: This route depends on Prisma models (Associate, GapScore, Session) that are created in Phases 1-4. If those phases have not been executed yet, the route will have TypeScript errors until the schema exists. This is expected — the route is forward-looking and will compile once prior phases are complete.
  </action>
  <acceptance_criteria>
    - grep "isAuthenticatedSession" src/app/api/associates/\[slug\]/gap-scores/route.ts
    - grep "export async function GET" src/app/api/associates/\[slug\]/gap-scores/route.ts
    - grep "found.*false" src/app/api/associates/\[slug\]/gap-scores/route.ts
    - grep "topic.*null" src/app/api/associates/\[slug\]/gap-scores/route.ts
    - grep "sessionCount" src/app/api/associates/\[slug\]/gap-scores/route.ts
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit src/app/api/associates/\[slug\]/gap-scores/route.ts 2>&1 || echo "TypeScript check — errors expected if Prisma models not yet generated"</automated>
  </verify>
  <done>
    - GET /api/associates/[slug]/gap-scores route exists with auth guard, slug validation, Prisma query
    - Returns GapScoreResponse shape: { found, sessionCount, scores[] }
    - Unknown slug returns found:false (not 404) per D-04 and anti-enumeration pattern
    - Only skill-level scores returned (topic: null filter) per Phase 4 D-06
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/associates/[slug]/gap-scores | Untrusted slug parameter from client URL path |
| API route -> Prisma/Supabase | Validated slug used in database query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | slug path param | mitigate | Validate slug with zod regex `^[a-z0-9-]+$` before any DB query; Prisma parameterizes queries |
| T-07-02 | Information Disclosure | gap scores for other associates | mitigate | `isAuthenticatedSession()` guard at route entry; only authenticated trainers can access |
| T-07-03 | Information Disclosure | slug enumeration | mitigate | Return `{ found: false }` with 200 status for unknown slugs — same response shape as new associate; no 404 distinction |
| T-07-04 | Denial of Service | repeated gap score lookups | accept | Existing app-level rate limiting covers this endpoint; no additional rate limit needed for MVP |
</threat_model>

<verification>
- src/lib/adaptiveSetup.ts exports mapGapScoresToWeights, SkillGapScore, GapScoreResponse
- Unit tests for weight mapping cover: normal range, single skill, all-equal, all-zero, empty, boundary
- API route authenticates, validates slug, queries skill-level gap scores, returns consistent shape
- No 404 responses that enable slug enumeration
</verification>

<success_criteria>
- Weight interpolation utility correctly maps gap scores to inverted weights 1-5 per D-02
- API endpoint follows existing auth patterns and returns GapScoreResponse shape
- All tests pass (or test files exist with correct structure if test runner not installed)
</success_criteria>

<output>
After completion, create `.planning/phases/07-adaptive-setup/07-01-SUMMARY.md`
</output>
