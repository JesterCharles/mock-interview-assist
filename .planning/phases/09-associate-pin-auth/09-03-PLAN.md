---
phase: 09-associate-pin-auth
plan: 03
type: execute
wave: 3
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - src/app/associate/login/page.tsx
  - src/app/associate/login/PinEntryForm.tsx
  - src/app/associate/[slug]/page.tsx
  - src/app/trainer/components/GeneratePinButton.tsx
  - src/app/trainer/[slug]/page.tsx
  - src/app/associate/[slug]/page.test.tsx
autonomous: true
requirements:
  - AUTH-02
  - AUTH-04
must_haves:
  truths:
    - "Visiting /associate/login shows a form accepting slug and PIN"
    - "Submitting a valid slug+PIN sets the cookie (via /api/associate/pin/verify) and navigates to ?next= or /associate/{slug}"
    - "Submitting an invalid PIN shows an inline error and does NOT navigate"
    - "After 5 failed attempts the UI shows a 'too many attempts' message (429 surfaced)"
    - "Unauthenticated visit to /associate/{slug} redirects to /associate/login?next=/associate/{slug}"
    - "Authenticated associate visiting /associate/{matchingSlug} renders the profile"
    - "Authenticated associate visiting /associate/{otherSlug} receives a 403 page"
    - "Trainer visiting /associate/{anySlug} renders the profile (trainer precedence)"
    - "Trainer dashboard shows a Generate PIN button per associate; clicking it displays the PIN once in a modal with a copy-to-clipboard control and a 'shown only once' warning"
  artifacts:
    - path: src/app/associate/login/page.tsx
      provides: "PIN entry page (server component shell)"
    - path: src/app/associate/login/PinEntryForm.tsx
      provides: "Client form calling /api/associate/pin/verify"
    - path: src/app/associate/[slug]/page.tsx
      provides: "Auth-guarded associate profile server component"
      contains: "getCallerIdentity"
    - path: src/app/trainer/components/GeneratePinButton.tsx
      provides: "Trainer UI for PIN generation with one-time display modal"
  key_links:
    - from: "src/app/associate/[slug]/page.tsx"
      to: "getCallerIdentity + getAssociateIdBySlug"
      via: "server component guard"
      pattern: "getCallerIdentity|getAssociateIdBySlug"
    - from: "src/app/associate/login/PinEntryForm.tsx"
      to: "/api/associate/pin/verify"
      via: "fetch POST with fingerprint"
      pattern: "api/associate/pin/verify"
    - from: "src/app/trainer/components/GeneratePinButton.tsx"
      to: "/api/associate/pin/generate"
      via: "fetch POST with associateId, render returned pin in modal"
      pattern: "api/associate/pin/generate"
---

<objective>
Ship the associate-facing PIN entry UI, the `/associate/[slug]` auth guard, and the trainer-side PIN generation control. This plan closes AUTH-02 (client half, per D-18) and AUTH-04 (per D-21, D-22), plus the trainer UI for AUTH-01 (per D-19).

Purpose: Connect the backend (Plan 01) and middleware identity (Plan 02) to real user-facing flows. This plan delivers the observable end-to-end experience: trainer generates PIN → associate enters PIN → associate views their profile.

Output: PIN entry page + form, guarded `/associate/[slug]` server component, trainer Generate-PIN button with one-time display modal, server-component test for the slug guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-associate-pin-auth/09-CONTEXT.md
@.planning/research/PITFALLS.md
@src/app/associate/[slug]/page.tsx
@src/app/trainer/page.tsx
@src/lib/auth-context.tsx

<interfaces>
Consumes (from Plans 09-01 and 09-02):
```typescript
// Plan 09-01
POST /api/associate/pin/generate  body: { associateId: number }  → { pin: string }  (trainer-auth required)
POST /api/associate/pin/verify    body: { slug, pin, fingerprint } → 200 { ok: true, slug } | 401 { ok: false } | 429

// Plan 09-02
import { getCallerIdentity } from '@/lib/identity'; // in server component via cookies()-based variant OR use getAssociateIdentity()
import { isAuthenticatedSession, getAssociateIdentity } from '@/lib/auth-server';
import { getAssociateIdBySlug } from '@/lib/associateService'; // Plan 09-01
```

Note: `getCallerIdentity` in identity.ts takes a NextRequest (middleware). For server components, compose using `isAuthenticatedSession()` (trainer) + `getAssociateIdentity()` (associate) from auth-server.ts.

Existing fingerprint pattern:
```typescript
// Reuse @fingerprintjs/fingerprintjs the same way existing public interview flow does.
// See existing usage in the public interview start page for the call pattern.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Associate PIN entry page + form + trainer Generate-PIN UI</name>
  <files>src/app/associate/login/page.tsx, src/app/associate/login/PinEntryForm.tsx, src/app/trainer/components/GeneratePinButton.tsx, src/app/trainer/[slug]/page.tsx</files>
  <action>
    `/associate/login/page.tsx` (server component): Read `?next=` from searchParams. If caller is already authenticated (trainer OR associate), redirect to `next` or `/`. Otherwise render `<PinEntryForm nextPath={next} />`. Basic Tailwind layout — utilitarian, per D-20 (full DESIGN.md cohesion is Phase 14).

    `PinEntryForm.tsx` (client component): Controlled inputs for `slug` and `pin` (6-digit numeric, use `inputMode="numeric"` + `pattern="[0-9]{6}"` + `maxLength={6}`). On submit: load fingerprint via `@fingerprintjs/fingerprintjs` (follow existing public-interview pattern). POST `/api/associate/pin/verify` with `{slug, pin, fingerprint}`. On 200: `router.push(nextPath ?? '/associate/' + slug)`. On 401: show inline error "Invalid slug or PIN". On 429: show "Too many attempts. Please wait 15 minutes." Disable the submit button while in-flight. Never surface internals.

    `GeneratePinButton.tsx` (client component): Takes `{ associateId: number; associateName: string }`. Button "Generate PIN". On click: POST `/api/associate/pin/generate` with `{ associateId }` (relies on trainer session cookie being present — no explicit auth header). On success: open a modal showing the 6-digit PIN in a large monospace font, a copy-to-clipboard button (use `navigator.clipboard.writeText`), and a prominent warning "This PIN will not be shown again. Copy and send it to {name} now." per D-19. Modal has a single "Done" close action. On error: toast or inline error.

    Drop `<GeneratePinButton />` into the trainer associate detail page `src/app/trainer/[slug]/page.tsx` near the associate header — exact placement per planner discretion, keep it visible but not the primary CTA.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    /associate/login renders and submits correctly; trainer [slug] page shows Generate PIN button that displays the PIN in a one-time modal with copy + warning; build succeeds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auth guard on /associate/[slug] with trainer + associate + mismatch paths</name>
  <files>src/app/associate/[slug]/page.tsx, src/app/associate/[slug]/page.test.tsx</files>
  <behavior>
    - Anonymous request → redirect to /associate/login?next=/associate/{slug}
    - Associate with matching associateId → render profile
    - Associate with mismatched associateId (different slug) → return 403 response
    - Trainer (nlm_session) → render profile regardless of slug
  </behavior>
  <action>
    Modify `src/app/associate/[slug]/page.tsx` (server component) per D-21. At the top of the component:
    1. `const trainer = await isAuthenticatedSession();`
    2. `const associateIdentity = await getAssociateIdentity();` (returns `{ associateId } | null`)
    3. If `!trainer && !associateIdentity` → `redirect('/associate/login?next=' + encodeURIComponent('/associate/' + slug))`.
    4. Resolve the slug → `const targetId = await getAssociateIdBySlug(slug);` If null → call Next's `notFound()`.
    5. If `!trainer && associateIdentity && associateIdentity.associateId !== targetId` → return a 403 response. Use `notFound()`-style pattern but throw a 403: render a server component that calls `const res = new Response('Forbidden', { status: 403 });` — cleanest is to create a tiny `Forbidden` component and set response status via Next's `unauthorized()` if available in Next 16, otherwise return a JSX page and use `export const dynamic = 'force-dynamic'` plus an explicit status. Planner may use whichever Next 16 API is idiomatic; the essential contract is: mismatched associate MUST NOT see the profile content.

    Keep the existing rendering path unchanged for authorized callers (existing profile fetch logic stays).

    Write `src/app/associate/[slug]/page.test.tsx` (or equivalent test location). Mock `@/lib/auth-server` and `@/lib/associateService`. Test the four behaviors above. Assert redirect target string, assert 403 path does not render profile data, assert trainer path renders regardless of slug match.
  </action>
  <verify>
    <automated>npm run test -- src/app/associate/[slug]/page.test.tsx && npm run lint && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    /associate/[slug] enforces the four-way identity matrix (anonymous / associate-match / associate-mismatch / trainer). Tests pass. Build succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/associate/pin/verify | PIN + slug from untrusted client |
| server component → DB | Associate slug → id resolution |
| trainer UI → /api/associate/pin/generate | Relies on trainer session cookie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12 | Information Disclosure | /associate/[slug] cross-associate viewing | mitigate | Server component checks associateIdentity.associateId === targetId; mismatch returns 403 before data fetch (D-21) |
| T-09-13 | Elevation of Privilege | Client-side-only guard bypass | mitigate | Guard runs in server component (not client), cannot be bypassed by DOM manipulation |
| T-09-14 | Information Disclosure | PIN shown on trainer screen | accept | One-time display with "will not be shown again" warning; user-mediated delivery; consistent with D-04 / D-19 |
| T-09-15 | Spoofing | Client-submitted slug to verify endpoint | mitigate | Verify endpoint (Plan 09-01) uses slug only to look up pinHash; success sets cookie with the server-resolved associateId, not client-supplied identity |
| T-09-16 | Denial of Service | Rapid PIN submissions | mitigate | Form disables during in-flight; 429 surfaced as visible error; server-side fingerprint rate limit (Plan 09-01) |
</threat_model>

<verification>
- Build green (`npm run build`)
- Tests green for the /associate/[slug] guard matrix
- Manual smoke: generate PIN as trainer, log out of trainer, navigate to /associate/{slug} → redirected to /associate/login, enter slug+PIN, land on profile
- Manual mismatch smoke: log in as associate-A, navigate to /associate/{slug-of-B} → 403
</verification>

<success_criteria>
1. /associate/login renders and accepts slug + PIN
2. Successful PIN entry sets the cookie (via backend) and redirects to next path or /associate/{slug}
3. Failed PIN entry shows inline error; 429 surfaces rate-limit message
4. /associate/[slug] redirects anonymous to /associate/login?next=...
5. /associate/[slug] returns 403 for associate with mismatched slug
6. Trainer can view any /associate/[slug]
7. Trainer dashboard has a working Generate PIN button with one-time-display modal
</success_criteria>

<output>
After completion, create `.planning/phases/09-associate-pin-auth/09-03-SUMMARY.md`
</output>
