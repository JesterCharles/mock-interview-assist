---
phase: 09-associate-pin-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/pinService.ts
  - src/lib/associateService.ts
  - src/app/api/associate/pin/generate/route.ts
  - src/app/api/associate/pin/verify/route.ts
  - src/app/api/associate/logout/route.ts
  - src/lib/pinService.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "A trainer-authenticated POST to /api/associate/pin/generate returns a 6-digit PIN once and stores its bcrypt hash on the Associate row"
    - "POST /api/associate/pin/verify with correct {slug, pin} returns 200 and sets an associate_session HttpOnly cookie"
    - "POST /api/associate/pin/verify with wrong PIN returns 401 and does NOT set a cookie"
    - "Repeated failed verifications from the same fingerprint are rate-limited to 5 per 15 minutes (429 after)"
    - "POST /api/associate/logout clears the associate_session cookie"
  artifacts:
    - path: prisma/schema.prisma
      provides: "Associate.pinHash + Associate.pinGeneratedAt fields"
      contains: "pinHash"
    - path: src/lib/pinService.ts
      provides: "generatePin, hashPin, verifyPin pure functions"
      exports: ["generatePin", "hashPin", "verifyPin"]
    - path: src/lib/associateSession.ts
      provides: "signAssociateToken, verifyAssociateToken"
      exports: ["signAssociateToken", "verifyAssociateToken"]
    - path: src/app/api/associate/pin/generate/route.ts
      provides: "Trainer-auth-guarded PIN generation endpoint"
    - path: src/app/api/associate/pin/verify/route.ts
      provides: "Public PIN verification endpoint (rate-limited)"
    - path: src/app/api/associate/logout/route.ts
      provides: "Associate logout endpoint"
  key_links:
    - from: "/api/associate/pin/generate"
      to: "prisma.associate.update({ pinHash })"
      via: "hashPin(pin) then prisma update"
      pattern: "prisma\\.associate\\.update"
    - from: "/api/associate/pin/verify"
      to: "rateLimitService + bcrypt.compare + Set-Cookie"
      via: "fingerprint check -> bcrypt.compare -> signAssociateToken -> cookie header"
      pattern: "bcrypt\\.compare|Set-Cookie"
---

<objective>
Ship the PIN generation and verification backbone: schema fields, pure PIN service, and three API endpoints (generate, verify, logout). No middleware changes, no UI. This plan delivers AUTH-01 (per D-01..D-05) and the server half of AUTH-02 (per D-14..D-17).

Purpose: Establish the cryptographic and persistence foundation for associate auth before wiring middleware or UI. Isolating this plan lets the other two plans build in parallel against a stable contract.

Output: Schema migration, `pinService.ts`, `associateSession.ts`, three route handlers, unit tests for pinService.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-associate-pin-auth/09-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
@prisma/schema.prisma
@src/lib/auth-server.ts
@src/lib/rateLimitService.ts
@src/lib/prisma.ts

<interfaces>
Existing patterns to follow:

From src/lib/auth-server.ts:
```typescript
export async function isAuthenticatedSession(): Promise<boolean>
// Trainer-only. Call this to guard /api/associate/pin/generate.
```

From src/lib/rateLimitService.ts:
```typescript
// Fingerprint-based rate limiter — follow existing call signature.
// Use for /api/associate/pin/verify: 5 attempts / 15 min per fingerprint.
```

New contracts this plan creates:

```typescript
// src/lib/pinService.ts
export function generatePin(): string;          // "000000"–"999999"
export async function hashPin(pin: string): Promise<string>;
export async function verifyPin(pin: string, hash: string): Promise<boolean>;

// src/lib/associateSession.ts
export function signAssociateToken(associateId: number): string;
export function verifyAssociateToken(token: string): { associateId: number } | null;

// src/lib/associateService.ts
export async function getAssociateIdBySlug(slug: string): Promise<number | null>;
export async function getAssociateById(id: number): Promise<{ id: number; slug: string } | null>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema fields + pinService + associateSession token signing</name>
  <files>prisma/schema.prisma, src/lib/pinService.ts, src/lib/pinService.test.ts, src/lib/associateSession.ts, src/lib/associateService.ts</files>
  <behavior>
    - pinService.generatePin returns a 6-char numeric string with leading zeros preserved
    - pinService.hashPin produces a bcrypt hash; verifyPin returns true for matching pin, false for wrong pin
    - associateSession.signAssociateToken + verifyAssociateToken roundtrip an associateId
    - verifyAssociateToken returns null for tampered, empty, or garbage tokens
    - associateService.getAssociateIdBySlug returns id for existing slug, null for unknown slug
  </behavior>
  <action>
    Add `pinHash String?` and `pinGeneratedAt DateTime?` to the Associate model per D-03. Install bcryptjs (`npm install bcryptjs` + `npm install -D @types/bcryptjs`). Generate Prisma client (`npx prisma generate`). Create a migration: `npx prisma migrate dev --name add_associate_pin --create-only` then apply.

    Implement `src/lib/pinService.ts` per D-01 (use `crypto.randomInt(0, 1_000_000)` zero-padded to 6 digits — NOT `Math.random`), D-02 (bcryptjs cost 10), D-24 (use bcrypt.compare, never string equality).

    Implement `src/lib/associateSession.ts` per D-08/D-09: HMAC-SHA256 over base64url-encoded payload `{aid, iat}` using `process.env.APP_PASSWORD` as the key. Format: `${base64url(payload)}.${base64url(sig)}`. `verifyAssociateToken` uses `crypto.timingSafeEqual` for signature comparison and returns null on any parse/signature failure. Do NOT throw on invalid input.

    Implement `src/lib/associateService.ts` with `getAssociateIdBySlug` and `getAssociateById`. Small, server-only, imports from `@/lib/prisma`.

    Write `src/lib/pinService.test.ts` with Vitest covering generatePin format (6 digits, leading zeros possible — assert with regex `^\d{6}$`), hash/verify roundtrip, and wrong-pin rejection. Add token tests inline in same file or split — planner discretion.
  </action>
  <verify>
    <automated>npm run test -- src/lib/pinService.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    Schema migration applied, Prisma client regenerated with pinHash field, pinService + associateSession + associateService exist and are tested, tsc clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PIN generate / verify / logout route handlers</name>
  <files>src/app/api/associate/pin/generate/route.ts, src/app/api/associate/pin/verify/route.ts, src/app/api/associate/logout/route.ts, src/app/api/associate/pin/verify/route.test.ts</files>
  <behavior>
    - POST /api/associate/pin/generate without trainer cookie returns 401
    - POST /api/associate/pin/generate with trainer cookie + valid associateId returns { pin } as a 6-digit string and persists a bcrypt hash on the Associate row
    - Regenerating overwrites the prior hash
    - POST /api/associate/pin/verify with correct {slug, pin} returns 200 with Set-Cookie associate_session (HttpOnly, SameSite=Strict, Max-Age=86400)
    - POST /api/associate/pin/verify with wrong pin returns 401 and sets no cookie
    - POST /api/associate/pin/verify with unknown slug returns 401 (no existence oracle)
    - 6th failed attempt from same fingerprint within 15 min returns 429
    - Successful verify resets the fingerprint failure counter
    - POST /api/associate/logout returns 200 and emits Set-Cookie clearing associate_session (Max-Age=0)
  </behavior>
  <action>
    `/api/associate/pin/generate` (per D-14): Use `isAuthenticatedSession()` for trainer guard — return 401 if not trainer. Zod-validate body `{ associateId: number }`. Call `pinService.generatePin()`, `hashPin(pin)`, `prisma.associate.update({ where: { id }, data: { pinHash, pinGeneratedAt: new Date() } })`. Return `{ pin }` (plaintext, once only — per D-04). NEVER log the PIN (D-25).

    `/api/associate/pin/verify` (per D-15, D-17): Public endpoint. Zod-validate `{ slug: string, pin: string, fingerprint: string }`. Check rate limit (5 failed per 15 min per fingerprint) — return 429 if exceeded. Look up associate by slug via `getAssociateIdBySlug`. If not found OR pinHash is null, record failure and return 401 (same response shape as wrong-pin — don't leak existence). Call `verifyPin(pin, associate.pinHash)`. On success: `signAssociateToken(associate.id)`, set cookie per D-07 (HttpOnly, Secure in prod, SameSite=Strict, Path=/, Max-Age=86400), reset fingerprint counter, return 200 `{ ok: true, slug }`. On failure: increment counter, return 401 `{ ok: false }`. Never echo the PIN back (D-25).

    `/api/associate/logout`: Emit `Set-Cookie: associate_session=; Max-Age=0; Path=/; HttpOnly; SameSite=Strict`. Return 200.

    Use Next.js App Router `Response` with headers for Set-Cookie — follow the existing trainer `/api/auth` pattern exactly for cookie formatting.

    Write integration-style test for verify endpoint using Vitest: mock prisma via `vi.mock('@/lib/prisma')` and exercise success, wrong-pin, unknown-slug, rate-limit paths.
  </action>
  <verify>
    <automated>npm run test -- src/app/api/associate/pin/verify/route.test.ts && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <done>
    Three route handlers exist, verify route tests pass, lint + tsc clean. Trainer-auth guard on generate confirmed. Rate limiting wired on verify. Logout clears cookie.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous client → /api/associate/pin/verify | Untrusted PIN + slug cross into bcrypt comparison and cookie issuance |
| anonymous client → /api/associate/pin/generate | Must be blocked by trainer-auth guard |
| cookie store → server | associate_session cookie value parsed + HMAC-verified before trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | /api/associate/pin/verify | mitigate | HMAC-signed token using APP_PASSWORD; tampered tokens fail timing-safe signature check in verifyAssociateToken (D-08, D-09) |
| T-09-02 | Tampering | associate_session cookie | mitigate | Opaque signed token; any payload mutation invalidates signature; HttpOnly prevents client JS tampering (D-07) |
| T-09-03 | Information Disclosure | PIN in logs/responses | mitigate | Plaintext PIN returned exactly once on generate; never logged; verify never echoes PIN (D-04, D-25) |
| T-09-04 | Denial of Service | PIN brute force | mitigate | bcrypt cost 10 + fingerprint rate limit 5/15min (D-02, D-17) |
| T-09-05 | Elevation of Privilege | /api/associate/pin/generate | mitigate | Hard trainer-auth guard via isAuthenticatedSession(); 401 otherwise (D-14) |
| T-09-06 | Information Disclosure | Slug enumeration via verify | accept | Rate limiter + identical 401 response for wrong-slug and wrong-pin reduces oracle; slugs already non-secret in v1.0 (D-23) |
| T-09-07 | Tampering | Modulo bias in PIN generation | mitigate | Use crypto.randomInt(0, 1_000_000) — uniform — not Math.random or modulo (D-01) |
</threat_model>

<verification>
- Schema migration runs cleanly against Supabase dev DB
- `npm run test` passes including new pinService tests
- `npx tsc --noEmit` clean
- Manual curl: generate with trainer cookie returns PIN; verify with that PIN returns 200 + cookie; verify with wrong PIN returns 401
</verification>

<success_criteria>
1. Associate.pinHash + pinGeneratedAt columns exist
2. generatePin produces cryptographically uniform 6-digit strings
3. Trainer-auth-guarded generate endpoint returns PIN once and hashes it
4. Public verify endpoint issues HttpOnly associate_session cookie on success, 401 on failure, 429 when rate-limited
5. Logout endpoint clears the cookie
6. All new code covered by tsc + lint + tests
</success_criteria>

<output>
After completion, create `.planning/phases/09-associate-pin-auth/09-01-SUMMARY.md`
</output>
