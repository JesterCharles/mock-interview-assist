---
phase: 10-automated-interview-pipeline
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/app/api/public/interview/complete/__integration__/pipeline.integration.test.ts
  - src/app/api/sync-check/route.ts
autonomous: false
requirements: [PIPE-01, PIPE-02]

must_haves:
  truths:
    - "Integration test confirms authenticated automated session produces non-null associateId, GapScore rows, and readinessStatus change end-to-end"
    - "Integration test confirms anonymous automated session produces associateId=null with no GapScore write"
    - "/api/sync-check shows no file↔DB divergence introduced by automated sessions (automated sessions correctly excluded from file-history comparison)"
    - "Checkpoint: trainer/user verifies a real automated interview completes and updates the associate's readiness on the trainer dashboard"
  artifacts:
    - path: "src/app/api/public/interview/complete/__integration__/pipeline.integration.test.ts"
      provides: "End-to-end pipeline verification against test DB"
      min_lines: 80
  key_links:
    - from: "pipeline.integration.test.ts"
      to: "/api/public/interview/complete"
      via: "HTTP POST with cookie-injected associate session"
      pattern: "fetch.*public/interview/complete"
    - from: "pipeline.integration.test.ts"
      to: "prisma.gapScore + prisma.associate"
      via: "DB reads verifying downstream writes"
      pattern: "prisma\\.(gapScore|associate)\\.find"
---

<objective>
Prove Plan 01 works end-to-end. Integration tests verify the full pipeline (cookie → session write → gap scores → readiness update) and confirm `/api/sync-check` parity remains intact. Human checkpoint validates a real automated interview in the UI updates the trainer dashboard.

Purpose: Goal-backward verification. Unit tests mock dependencies; this plan exercises the real stack.
Output: Integration test suite + sync-check hardening + human verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-automated-interview-pipeline/10-CONTEXT.md
@.planning/phases/10-automated-interview-pipeline/10-01-PLAN.md

@src/app/api/public/interview/complete/route.ts
@src/app/api/sync-check/route.ts
@src/lib/sessionPersistence.ts
@src/lib/gapPersistence.ts
@src/lib/readinessService.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration test — authenticated + anonymous pipeline</name>
  <files>src/app/api/public/interview/complete/__integration__/pipeline.integration.test.ts</files>
  <behavior>
    - Test A (authenticated): Seed Associate{slug:"test-assoc"} + 2 prior completed sessions → POST complete with associate_session cookie resolving to that associate → poll/await fan-out completion → assert Session row exists with associateId matching, GapScore rows exist for assessed skills, Associate.readinessStatus is one of ready|improving|not_ready (not null), Associate.lastComputedAt is recent (within last 60s)
    - Test B (anonymous): POST complete with no cookie → assert Session row exists with associateId=null, NO new GapScore rows appear for any associate, no readiness recompute triggered
    - Test C (spoofing): Seed Associate{slug:"victim"} → POST complete WITH cookie resolving to "attacker" AND client payload `associateSlug:"victim"` → assert Session.associateId resolves to attacker (cookie wins), victim untouched
    - Test D (anonymous preserves client slug): No cookie but client provides `associateSlug:"self-id"` → behavior matches current production (slug honored, upsert runs) — regression guard
  </behavior>
  <action>
    Create integration test file at `src/app/api/public/interview/complete/__integration__/pipeline.integration.test.ts`.

    Use Vitest. Import the route handler directly (`import { POST } from '@/app/api/public/interview/complete/route'`) and invoke with a constructed `Request` instance — avoid spinning up the full Next server.

    Mock `getAssociateSession` at the module boundary to inject cookie identity per test case (do NOT mock Prisma — use the real test DB).

    DB setup: Use a separate `DATABASE_URL` for tests (existing pattern if any, else document requirement). Before each test: truncate `Session`, `GapScore`, `Associate` tables. Seed fixtures inline.

    Fan-out is fire-and-forget — tests must `await` completion. Use a short poll loop (max ~2s) checking `prisma.gapScore.findFirst({ where: { associateId } })` or export a test-only flush helper from `readinessPipeline.ts`.

    Skip file-history assertions entirely — public route is DB-only.

    If a local Supabase test DB is not configured, mark tests with `.skipIf(!process.env.TEST_DATABASE_URL)` so CI can opt-in once infra lands. Document required env var in test file header.
  </action>
  <verify>
    <automated>TEST_DATABASE_URL=$TEST_DATABASE_URL npm run test -- src/app/api/public/interview/complete/__integration__/pipeline.integration.test.ts</automated>
  </verify>
  <done>All 4 integration tests green against a real test DB (or skipped with clear env-var message if DB not configured). Spoofing test passes — cookie identity wins over client payload.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm sync-check parity for automated sessions</name>
  <files>src/app/api/sync-check/route.ts</files>
  <action>
    Read current `src/app/api/sync-check/route.ts`. Confirm it compares file-history JSON (trainer-led sessions) vs DB. Automated sessions are DB-only (never written to file-history per `complete/route.ts` comment L48-49).

    Verify sync-check treats DB sessions NOT in file-history as acceptable (not divergence) when `Session.mode === "automated"` OR when the session ID is not present in file-history at all.

    If the current implementation flags DB-only sessions as divergence, add a filter: exclude `mode === "automated"` sessions from the divergence check. If the implementation already correctly handles this (DB-superset is normal), add a code comment explaining the invariant and skip modification.

    Run `/api/sync-check` locally before and after any change; confirm response structure unchanged.
  </action>
  <verify>
    <automated>npm run test && curl -sS http://localhost:3000/api/sync-check | grep -v '"divergent":true.*mode":"automated"'</automated>
  </verify>
  <done>Sync-check endpoint reports no divergence caused by automated sessions; existing trainer-led divergence logic unchanged. Code comment documents the automated-session invariant.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 10 complete: authenticated associates completing automated interviews now trigger gap score computation and readiness updates. Anonymous path preserved. Spoofing prevented.
  </what-built>
  <how-to-verify>
    1. Generate an associate + PIN via the trainer dashboard (Phase 9 flow).
    2. In an incognito window, enter the PIN and complete a full automated interview (`/api/public/interview/start` → agent loop → completion).
    3. After completion, return to the trainer dashboard and open `/trainer/[slug]` for that associate.
    4. Verify:
       - The new session appears in the session history list.
       - If the associate now has ≥3 completed sessions, the gap trend chart updates with the new data point.
       - The readiness badge on the roster reflects recomputed status (ready / improving / not_ready).
       - `Associate.lastComputedAt` is within the last minute (visible in any debug view or via direct DB query).
    5. Separately, complete a public interview WITHOUT a PIN (anonymous path) — verify it still completes successfully and does not appear under any associate's history.
    6. Call `/api/sync-check` — confirm no new divergence entries.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe observed issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test DB→production DB | Integration tests must never touch production — guarded by TEST_DATABASE_URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | Integration test DB | mitigate | Tests guarded by TEST_DATABASE_URL env var; fail loudly if pointed at production-looking connection string |
| T-10-08 | Information Disclosure | Test fixtures | accept | Synthetic data only; no real associate PII in fixtures |
</threat_model>

<verification>
- Integration tests pass against test DB
- `/api/sync-check` returns clean after pipeline run
- Human checkpoint confirms end-to-end flow on real infrastructure
</verification>

<success_criteria>
- ROADMAP Phase 10 success criteria 1-4 all verified
- PIPE-01 and PIPE-02 marked Complete in traceability matrix
- No regression in anonymous public-interview path
</success_criteria>

<output>
Create `.planning/phases/10-automated-interview-pipeline/10-02-SUMMARY.md` on completion.
</output>
