---
phase: 10-automated-interview-pipeline
plan: 01
type: execute
wave: 1
depends_on: [09-01, 09-02]
files_modified:
  - src/app/api/public/interview/complete/route.ts
  - src/lib/readinessPipeline.ts
autonomous: true
requirements: [PIPE-01, PIPE-02]

must_haves:
  truths:
    - "Authenticated associate completing an automated interview produces a Session row with non-null associateId matching their cookie identity"
    - "GapScore rows for the associate reflect the newly-completed session within a short window after POST returns"
    - "Associate.readinessStatus and lastComputedAt are updated after an authenticated automated session"
    - "Anonymous (no cookie) POSTs to /api/public/interview/complete continue to return 200 with persisted: 'db' and no fan-out"
    - "Client-supplied associateSlug is ignored when an associate session cookie is present (no identity spoofing)"
  artifacts:
    - path: "src/app/api/public/interview/complete/route.ts"
      provides: "Identity resolution from cookie + pipeline fan-out wiring"
      contains: "getAssociateSession"
    - path: "src/lib/readinessPipeline.ts"
      provides: "Shared fire-and-forget runReadinessPipeline(associateId) helper"
      exports: ["runReadinessPipeline"]
  key_links:
    - from: "src/app/api/public/interview/complete/route.ts"
      to: "src/lib/auth-server.ts"
      via: "getAssociateSession() call"
      pattern: "getAssociateSession"
    - from: "src/app/api/public/interview/complete/route.ts"
      to: "src/lib/readinessPipeline.ts"
      via: "runReadinessPipeline(associateId) fire-and-forget after persistSessionToDb"
      pattern: "runReadinessPipeline"
    - from: "src/lib/readinessPipeline.ts"
      to: "src/lib/gapPersistence.ts + src/lib/readinessService.ts"
      via: "saveGapScores then updateAssociateReadiness sequential"
      pattern: "saveGapScores.*updateAssociateReadiness"
---

<objective>
Wire `/api/public/interview/complete` into the gap/readiness pipeline so authenticated automated interviews feed the same downstream signals as trainer-led sessions (PIPE-01, PIPE-02). Identity resolves from the Phase 9 `associate_session` cookie server-side, not from client payload. Anonymous path preserved.

Purpose: Close the loop between automated interviews and the readiness engine. Today automated sessions persist but leave GapScore/Associate.readinessStatus stale.
Output: Modified public-complete route + shared readiness pipeline helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-automated-interview-pipeline/10-CONTEXT.md

@src/app/api/public/interview/complete/route.ts
@src/app/api/history/route.ts
@src/lib/sessionPersistence.ts
@src/lib/gapPersistence.ts
@src/lib/readinessService.ts
@src/lib/settingsService.ts

<interfaces>
<!-- Contracts the executor needs. Phase 9 delivers getAssociateSession; infer from CONTEXT. -->

From src/lib/auth-server.ts (provided by Phase 9):
```typescript
export async function getAssociateSession(): Promise<{ associateId: number; slug: string } | null>;
```

From src/lib/sessionPersistence.ts:
```typescript
export async function persistSessionToDb(session: InterviewSession): Promise<boolean>;
```

From src/lib/gapPersistence.ts:
```typescript
export async function saveGapScores(associateId: number): Promise<void>;
```

From src/lib/readinessService.ts:
```typescript
export async function updateAssociateReadiness(associateId: number, threshold: number): Promise<void>;
```

From src/lib/settingsService.ts:
```typescript
export async function getSettings(): Promise<{ readinessThreshold: number }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared readiness pipeline helper</name>
  <files>src/lib/readinessPipeline.ts, src/lib/readinessPipeline.test.ts</files>
  <behavior>
    - Test 1: runReadinessPipeline(id) calls saveGapScores then updateAssociateReadiness in order
    - Test 2: uses threshold from getSettings when available
    - Test 3: falls back to threshold 75 when getSettings throws
    - Test 4: swallows errors from saveGapScores (logs, does not re-throw)
    - Test 5: swallows errors from updateAssociateReadiness (logs, does not re-throw)
  </behavior>
  <action>
    Create `src/lib/readinessPipeline.ts` exporting `runReadinessPipeline(associateId: number): Promise<void>`. Mirror the fire-and-forget logic currently inlined in `src/app/api/history/route.ts` lines 53-70: fetch threshold via `getSettings()` with fallback 75, call `saveGapScores(associateId)`, then `updateAssociateReadiness(associateId, threshold)`. Catch and log errors with prefix `[readiness-pipeline]`. Do not re-throw — this helper is intentionally never the source of an HTTP error (per D-07, D-10).

    Write Vitest tests mocking `@/lib/gapPersistence`, `@/lib/readinessService`, `@/lib/settingsService`. Verify call order and error swallowing.
  </action>
  <verify>
    <automated>npm run test -- src/lib/readinessPipeline.test.ts</automated>
  </verify>
  <done>readinessPipeline.ts exported, all 5 tests green, no re-throws escape the helper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire public-interview-complete to auth + pipeline</name>
  <files>src/app/api/public/interview/complete/route.ts, src/app/api/public/interview/complete/route.test.ts</files>
  <behavior>
    - Test 1: Authenticated request (mocked getAssociateSession returns {associateId:42, slug:"jane-doe"}) — persistSessionToDb called with session where associateSlug === "jane-doe" (overriding client value), response 200, runReadinessPipeline(42) invoked
    - Test 2: Anonymous request (getAssociateSession returns null) — persistSessionToDb called with original payload, response 200, runReadinessPipeline NOT called
    - Test 3: Client-supplied associateSlug "attacker-slug" is overridden when cookie resolves to "jane-doe" (no spoofing)
    - Test 4: persistSessionToDb returning false → 500 response, runReadinessPipeline NOT called
    - Test 5: Rate-limit rejection still returns 429 before any identity resolution
    - Test 6: Oversized payload still returns 413
    - Test 7: Invalid session shape still returns 400
  </behavior>
  <action>
    Modify `src/app/api/public/interview/complete/route.ts`:

    1. Import `getAssociateSession` from `@/lib/auth-server` (per D-02, Phase 9 dependency).
    2. Import `runReadinessPipeline` from `@/lib/readinessPipeline`.
    3. Preserve existing order: body parse → shape/size validation → rate-limit check.
    4. After rate-limit passes, call `const assocSession = await getAssociateSession().catch(() => null)` (D-03, D-11 — cookie errors treated as anonymous).
    5. If `assocSession` is non-null, inject slug into the session payload: `session.associateSlug = assocSession.slug` (D-12 — overrides any client value, prevents spoofing).
    6. Also set `session.mode = "automated"` (D-05) — cast via existing InterviewSession typing.
    7. Call `persistSessionToDb(session)`. If it returns false, respond 500 with `{ error: 'Failed to save interview session' }` (D-09). Do NOT trigger pipeline.
    8. If persisted AND `assocSession` present, fire-and-forget: `void runReadinessPipeline(assocSession.associateId)` — do NOT `await`, do NOT let errors escape the response (D-07).
    9. Return 200 `{ success: true, persisted: 'db' }` as before.

    Write Vitest tests mocking `@/lib/auth-server`, `@/lib/sessionPersistence`, `@/lib/readinessPipeline`, `@/lib/rateLimitService`. Use `NextRequest` helpers or direct POST invocation.

    Do NOT modify `persistSessionToDb` itself (D-13). Do NOT write to file-history — public sessions remain DB-only (preserve L48-49 comment intent).
  </action>
  <verify>
    <automated>npm run test -- src/app/api/public/interview/complete/route.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>All 7 tests green, typecheck clean, anonymous + authenticated paths both covered, slug-spoofing test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/api/public/interview/complete | Untrusted POST body; client controls session payload including associateSlug |
| cookie→server | `associate_session` cookie (Phase 9) — server-signed, HttpOnly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Spoofing | complete/route.ts identity | mitigate | Server overrides `session.associateSlug` with cookie-derived slug; client value ignored when cookie present (D-01, D-12). Test 3 enforces. |
| T-10-02 | Tampering | Session payload | mitigate | Existing shape + size validation retained; `session.mode` set server-side, not trusted from client |
| T-10-03 | Information Disclosure | Pipeline errors | mitigate | Errors logged server-side only; client receives generic 200 or 500 — no internal state leaks (D-10) |
| T-10-04 | Denial of Service | Unbounded gap/readiness work | mitigate | Fire-and-forget fan-out + existing rate limit (2/13hr per fingerprint) caps pipeline invocations |
| T-10-05 | Repudiation | Who completed the session | accept | Cookie-derived associateId is sufficient audit trail; no separate audit log required for MVP |
| T-10-06 | Elevation of Privilege | Associate→trainer | accept | Associate cookie cannot reach trainer routes (Phase 9 AUTH-03); this route grants no elevated privileges |
</threat_model>

<verification>
- `npm run test` passes (new tests + existing)
- `npx tsc --noEmit` clean
- `npm run lint` clean
- Manual smoke: complete a public interview anonymously → verify Session row with associateId=null, no GapScore change. (Covered more thoroughly in Plan 02.)
</verification>

<success_criteria>
- PIPE-01 satisfied: authenticated automated sessions link to associateId via cookie (no slug input required)
- PIPE-02 satisfied: gap scores + readiness recomputed after authenticated automated session
- Anonymous path regression-free (Success Criterion 4)
- No modifications to `persistSessionToDb` contract
</success_criteria>

<output>
Create `.planning/phases/10-automated-interview-pipeline/10-01-SUMMARY.md` on completion.
</output>
