---
phase: 20-middleware-cutover-rls
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/trainer/route.ts
  - src/app/api/trainer/[slug]/route.ts
  - src/app/api/associates/[slug]/gap-scores/route.ts
  - src/app/api/cohorts/route.ts
  - src/app/api/cohorts/[id]/route.ts
  - src/app/api/cohorts/[id]/curriculum/route.ts
  - src/app/api/cohorts/[id]/curriculum/[weekId]/route.ts
  - src/app/api/settings/route.ts
  - src/app/api/history/route.ts
  - src/app/api/sync-check/route.ts
  - src/app/api/admin/readiness-sweep/route.ts
  - src/app/api/trainer/invites/bulk/route.ts
  - src/app/api/trainer/associates/route.ts
  - src/app/api/trainer/associates/[id]/route.ts
  - src/app/api/trainer/associates/[id]/invite/route.ts
  - src/app/api/trainer/associates/preview/route.ts
  - src/app/api/github/cache/invalidate/route.ts
  - .planning/PROJECT.md
autonomous: true
requirements:
  - AUTH-09
  - AUTH-10

must_haves:
  truths:
    - "Every non-public route handler has an AUDIT-VERIFIED comment on its identity check line"
    - "PROJECT.md documents the BYPASSRLS + Transaction Pooler architecture"
    - "PROJECT.md documents the explicit-filter requirement for route handlers"
    - "AUTH-09 is documented as already satisfied by Phase 18 middleware rewrite"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Database Access Architecture documentation"
      contains: "BYPASSRLS"
  key_links:
    - from: "route handlers (all 17)"
      to: "getCallerIdentity()"
      via: "// [AUDIT-VERIFIED: P20] comment"
      pattern: "AUDIT-VERIFIED: P20"
---

<objective>
Annotate all 17 non-public route handlers with audit-verified comments and document the BYPASSRLS + Transaction Pooler architecture in PROJECT.md.

Purpose: Per D-07/D-08, the audit proves every route handler explicitly gates on caller identity. Per D-10, PROJECT.md becomes the canonical reference for the explicit-filter requirement. AUTH-09 is satisfied by P18's middleware rewrite -- this plan documents that fact.
Output: 17 annotated route handlers + updated PROJECT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-middleware-cutover-rls/20-CONTEXT.md
@.planning/phases/20-middleware-cutover-rls/20-RESEARCH.md

<interfaces>
<!-- CallerIdentity type from src/lib/identity.ts -->
From src/lib/identity.ts:
```typescript
export type CallerIdentity =
  | { kind: 'admin'; userId: string; email: string }
  | { kind: 'trainer'; userId: string; email: string }
  | { kind: 'associate'; userId: string; email: string; associateId: number; associateSlug: string }
  | { kind: 'anonymous' };

export async function getCallerIdentity(): Promise<CallerIdentity>;
```

<!-- Existing audit pattern from Research -->
Annotation pattern:
```typescript
const caller = await getCallerIdentity() // [AUDIT-VERIFIED: P20]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate all 17 route handlers with AUDIT-VERIFIED comment</name>
  <files>
    src/app/api/trainer/route.ts
    src/app/api/trainer/[slug]/route.ts
    src/app/api/associates/[slug]/gap-scores/route.ts
    src/app/api/cohorts/route.ts
    src/app/api/cohorts/[id]/route.ts
    src/app/api/cohorts/[id]/curriculum/route.ts
    src/app/api/cohorts/[id]/curriculum/[weekId]/route.ts
    src/app/api/settings/route.ts
    src/app/api/history/route.ts
    src/app/api/sync-check/route.ts
    src/app/api/admin/readiness-sweep/route.ts
    src/app/api/trainer/invites/bulk/route.ts
    src/app/api/trainer/associates/route.ts
    src/app/api/trainer/associates/[id]/route.ts
    src/app/api/trainer/associates/[id]/invite/route.ts
    src/app/api/trainer/associates/preview/route.ts
    src/app/api/github/cache/invalidate/route.ts
  </files>
  <read_first>
    - .planning/phases/20-middleware-cutover-rls/20-RESEARCH.md (Route Handler Audit Findings table + annotation pattern)
    - src/lib/identity.ts (CallerIdentity type)
    - Each of the 17 route files listed above (read each before modifying)
  </read_first>
  <action>
For each of the 17 route handler files listed:

1. Read the file.
2. Find the line where `getCallerIdentity()` is called.
3. Add `// [AUDIT-VERIFIED: P20]` as an inline comment on that line.

The annotation pattern is:
```typescript
const caller = await getCallerIdentity() // [AUDIT-VERIFIED: P20]
```

If a file has multiple exported handlers (e.g., GET + POST + DELETE), each handler that calls `getCallerIdentity()` gets the annotation. If a file calls `getCallerIdentity()` once and shares the result across handlers, annotate the single call.

Do NOT modify any logic. This is annotation-only.

Per D-09, the following routes are EXEMPT (intentionally anonymous, do NOT annotate):
- `/api/public/interview/*` (3 routes)
- `/api/health`
- `/api/associate/status`

Per the Research audit table, all 17 routes already have correct identity gates. If any route is found to be MISSING an identity check, add the `getCallerIdentity()` call + guard + annotation. Document the finding in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/jestercharles/mock-interview-assist && grep -r "AUDIT-VERIFIED: P20" src/app/api/ --include="*.ts" -l | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - At least 17 route files contain `// [AUDIT-VERIFIED: P20]`
    - grep -r "AUDIT-VERIFIED: P20" src/app/api/ returns matches in all 17 listed files
    - No logic changes -- only comment additions
    - `npm run test` passes
    - `npm run build` succeeds (comments don't break compilation)
  </acceptance_criteria>
  <done>All 17 non-public route handlers annotated with AUDIT-VERIFIED: P20 comment. Test suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Document BYPASSRLS architecture in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (full file -- must understand existing structure before editing)
    - .planning/phases/20-middleware-cutover-rls/20-CONTEXT.md (D-10 documentation spec)
  </read_first>
  <action>
Add a new section to `.planning/PROJECT.md` titled `## Database Access Architecture` (per D-10).

Place it after the "Current State" section and before "Current Milestone" (or wherever the architecture sections begin -- read the file first to find the right location).

Content MUST include exactly these four points (per D-10):

```markdown
## Database Access Architecture

### Prisma + Service-Role (BYPASSRLS)

Prisma connects via `DATABASE_URL` (Supabase Transaction Pooler, port 6543) using the service-role key. Service-role connections **bypass RLS** — Postgres Row Level Security policies have zero effect on Prisma queries. This is intentional: Prisma is the primary data access layer, and all access control is enforced at the application layer.

### RLS as Defense-in-Depth

RLS policies are deployed on `Associate`, `Session`, `GapScore`, `Cohort`, and `CurriculumWeek` as a **defense-in-depth** layer. They protect against unauthorized direct `supabase-js` reads (e.g., from client-side code or edge functions that use the anon key). RLS is NOT the primary access control mechanism.

Policies:
- **Associate**: Self (authUserId = auth.uid()) OR trainer/admin
- **Session, GapScore**: Owner (via Associate FK) OR trainer/admin
- **Cohort, CurriculumWeek**: Trainer/admin only

A `public.is_trainer()` SECURITY DEFINER function checks `auth.jwt() -> user_metadata.role` for `'trainer'` or `'admin'`.

### Explicit-Filter Requirement

Every route handler MUST call `getCallerIdentity()` and filter database queries by the caller's identity. Do NOT rely on RLS to enforce access control for Prisma queries — service-role bypasses it.

Pattern:
```typescript
const caller = await getCallerIdentity() // [AUDIT-VERIFIED: P20]
if (caller.kind !== 'trainer' && caller.kind !== 'admin') {
  return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
}
\```

All existing route handlers were audited in Phase 20 and annotated with `// [AUDIT-VERIFIED: P20]`.

### AUTH-09 Status

Middleware (`src/middleware.ts`) was rewritten to Supabase-primary in Phase 18. It guards:
- `/trainer/*` — trainer role only
- `/associate/*` except `/signin` — trainer or matching associate
- `/interview/*`, `/review/*` — trainer only

The PIN-based associate auth was never shipped to production. No grace window code exists — `getCallerIdentity()` reads Supabase session only.
```

Also update the CLAUDE.md Authentication section to add a one-line note: "RLS deployed as defense-in-depth (Phase 20). See PROJECT.md > Database Access Architecture for details."
  </action>
  <verify>
    <automated>cd /Users/jestercharles/mock-interview-assist && grep -c "BYPASSRLS" .planning/PROJECT.md && grep "Explicit-Filter Requirement" .planning/PROJECT.md && grep "Defense-in-Depth" .planning/PROJECT.md | head -1 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains "Database Access Architecture" section
    - Section mentions "BYPASSRLS", "defense-in-depth", "Explicit-Filter Requirement", and "AUTH-09 Status"
    - Section documents that Prisma uses service-role and bypasses RLS
    - Section documents that every route handler MUST filter by identity
    - Section documents that middleware is already Supabase-primary (P18)
    - CLAUDE.md has a one-line RLS note in the Authentication section
  </acceptance_criteria>
  <done>PROJECT.md documents the full BYPASSRLS architecture, explicit-filter requirement, and AUTH-09 status. CLAUDE.md cross-references it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route handler -> Prisma | App-layer identity filtering is the primary access control |
| Documentation -> Future developers | PROJECT.md must be clear enough to prevent future routes from skipping identity checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06 | Elevation of Privilege | Route handler missing identity gate | mitigate | Audit annotation `// [AUDIT-VERIFIED: P20]` on all 17 handlers; documented requirement in PROJECT.md |
| T-20-07 | Information Disclosure | Future route handler skips identity check | mitigate | PROJECT.md "Explicit-Filter Requirement" section serves as onboarding doc for new routes |
</threat_model>

<verification>
- `grep -r "AUDIT-VERIFIED: P20" src/app/api/` returns 17+ files
- `grep "BYPASSRLS" .planning/PROJECT.md` returns matches
- `npm run test` passes
- `npm run build` succeeds
</verification>

<success_criteria>
- All 17 non-public route handlers have AUDIT-VERIFIED annotations
- PROJECT.md has complete Database Access Architecture section
- AUTH-09 documented as satisfied by P18
- Test suite and build remain green
</success_criteria>

<output>
After completion, create `.planning/phases/20-middleware-cutover-rls/20-02-SUMMARY.md`
</output>
