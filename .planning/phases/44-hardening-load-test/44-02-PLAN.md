---
phase: 44-hardening-load-test
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/44-hardening-load-test/44-CSO-REPORT.md
  - .planning/phases/44-hardening-load-test/44-CODEX-ADVERSARIAL-REPORT.md
autonomous: false
requirements:
  - HARD-03

must_haves:
  truths:
    - "gstack `/cso` was run scoped to Phases 38 + 39 + 43 diff (per D-07)"
    - "codex `adversarial-review` was run against the same diff scope (per D-08)"
    - "Both reports return PASS verdict with zero HIGH severity findings before Phase 44 merge (per D-08)"
    - "STRIDE coverage matrix is present and maps to each component in D-09 (Judge0 sandbox / Execution API / Terraform-CI)"
    - "Any MEDIUM/LOW findings are either resolved or explicitly accepted with recorded rationale"
  artifacts:
    - path: ".planning/phases/44-hardening-load-test/44-CSO-REPORT.md"
      provides: "gstack /cso skill output covering Phases 38 + 39 + 43"
      min_lines: 80
      contains: "STRIDE"
    - path: ".planning/phases/44-hardening-load-test/44-CODEX-ADVERSARIAL-REPORT.md"
      provides: "codex adversarial-review output against Phase 38+39+43 diff"
      min_lines: 40
      contains: "VERDICT"
  key_links:
    - from: "44-CSO-REPORT.md"
      to: "Phase 38 + 39 + 43 source code"
      via: "scoped review citing specific files + line numbers"
      pattern: "src/(lib/judge0Client|app/api/coding|lib/auth)|infra/terraform|\\.github/workflows"
    - from: "44-CSO-REPORT.md"
      to: "D-09 STRIDE coverage"
      via: "table mapping each listed component → threats → mitigation pointer"
      pattern: "\\| Judge0 sandbox \\|.*\\| Execution API \\|.*\\| Terraform/CI \\|"
    - from: "44-CODEX-ADVERSARIAL-REPORT.md"
      to: "explicit PASS verdict line"
      via: "final section"
      pattern: "VERDICT:\\s*PASS"
    - from: "both reports"
      to: "HIGH severity count check"
      via: "grep -c 'SEVERITY: HIGH' returns 0 in both files"
      pattern: "SEVERITY:\\s*HIGH"
---

<objective>
Run the two gating security reviews for v1.4: gstack `/cso` (STRIDE-scoped security audit per D-07, D-09) and codex `adversarial-review` (per D-08). Both must return PASS with zero HIGH findings before Phase 44 can merge. This plan is a parallel gate to Plan 44-01 — security review runs independently from load/abuse testing.

Purpose: Empirical + adversarial validation that Phases 38 (Judge0 infra), 39 (Execution API), and 43 (MSA deployment) meet the security bar before production traffic lands. The load tests prove the sandbox holds under volume; these reviews prove the design holds under scrutiny.

Output: Two committed review reports with explicit PASS verdicts, HIGH findings count = 0, and a STRIDE coverage matrix per D-09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-hardening-load-test/44-CONTEXT.md
@.planning/phases/38-judge0-infrastructure/38-CONTEXT.md
@.planning/phases/43-msa-deployment/43-CONTEXT.md

<interfaces>
<!-- External tooling contracts — not code interfaces -->

- gstack `/cso` skill: produces STRIDE-organized security audit. Invocation pattern: `/cso --scope "phases 38, 39, 43" --output .planning/phases/44-hardening-load-test/44-CSO-REPORT.md`
- codex `adversarial-review`: plugin command. Reviews a git diff range for adversarial/edge-case issues. Invocation: `codex adversarial-review <base-sha>..<head-sha>`

Diff scope for both tools: The merge base of Phase 38 onto v1.3 → HEAD of Phase 43 (everything in v1.4 up to and including MSA deployment). Compute exact SHAs at run time via `git log --oneline main..HEAD | head` + reference to the first Phase 38 commit.

Review scope files (non-exhaustive, enumerated in both reports):
- `src/lib/judge0Client.ts`, `src/app/api/coding/submit/route.ts`, `src/app/api/coding/attempts/[id]/route.ts`, `src/app/api/coding/challenges/route.ts`
- `src/lib/auth-server.ts`, `src/lib/identity.ts`, `src/lib/rateLimitService.ts` (touched by Phase 39)
- `docker-compose.yml` (Phase 38 Judge0 additions)
- `infra/terraform/*.tf`, `.github/workflows/deploy-app.yml`, `.github/workflows/deploy-judge0.yml`, `.github/workflows/pr-checks.yml` (Phase 43)
- `scripts/push-judge0-metrics.mjs` (Phase 43 D-11)
- `docs/runbooks/coding-stack.md` (Phase 43 D-13)

STRIDE mapping required by D-09:
- Judge0 sandbox → Tampering, Elevation
- Execution API → Spoofing, Tampering, Info Disclosure, DoS
- Terraform/CI → Elevation, Tampering
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 [BLOCKING GATE]: Run gstack /cso scoped to Phases 38 + 39 + 43 (HARD-03 first half)</name>
  <files>.planning/phases/44-hardening-load-test/44-CSO-REPORT.md</files>
  <action>
Operator (human) runs the gstack `/cso` skill against the Phase 38+39+43 diff scope and commits the resulting report. This is an interactive tool invocation — cannot be automated by Claude within plan execution. Claude's job during execution is to prep the diff scope (SHA range + file list) and invoke the tool; human confirms output meets the gate criteria before unblocking.
  </action>
  <what-built>Structured STRIDE audit targeting Phase 38 sandbox config, Phase 39 execution API auth/rate-limit/hidden-test handling, Phase 43 Terraform IAM + CI/CD secret flow. Report is committed evidence that D-09's per-component STRIDE coverage was examined.</what-built>
  <how-to-verify>
    1. Compute diff scope: `git log --oneline main..HEAD --grep='Phase 3[89]\|Phase 43' | tail -1` (oldest Phase 38 commit) → use as base SHA. HEAD is current Phase 43 tip.
    2. Invoke gstack `/cso` with that diff scope and explicit scope note: "Phases 38 (Judge0 infra), 39 (execution API), 43 (MSA deployment). STRIDE matrix per D-09."
    3. Direct /cso output to `.planning/phases/44-hardening-load-test/44-CSO-REPORT.md`.
    4. Report MUST include:
       - Diff scope (base..head SHAs + file list)
       - STRIDE coverage matrix (D-09): Judge0 sandbox × {T,E}; Execution API × {S,T,I,D}; Terraform/CI × {E,T}
       - Each finding tagged `SEVERITY: HIGH|MEDIUM|LOW|INFO` and `MITIGATION:` line
       - Final `SUMMARY` section with counts per severity and a `VERDICT: PASS|FAIL` line
    5. Gate check: `grep -c "SEVERITY: HIGH" 44-CSO-REPORT.md` must return `0`. If non-zero, fix the HIGH finding (may require follow-up PR against Phases 38/39/43 code), re-run /cso, commit new report. Do NOT merge Phase 44 with a HIGH in /cso.
    6. Commit 44-CSO-REPORT.md.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/44-hardening-load-test/44-CSO-REPORT.md && grep -q "STRIDE" .planning/phases/44-hardening-load-test/44-CSO-REPORT.md && grep -q "VERDICT: PASS" .planning/phases/44-hardening-load-test/44-CSO-REPORT.md && [ "$(grep -c 'SEVERITY: HIGH' .planning/phases/44-hardening-load-test/44-CSO-REPORT.md)" = "0" ]</automated>
  </verify>
  <done>
    - 44-CSO-REPORT.md committed with STRIDE coverage matrix per D-09
    - `VERDICT: PASS` line present
    - 0 occurrences of `SEVERITY: HIGH`
    - MEDIUM/LOW findings resolved or explicitly accepted with rationale
  </done>
  <resume-signal>Type "approved" after 44-CSO-REPORT.md shows `VERDICT: PASS` and 0 HIGH findings, AND the report is committed. If any HIGH: fix the underlying code first.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 [BLOCKING GATE]: Run codex adversarial-review against same scope (HARD-03 second half)</name>
  <files>.planning/phases/44-hardening-load-test/44-CODEX-ADVERSARIAL-REPORT.md</files>
  <action>
Operator runs `codex adversarial-review` on the same base..head SHA range as Task 1 and commits the output. The two reviews are DUAL GATES — both must independently pass before Phase 44 can merge. Use the same SHA range computed in Task 1 to guarantee orthogonal review of the same code.
  </action>
  <what-built>Adversarial code review from codex plugin targeting the same Phase 38+39+43 diff. Complements /cso's STRIDE lens with codex's edge-case/adversarial lens. Both must PASS for merge.</what-built>
  <how-to-verify>
    1. Use the same base..head SHA range as Task 1.
    2. Run `codex adversarial-review <base-sha>..<head-sha>` (or the plugin's equivalent invocation — check `codex --help`).
    3. Direct output to `.planning/phases/44-hardening-load-test/44-CODEX-ADVERSARIAL-REPORT.md`.
    4. Report MUST include:
       - Diff scope + commit list
       - Per-finding entry with `SEVERITY: HIGH|MEDIUM|LOW|INFO`
       - Final `VERDICT: PASS|FAIL` line
    5. Gate check: `grep -c "SEVERITY: HIGH" 44-CODEX-ADVERSARIAL-REPORT.md` returns `0`.
    6. Also verify `grep "VERDICT: PASS" 44-CODEX-ADVERSARIAL-REPORT.md` matches (non-empty).
    7. If any HIGH or `VERDICT: FAIL`: remediate in Phases 38/39/43 code (new follow-up PR), re-run, commit new report.
    8. Commit 44-CODEX-ADVERSARIAL-REPORT.md.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/44-hardening-load-test/44-CODEX-ADVERSARIAL-REPORT.md && grep -q "VERDICT: PASS" .planning/phases/44-hardening-load-test/44-CODEX-ADVERSARIAL-REPORT.md && [ "$(grep -c 'SEVERITY: HIGH' .planning/phases/44-hardening-load-test/44-CODEX-ADVERSARIAL-REPORT.md)" = "0" ]</automated>
  </verify>
  <done>
    - 44-CODEX-ADVERSARIAL-REPORT.md committed
    - `VERDICT: PASS` line present
    - 0 occurrences of `SEVERITY: HIGH`
    - Same base..head SHA range as Task 1's report (cross-verifiable)
  </done>
  <resume-signal>Type "approved" after 44-CODEX-ADVERSARIAL-REPORT.md shows `VERDICT: PASS` and 0 HIGH findings, AND the report is committed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewer tool → source code | /cso + codex read repo and diff; no code execution; no outbound beyond LLM provider |
| report file → git history | Review findings become permanent audit record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02-R | Repudiation | Review was skipped or run on wrong scope | mitigate | Both reports include explicit diff SHA range + file list; grep gate enforces HIGH=0; commit-based receipt |
| T-44-02-I | Info Disclosure | Review reports may embed secrets/tokens quoted from code | mitigate | Before committing each report, grep for `AUTH_TOKEN`, `SECRET_KEY`, `ssh-rsa`, `PRIVATE KEY` patterns; redact if present |
| T-44-02-T | Tampering | Report edited post-review to remove findings | accept | Git history provides audit trail; reviewer is sole operator; reports are signed by commit SHA |
| T-44-02-E | Elevation | /cso or codex false-negative misses a real HIGH | mitigate | DUAL-GATE design (both /cso AND codex must pass) — a miss in one tool is caught by the other's orthogonal lens (STRIDE vs adversarial) |
</threat_model>

<verification>
- 44-CSO-REPORT.md exists, contains STRIDE matrix per D-09, `VERDICT: PASS`, 0 HIGH findings
- 44-CODEX-ADVERSARIAL-REPORT.md exists, `VERDICT: PASS`, 0 HIGH findings
- Both reports reference the same diff SHA range
- Both reports committed to git
- Pre-commit secret scan (grep for token patterns) clean
</verification>

<success_criteria>
HARD-03 satisfied when both committed reports show `VERDICT: PASS` and `grep -c "SEVERITY: HIGH"` returns 0 on each. STRIDE coverage matrix in 44-CSO-REPORT.md explicitly maps to D-09 components. Any MEDIUM/LOW findings are either resolved in follow-up PRs or explicitly accepted with recorded rationale in the report.
</success_criteria>

<output>
After completion, create `.planning/phases/44-hardening-load-test/44-02-SUMMARY.md` summarizing the review scope, verdict from each tool, count by severity, and any accepted-risk rationale.
</output>
