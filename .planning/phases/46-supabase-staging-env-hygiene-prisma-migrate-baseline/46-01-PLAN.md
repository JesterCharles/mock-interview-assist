---
phase: 46-supabase-staging-env-hygiene-prisma-migrate-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/assert-staging-env.ts
  - scripts/seed-staging.ts
  - scripts/__tests__/seed-staging.test.ts
  - scripts/__tests__/assert-staging-env.test.ts
  - package.json
autonomous: true
requirements:
  - DATA-03
threats_mitigated:
  - T-46-01
  - T-46-02
must_haves:
  truths:
    - "Shared env-guard helper refuses staging ops when DATABASE_URL lacks ref lzuqbpqmqlvzwebliptj"
    - "seed-staging.ts is idempotent: running twice produces identical row counts across Cohort, Associate, CurriculumWeek, Session, Settings"
    - "Faker output is deterministic: faker.seed(1337) on @faker-js/faker/locale/en produces stable firstName/lastName across runs"
    - "Vitest unit tests for assert-staging-env guard and faker determinism pass (`npm run test -- scripts/__tests__/seed-staging.test.ts` exits 0)"
    - "@faker-js/faker ^10.4.0 installed as devDependency"
    - "package.json scripts block contains seed-staging script entry"
  artifacts:
    - path: "scripts/lib/assert-staging-env.ts"
      provides: "assertStagingDatabase() + assertProdDatabase(expectedProdRef) + maskUrl() helpers; STAGING_REF constant = lzuqbpqmqlvzwebliptj"
      contains: "lzuqbpqmqlvzwebliptj"
    - path: "scripts/seed-staging.ts"
      provides: "Idempotent Faker+prisma.upsert seeder — 3 Cohorts, 30 Associates, 36 CurriculumWeeks, 15 Sessions, Settings singleton. assertStagingDatabase() at top, faker.seed(1337) immediately after locale import."
      contains: "faker.seed(1337)"
    - path: "scripts/__tests__/seed-staging.test.ts"
      provides: "Vitest tests: (a) two fresh faker instances with seed 1337 produce identical firstName sequence; (b) assertStagingDatabase throws when DATABASE_URL mutated to lack staging ref; (c) assertStagingDatabase passes when DATABASE_URL contains staging ref"
    - path: "scripts/__tests__/assert-staging-env.test.ts"
      provides: "Dedicated guard unit tests for both assertStagingDatabase + assertProdDatabase (inverse semantics); maskUrl redacts password"
    - path: "package.json"
      provides: "devDependency @faker-js/faker ^10.4.0; scripts.seed-staging = tsx scripts/seed-staging.ts"
      contains: "@faker-js/faker"
  key_links:
    - from: "scripts/seed-staging.ts"
      to: "scripts/lib/assert-staging-env.ts"
      via: "import { assertStagingDatabase } from './lib/assert-staging-env.js'; called on line 1 of main"
      pattern: "assertStagingDatabase\\(\\)"
    - from: "scripts/seed-staging.ts"
      to: "../src/lib/prisma.js"
      via: "prisma singleton import (matches scripts/seed-demo-data.ts style)"
      pattern: "from '../src/lib/prisma.js'"
    - from: "scripts/seed-staging.ts"
      to: "@faker-js/faker/locale/en"
      via: "locale-pinned import (RESEARCH pitfall 6) followed immediately by faker.seed(1337)"
      pattern: "@faker-js/faker/locale/en"
---

<objective>
Ship the **seed infrastructure** for Phase 46: a shared env-guard helper, an idempotent Faker+Prisma seeder, vitest unit tests, and the `@faker-js/faker` devDependency. Fully autonomous — no staging DB needed to merge this plan (tests mock env + Prisma). Plans 02 and 03 import the guard; Plan 04 ships the hygiene verifier.

Purpose: Make `npx tsx scripts/seed-staging.ts` safe to run against staging and impossible to run against prod (T-46-02), and guarantee re-runs never duplicate data (T-46-01, D-07/D-09).
Output: `scripts/lib/assert-staging-env.ts`, `scripts/seed-staging.ts`, two vitest files, `package.json` updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-supabase-staging-env-hygiene-prisma-migrate-baseline/46-CONTEXT.md
@.planning/phases/46-supabase-staging-env-hygiene-prisma-migrate-baseline/46-RESEARCH.md
@.planning/phases/46-supabase-staging-env-hygiene-prisma-migrate-baseline/46-VALIDATION.md
@prisma/schema.prisma
@scripts/seed-demo-data.ts
@scripts/seed-coding-demo.ts
@scripts/list-associates.ts
@src/lib/prisma.ts
@package.json

<interfaces>
<!-- Extracted from scripts/seed-demo-data.ts + prisma/schema.prisma -->
<!-- MATCH THIS IMPORT STYLE EXACTLY. Planner verified 2026-04-18. -->

Existing script import pattern (scripts/seed-demo-data.ts lines 12-13):
```typescript
import 'dotenv/config';
import { prisma } from '../src/lib/prisma.js';
```

Note: scripts/seed-coding-demo.ts uses a different pattern (`import { PrismaClient } from '../src/generated/prisma'` + its own PrismaPg + Pool). For Phase 46 seed-staging.ts, USE the seed-demo-data.ts style — import the singleton `prisma` from `../src/lib/prisma.js`. This is the canonical pattern per CONTEXT.md §Existing Code Insights.

Prisma unique constraints (from prisma/schema.prisma, verified):
- Associate.slug @unique → direct prisma.associate.upsert where: { slug }
- Associate.email @unique → do NOT upsert on email (slug is canonical)
- CurriculumWeek @@unique([cohortId, weekNumber]) → upsert where: { cohortId_weekNumber: { cohortId, weekNumber } }
- Settings.id PK (singleton id=1) → prisma.settings.upsert where: { id: 1 }
- CodingChallenge.slug @unique → direct upsert on slug
- Cohort has NO @unique beyond id → use findFirst({ where: { name } }) + update/create pattern (matches scripts/seed-demo-data.ts)
- Session.id is @id (not auto) → prisma.session.upsert where: { id } with deterministic composite id string
- GapScore @@unique([associateId, skill, topic])

Faker 10.x determinism (RESEARCH pitfall 6 — locale pinning mandatory):
```typescript
// CORRECT — pin locale, seed before ANY faker.* call
import { faker } from '@faker-js/faker/locale/en';
faker.seed(1337);
```

Env-guard constants:
- STAGING_REF = 'lzuqbpqmqlvzwebliptj'
- Prod ref is user-injected via PROD_SUPABASE_REF env var (Plan 02 runbook sets it)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared env-guard helper + unit tests</name>
  <files>scripts/lib/assert-staging-env.ts, scripts/__tests__/assert-staging-env.test.ts</files>
  <read_first>
    - .planning/phases/46-supabase-staging-env-hygiene-prisma-migrate-baseline/46-CONTEXT.md D-11, D-18, D-20
    - .planning/phases/46-supabase-staging-env-hygiene-prisma-migrate-baseline/46-RESEARCH.md §Architecture Patterns — Pattern 1 (copy-paste skeleton)
    - scripts/abuse-test-coding.ts (existing env-require pattern)
  </read_first>
  <behavior>
    - assertStagingDatabase() throws Error when process.env.DATABASE_URL is undefined
    - assertStagingDatabase() throws Error when DATABASE_URL does not contain 'lzuqbpqmqlvzwebliptj'
    - assertStagingDatabase() returns void (no throw) when DATABASE_URL contains 'lzuqbpqmqlvzwebliptj'
    - assertProdDatabase(expectedProdRef) throws when DATABASE_URL contains STAGING_REF (cross-env protection)
    - assertProdDatabase(expectedProdRef) throws when DATABASE_URL lacks expectedProdRef substring
    - assertProdDatabase(expectedProdRef) returns void when DATABASE_URL contains expectedProdRef and NOT staging ref
    - maskUrl(url) replaces the password segment between `:` and `@` with `***`
    - Error messages do NOT leak the raw DATABASE_URL (must call maskUrl() before embedding)
  </behavior>
  <action>
    Create `scripts/lib/assert-staging-env.ts` exporting:
    - `const STAGING_REF = 'lzuqbpqmqlvzwebliptj'` (exported for reuse in tests + Plan 04 verify-env-hygiene)
    - `assertStagingDatabase(): void` — reads `process.env.DATABASE_URL ?? ''`, throws with multi-line error message referencing docs/ENV-HYGIENE.md when ref absent (per D-11 and RESEARCH Pattern 1)
    - `assertProdDatabase(expectedProdRef: string): void` — inverse: refuses to run if DATABASE_URL contains STAGING_REF; also throws if DATABASE_URL doesn't contain expectedProdRef. Used by Plan 02 wipe-prod.ts
    - `maskUrl(url: string): string` — replaces `/:[^:@]*@/` with `:***@` (private helper exported only for test)

    Write `scripts/__tests__/assert-staging-env.test.ts` using vitest — save current `process.env.DATABASE_URL` in `beforeEach`, restore in `afterEach`, mutate in each test case. Test cases cover all 8 behaviors above. Import from `../lib/assert-staging-env.js` (match tsx `.js`-extension convention seen in scripts/seed-demo-data.ts).

    DO NOT leak the unmasked URL in any thrown message — call maskUrl() first.
  </action>
  <verify>
    <automated>npm run test -- scripts/__tests__/assert-staging-env.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- scripts/__tests__/assert-staging-env.test.ts` exits 0 with at least 8 passing test cases
    - `npx tsc --noEmit` passes
    - `grep -q "lzuqbpqmqlvzwebliptj" scripts/lib/assert-staging-env.ts` matches (STAGING_REF constant present)
    - `grep -q "export function assertStagingDatabase" scripts/lib/assert-staging-env.ts` matches
    - `grep -q "export function assertProdDatabase" scripts/lib/assert-staging-env.ts` matches
  </acceptance_criteria>
  <done>
    Both files land, all assertions tested, TypeScript clean, no raw URL leaks in error output.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install @faker-js/faker + deterministic-seed test</name>
  <files>package.json, scripts/__tests__/seed-staging.test.ts</files>
  <read_first>
    - .planning/phases/46-supabase-staging-env-hygiene-prisma-migrate-baseline/46-CONTEXT.md D-09 (faker.seed(1337) + deterministic)
    - .planning/phases/46-supabase-staging-env-hygiene-prisma-migrate-baseline/46-RESEARCH.md §Pitfall 6 (locale pinning mandatory)
    - package.json (confirm no @faker-js/faker yet)
  </read_first>
  <behavior>
    - Two separate `import { faker as fakerA } from '@faker-js/faker/locale/en'` + seed(1337) + call `faker.person.firstName()` N=5 times → produces identical 5-name sequence as a second freshly-imported instance with same seed
    - Seed determinism works with `faker.number.float({ min: 50, max: 95, fractionDigits: 1 })` — two runs produce same float sequence
    - Importing `@faker-js/faker/locale/en` (NOT bare `@faker-js/faker`) succeeds
  </behavior>
  <action>
    Run `npm install --save-dev @faker-js/faker@^10.4.0`. Verify `package.json` devDependencies block now contains the exact entry. Also add to package.json `scripts` block: `"seed-staging": "tsx scripts/seed-staging.ts"` (matches existing tsx-script convention from `seed-demo-data`, `seed-coding-demo`, `wipe-demo-data`, `abuse-test-coding`).

    Write the first draft of `scripts/__tests__/seed-staging.test.ts` with ONE test group: "faker determinism". Test cases:
    - Fresh faker instance with seed 1337 produces firstName sequence `[n1, n2, n3, n4, n5]`; a second fresh instance with seed 1337 produces same sequence → deep-equal assertion
    - Same for `faker.number.float({ min: 50, max: 95, fractionDigits: 1 })` → 5 floats deep-equal
    - The `@faker-js/faker/locale/en` import does not throw

    To get two independent faker instances in one test file, either: (a) use `import { en, Faker } from '@faker-js/faker'` + `new Faker({ locale: [en] })` twice (RESEARCH pitfall 6 alternative); OR (b) import twice with `await import('@faker-js/faker/locale/en')` — pick (a) for cleanliness. DO NOT pollute a shared faker singleton.

    Leave the seeder-idempotency test stubs as `it.todo(...)` placeholders — those require mocked Prisma and ship in Task 3.
  </action>
  <verify>
    <automated>npm run test -- scripts/__tests__/seed-staging.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '"@faker-js/faker":\s*"\^10\.' package.json` matches (pins ^10.x)
    - `grep -q '"seed-staging":' package.json` matches
    - `npm run test -- scripts/__tests__/seed-staging.test.ts` exits 0 with faker-determinism cases green
    - `npx tsc --noEmit` passes
    - Both firstName sequences are identical across two fresh Faker instances with seed 1337 (asserted in test)
  </acceptance_criteria>
  <done>
    Faker installed, locale-pinned determinism proven via vitest, package.json scripts entry wired.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Idempotent seed-staging.ts + mocked-Prisma idempotency tests</name>
  <files>scripts/seed-staging.ts, scripts/__tests__/seed-staging.test.ts</files>
  <read_first>
    - .planning/phases/46-supabase-staging-env-hygiene-prisma-migrate-baseline/46-CONTEXT.md D-07, D-08, D-09, D-10, D-11
    - .planning/phases/46-supabase-staging-env-hygiene-prisma-migrate-baseline/46-RESEARCH.md §Architecture Patterns — Pattern 2 (seeder skeleton copy-paste)
    - .planning/phases/46-supabase-staging-env-hygiene-prisma-migrate-baseline/46-RESEARCH.md §Pitfall 7 (Cohort has no @unique — use findFirst+update/create)
    - prisma/schema.prisma — confirm exact unique keys per model
    - scripts/seed-demo-data.ts (import style reference — `import { prisma } from '../src/lib/prisma.js'`)
  </read_first>
  <behavior>
    - Calling `main()` twice (mocked prisma; reset faker seed between runs) yields identical final counts: 3 cohorts, 30 associates, 36 curriculum weeks, 15 sessions, 1 settings row
    - No `prisma.*.create()` call appears on the second run for associates/curriculum weeks/sessions/settings (every mutating op is upsert OR findFirst+update)
    - All generated associate emails end with `@example.com` (D-06 test-heuristic compliance)
    - All associate slugs have prefix `staging-{cohortSlug}-assoc-` (so verify-prod wipe heuristic cleanly categorizes them)
    - faker.seed(1337) is called on line immediately after `import { faker } from '@faker-js/faker/locale/en'` (RESEARCH pitfall 6)
    - assertStagingDatabase() is called before any prisma operation (FIRST statement of main())
    - Script calls `await prisma.$disconnect()` in both success and error paths
  </behavior>
  <action>
    Create `scripts/seed-staging.ts` from the RESEARCH §Pattern 2 skeleton with these exact adaptations:

    1. Imports (top of file, in this order):
    ```typescript
    import 'dotenv/config';
    import { faker } from '@faker-js/faker/locale/en';
    import { prisma } from '../src/lib/prisma.js';
    import { assertStagingDatabase } from './lib/assert-staging-env.js';
    ```

    2. Top-level guard + seed (BEFORE any other code executes):
    ```typescript
    assertStagingDatabase();
    faker.seed(1337);
    ```

    3. Const data per D-08:
    - `COHORT_SPECS` — 3 entries: alpha-2026 (startDate 2026-01-15), beta-2026 (2026-02-15), gamma-2026 (2026-03-15), each with 12 weeks
    - `SKILLS_12` — 12 entries (React, TypeScript, Node, Next.js, SQL, Testing, Python, JavaScript, Java, System Design, API Testing, CSS) each with `slug` and `topics[]`

    4. Upsert helpers (match existing pattern from scripts/seed-demo-data.ts):
    - `upsertCohort(spec)` — **findFirst({ where: { name } }) + update/create** (Cohort has no @unique beyond id per Pitfall 7)
    - `upsertAssociate(cohortId, cohortSlug, i)` — `prisma.associate.upsert({ where: { slug: `staging-${cohortSlug}-assoc-${i.toString().padStart(2,'0')}` }, update, create })`, email = `${slug}@example.com`
    - `upsertCurriculumWeek(cohortId, weekNumber)` — `prisma.curriculumWeek.upsert({ where: { cohortId_weekNumber: { cohortId, weekNumber } }, ... })`
    - `upsertSession(associateId, cohortId, ix)` — deterministic composite id `staging-sess-${associateId}-${ix}`, full Session fields per schema (candidateName, interviewerName, date, status, questionCount, selectedWeeks, overallTechnicalScore, overallSoftSkillScore, questions, starterQuestions, assessments, techMap, associateId, cohortId, mode alternates 'trainer-led'/'automated', readinessRecomputeStatus='done')
    - `upsertSettings()` — `prisma.settings.upsert({ where: { id: 1 }, update: { readinessThreshold: 75 }, create: { id: 1, readinessThreshold: 75 } })`

    5. `main()` orchestration:
    ```
    for each of 3 cohort specs:
      cohort = upsertCohort(spec)
      for each of 12 weeks:
        upsertCurriculumWeek(cohort.id, weekNumber)
      for i = 0..9:
        assoc = upsertAssociate(cohort.id, spec.slug, i)
        if i % 2 === 0:           // 5 per cohort × 3 cohorts = 15 total sessions
          upsertSession(assoc.id, cohort.id, Math.floor(i/2))
    upsertSettings()
    console.log('[seed-staging] Done:', counts)
    await prisma.$disconnect()
    ```

    6. Error handler: `main().catch(async (err) => { console.error('[seed-staging] FATAL:', err); await prisma.$disconnect(); process.exit(1); })`

    7. CodingChallenges/CodingAttempts: **DEFER** — D-08 mentions 10 challenges but RESEARCH open-question 8 notes the bank-slug selection is a downstream concern. Add a `// TODO(46-03): import 10 stable challenge slugs from coding bank manifest` comment and LEAVE the challenge seed as empty in this task. This keeps Plan 01 shippable without depending on the coding bank.

    NOW extend `scripts/__tests__/seed-staging.test.ts` with a second `describe` block "seeder idempotency" that:
    - Mocks `../src/lib/prisma.js` via `vi.mock()` with jest-style stub methods (upsert, findFirst, update, create, $disconnect). Use `vi.fn()` to track call counts.
    - Stub assertStagingDatabase to no-op (vi.mock of '../lib/assert-staging-env.js')
    - Imports the seeder dynamically, runs main() twice
    - Asserts: same number of upsert calls each run; same arguments (stringified) passed to prisma.associate.upsert for the same associate slug; no call to prisma.*.create() for associate/curriculumWeek/session/settings in second run (those are upserts, so only `.upsert` should appear)
    - Asserts: across both runs, 30 distinct associate slugs appear (3 cohorts × 10), 36 distinct (cohortId, weekNumber) curriculum-week keys, 15 distinct session ids
    - Asserts: all associate emails match `/@example\.com$/`

    Remove any `it.todo(...)` placeholders from Task 2's file.
  </action>
  <verify>
    <automated>npm run test -- scripts/__tests__/seed-staging.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- scripts/__tests__/seed-staging.test.ts` exits 0 with BOTH test groups green (faker determinism + seeder idempotency)
    - `npx tsc --noEmit` passes
    - `grep -q "assertStagingDatabase()" scripts/seed-staging.ts` matches
    - `grep -q "faker.seed(1337)" scripts/seed-staging.ts` matches
    - `grep -q "@faker-js/faker/locale/en" scripts/seed-staging.ts` matches (locale-pinned import)
    - `grep -q "from '../src/lib/prisma.js'" scripts/seed-staging.ts` matches (canonical singleton import)
    - `npx tsc --noEmit` passes — including type compatibility of Session.assessments (JSON), Session.selectedWeeks (Int[]), Session.techMap (Json), etc.
    - Per 46-VALIDATION Task 46-01-01: `npm run test -- scripts/__tests__/seed-staging.test.ts` exits 0
  </acceptance_criteria>
  <done>
    Seeder + complete test file land, all tests green, guard invoked at top, faker locale-pinned + seeded before first call.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → DATABASE_URL env var | Operator may export a prod URL into a shell meant for staging |
| scripts/seed-staging.ts → Supabase DB via Prisma | Bulk write path; destructive if pointed at prod |
| @faker-js/faker (new devDep) → npm registry | Supply-chain exposure for a devDep run only on operator machines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | scripts/seed-staging.ts (duplicate rows on re-run) | mitigate | All mutating ops are upsert keyed on @unique (Associate.slug, CurriculumWeek [cohortId,weekNumber], Settings.id=1, Session.id) or findFirst+update (Cohort.name). faker.seed(1337) + @faker-js/faker/locale/en ensure deterministic inputs. Task 3 mocked-Prisma test asserts zero duplicate keys across two runs. |
| T-46-02 | Spoofing | scripts/seed-staging.ts (seeder writes to prod by mistake) | mitigate | assertStagingDatabase() is FIRST statement of main(), before any prisma client is instantiated. Throws non-recoverable Error if DATABASE_URL lacks `lzuqbpqmqlvzwebliptj`. Task 1 unit tests cover both pass + fail branches. |
</threat_model>

<verification>
- `npm run test -- scripts/__tests__/assert-staging-env.test.ts` → exit 0
- `npm run test -- scripts/__tests__/seed-staging.test.ts` → exit 0 (faker determinism + seeder idempotency groups green)
- `npx tsc --noEmit` → exit 0
- `npm run lint` → exit 0 on new files
- `grep -q "@faker-js/faker" package.json` → match
</verification>

<success_criteria>
- DATA-03 infrastructure exists: seeder + guard + tests; Plan 02 can import assertProdDatabase; Plan 04 can import STAGING_REF for its env-hygiene scanner.
- Seeder is demonstrably idempotent (mocked test proves it) and demonstrably refuses to run against non-staging DATABASE_URLs (unit test proves it).
- `@faker-js/faker@^10.4.0` devDep committed; no runtime dependency added.
</success_criteria>

<output>
After completion, create `.planning/phases/46-supabase-staging-env-hygiene-prisma-migrate-baseline/46-01-SUMMARY.md` summarizing: assertStagingDatabase/assertProdDatabase signatures (for Plan 02 reuse), STAGING_REF export (for Plan 04 reuse), seeder entity counts (3/30/36/15/1), and deferred CodingChallenge seeding (TODO comment referenced in code).
</output>
