---
phase: 11-cohort-management
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/app/api/trainer/[slug]/route.ts
  - src/app/api/trainer/[slug]/route.test.ts
  - src/app/trainer/[slug]/page.tsx
  - src/app/trainer/[slug]/AssociateCohortSelect.tsx
  - src/lib/trainer-types.ts
autonomous: false
requirements: [COHORT-02]
must_haves:
  truths:
    - "Trainer viewing /trainer/[slug] sees the associate's current cohort (or 'Unassigned')"
    - "Trainer can change the associate's cohort via dropdown — selection persists after reload"
    - "Selecting 'Unassigned' sets Associate.cohortId = null"
    - "Associates with no cohort remain fully functional — sessions persist, gap/readiness pipelines unaffected"
    - "PATCH /api/trainer/[slug] accepts { cohortId: number | null } and updates the associate"
  artifacts:
    - path: "src/app/trainer/[slug]/AssociateCohortSelect.tsx"
      provides: "Client component: cohort dropdown with optimistic update"
      min_lines: 50
    - path: "src/app/api/trainer/[slug]/route.ts"
      provides: "Adds PATCH handler for cohort assignment (preserves existing GET)"
      exports: ["GET", "PATCH"]
  key_links:
    - from: "AssociateCohortSelect.tsx onChange"
      to: "PATCH /api/trainer/[slug]"
      via: "fetch"
      pattern: "fetch.*\\/api\\/trainer\\/"
    - from: "AssociateCohortSelect.tsx mount"
      to: "GET /api/cohorts"
      via: "fetch for dropdown options"
      pattern: "fetch.*api/cohorts"
---

<objective>
Wire associate → cohort assignment. Add PATCH to the existing trainer associate route, and add a cohort dropdown to the associate detail page.

Purpose: Satisfy COHORT-02 — trainer-driven assignment without a bulk UI.
Output: PATCH endpoint + dropdown component on `/trainer/[slug]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-cohort-management/11-CONTEXT.md
@.planning/phases/11-cohort-management/11-01-SUMMARY.md
@DESIGN.md
@src/app/api/trainer/[slug]/route.ts
@src/app/trainer/[slug]/page.tsx
@src/lib/trainer-types.ts
@src/lib/cohort-types.ts
@src/lib/auth-server.ts
@src/lib/prisma.ts

<interfaces>
Existing GET /api/trainer/[slug] returns `AssociateDetail` (see `src/lib/trainer-types.ts`). This plan ADDS PATCH; do not change GET.

```typescript
// PATCH /api/trainer/[slug]
// Request: { cohortId: number | null }
// Response 200: { slug: string, cohortId: number | null }
// Response 400: invalid payload
// Response 401: unauthenticated
// Response 404: associate not found

// AssociateDetail (extend in src/lib/trainer-types.ts)
export interface AssociateDetail {
  // ...existing fields preserved
  cohortId: number | null;
  cohortName: string | null;
}
```

Cohort list for dropdown from Plan 11-01:
```typescript
// GET /api/cohorts → CohortDTO[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PATCH to /api/trainer/[slug] for cohort assignment</name>
  <files>src/app/api/trainer/[slug]/route.ts, src/app/api/trainer/[slug]/route.test.ts, src/lib/trainer-types.ts</files>
  <behavior>
    - Test: unauthenticated PATCH returns 401
    - Test: PATCH with invalid slug returns 400
    - Test: PATCH with non-number cohortId (and not null) returns 400
    - Test: PATCH with valid cohortId updates associate and returns 200 with { slug, cohortId }
    - Test: PATCH with cohortId=null nullifies the field and returns 200
    - Test: PATCH with cohortId pointing to non-existent cohort returns 400 (Prisma P2003 FK violation, mapped to 400)
    - Test: PATCH for non-existent associate slug returns 404
    - Test: existing GET behavior unchanged (regression test — returns AssociateDetail with new cohortId + cohortName fields populated when assigned)
  </behavior>
  <action>
    Extend `src/lib/trainer-types.ts` — add `cohortId: number | null` and `cohortName: string | null` to `AssociateDetail` (per D-05). Update any existing consumers if they destructure — add defaults.

    In `src/app/api/trainer/[slug]/route.ts`:
    - Keep existing GET. Extend the Prisma query's `include` to include `cohort: { select: { id: true, name: true } }`. Map into the response as `cohortId` and `cohortName`.
    - Add `export async function PATCH(req, { params })`:
      - Auth guard (`isAuthenticatedSession()` → 401 if missing, matches existing pattern).
      - Await params, validate slug with existing `SLUG_RE` regex (400 if invalid).
      - Parse body; zod schema: `z.object({ cohortId: z.number().int().positive().nullable() })`. On parse failure → 400 with issues.
      - `prisma.associate.update({ where: { slug }, data: { cohortId }, select: { slug: true, cohortId: true } })`.
      - Catch Prisma P2025 → 404 ("Associate not found"). Catch P2003 → 400 ("Invalid cohortId — cohort does not exist"). Other → 500.
      - Return 200 with `{ slug, cohortId }`.

    Write vitest tests covering all behavior cases. Mock `isAuthenticatedSession` and `prisma`.
  </action>
  <verify>
    <automated>npm run test -- src/app/api/trainer/[slug]/route.test.ts</automated>
  </verify>
  <done>All behavior tests pass; GET regression test passes; typecheck + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: AssociateCohortSelect component + integration into /trainer/[slug]</name>
  <files>src/app/trainer/[slug]/AssociateCohortSelect.tsx, src/app/trainer/[slug]/page.tsx</files>
  <action>
    Create `AssociateCohortSelect.tsx` (client component, per D-13, D-14, D-18):
    - Props: `{ slug: string, initialCohortId: number | null, initialCohortName: string | null }`.
    - State: `cohorts: CohortDTO[] | null` (loaded on mount from GET /api/cohorts), `selectedId: number | null`, `status: 'idle' | 'saving' | 'saved' | 'error'`, `errorMsg: string | null`.
    - `useEffect` on mount: fetch /api/cohorts, set state. Show "Loading cohorts..." until loaded.
    - Render: a labeled `<select>` with options `<option value="">Unassigned</option>` + one per cohort. `value={selectedId ?? ''}`.
    - onChange: optimistically update `selectedId` → PATCH /api/trainer/[slug] with `{ cohortId: value === '' ? null : Number(value) }`. On success set status='saved' for 2s then 'idle'. On failure revert selectedId, set status='error' with errorMsg from response.
    - Inline success/error text next to the select (green for saved, red for error). No toast library (per D-14 discretion).
    - Styling per DESIGN.md tokens — match existing `/trainer/[slug]` visual language.

    Modify `src/app/trainer/[slug]/page.tsx`:
    - Read the `AssociateDetail` response (already has `cohortId` and `cohortName` after Task 1).
    - Add a "Cohort" row/card in an appropriate location (near the readiness status or at the top of the detail view — Claude's discretion). Display current cohort name (or "Unassigned") and render `<AssociateCohortSelect slug={slug} initialCohortId={detail.cohortId} initialCohortName={detail.cohortName} />`.
    - Do not disturb existing session history, gap charts, or calibration views.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit</automated>
  </verify>
  <done>Dropdown renders on /trainer/[slug], cohort change persists across reload, typecheck + lint clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual verification of assignment flow + backward-compat</name>
  <what-built>PATCH /api/trainer/[slug] for cohort assignment + cohort dropdown on associate detail page.</what-built>
  <how-to-verify>
    1. Run `npm run dev`, log in as trainer.
    2. Create two cohorts via `/trainer/cohorts` (e.g., "Spring 2026", "Fall 2026").
    3. Open `/trainer/[some-existing-slug]`. Confirm a "Cohort" section appears with "Unassigned" and a dropdown.
    4. Select "Spring 2026". Inline "Saved" message appears. Reload page — selection persists.
    5. Select "Unassigned". Reload — associate shows Unassigned.
    6. Test backward compat: open any associate that has never been assigned to a cohort. Confirm `/trainer/[slug]` renders without error, sessions list renders, gap scores render.
    7. Complete a new trainer-led interview for an unassigned associate (end-to-end). Run `curl http://localhost:3000/api/sync-check` (or equivalent) — should show no divergence. Verify gap scores + readiness update as before.
    8. In DB: pick an associate, note their id, manually assign via UI, then verify `SELECT cohortId FROM "Associate" WHERE slug = '...'` matches.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Plan 11-01 API tests still pass
- PATCH tests all pass
- Manual checkpoint confirms end-to-end assignment + backward compat
- `npm run lint` + `npx tsc --noEmit` clean
</verification>

<success_criteria>
- COHORT-02 satisfied: associates can be assigned to a cohort via trainer UI
- Associates without cohorts remain fully functional (sessions, gap scores, readiness all work)
- PATCH endpoint returns correct status codes for all error paths
- Cohort change persists across page reload
</success_criteria>

<output>
After completion, create `.planning/phases/11-cohort-management/11-03-SUMMARY.md`
</output>
