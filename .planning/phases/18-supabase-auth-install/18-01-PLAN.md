---
phase: 18-supabase-auth-install
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/supabase/server.ts
  - src/lib/supabase/middleware.ts
  - src/lib/supabase/admin.ts
  - src/lib/supabase/browser.ts
  - src/lib/env.ts
  - src/lib/env.test.ts
  - src/lib/authRateLimit.ts
  - src/lib/authRateLimit.test.ts
  - src/instrumentation.ts
  - prisma/schema.prisma
  - .env.example
  - .env.docker
autonomous: true
requirements: [AUTH-05]
user_setup:
  - service: supabase
    why: "Supabase Auth requires project credentials"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Settings -> API -> anon public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Settings -> API -> service_role secret key"
      - name: NEXT_PUBLIC_SITE_URL
        source: "Your production domain e.g. https://nlm.example.com (use http://localhost:3000 for dev)"
      - name: ADMIN_EMAILS
        source: "Comma-separated admin email addresses for abuse flag notifications"
    dashboard_config:
      - task: "Set OTP expiry to 604800 seconds (7 days)"
        location: "Supabase Dashboard -> Authentication -> Email -> OTP Expiry"
      - task: "Enable PKCE flow"
        location: "Supabase Dashboard -> Authentication -> Providers -> Email (verify PKCE enabled)"
      - task: "Add redirect URLs"
        location: "Supabase Dashboard -> Authentication -> URL Configuration -> Redirect URLs: add https://yourdomain.com/** and http://localhost:3000/**"
must_haves:
  truths:
    - "npm run build succeeds with @supabase/ssr + @supabase/supabase-js installed"
    - "Three Supabase client modules export correct functions"
    - "Boot-time assert throws when NEXT_PUBLIC_SITE_URL is localhost in production"
    - "AuthEvent model exists in Prisma schema for rate-limit abuse logging"
    - "authRateLimit module enforces per-type, per-email, per-IP sliding window limits"
  artifacts:
    - path: "src/lib/supabase/server.ts"
      provides: "Server component + route handler Supabase client"
      exports: ["createSupabaseServerClient"]
    - path: "src/lib/supabase/middleware.ts"
      provides: "Middleware Supabase client that mutates response"
      exports: ["createSupabaseMiddlewareClient"]
    - path: "src/lib/supabase/admin.ts"
      provides: "Service-role Supabase client (server-only)"
      exports: ["supabaseAdmin"]
    - path: "src/lib/supabase/browser.ts"
      provides: "Browser Supabase client for client components"
      exports: ["createSupabaseBrowserClient"]
    - path: "src/lib/env.ts"
      provides: "Boot-time environment assertions"
      exports: ["assertProductionEnv"]
    - path: "src/lib/authRateLimit.ts"
      provides: "Rate limiter for magic-link and password-reset requests"
      exports: ["checkAuthRateLimit", "recordAuthEvent"]
    - path: "prisma/schema.prisma"
      provides: "AuthEvent model for abuse logging"
      contains: "model AuthEvent"
  key_links:
    - from: "src/instrumentation.ts"
      to: "src/lib/env.ts"
      via: "import assertProductionEnv"
      pattern: "assertProductionEnv"
---

<objective>
Install Supabase Auth packages, scaffold all client modules, add boot-time env assertions, create AuthEvent model, and build the auth rate limiter.

Purpose: Foundation layer that all subsequent plans depend on. No auth flow works without these clients and infrastructure.
Output: 4 Supabase client files, env assertion module, AuthEvent migration, rate limiter module, updated .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-supabase-auth-install/18-CONTEXT.md
@.planning/phases/18-supabase-auth-install/18-RESEARCH.md
@src/lib/rateLimitService.ts
@src/instrumentation.ts
@prisma/schema.prisma
@.env.example

<interfaces>
<!-- Existing patterns to follow -->

From src/lib/rateLimitService.ts (rate limit pattern — in-memory Map):
```typescript
const rateLimitMap = new Map<string, { count: number, timestamp: number }>();
```

From src/lib/prisma.ts:
```typescript
export const prisma: PrismaClient;
```

From src/instrumentation.ts:
```typescript
export async function register() { ... } // Next.js boot hook — extend, do not replace
```

From prisma/schema.prisma:
```prisma
// Add AuthEvent model after existing models
// Associate.authUserId String? @unique already exists from Phase 17
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install packages + scaffold Supabase clients + update env</name>
  <files>package.json, src/lib/supabase/server.ts, src/lib/supabase/middleware.ts, src/lib/supabase/admin.ts, src/lib/supabase/browser.ts, .env.example, .env.docker</files>
  <behavior>
    - createSupabaseServerClient() returns a Supabase client configured with cookie getAll/setAll from next/headers cookies()
    - createSupabaseMiddlewareClient(request) returns { supabase, user, response } where response is a single mutated NextResponse
    - supabaseAdmin is a service-role client with autoRefreshToken: false, persistSession: false
    - createSupabaseBrowserClient() returns a browser Supabase client (singleton pattern)
    - admin.ts uses import 'server-only' to prevent client-side bundling
  </behavior>
  <action>
    1. Run `npm install @supabase/supabase-js@2.103.2 @supabase/ssr@0.10.2`
    2. Create `src/lib/supabase/server.ts` — export `createSupabaseServerClient()` using `createServerClient` from `@supabase/ssr` with `cookies()` from `next/headers`. Follow exact pattern from RESEARCH.md Pattern 1 (getAll/setAll with try-catch on setAll for read-only Server Component context).
    3. Create `src/lib/supabase/middleware.ts` — export `createSupabaseMiddlewareClient(request: NextRequest)` returning `{ supabase, user, response }`. CRITICAL: create `NextResponse.next({ request })` ONCE, write cookies to both request AND response in setAll. Call `supabase.auth.getUser()` inside this function and return the user. Follow RESEARCH.md Pattern 2 exactly.
    4. Create `src/lib/supabase/admin.ts` — `import 'server-only'` at top. Export `supabaseAdmin` as `createClient(URL, SERVICE_ROLE_KEY, { auth: { autoRefreshToken: false, persistSession: false } })`. Follow RESEARCH.md Pattern 3.
    5. Create `src/lib/supabase/browser.ts` — export `createSupabaseBrowserClient()` using `createBrowserClient` from `@supabase/ssr`. Use singleton pattern (module-level variable) to avoid recreating on every render.
    6. Update `.env.example` and `.env.docker` — add `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SITE_URL`, `ADMIN_EMAILS` with comments explaining each.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Four Supabase client modules export correctly, build passes, .env.example updated with all 5 new vars.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Boot-time env assert + AuthEvent migration + auth rate limiter</name>
  <files>src/lib/env.ts, src/lib/env.test.ts, src/lib/authRateLimit.ts, src/lib/authRateLimit.test.ts, src/instrumentation.ts, prisma/schema.prisma</files>
  <behavior>
    - assertProductionEnv() throws when NODE_ENV=production and NEXT_PUBLIC_SITE_URL contains localhost/127.0.0.1/0.0.0.0/::1
    - assertProductionEnv() throws when NODE_ENV=production and any required Supabase env var is missing
    - assertProductionEnv() does nothing when NODE_ENV !== production
    - checkAuthRateLimit({ email, ip, type }) returns { allowed: boolean, remaining: number } with separate namespaces for 'magic-link' and 'reset'
    - magic-link and reset both enforce 3/hr/email + 10/hr/IP
    - recordAuthEvent() persists to AuthEvent table for admin visibility
    - AuthEvent model has id, type, email, ip, metadata, createdAt
  </behavior>
  <action>
    1. Create `src/lib/env.ts` — export `assertProductionEnv()`. Check `NODE_ENV === 'production'`. Reject `NEXT_PUBLIC_SITE_URL` containing localhost/127.0.0.1/0.0.0.0/::1. Require `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SITE_URL`. Throw with `[FATAL]` prefix for clear boot logs. Follow RESEARCH.md Pattern 8.
    2. Create `src/lib/env.test.ts` — test assertProductionEnv: (a) no-op in non-production, (b) throws for localhost URL in production, (c) throws for missing env vars in production, (d) passes for valid production config.
    3. Extend `src/instrumentation.ts` `register()` — add `import { assertProductionEnv } from '@/lib/env'` and call `assertProductionEnv()` at the start of register(). Keep existing cleanup logic intact.
    4. Add `AuthEvent` model to `prisma/schema.prisma`:
       ```prisma
       model AuthEvent {
         id        String   @id @default(cuid())
         type      String   // 'magic-link' | 'reset' | 'reset-abuse-flag' | 'login-failure'
         email     String
         ip        String
         metadata  Json?    // e.g. { flagCount: N, deduplicatedUntil: ISO }
         createdAt DateTime @default(now())
         @@index([email, type])
         @@index([createdAt])
       }
       ```
    5. Run `npx prisma migrate dev --name add-auth-event` to create migration.
    6. Create `src/lib/authRateLimit.ts` — in-memory sliding window Map (matching `rateLimitService.ts` pattern) with separate key namespaces: `auth:{type}:email:{sha256(email)}` and `auth:{type}:ip:{ip}`. Limits: 3/hr per email key, 10/hr per IP key. Export `checkAuthRateLimit({ email, ip, type: 'magic-link' | 'reset' })` returning `{ allowed, remaining, retryAfterMs }`. Export `recordAuthEvent(data)` that writes to `AuthEvent` table via Prisma (fire-and-forget, catch errors). Clean expired entries on check (lazy cleanup, no timer).
    7. Create `src/lib/authRateLimit.test.ts` — test: (a) allows first request, (b) blocks after 3 same-email requests within hour, (c) blocks after 10 same-IP requests within hour, (d) separate namespaces (reset vs magic-link don't interfere), (e) expired entries are cleaned.
  </action>
  <verify>
    <automated>npm run test -- --run src/lib/env.test.ts src/lib/authRateLimit.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Boot assert active in instrumentation.ts, AuthEvent migration applied, rate limiter passes all tests with namespace isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin.ts service-role key | Server-only — bypasses RLS, can delete any user |
| Boot environment | Production must not use localhost URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | src/lib/supabase/admin.ts | mitigate | `import 'server-only'` prevents client-side bundling of service role key |
| T-18-02 | Spoofing | NEXT_PUBLIC_SITE_URL | mitigate | Boot-time assert in instrumentation.ts rejects localhost in production |
| T-18-03 | Denial of Service | authRateLimit.ts | mitigate | Separate per-email (3/hr) + per-IP (10/hr) sliding windows with namespace isolation |
</threat_model>

<verification>
- `npm run build` passes
- `npm run test -- --run src/lib/env.test.ts src/lib/authRateLimit.test.ts` all green
- `npx tsc --noEmit` no type errors
- `.env.example` contains all 5 new Supabase env vars
</verification>

<success_criteria>
SC-1 (partial): npm run build succeeds with packages installed and clients scaffolded
SC-2: Boot-time assert prevents localhost in production
</success_criteria>

<output>
After completion, create `.planning/phases/18-supabase-auth-install/18-01-SUMMARY.md`
</output>
