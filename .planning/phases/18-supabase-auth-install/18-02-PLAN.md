---
phase: 18-supabase-auth-install
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - src/middleware.ts
  - src/lib/identity.ts
  - src/lib/identity.test.ts
  - src/app/api/github/cache/invalidate/route.ts
  - src/app/api/trainer/associates/route.ts
  - src/app/api/trainer/associates/[id]/route.ts
  - src/app/api/trainer/associates/preview/route.ts
  - src/app/api/trainer/associates/route.test.ts
  - src/app/api/trainer/associates/[id]/route.test.ts
  - src/app/api/trainer/associates/__tests__/integration.test.ts
  - src/app/api/trainer/associates/preview/route.test.ts
  - src/app/api/github/cache/invalidate/route.test.ts
autonomous: true
requirements: [AUTH-06, AUTH-08]
must_haves:
  truths:
    - "Middleware refreshes Supabase session BEFORE route guard on every matched request"
    - "Middleware returns the same mutated NextResponse (never creates a fresh one)"
    - "getCallerIdentity() returns admin|trainer|associate|anonymous from Supabase session only"
    - "getCallerIdentity() resolves associate via Associate.authUserId FK lookup"
    - "getCallerIdentity() signature no longer takes NextRequest — all callers updated"
    - "Middleware bounce goes to /signin with return-to query param"
  artifacts:
    - path: "src/middleware.ts"
      provides: "Supabase session refresh + role-based route guard"
      min_lines: 40
    - path: "src/lib/identity.ts"
      provides: "Supabase-based CallerIdentity resolver"
      exports: ["getCallerIdentity", "CallerIdentity"]
    - path: "src/lib/identity.test.ts"
      provides: "Tests for Supabase-based identity resolution"
      min_lines: 40
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/supabase/middleware.ts"
      via: "createSupabaseMiddlewareClient"
      pattern: "createSupabaseMiddlewareClient"
    - from: "src/lib/identity.ts"
      to: "src/lib/supabase/server.ts"
      via: "createSupabaseServerClient"
      pattern: "createSupabaseServerClient"
    - from: "src/lib/identity.ts"
      to: "prisma.associate"
      via: "findUnique by authUserId"
      pattern: "prisma\\.associate\\.findUnique"
---

<objective>
Rewrite middleware to use Supabase session refresh before route guard, and replace getCallerIdentity to read Supabase session as sole identity source.

Purpose: This is the auth backbone. All route handlers and server components depend on getCallerIdentity for authorization. Middleware must refresh tokens before any guard logic runs.
Output: Rewritten middleware.ts, identity.ts, updated callers, comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-supabase-auth-install/18-CONTEXT.md
@.planning/phases/18-supabase-auth-install/18-RESEARCH.md
@.planning/phases/18-supabase-auth-install/18-01-SUMMARY.md
@src/middleware.ts
@src/lib/identity.ts
@src/lib/identity.test.ts

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/supabase/middleware.ts:
```typescript
export async function createSupabaseMiddlewareClient(request: NextRequest): Promise<{
  supabase: SupabaseClient;
  user: User | null;
  response: NextResponse;
}>;
```

From src/lib/supabase/server.ts:
```typescript
export async function createSupabaseServerClient(): Promise<SupabaseClient>;
```

From src/lib/prisma.ts:
```typescript
export const prisma: PrismaClient;
```

<!-- Current callers that pass NextRequest to getCallerIdentity (MUST update all) -->
Callers to update (grep-verified):
- src/middleware.ts:37 — will use `user` from middleware client directly instead
- src/app/api/github/cache/invalidate/route.ts:32 — remove request arg
- src/app/api/trainer/associates/route.ts:11 — remove request arg
- src/app/api/trainer/associates/[id]/route.ts:44,98 — remove request arg (both GET and DELETE)
- src/app/api/trainer/associates/preview/route.ts:9 — remove request arg

<!-- Current CallerIdentity type (being replaced) -->
From src/lib/identity.ts (current — replace):
```typescript
export type CallerIdentity =
  | { type: 'trainer' }
  | { type: 'associate'; associateId: number; ver: string }
  | { type: 'anonymous' };
export async function getCallerIdentity(request: NextRequest): Promise<CallerIdentity>;
```

<!-- New CallerIdentity type (from RESEARCH.md Pattern 5) -->
New signature:
```typescript
export type CallerIdentity =
  | { kind: 'admin'; userId: string; email: string }
  | { kind: 'trainer'; userId: string; email: string }
  | { kind: 'associate'; userId: string; email: string; associateId: number; associateSlug: string }
  | { kind: 'anonymous' };
export async function getCallerIdentity(): Promise<CallerIdentity>;
```

BREAKING CHANGES:
- `type` field renamed to `kind` (discriminant)
- No NextRequest parameter
- Associate shape includes userId, email, associateId, associateSlug (not ver)
- New `admin` variant
- Callers must check `caller.kind` not `caller.type`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite middleware with Supabase session refresh + role guard</name>
  <files>src/middleware.ts</files>
  <behavior>
    - Middleware calls createSupabaseMiddlewareClient BEFORE any route guard logic
    - For public paths (/, /signin, /auth/callback, public API), returns the mutated response without guard
    - Trainer paths (/dashboard, /interview, /review, /trainer) require role admin or trainer; non-match redirects to /signin?next={path}
    - Associate paths (/associate/*) require any authenticated user; non-match redirects to /signin?next={path}
    - Redirect responses forward session cookies from the mutated response via headers
    - Never creates a fresh NextResponse.next() — always returns the one from createSupabaseMiddlewareClient
    - matcher config unchanged
  </behavior>
  <action>
    1. Rewrite `src/middleware.ts` following RESEARCH.md Pattern 4 exactly.
    2. Import `createSupabaseMiddlewareClient` from `@/lib/supabase/middleware`.
    3. STEP 1: Always call `createSupabaseMiddlewareClient(request)` to get `{ user, response }`. Extract role via `user?.user_metadata?.role ?? (user ? 'associate' : null)`.
    4. Public paths list: `['/', '/signin', '/auth/callback']`. Also keep `/associate/login` as public for backward compat (Phase 25 removes). If public, return `response` (the mutated one).
    5. STEP 2: Trainer-only paths (`/dashboard`, `/interview`, `/review`, `/trainer`). If role is `trainer` or `admin`, return `response`. Otherwise redirect to `/signin?next={pathname}`.
    6. STEP 3: Associate paths (`/associate/*`). Any authenticated user (user is truthy) returns `response`. Otherwise redirect to `/signin?next={pathname}`.
    7. CRITICAL: When building redirect responses, forward cookies: `const redirect = NextResponse.redirect(url); response.headers.getSetCookie().forEach(c => redirect.headers.append('set-cookie', c)); return redirect;`. This preserves the refreshed session cookies even on redirect.
    8. Remove the old `getCallerIdentity(request)` import — middleware no longer uses it.
    9. Keep the existing `config.matcher` array unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Middleware refreshes Supabase session before route guard, returns mutated response on all paths, forwards cookies on redirects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite getCallerIdentity + update all callers + tests</name>
  <files>src/lib/identity.ts, src/lib/identity.test.ts, src/app/api/github/cache/invalidate/route.ts, src/app/api/trainer/associates/route.ts, src/app/api/trainer/associates/[id]/route.ts, src/app/api/trainer/associates/preview/route.ts</files>
  <behavior>
    - getCallerIdentity() (no args) returns CallerIdentity with kind discriminant
    - admin user_metadata.role='admin' returns { kind: 'admin', userId, email }
    - trainer user_metadata.role='trainer' returns { kind: 'trainer', userId, email }
    - associate with authUserId match returns { kind: 'associate', userId, email, associateId, associateSlug }
    - authenticated user with no matching Associate row returns { kind: 'anonymous' }
    - no Supabase session returns { kind: 'anonymous' }
    - all route handler callers updated to use new signature (no request arg, check .kind not .type)
  </behavior>
  <action>
    1. Rewrite `src/lib/identity.ts` per RESEARCH.md Pattern 5:
       - Import `createSupabaseServerClient` from `@/lib/supabase/server` and `prisma` from `@/lib/prisma`.
       - Export `CallerIdentity` type with `kind` discriminant (admin | trainer | associate | anonymous).
       - Export `async function getCallerIdentity(): Promise<CallerIdentity>` — no parameters.
       - Call `supabase.auth.getUser()` (NOT getSession — Pitfall 2 from RESEARCH.md).
       - If no user, return `{ kind: 'anonymous' }`.
       - Read `user.user_metadata?.role`. Default to `'associate'` if role not set.
       - For `admin`: return `{ kind: 'admin', userId: user.id, email: user.email! }`.
       - For `trainer`: return `{ kind: 'trainer', userId: user.id, email: user.email! }`.
       - For associate: query `prisma.associate.findUnique({ where: { authUserId: user.id }, select: { id: true, slug: true } })`. If no match, return `{ kind: 'anonymous' }`. If match, return full associate shape.
       - Remove the old `import { verifyAssociateToken }` and all PIN-related code.
    2. Rewrite `src/lib/identity.test.ts`:
       - Mock `@/lib/supabase/server` to return a mock `supabase.auth.getUser()`.
       - Mock `@/lib/prisma` for associate lookup.
       - Test cases: (a) no user → anonymous, (b) admin role → admin shape, (c) trainer role → trainer shape, (d) associate role with matching authUserId → associate shape with id+slug, (e) associate role with NO matching Associate row → anonymous, (f) no role metadata defaults to associate behavior.
    3. Update all callers — the breaking change is: remove the `request` argument AND change `.type` checks to `.kind`:
       - `src/app/api/github/cache/invalidate/route.ts:32` — change `getCallerIdentity(request)` to `getCallerIdentity()`, change `caller.type !== 'trainer'` to `caller.kind !== 'trainer' && caller.kind !== 'admin'`.
       - `src/app/api/trainer/associates/route.ts:11` — same pattern.
       - `src/app/api/trainer/associates/[id]/route.ts:44,98` — same pattern (both GET and DELETE handlers).
       - `src/app/api/trainer/associates/preview/route.ts:9` — same pattern.
       - In each file, also update the import: remove `NextRequest` from identity import if it was imported from there.
    4. Update test files that mock `getCallerIdentity` with old shape (`{ type: 'trainer' }` → `{ kind: 'trainer', userId: 'u1', email: 'trainer@test.com' }`):
       - `src/app/api/trainer/associates/route.test.ts`
       - `src/app/api/trainer/associates/[id]/route.test.ts`
       - `src/app/api/trainer/associates/__tests__/integration.test.ts`
       - `src/app/api/trainer/associates/preview/route.test.ts`
       - `src/app/api/github/cache/invalidate/route.test.ts`
       Also update any `.type` checks in test assertions to `.kind`.
    5. Run `npm run test -- --run` to verify all tests pass with updated callers and mocks.
  </action>
  <verify>
    <automated>npm run test -- --run src/lib/identity.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>getCallerIdentity reads Supabase session only, all 5 caller files updated to new signature, all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> middleware | Untrusted cookies; middleware validates via Supabase getUser() |
| middleware -> route handlers | Route handlers trust middleware has refreshed session |
| route handler -> database | getCallerIdentity gates all DB access by role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Elevation of Privilege | src/middleware.ts | mitigate | Middleware calls getUser() (server-validated) not getSession() (unvalidated cookie read) |
| T-18-05 | Elevation of Privilege | src/middleware.ts | mitigate | Returns same mutated response object — never discards refreshed cookies |
| T-18-06 | Spoofing | src/lib/identity.ts | mitigate | Role read from Supabase-signed JWT user_metadata; only Supabase admin can set role |
| T-18-07 | Information Disclosure | src/lib/identity.ts | mitigate | Associate with no authUserId match returns anonymous, not a partial identity |
| T-18-08 | Elevation of Privilege | Route handler callers | mitigate | Admin role accepted alongside trainer for trainer-gated routes (admin subsumes trainer) |
</threat_model>

<verification>
- `npm run test -- --run` all tests green
- `npx tsc --noEmit` no type errors
- `grep -r 'getCallerIdentity(request' src/` returns zero matches (all callers updated)
- `grep -r '\.type.*trainer\|\.type.*associate' src/lib/identity` returns zero matches (kind, not type)
</verification>

<success_criteria>
SC-5: getCallerIdentity() returns admin|trainer|associate|anonymous from Supabase session only
SC-6: Middleware refreshes Supabase session BEFORE route guard and returns mutated NextResponse
</success_criteria>

<output>
After completion, create `.planning/phases/18-supabase-auth-install/18-02-SUMMARY.md`
</output>
