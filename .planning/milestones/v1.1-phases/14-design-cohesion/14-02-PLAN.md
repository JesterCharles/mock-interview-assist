---
phase: 14-design-cohesion
plan: 02
type: execute
wave: 2
depends_on: ["14-01", "11", "12", "13"]
files_modified:
  - src/components/cohort/CohortCard.tsx
  - src/components/cohort/CohortForm.tsx
  - src/components/cohort/CohortFilter.tsx
  - src/components/cohort/ReadinessSummaryBar.tsx
  - src/components/curriculum/CurriculumWeekList.tsx
  - src/components/curriculum/CurriculumWeekForm.tsx
  - src/app/trainer/cohorts/page.tsx
  - src/app/trainer/cohorts/[id]/page.tsx
  - src/app/trainer/cohorts/[id]/curriculum/page.tsx
  - src/app/trainer/page.tsx
  - tests/visual/phase-14/trainer-cohort.spec.ts
autonomous: false
requirements: [DESIGN-02]
must_haves:
  truths:
    - "Cohort management UI uses DESIGN.md tokens from initial polish — no visual debt vs trainer dashboard"
    - "Cohort filter dropdown + readiness summary bar on /trainer match token palette (burnt orange accent for active state, matte surfaces)"
    - "Curriculum week list uses typographic hierarchy: Clash Display week number, DM Sans skill name, burnt orange 'today' marker rule"
    - "No cohort or curriculum UI uses gradient backgrounds, glass morphism, or glow effects"
    - "Existing trainer roster (/trainer) and associate detail (/trainer/[slug]) remain functional and visually consistent with new cohort additions"
  artifacts:
    - path: src/components/cohort/CohortCard.tsx
      provides: "DESIGN.md-styled cohort list item"
      min_lines: 20
    - path: src/components/cohort/ReadinessSummaryBar.tsx
      provides: "Aggregate readiness counts per DESIGN.md typography pattern"
      min_lines: 20
    - path: src/components/curriculum/CurriculumWeekList.tsx
      provides: "Week-by-week schedule with today marker"
      min_lines: 30
  key_links:
    - from: src/app/trainer/cohorts/page.tsx
      to: src/components/cohort/CohortCard.tsx
      via: "import + map render"
      pattern: "CohortCard"
    - from: src/app/trainer/page.tsx
      to: src/components/cohort/CohortFilter.tsx
      via: "import + render"
      pattern: "CohortFilter"
    - from: src/app/trainer/page.tsx
      to: src/components/cohort/ReadinessSummaryBar.tsx
      via: "import + render when cohort selected"
      pattern: "ReadinessSummaryBar"
    - from: src/app/trainer/cohorts/[id]/curriculum/page.tsx
      to: src/components/curriculum/CurriculumWeekList.tsx
      via: "import + render"
      pattern: "CurriculumWeekList"
---

<objective>
Polish the cohort management, cohort filter + summary, and curriculum schedule UIs introduced in Phases 11, 12, and 13 so they ship on DESIGN.md tokens from initial build. Extract reusable components (`CohortCard`, `CohortForm`, `CohortFilter`, `ReadinessSummaryBar`, `CurriculumWeekList`, `CurriculumWeekForm`). No retrofit debt.

Purpose: Satisfy DESIGN-02. Ensure v1.1 trainer-facing features feel consistent with the v1.0 trainer dashboard and DESIGN.md warm editorial direction.
Output: Token-driven cohort + curriculum UI components consumed by Phase 11/12/13 routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-design-cohesion/14-CONTEXT.md
@.planning/phases/14-design-cohesion/14-01-SUMMARY.md
@DESIGN.md
@src/app/trainer/page.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build cohort UI components (Card, Form, Filter, SummaryBar)</name>
  <files>src/components/cohort/CohortCard.tsx, src/components/cohort/CohortForm.tsx, src/components/cohort/CohortFilter.tsx, src/components/cohort/ReadinessSummaryBar.tsx</files>
  <read_first>DESIGN.md, src/app/globals.css, src/app/trainer/page.tsx, src/components/layout/PublicShell.tsx, src/components/readiness/ReadinessSignal.tsx</read_first>
  <action>
    Per D-08, D-09, D-17, D-18, D-19 and DESIGN.md:

    1. `src/components/cohort/CohortCard.tsx`:
       - Props: `{ cohort: { id: string; name: string; startDate: Date; endDate: Date; description?: string; associateCount: number; readyCount: number; improvingCount: number; notReadyCount: number }; onEdit?: () =&gt; void; onDelete?: () =&gt; void }`
       - Renders: `bg-[var(--surface)] border border-[var(--border)] rounded-xl p-6 hover:border-[var(--accent)] transition-colors`
       - Name in Clash Display 22px, dates in DM Sans 14px tabular-nums `text-[var(--muted)]`, description in DM Sans 16px
       - Horizontal rule `border-t border-[var(--border-subtle)] mt-4 pt-4` separating header from stats
       - Stats row: 4 mini columns (total, ready, improving, not_ready) with Clash Display 28px number + 11px mono uppercase label (`text-[var(--muted)] tracking-[0.08em]`)
       - Edit/Delete buttons (if handlers provided): small ghost buttons, right-aligned, `text-[var(--muted)] hover:text-[var(--accent)]`

    2. `src/components/cohort/CohortForm.tsx`:
       - Client component
       - Props: `{ initial?: CohortInput; onSubmit: (input: CohortInput) =&gt; Promise&lt;void&gt;; onCancel?: () =&gt; void }`
       - Fields: name (text), startDate (date), endDate (date), description (textarea)
       - Input styling from Plan 01 pattern: `bg-[var(--bg)] border border-[var(--border)] rounded-lg px-4 py-3 focus:outline-none focus:border-[var(--accent)]`
       - Labels: DM Sans 13px 500 `text-[var(--muted)]` uppercase tracking-wider above each field
       - Submit: `.btn-primary`; Cancel: ghost variant
       - Client-side validation: name 1-80 chars, endDate &gt; startDate, inline errors `text-[var(--danger)] text-sm`

    3. `src/components/cohort/CohortFilter.tsx`:
       - Client component
       - Props: `{ cohorts: Array&lt;{ id: string; name: string }&gt;; selectedId: string | null; onChange: (id: string | null) =&gt; void }`
       - Native `<select>` styled: `bg-[var(--surface)] border border-[var(--border)] rounded-lg px-4 py-2 text-[var(--ink)] focus:border-[var(--accent)]`
       - Default option: "All Associates" (value = empty)
       - Label "Cohort" in 11px mono uppercase to the left

    4. `src/components/cohort/ReadinessSummaryBar.tsx`:
       - Server-compatible
       - Props: `{ ready: number; improving: number; notReady: number; cohortName: string }`
       - Renders horizontal bar: `bg-[var(--surface-muted)] border border-[var(--border-subtle)] rounded-xl px-6 py-4 flex items-center gap-12`
       - Cohort name in Clash Display 22px on left
       - Three stat blocks: each with Clash Display 48px number (`tabular-nums`), below a 11px mono uppercase label ("READY" `text-[var(--success)]`, "IMPROVING" `text-[var(--accent)]`, "NOT READY" `text-[var(--danger)]`)

    Export each as default + named. Zero ad-hoc hex colors. Zero `glass-card` / `gradient` / `blur` / `glow` / `nlm-` references.
  </action>
  <verify>npx tsc --noEmit &amp;&amp; grep -rE "glass-card|gradient-text|backdrop-blur|nlm-|#[0-9A-Fa-f]{6}" src/components/cohort (expect zero)</verify>
  <acceptance_criteria>
    - All 4 files exist in `src/components/cohort/`
    - Each component uses `var(--bg|surface|ink|accent|muted|border)` tokens
    - Zero matches for `glass-card|gradient|backdrop-blur|nlm-|#[0-9A-Fa-f]{6}` across `src/components/cohort/`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Cohort component library ready for consumption by Phase 11/12 routes.</done>
</task>

<task type="auto">
  <name>Task 2: Build curriculum UI components + wire all trainer routes</name>
  <files>src/components/curriculum/CurriculumWeekList.tsx, src/components/curriculum/CurriculumWeekForm.tsx, src/app/trainer/cohorts/page.tsx, src/app/trainer/cohorts/[id]/page.tsx, src/app/trainer/cohorts/[id]/curriculum/page.tsx, src/app/trainer/page.tsx</files>
  <read_first>DESIGN.md, src/app/trainer/page.tsx, src/components/cohort/CohortCard.tsx, src/components/cohort/CohortFilter.tsx, src/components/cohort/ReadinessSummaryBar.tsx</read_first>
  <action>
    Per D-10 and DESIGN.md:

    1. `src/components/curriculum/CurriculumWeekList.tsx`:
       - Props: `{ weeks: Array&lt;{ id: string; weekNumber: number; skillName: string; topicTags: string[]; startDate: Date }&gt;; onEdit?: (id: string) =&gt; void; onDelete?: (id: string) =&gt; void }`
       - Vertical list, each row: `px-6 py-5 border-b border-[var(--border-subtle)] flex items-center gap-8`
       - Today marker: compute `isToday = startDate <= now && (nextWeek.startDate > now || !nextWeek)`; if true, prepend a 3px left border `border-l-[3px] border-l-[var(--accent)]`
       - Week number: Clash Display 28px `tabular-nums` left column (fixed width)
       - Skill name: Clash Display 22px; below it topic tags rendered as small chips `bg-[var(--surface-muted)] border border-[var(--border-subtle)] rounded-full px-3 py-1 text-[12px] text-[var(--muted)]`
       - Start date: right-aligned DM Sans 14px tabular-nums `text-[var(--muted)]`
       - Status label below date: if past → "taught" `text-[var(--success)]`, if today → "this week" `text-[var(--accent)]`, if future → "upcoming" `text-[var(--muted)]` — all 11px mono uppercase tracking-wider

    2. `src/components/curriculum/CurriculumWeekForm.tsx`:
       - Mirror `CohortForm.tsx` pattern. Fields: weekNumber (number), skillName (text), topicTags (comma-separated → array), startDate (date).

    3. `src/app/trainer/cohorts/page.tsx` (cohort list/management — Phase 11 page):
       - Wrap existing Phase 11 content in trainer layout
       - Render grid of `<CohortCard>` components (`grid grid-cols-1 md:grid-cols-2 gap-6 max-w-[1120px]`)
       - "New cohort" button at top: `.btn-primary` → opens `<CohortForm>` in a modal or inline section
       - Replace any raw cohort card markup that Phase 11 authored with the new `CohortCard` component
       - If Phase 11 has not yet authored this page, create a scaffold that reads cohorts from `/api/cohorts` (Phase 11 endpoint — use actual endpoint or TODO comment)

    4. `src/app/trainer/cohorts/[id]/page.tsx` (cohort detail — Phase 11):
       - Header: cohort name in Clash Display 48px, dates in DM Sans 18px `text-[var(--muted)]`
       - Section: associate list for this cohort (reuse existing roster row pattern from `/trainer/page.tsx`)
       - Link to curriculum: `<Link href="./curriculum">Manage curriculum →</Link>` styled as burnt orange link
       - Edit/Delete cohort buttons calling `CohortForm`

    5. `src/app/trainer/cohorts/[id]/curriculum/page.tsx` (curriculum — Phase 13):
       - Header: "Curriculum — {cohortName}" Clash Display 48px
       - "Add week" button: `.btn-primary` → opens `<CurriculumWeekForm>`
       - Render `<CurriculumWeekList weeks={weeks} />` inside `bg-[var(--surface)] border border-[var(--border)] rounded-xl`
       - If Phase 13 has not yet authored this page, create scaffold reading from `/api/cohorts/[id]/curriculum`

    6. `src/app/trainer/page.tsx` (existing roster — Phase 12 additions):
       - Preserve existing roster table structure (do NOT restyle; D-15)
       - Add above the roster table: a flex row with `<CohortFilter cohorts={...} selectedId={...} onChange={...} />` on the left
       - When `selectedId` is not null, render `<ReadinessSummaryBar>` below the filter, above the table
       - Pass the filtered cohort ID down to the roster fetch so it only shows associates in that cohort; "All Associates" (null) shows full roster unchanged
       - Make the filter state a client component (`"use client"` on a small wrapper) if the page is currently server-rendered

    For all 6 files: zero ad-hoc hex, zero `glass-card|gradient-text|backdrop-blur|nlm-|indigo-|cyan-|purple-500` references.
  </action>
  <verify>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build</verify>
  <acceptance_criteria>
    - `src/app/trainer/cohorts/page.tsx` imports `CohortCard` and `CohortForm`
    - `src/app/trainer/page.tsx` imports `CohortFilter` and `ReadinessSummaryBar`
    - `src/app/trainer/cohorts/[id]/curriculum/page.tsx` imports `CurriculumWeekList`
    - `grep -rE "glass-card|gradient-text|backdrop-blur|nlm-" src/app/trainer src/components/cohort src/components/curriculum` returns zero
    - `grep -rE "#[0-9A-Fa-f]{6}" src/app/trainer/cohorts src/components/cohort src/components/curriculum` returns zero
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Cohort + curriculum routes rendered via token-driven components; roster has cohort filter + summary.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright visual tests + start dev server</name>
  <files>tests/visual/phase-14/trainer-cohort.spec.ts</files>
  <read_first>tests/visual/phase-14/public-flow.spec.ts, DESIGN.md</read_first>
  <action>
    Per D-20:

    1. Create `tests/visual/phase-14/trainer-cohort.spec.ts`:
       - Authenticated session helper: reuse existing Playwright auth fixture if present; else POST to `/api/login` with `APP_PASSWORD` from env and persist cookies
       - Test `cohort list renders DESIGN.md tokens`: navigate to `/trainer/cohorts`, assert body bg `rgb(245, 240, 232)`, assert at least one `CohortCard` renders with Clash Display heading (check computed `font-family` contains "Clash Display")
       - Test `cohort filter appears on roster`: navigate to `/trainer`, assert `select` with "All Associates" option exists, select an option, assert `ReadinessSummaryBar` appears with three numeric stats
       - Test `curriculum list today marker`: navigate to `/trainer/cohorts/[seeded-id]/curriculum`, assert at least one week row has a left border in `rgb(200, 90, 46)` (accent) if test fixture provides a current-week entry — otherwise `test.skip()` with TODO
       - Screenshot each page under `tests/visual/phase-14/screenshots/`
       - Tests skip cleanly if seed data for cohorts/curriculum is unavailable

    2. Start dev server: `npm run dev`, wait for `GET http://localhost:3000/trainer` to return 200 (after auth).
  </action>
  <verify>npx playwright test tests/visual/phase-14/trainer-cohort.spec.ts --reporter=list</verify>
  <acceptance_criteria>
    - `tests/visual/phase-14/trainer-cohort.spec.ts` contains at least 3 test blocks
    - Dev server responds 200 at `/trainer/cohorts` when authenticated
    - Screenshots saved under `tests/visual/phase-14/screenshots/`
  </acceptance_criteria>
  <done>Visual tests committed; dev server running for checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Cohort + curriculum UI polished to DESIGN.md — dev server at http://localhost:3000</what-built>
  <how-to-verify>
    Sign in with `APP_PASSWORD`, then:

    1. http://localhost:3000/trainer/cohorts
       - Warm parchment bg, grid of cohort cards on matte white surfaces with warm borders
       - Cohort name in Clash Display (geometric)
       - Stats row inside each card: big numbers in Clash Display + tiny mono uppercase labels
       - "New cohort" button is flat burnt orange
       - No glass morphism, no indigo/cyan

    2. http://localhost:3000/trainer/cohorts/[pick any id]/curriculum
       - Week list with typographic hierarchy: big week number, skill name in Clash Display, topic tags as small warm chips
       - Today marker (if any week is current): 3px burnt orange left border
       - "Taught / this week / upcoming" labels in tiny mono uppercase

    3. http://localhost:3000/trainer
       - Cohort filter dropdown appears above the existing roster (roster itself unchanged)
       - Select a cohort → ReadinessSummaryBar appears below filter with three big numbers (ready / improving / not ready) in Clash Display
       - Select "All Associates" → summary bar disappears, full roster returns
       - Existing roster table styling is unchanged from v1.0

    4. REGRESSION CHECK: http://localhost:3000/trainer/[any-associate-slug]
       - Associate detail view loads and functions as before
       - Gap trend chart renders

    Confirm all four before approval.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run lint` passes
- [ ] `npm run build` succeeds
- [ ] `grep -rE "glass-card|gradient-text|backdrop-blur|nlm-" src/components/cohort src/components/curriculum src/app/trainer/cohorts` returns zero
- [ ] `grep -rE "#[0-9A-Fa-f]{6}" src/components/cohort src/components/curriculum src/app/trainer/cohorts` returns zero
- [ ] Existing `/trainer` roster and `/trainer/[slug]` detail pages remain functional (human verified)
- [ ] Playwright tests pass or skip cleanly
</verification>

<success_criteria>
- All 4 tasks completed
- DESIGN-02 requirement satisfied (cohort + curriculum UI on DESIGN.md tokens from initial build)
- No visual debt between v1.1 features and v1.0 trainer dashboard
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/14-design-cohesion/14-02-SUMMARY.md`
</output>
