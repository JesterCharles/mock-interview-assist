---
phase: 08-schema-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/0001_v11_cohorts/migration.sql
  - prisma/migrations/migration_lock.toml
autonomous: true
requirements: ["COHORT-02"]

must_haves:
  truths:
    - "prisma/schema.prisma defines Cohort and CurriculumWeek models per ARCHITECTURE.md"
    - "Associate has nullable cohortId FK referencing Cohort"
    - "Session has nullable cohortId FK and a mode column defaulting to 'trainer-led'"
    - "Session has a readinessRecomputeStatus column defaulting to 'not_applicable' (Codex finding #5 — supports Phase 10 readiness repair sweep)"
    - "CurriculumWeek has a skillSlug column (canonical path-based skill identifier, Codex finding #9)"
    - "CurriculumWeek has a UNIQUE constraint on (cohortId, weekNumber) — added NOW, not deferred (Codex finding #9)"
    - "A migration SQL file exists that creates the new tables, backfills Session.mode, and backfills Session.readinessRecomputeStatus"
    - "Prisma client regenerates with the new models exported"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Cohort, CurriculumWeek model definitions + Associate.cohortId, Session.cohortId, Session.mode, Session.readinessRecomputeStatus, CurriculumWeek.skillSlug, UNIQUE(cohortId, weekNumber)"
      contains: "model Cohort"
    - path: "prisma/migrations/0001_v11_cohorts/migration.sql"
      provides: "Atomic, additive migration creating Cohort, CurriculumWeek (with skillSlug + unique), FK columns, and backfilling Session.mode + Session.readinessRecomputeStatus"
      contains: "CREATE TABLE \"Cohort\""
    - path: "prisma/migrations/migration_lock.toml"
      provides: "Prisma migration provider lock"
      contains: "provider = \"postgresql\""
  key_links:
    - from: "prisma/schema.prisma Associate.cohortId"
      to: "Cohort.id"
      via: "@relation(fields: [cohortId], references: [id], onDelete: SetNull)"
      pattern: "cohortId\\s+Int\\?"
    - from: "prisma/schema.prisma Session.cohortId"
      to: "Cohort.id"
      via: "@relation(fields: [cohortId], references: [id], onDelete: SetNull)"
      pattern: "cohortId\\s+Int\\?"
    - from: "prisma/schema.prisma CurriculumWeek"
      to: "Cohort.id"
      via: "@@unique([cohortId, weekNumber]) enforced at DB level"
      pattern: "@@unique.*cohortId.*weekNumber"
    - from: "prisma/migrations/0001_v11_cohorts/migration.sql"
      to: "Session.mode + readinessRecomputeStatus backfill"
      via: "ALTER TABLE + UPDATE backfill"
      pattern: "UPDATE \"Session\" SET"
---

<objective>
Add Cohort and CurriculumWeek tables plus nullable cohortId FKs on Associate and Session, Session.mode (default "trainer-led"), Session.readinessRecomputeStatus (default "not_applicable"), and CurriculumWeek.skillSlug + UNIQUE(cohortId, weekNumber). Single atomic migration. Additive only — zero impact to existing data.

Patched per Codex findings #5 (readinessRecomputeStatus column added) and #9 (skillSlug + unique constraint added NOW, not deferred).

Purpose: Lay the schema foundation that Phases 9-13 build on AND the repair-path column that Phase 10 Plan 10-03 consumes. Per CONTEXT decisions, all changes are additive and FKs are nullable for v1.0 backward compat.

Output: Updated prisma/schema.prisma, a single migration SQL file, regenerated Prisma client.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-schema-migration/08-CONTEXT.md
@.planning/phases/10-automated-interview-pipeline/10-CONTEXT.md
@.planning/phases/13-curriculum-schedule/13-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@prisma/schema.prisma
@CLAUDE.md

<interfaces>
Current prisma/schema.prisma exports: HealthCheck, Associate, Session, GapScore, Settings.

Pattern in use (from existing schema):
- Int autoincrement IDs: `id Int @id @default(autoincrement())`
- Nullable FK: `associateId Int?` + `associate Associate? @relation(fields: [associateId], references: [id])`
- Timestamps: `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`
- Cascade pattern: `@relation(..., onDelete: Cascade)` (see GapScore)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend prisma/schema.prisma with Cohort, CurriculumWeek (skillSlug+unique), and FK/column additions</name>
  <files>prisma/schema.prisma</files>
  <action>
Append two new models and modify Associate + Session per CONTEXT.md decisions plus the Codex-review patches.

1. Add `model Cohort`:
   - `id Int @id @default(autoincrement())`
   - `name String`
   - `startDate DateTime`
   - `endDate DateTime?`
   - `description String?`
   - `createdAt DateTime @default(now())`
   - `updatedAt DateTime @updatedAt`
   - `associates Associate[]`
   - `sessions Session[]`
   - `curriculumWeeks CurriculumWeek[]`

2. Add `model CurriculumWeek` (per D-07, D-08 + Codex finding #9):
   - `id Int @id @default(autoincrement())`
   - `cohortId Int`
   - `weekNumber Int`
   - `skillName String`           // display text only (per Codex #9)
   - `skillSlug String`           // NEW — canonical matcher value (e.g. "react", "node"). Populated by trainer UI in Phase 13.
   - `topicTags String[]` (Postgres native array)
   - `startDate DateTime`
   - `cohort Cohort @relation(fields: [cohortId], references: [id], onDelete: Cascade)`
   - `@@index([cohortId])`
   - `@@unique([cohortId, weekNumber])`   // NEW per Codex finding #9 — no deferred unique; prevents ambiguous duplicates at DB level.

3. Modify `model Associate` (per D-02):
   - Add `cohortId Int?`
   - Add `cohort Cohort? @relation(fields: [cohortId], references: [id], onDelete: SetNull)`
   - Add `@@index([cohortId])`.

4. Modify `model Session` (per D-10, D-12 + Codex finding #5):
   - Add `cohortId Int?`
   - Add `cohort Cohort? @relation(fields: [cohortId], references: [id], onDelete: SetNull)`
   - Add `mode String @default("trainer-led")` (valid values enforced in TS layer)
   - Add `readinessRecomputeStatus String @default("not_applicable")`   // NEW per Codex finding #5. Values: not_applicable | pending | done | failed. Enforced in TS layer.
   - Add `@@index([cohortId])`
   - Add `@@index([readinessRecomputeStatus])` — the sweep query in Plan 10-03 filters by this column; index avoids full table scans as session volume grows.

DO NOT use Prisma enums (D-11). DO NOT add `onDelete: Cascade` on Associate.cohort or Session.cohort — use `onDelete: SetNull`.
  </action>
  <verify>
    <automated>npx prisma format && npx prisma validate</automated>
  </verify>
  <done>
`prisma validate` exits 0. Schema contains `model Cohort`, `model CurriculumWeek` (with `skillSlug` and `@@unique([cohortId, weekNumber])`), `cohortId Int?` on Associate, `cohortId Int?` + `mode String @default("trainer-led")` + `readinessRecomputeStatus String @default("not_applicable")` on Session, plus `@@index([readinessRecomputeStatus])`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate migration SQL with backfills and regen Prisma client</name>
  <files>prisma/migrations/0001_v11_cohorts/migration.sql, prisma/migrations/migration_lock.toml, src/generated/prisma/</files>
  <action>
Create the migration via Prisma's migrate command, then verify the SQL includes all new columns, constraints, and backfills.

1. Run `npx prisma migrate dev --name v11_cohorts --create-only`.

2. Open the generated `migration.sql` and verify it contains:
   - `CREATE TABLE "Cohort"` with all fields
   - `CREATE TABLE "CurriculumWeek"` with `skillSlug TEXT NOT NULL` and `topicTags TEXT[]`
   - `CREATE UNIQUE INDEX ... ON "CurriculumWeek"("cohortId", "weekNumber")` OR an equivalent unique constraint
   - `ALTER TABLE "Associate" ADD COLUMN "cohortId" INTEGER` (nullable)
   - `ALTER TABLE "Session" ADD COLUMN "cohortId" INTEGER` (nullable)
   - `ALTER TABLE "Session" ADD COLUMN "mode" TEXT NOT NULL DEFAULT 'trainer-led'`
   - `ALTER TABLE "Session" ADD COLUMN "readinessRecomputeStatus" TEXT NOT NULL DEFAULT 'not_applicable'`
   - FK constraints: `Associate_cohortId_fkey` (`SET NULL`), `Session_cohortId_fkey` (`SET NULL`), `CurriculumWeek_cohortId_fkey` (`CASCADE`)
   - Index statements for `cohortId` columns AND for `Session(readinessRecomputeStatus)`

3. **Append explicit backfill statements** at the end of the migration SQL file (belt-and-suspenders against any Prisma oddities):
   ```sql
   UPDATE "Session" SET "mode" = 'trainer-led' WHERE "mode" IS NULL;
   UPDATE "Session" SET "readinessRecomputeStatus" = 'not_applicable' WHERE "readinessRecomputeStatus" IS NULL;
   ```

4. Verify `prisma/migrations/migration_lock.toml` exists with `provider = "postgresql"`.

5. Regenerate the Prisma client: `npx prisma generate`. Confirm `src/generated/prisma/index.d.ts` exports `Cohort`, `CurriculumWeek`, and that `CurriculumWeek` includes `skillSlug` and `Session` includes `readinessRecomputeStatus`.

DO NOT run `prisma migrate deploy` in this plan — that runs against the live DB and is verified in plan 02.

If `prisma migrate dev --create-only` requires DB access and DIRECT_URL is unavailable, write the SQL by hand following the structure above and run `npx prisma generate` directly. Note this in the SUMMARY.
  </action>
  <verify>
    <automated>test -f prisma/migrations/0001_v11_cohorts/migration.sql && grep -q "CREATE TABLE \"Cohort\"" prisma/migrations/0001_v11_cohorts/migration.sql && grep -q "CREATE TABLE \"CurriculumWeek\"" prisma/migrations/0001_v11_cohorts/migration.sql && grep -q "skillSlug" prisma/migrations/0001_v11_cohorts/migration.sql && grep -q "cohortId.*weekNumber\\|weekNumber.*cohortId" prisma/migrations/0001_v11_cohorts/migration.sql && grep -q "readinessRecomputeStatus" prisma/migrations/0001_v11_cohorts/migration.sql && grep -q "Cohort" src/generated/prisma/index.d.ts</automated>
  </verify>
  <done>
Migration SQL file exists with all schema changes including `skillSlug`, the unique `(cohortId, weekNumber)` constraint, and `readinessRecomputeStatus`. Prisma client regenerated. No existing tables had columns dropped or modified destructively.
  </done>
</task>

</tasks>

<verification>
- `npx prisma validate` exits 0
- Migration SQL is additive only — verify no `DROP COLUMN`, `DROP TABLE`, or `ALTER COLUMN ... DROP DEFAULT` against existing tables
- `src/generated/prisma/` exports new model types with `skillSlug` and `readinessRecomputeStatus`
- `git diff prisma/schema.prisma` shows only additions
</verification>

<success_criteria>
1. prisma/schema.prisma contains Cohort, CurriculumWeek models with `skillSlug` + `@@unique([cohortId, weekNumber])`
2. Associate.cohortId and Session.cohortId are nullable Int? FKs with SetNull on delete
3. Session.mode is `String @default("trainer-led")` with backfill
4. Session.readinessRecomputeStatus is `String @default("not_applicable")` with backfill (Codex #5)
5. CurriculumWeek has `skillSlug` (Codex #9) and enforces `@@unique([cohortId, weekNumber])` (Codex #9)
6. Single migration file contains all DDL atomically
7. No destructive changes to existing tables
</success_criteria>

<output>
After completion, create `.planning/phases/08-schema-migration/08-01-SUMMARY.md` documenting:
- Final schema diff (added models + fields + unique constraint)
- Migration SQL highlights (backfill clauses)
- Whether `--create-only` succeeded or hand-written SQL was used
- Confirmation that Prisma client regenerated successfully
</output>
