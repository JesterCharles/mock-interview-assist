---
phase: 15-design-cohesion-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Navbar.tsx
  - src/components/QuestionCard.tsx
  - src/components/SpeechToText.tsx
  - src/components/ProgressBar.tsx
autonomous: true
requirements: [DESIGN-03]
must_haves:
  truths:
    - "Navbar, QuestionCard, SpeechToText, ProgressBar render identically (behavior) but source all tokens from DESIGN.md vars"
    - "Zero references to legacy utilities (.nlm-bg, .glass-card, .gradient-text, .glow-border, .progress-gradient, .recording-ring, .btn-primary, .btn-accent) or --nlm-* tokens remain in these four files"
    - "No kill-list animations (gradient-shift, float, pulse-glow, shimmer, recording-pulse, progress-glow, border-glow-pulse) referenced from these four files"
    - "All existing Vitest unit/integration tests that import these components still pass"
  artifacts:
    - path: "src/components/Navbar.tsx"
      provides: "Token-driven nav chrome (bg var(--surface), border var(--border), ink var(--ink), accent var(--accent))"
    - path: "src/components/QuestionCard.tsx"
      provides: "Surface card using var(--surface) + border var(--border), Clash Display heading in var(--ink), flat accent CTA via .btn-accent-flat"
    - path: "src/components/SpeechToText.tsx"
      provides: "Static recording indicator (flat dot + text-[var(--accent)] 'Recording' label), no recording-pulse animation"
    - path: "src/components/ProgressBar.tsx"
      provides: "Flat bg-[var(--accent)] fill on bg-[var(--surface-muted)] track, no progress-gradient / progress-glow"
  key_links:
    - from: "src/components/QuestionCard.tsx"
      to: "globals.css DESIGN tokens"
      via: "var(--surface), var(--border), var(--ink), var(--accent)"
      pattern: "var\\(--(surface|border|ink|accent)\\)"
    - from: "src/components/SpeechToText.tsx"
      to: "DESIGN.md motion rule (no decorative motion)"
      via: "static indicator"
      pattern: "(?!recording-pulse)"
---

<objective>
Migrate the four shared components — Navbar, QuestionCard, SpeechToText, ProgressBar — onto DESIGN.md tokens. These components are consumed by every legacy surface (/, /interview, /review, /dashboard, /pdf, /history, /question-banks) so they MUST migrate before their consumer pages are touched in Wave 2.

Purpose: Break the legacy-utility dependency at the leaf level. Once these four files are token-only, every consumer page becomes eligible for Wave 2 migration without risking shared-component regressions.

Output: Four restyled component files, each with zero legacy-utility references, all existing tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@DESIGN.md
@.planning/phases/15-design-cohesion-sweep/15-CONTEXT.md
@.planning/phases/14-design-cohesion/14-01-SUMMARY.md
@src/app/globals.css
@src/components/Navbar.tsx
@src/components/QuestionCard.tsx
@src/components/SpeechToText.tsx
@src/components/ProgressBar.tsx

<interfaces>
<!-- DESIGN.md tokens (already in globals.css from Phase 14) — use these directly. -->
<!-- Light mode. Dark mode vars exist under [data-theme="dark"] but are not wired. -->

CSS custom properties available on :root:
  --bg            #F5F0E8   page background (warm parchment)
  --surface       #FFFFFF   cards, panels
  --surface-muted #F0EBE2   subtle bg, sidebar, KPI
  --ink           #1A1A1A   primary text
  --muted         #7A7267   secondary text
  --accent        #C85A2E   primary action, CTAs, accent
  --accent-hover  #B04E27
  --success       #2D6A4F
  --warning       #B7791F
  --danger        #B83B2E
  --border        #DDD5C8
  --border-subtle #E8E2D9
  --highlight     #FFF8F0   hover rows, active nav bg

Utility classes (new, from Phase 14):
  .btn-accent-flat      primary CTA — flat burnt orange, white text
  .btn-secondary-flat   secondary CTA — outlined surface

Theme vars (via @theme inline):
  --font-display        Clash Display
  --font-sans-new       DM Sans
  --font-jetbrains-mono JetBrains Mono

Legacy (DO NOT USE in migrated files — being deleted in Plan 04):
  --nlm-bg-primary, --nlm-primary, --nlm-accent, --nlm-teal, --nlm-purple,
  --nlm-success/warning/danger, .nlm-bg, .glass-card, .glass-card-strong,
  .gradient-text, .gradient-text-static, .glow-border, .glow-border-cyan,
  .progress-gradient, .recording-ring, .btn-primary, .btn-accent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline grep + migrate Navbar.tsx and ProgressBar.tsx</name>
  <files>src/components/Navbar.tsx, src/components/ProgressBar.tsx</files>
  <action>
  Step 1 (baseline): Run and capture the pre-migration grep counts — include in commit body:
    grep -cE "nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|btn-primary|btn-accent[^-]" src/components/Navbar.tsx src/components/ProgressBar.tsx

  Step 2 (Navbar.tsx — 1 legacy hit):
    - Replace any legacy class / --nlm-* token reference with DESIGN.md equivalents per D-01 mapping.
    - Nav chrome: bg var(--surface), bottom border border-[var(--border)], ink text var(--ink), hover bg var(--highlight).
    - Active nav item: bg var(--highlight) + text var(--accent). Inactive: text var(--muted).
    - Any .btn-primary / .btn-accent → .btn-accent-flat.
    - NO gradient-text on the wordmark — plain Clash Display in var(--ink).

  Step 3 (ProgressBar.tsx — 2 legacy hits):
    - Track: bg-[var(--surface-muted)] rounded-full.
    - Fill: flat bg-[var(--accent)] with width: `${percent}%`. NO progress-gradient, NO progress-glow animation, NO shimmer.
    - Preserve all existing props, aria attrs, percent calc logic. Swap classes only.

  Step 4 (verify):
    - npx tsc --noEmit
    - npm run lint (scoped: Navbar.tsx + ProgressBar.tsx must have no NEW errors vs. baseline)
    - npm run test -- --run
    - Scoped grep gate (MUST return 0):
        grep -cE "nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\\bbtn-primary\\b|\\bbtn-accent\\b" src/components/Navbar.tsx src/components/ProgressBar.tsx

  Step 5 (atomic commit): `refactor(15-01): migrate Navbar + ProgressBar to DESIGN tokens`
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run test -- --run &amp;&amp; test "$(grep -cE 'nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\bbtn-primary\b|\bbtn-accent\b' src/components/Navbar.tsx src/components/ProgressBar.tsx | awk -F: '{s+=$2} END {print s}')" = "0"</automated>
  </verify>
  <done>Navbar.tsx and ProgressBar.tsx contain zero legacy utility/token references; typecheck + vitest green; atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate QuestionCard.tsx and SpeechToText.tsx (kill recording-pulse)</name>
  <files>src/components/QuestionCard.tsx, src/components/SpeechToText.tsx</files>
  <action>
  Step 1 (baseline grep):
    grep -cE "nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|btn-primary|btn-accent[^-]|recording-pulse|pulse-glow|shimmer|border-glow-pulse" src/components/QuestionCard.tsx src/components/SpeechToText.tsx

  Step 2 (QuestionCard.tsx — 4 legacy hits):
    - Outer card: replace .glass-card / .glass-card-strong with:
        bg-[var(--surface)] border border-[var(--border)] rounded-xl shadow-[0_1px_2px_rgba(0,0,0,0.04)] p-6
    - Heading: Clash Display (className uses `var(--font-display)` via the --font-display theme token, or `fontFamily: 'var(--font-display)'` inline style), 22px, color var(--ink). NO gradient-text.
    - Body text: DM Sans, color var(--ink) primary / var(--muted) secondary.
    - CTA buttons: any .btn-primary / .btn-accent → .btn-accent-flat (primary) or .btn-secondary-flat (secondary).
    - Keyword chips: bg-[var(--surface-muted)] text-[var(--muted)] rounded-full.
    - Preserve ALL component props, event handlers, conditional rendering.

  Step 3 (SpeechToText.tsx — 6 legacy hits, critical: recording-pulse / recording-ring animation MUST go):
    - Replace .recording-ring and any `animation: recording-pulse ...` with a STATIC indicator per D-02:
        <span style={{ display: 'inline-flex', alignItems: 'center', gap: 8 }}>
          <span style={{ width: 8, height: 8, borderRadius: 9999, background: 'var(--accent)' }} />
          <span style={{ fontSize: 11, fontFamily: 'var(--font-jetbrains-mono)', textTransform: 'uppercase', letterSpacing: '0.08em', color: 'var(--accent)' }}>Recording</span>
        </span>
    - Transcript container: bg-[var(--surface-muted)] border border-[var(--border-subtle)] rounded-lg p-4, text var(--ink).
    - Start/Stop buttons: .btn-accent-flat / .btn-secondary-flat.
    - Remove ANY inline animation / keyframe reference tied to kill-list keyframes. Preserve Web Speech API logic, state, refs.

  Step 4 (verify):
    - npx tsc --noEmit
    - npm run lint (no new errors on these two files)
    - npm run test -- --run
    - Scoped grep gate (MUST return 0):
        grep -cE "nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\\bbtn-primary\\b|\\bbtn-accent\\b|recording-pulse|pulse-glow|shimmer|border-glow-pulse" src/components/QuestionCard.tsx src/components/SpeechToText.tsx

  Step 5 (atomic commit): `refactor(15-01): migrate QuestionCard + SpeechToText to DESIGN tokens (kill recording-pulse)`
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run test -- --run &amp;&amp; test "$(grep -cE 'nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\bbtn-primary\b|\bbtn-accent\b|recording-pulse|pulse-glow|shimmer|border-glow-pulse' src/components/QuestionCard.tsx src/components/SpeechToText.tsx | awk -F: '{s+=$2} END {print s}')" = "0"</automated>
  </verify>
  <done>QuestionCard.tsx + SpeechToText.tsx on DESIGN tokens, zero kill-list animations, zero legacy classes; tests green; atomic commit landed.</done>
</task>

</tasks>

<verification>
Plan-level gate after both tasks:

```
grep -rE "nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\bbtn-primary\b|\bbtn-accent\b|recording-pulse|pulse-glow|shimmer|border-glow-pulse" \
  src/components/Navbar.tsx src/components/QuestionCard.tsx src/components/SpeechToText.tsx src/components/ProgressBar.tsx
# MUST return nothing.
```

Full suite: `npx tsc --noEmit && npm run lint && npm run test -- --run` all green.
</verification>

<success_criteria>
- All four shared components migrated.
- Zero legacy utility / --nlm-* / kill-list animation references in the four files.
- Typecheck + vitest + lint (no new errors) green.
- Two atomic commits landed (one per task).
- Wave 2 plans (15-02, 15-03) now safe to start — their consumer pages import already-migrated components.
</success_criteria>

<output>
After completion, create `.planning/phases/15-design-cohesion-sweep/15-01-SUMMARY.md` per the summary template. Include: baseline vs. post grep counts, commit SHAs, any deviations.
</output>
