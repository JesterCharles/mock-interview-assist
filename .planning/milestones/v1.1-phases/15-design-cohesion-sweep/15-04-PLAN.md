---
phase: 15-design-cohesion-sweep
plan: 04
type: execute
wave: 3
depends_on: [15-01, 15-02, 15-03]
files_modified:
  - src/app/globals.css
  - tests/visual/phase-15/legacy-deletion.spec.ts
  - tests/visual/phase-15/route-regression.spec.ts
autonomous: false
requirements: [DESIGN-03]
must_haves:
  truths:
    - "Zero references to --nlm-* tokens, legacy utilities (.nlm-bg, .glass-card, .glass-card-strong, .gradient-text, .gradient-text-static, .glow-border, .glow-border-cyan, .progress-gradient, .recording-ring, .btn-primary, .btn-accent), and kill-list keyframes (gradient-shift, float, pulse-glow, shimmer, recording-pulse, progress-glow, border-glow-pulse) anywhere in src/ — including globals.css itself"
    - "All routes (/, /login, /dashboard, /interview, /review, /history, /pdf, /question-banks, /associate/login, /associate/[slug], /associate/[slug]/interview, /trainer, /trainer/[slug], /trainer/cohorts, /trainer/cohorts/[id], /trainer/cohorts/[id]/curriculum) compile and render on warm parchment bg"
    - "Playwright visual regression suite captures each route post-deletion; all routes return 2xx / expected auth redirect"
    - "Full test suite green — typecheck, lint (no new errors), vitest, build"
    - "Trainer-led full-interview smoke (setup → interview → review → PDF) re-verified post-deletion"
    - "A11y contract: .btn-accent-flat has visible :focus-visible outline (2px var(--accent)) and min 44x44px hit area; burnt orange #C85A2E only used for buttons, icons, large text (>=18px or >=14px bold) — never for body copy on parchment bg (fails WCAG AA 4.5:1)"
    - "Interaction states preserved per page: /review score-pending + score-error; /dashboard wizard step-transition + repo-fetch-error; /associate/login submitting + rate-limited + invalid-pin; /interview STT recording/idle/transcribing; PublicShell loading skeleton where applicable"
    - "Responsive behavior preserved — no breakpoint changes, no restructured layouts at <768px"
  artifacts:
    - path: "src/app/globals.css"
      provides: "DESIGN.md tokens ONLY — no --nlm-*, no legacy utilities, no kill-list keyframes"
    - path: "tests/visual/phase-15/legacy-deletion.spec.ts"
      provides: "Playwright advisory check asserting .nlm-bg / .glass-card / .gradient-text selectors resolve to zero elements on every migrated route"
    - path: "tests/visual/phase-15/route-regression.spec.ts"
      provides: "Playwright full-page screenshot of each route (advisory diff vs. pre-phase-15 baseline capture)"
  key_links:
    - from: "src/app/globals.css"
      to: "DESIGN.md §Color, §Typography, §Motion"
      via: ":root tokens + @theme inline mappings"
      pattern: "--bg|--surface|--ink|--accent"
---

<objective>
FINAL COMMIT OF PHASE. Execute the deletion gate (D-12 / D-13): confirm zero legacy references outside globals.css, add Playwright visual coverage for every route, then delete --nlm-* tokens + legacy utilities + kill-list keyframes from globals.css in ONE atomic commit. No partial deletion (D-13).

Purpose: End state = single unified design system. Phase 15's reason to exist.

Output: globals.css stripped to DESIGN.md tokens only, Playwright regression spec, passed full-suite verification, re-verified trainer smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@DESIGN.md
@.planning/phases/15-design-cohesion-sweep/15-CONTEXT.md
@.planning/phases/15-design-cohesion-sweep/15-01-SUMMARY.md
@.planning/phases/15-design-cohesion-sweep/15-02-SUMMARY.md
@.planning/phases/15-design-cohesion-sweep/15-03-SUMMARY.md
@src/app/globals.css

<interfaces>
<!-- Playwright config + screenshots infra from Phase 14 already exists: -->
<!--   tests/visual/phase-14/playwright.config.ts -->
<!--   tests/visual/phase-14/screenshots/*.png -->
<!-- Pattern: copy the config pattern for phase-15. Vitest already excludes tests/visual/**. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deletion grep gate + Playwright route regression spec</name>
  <files>tests/visual/phase-15/legacy-deletion.spec.ts, tests/visual/phase-15/route-regression.spec.ts, tests/visual/phase-15/playwright.config.ts</files>
  <action>
  Step 1 (HARD GATE — must pass before anything else in this task):
    Run the D-12 grep sweep across src/ excluding globals.css. Output MUST be empty:

    grep -rE "nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\\bbtn-primary\\b|\\bbtn-accent\\b|animation:.*(gradient-shift|\\bfloat\\b|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse)" \
      src/ \
      --exclude=globals.css

    If ANY match remains — STOP, route it back: plans 15-01/02/03 left work undone. Do NOT proceed to deletion. Report the hit list as a planning-feedback item.

  Step 2 (Playwright config): Copy tests/visual/phase-14/playwright.config.ts → tests/visual/phase-15/playwright.config.ts. Adjust testDir to "./" (phase-15 folder). Keep baseURL = http://localhost:3000.

  Step 3 (route-regression.spec.ts): For each route, navigate and capture a full-page screenshot. Advisory only (no pixel-diff gate). Include routes: /, /login, /associate/login, /dashboard (skip if not auth'd — best-effort), /interview (skip if not auth'd), /review (skip if not auth'd), /history, /pdf, /question-banks, /trainer (skip if not auth'd), /trainer/cohorts (skip if not auth'd).

    Pattern:
      test('route: /', async ({ page }) => {
        const res = await page.goto('/');
        expect(res?.status()).toBeLessThan(400);
        await page.screenshot({ path: 'tests/visual/phase-15/screenshots/route-root.png', fullPage: true });
      });

  Step 4 (legacy-deletion.spec.ts): For each route, assert zero elements match kill-list selectors after deletion (i.e., these selectors must return 0 elements because the classes no longer exist):

    const DEAD_SELECTORS = ['.nlm-bg', '.glass-card', '.glass-card-strong', '.gradient-text', '.gradient-text-static', '.glow-border', '.glow-border-cyan', '.progress-gradient', '.recording-ring'];

    test('/ has zero legacy class elements', async ({ page }) => {
      await page.goto('/');
      for (const sel of DEAD_SELECTORS) {
        expect(await page.locator(sel).count()).toBe(0);
      }
    });

    Add similar tests for /login, /associate/login, /history, /pdf, /question-banks (public-reachable routes). Auth-gated routes can be skipped best-effort.

  Step 5 (verify specs compile — do NOT run them yet, the deletion hasn't happened):
    - npx tsc --noEmit
    - Confirm tests/visual/** still excluded from vitest (already configured in Phase 14-02).

  Step 6 (atomic commit): `test(15-04): add Playwright regression + legacy-deletion specs`
  </action>
  <verify>
    <automated>test -z "$(grep -rE 'nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\bbtn-primary\b|\bbtn-accent\b|animation:.*(gradient-shift|\bfloat\b|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse)' src/ --exclude=globals.css)" &amp;&amp; npx tsc --noEmit &amp;&amp; test -f tests/visual/phase-15/playwright.config.ts &amp;&amp; test -f tests/visual/phase-15/route-regression.spec.ts &amp;&amp; test -f tests/visual/phase-15/legacy-deletion.spec.ts</automated>
  </verify>
  <done>D-12 grep gate passed (zero legacy references outside globals.css). Playwright specs created + compile. Atomic commit landed. Ready for Task 2 (final deletion).</done>
</task>

<task type="auto">
  <name>Task 2: Delete --nlm-* tokens, legacy utilities, kill-list keyframes from globals.css</name>
  <files>src/app/globals.css</files>
  <action>
  This is the D-13 FINAL commit. All deletion in one atomic change.

  Step 1 (baseline capture — for commit body):
    grep -cE "--nlm-|\\.nlm-bg|\\.glass-card|\\.gradient-text|\\.glow-border|\\.progress-gradient|\\.recording-ring|\\.btn-primary\\b|\\.btn-accent\\b|@keyframes (gradient-shift|float|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse)" src/app/globals.css

  Step 2 (delete from globals.css):
    Remove the following blocks/definitions. Leave DESIGN.md tokens + .btn-accent-flat + .btn-secondary-flat + @theme inline DESIGN mappings + slide-up/fade-in keyframes intact.

    DELETE:
      - All `--nlm-*` custom properties on :root (bg-primary, primary, accent, teal, purple, success, warning, danger, and any others in the --nlm- namespace).
      - `.nlm-bg` utility class.
      - `.glass-card`, `.glass-card-strong` utility classes.
      - `.gradient-text`, `.gradient-text-static` utility classes.
      - `.glow-border`, `.glow-border-cyan` utility classes.
      - `.progress-gradient` utility class.
      - `.recording-ring` utility class.
      - `.btn-primary` utility class (legacy — the new `.btn-accent-flat` is the replacement).
      - `.btn-accent` utility class (legacy — the new `.btn-accent-flat` is the replacement). Be careful: `.btn-accent-flat` MUST survive. Grep with word boundaries.
      - @keyframes gradient-shift, @keyframes float, @keyframes pulse-glow, @keyframes shimmer, @keyframes recording-pulse, @keyframes progress-glow, @keyframes border-glow-pulse.
      - Any `--background` / `--foreground` custom properties if (and only if) they are referenced exclusively by the deleted utilities. If Tailwind base or any surviving utility references them, leave them.
      - @theme inline entries that map --nlm-* tokens if present.

    PRESERVE:
      - All DESIGN.md tokens on :root (--bg, --surface, --surface-muted, --ink, --muted, --accent, --accent-hover, --success, --warning, --danger, --border, --border-subtle, --highlight).
      - The [data-theme="dark"] block (dark-mode tokens defined but not wired — leave in place; deletion of dark mode scaffolding is out of scope per D-17/D-18).
      - @theme inline mappings for DESIGN tokens + --font-display / --font-sans-new / --font-jetbrains-mono.
      - .btn-accent-flat, .btn-secondary-flat.
      - Any Tailwind @tailwind directives / base styles.
      - body { background: var(--bg); color: var(--ink); ... } global rule (ensure body bg is var(--bg) not --nlm-bg-primary — fix if stale).

  Step 3 (verify globals.css):
    grep -cE "--nlm-|\\.nlm-bg|\\.glass-card|\\.gradient-text|\\.glow-border|\\.progress-gradient|\\.recording-ring|\\.btn-primary\\b|\\.btn-accent\\b|@keyframes (gradient-shift|float|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse)" src/app/globals.css
    MUST be 0.

    Also confirm survivors present:
    grep -c "btn-accent-flat" src/app/globals.css   # >= 1
    grep -c "btn-secondary-flat" src/app/globals.css # >= 1
    grep -c -- "--bg" src/app/globals.css            # >= 1
    grep -c -- "--accent" src/app/globals.css        # >= 1

  Step 4 (full regression):
    - npx tsc --noEmit
    - npm run lint (no new errors)
    - npm run test -- --run  (expect all 299+ vitest green)
    - npm run build  (must succeed — catches CSS compile + Next standalone bundle issues)
    - Start dev server: npm run dev (background) — wait for ready. Run Playwright spec against it:
        npx playwright test --config=tests/visual/phase-15/playwright.config.ts
      Advisory; capture failures in commit body. legacy-deletion.spec.ts assertions MUST pass (zero kill-list elements on tested routes). Screenshot capture is advisory.

  Step 5 (atomic commit — FINAL COMMIT OF PHASE):
    `refactor(15-04)!: delete --nlm-* tokens + legacy utilities + kill-list keyframes`
    Commit body: baseline count, post count (0), list of deleted items, Playwright advisory result, link to Plan 02 manual-smoke approval.
  </action>
  <verify>
    <automated>test "$(grep -cE -- '--nlm-|\.nlm-bg|\.glass-card|\.gradient-text|\.glow-border|\.progress-gradient|\.recording-ring|\.btn-primary\b|\.btn-accent\b|@keyframes (gradient-shift|float|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse)' src/app/globals.css)" = "0" &amp;&amp; test "$(grep -c 'btn-accent-flat' src/app/globals.css)" != "0" &amp;&amp; npx tsc --noEmit &amp;&amp; npm run test -- --run &amp;&amp; npm run build</automated>
  </verify>
  <done>globals.css stripped to DESIGN tokens only. Survivors verified. Full regression green. Playwright legacy-deletion spec passes. Final atomic commit landed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Post-deletion trainer-led smoke + visual sign-off</name>
  <what-built>
  All legacy tokens, utilities, and motion keyframes have been deleted from globals.css. Single unified DESIGN.md system. This is the final check before phase close.
  </what-built>
  <how-to-verify>
  1. `npm run dev` — open http://localhost:3000.
  2. Repeat the full trainer-led smoke from Plan 02 Task 3 — setup wizard → interview → review → PDF → email UI. Everything must still work.
  3. Anonymous /: confirm automated-interview flow still runs end-to-end.
  4. Visit every route, confirm:
     - /login, /associate/login, /, /dashboard, /interview, /review, /history, /pdf, /question-banks, /trainer, /trainer/[slug], /trainer/cohorts, /trainer/cohorts/[id]/curriculum, /associate/[slug], /associate/[slug]/interview
     - All warm parchment bg (#F5F0E8), Clash Display headings, burnt orange flat accent, NO glass / gradient / glow / decorative pulse anywhere.
  5. Browser devtools > Elements: confirm body computed background-color = rgb(245, 240, 232). No .glass-card / .nlm-bg classes on any element across any route.
  6. Review Playwright screenshots in tests/visual/phase-15/screenshots/ — advisory. Flag any semantic break.
  7. A11y spot-check:
     - Tab through /associate/login, /login, /dashboard — every focusable control shows a visible focus ring (2px burnt orange outline).
     - Verify no body copy (paragraph text) uses var(--accent) on var(--bg) — contrast fails WCAG AA for small text. Accent should appear only on buttons, CTAs, links >=18px, icons, and large headings.
     - Tap buttons on mobile viewport (375px) — hit areas >= 44x44px.
  8. State preservation spot-check: trigger (a) invalid PIN on /associate/login → error visible, (b) score pending state on /review (a question mid-scoring) → loading indicator visible, (c) STT recording indicator on /interview → static flat dot + label visible.

  Type "approved — phase 15 complete" OR describe any regression.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe regression for a gap-closure plan.</resume-signal>
</task>

</tasks>

<verification>
Repo-wide final gate:
```
# Anywhere in src/ (INCLUDING globals.css now):
grep -rE "nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\bbtn-primary\b|\bbtn-accent\b|animation:.*(gradient-shift|\bfloat\b|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse)|@keyframes (gradient-shift|float|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse)" src/
# MUST return nothing.
```

Plus: full suite (typecheck + lint + vitest + build) green, Playwright advisory passed, manual smoke signed off.
</verification>

<success_criteria>
- globals.css contains ONLY DESIGN.md tokens, .btn-accent-flat, .btn-secondary-flat, @theme inline DESIGN mappings, slide-up/fade-in keyframes, Tailwind base, body rule.
- Repo-wide grep for --nlm-* / legacy utilities / kill-list keyframes returns zero.
- Playwright phase-15 spec runs; legacy-deletion assertions pass on tested routes.
- npx tsc, npm run lint, npm run test, npm run build all green.
- Trainer-led full smoke re-verified post-deletion (D-20 second pass).
- Two atomic commits (Task 1 specs, Task 2 deletion) landed. Task 2 commit is the final commit of Phase 15.
</success_criteria>

<output>
After completion, create `.planning/phases/15-design-cohesion-sweep/15-04-SUMMARY.md`. Include:
- Baseline vs. post globals.css grep counts
- Full list of deleted tokens/utilities/keyframes
- Full list of preserved items
- Playwright spec results
- Repo-wide grep output (must be empty)
- Both commit SHAs (deletion SHA is the phase-close marker)
- Manual smoke approval quote
</output>
