---
phase: 15-design-cohesion-sweep
plan: 03
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/app/dashboard/page.tsx
  - src/app/pdf/page.tsx
  - src/app/history/page.tsx
  - src/app/question-banks/page.tsx
autonomous: true
requirements: [DESIGN-03]
must_haves:
  truths:
    - "/dashboard setup wizard renders on DESIGN.md tokens across all three wizard phases with flow preserved"
    - "/pdf, /history, /question-banks render on DESIGN.md tokens with their existing data flows unchanged"
    - "Zero legacy utility / --nlm-* / kill-list animation references remain in these four files"
    - "All vitest suites touching these pages stay green"
  artifacts:
    - path: "src/app/dashboard/page.tsx"
      provides: "Setup wizard on var(--bg) + surface cards, token-driven phase nav, flat accent Submit, adaptive setup + curriculum filter logic untouched"
    - path: "src/app/pdf/page.tsx"
      provides: "PDF preview surface on DESIGN tokens; react-pdf render logic unchanged"
    - path: "src/app/history/page.tsx"
      provides: "History list on surface cards with tabular-nums tokens, existing fetch unchanged"
    - path: "src/app/question-banks/page.tsx"
      provides: "Question bank browser on DESIGN tokens, GitHub fetch unchanged"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "src/lib/adaptiveSetup.ts, /api/associates/[slug]/gap-scores, /api/cohorts/[id]/curriculum-weeks"
      via: "existing fetch calls — UNCHANGED"
      pattern: "fetch\\(.*(gap-scores|curriculum-weeks|cohorts)"
---

<objective>
Migrate the four lower-risk legacy surfaces — `/dashboard`, `/pdf`, `/history`, `/question-banks` — onto DESIGN.md tokens. Runs PARALLEL to Plan 15-02 because `files_modified` is disjoint.

Purpose: Clear the remaining legacy consumers so Plan 04 can delete the tokens + utilities. These four pages together hold 21 legacy references (dashboard 4, pdf 3, history 8, question-banks 6) but carry lower regression risk than the interview flow.

Output: Four restyled pages, zero legacy references, tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@DESIGN.md
@.planning/phases/15-design-cohesion-sweep/15-CONTEXT.md
@.planning/phases/14-design-cohesion/14-01-SUMMARY.md
@.planning/phases/14-design-cohesion/14-02-SUMMARY.md
@.planning/phases/15-design-cohesion-sweep/15-01-SUMMARY.md
@src/app/globals.css
@src/app/dashboard/page.tsx
@src/app/pdf/page.tsx
@src/app/history/page.tsx
@src/app/question-banks/page.tsx

<interfaces>
<!-- Already-migrated shared components from Plan 15-01 — use as-is. -->
import Navbar from '@/components/Navbar';
import ProgressBar from '@/components/ProgressBar';
import QuestionCard from '@/components/QuestionCard';

<!-- DESIGN tokens — same as Plan 15-01 interfaces block. -->
<!-- Dashboard uses adaptive setup + curriculum filter wiring from Phase 7 + Phase 13 — DO NOT TOUCH logic. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate /dashboard (setup wizard) and /question-banks</name>
  <files>src/app/dashboard/page.tsx, src/app/question-banks/page.tsx</files>
  <action>
  /dashboard (4 legacy hits) is the multi-phase setup wizard. Flow is: repo config → tech selection + weights → question count + candidate info. Adaptive setup pre-population (Phase 7) and curriculum filter (Phase 13) must remain functional.

  Step 1 (baseline):
    grep -cE "nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\\bbtn-primary\\b|\\bbtn-accent\\b|gradient-shift|\\bfloat\\b|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse" src/app/dashboard/page.tsx src/app/question-banks/page.tsx

  Step 2 (/dashboard):
    - Outer: bg-[var(--bg)] min-h-screen. (Navbar comes from root layout — already migrated in Plan 01.)
    - Wizard phase container: bg-[var(--surface)] border border-[var(--border)] rounded-xl p-6 shadow-[0_1px_2px_rgba(0,0,0,0.04)].
    - Phase step indicator: active step text var(--accent), inactive text var(--muted), separator border-[var(--border-subtle)].
    - Tech weight sliders/inputs: track bg-[var(--surface-muted)], thumb bg-[var(--accent)].
    - Curriculum-filtered skill chips (from Phase 13): selected bg-[var(--highlight)] border-[var(--accent)], unselected bg-[var(--surface-muted)] border-[var(--border)]. Badge showing "curriculum filtered" uses semantic badge colors (success tone).
    - "Start interview" CTA: .btn-accent-flat. Back/Cancel: .btn-secondary-flat.
    - Preserve ALL Zustand store bindings, fetch calls to /api/associates/[slug]/gap-scores and /api/cohorts/[id]/curriculum-weeks, adaptive weight pre-population logic.

  Step 3 (/question-banks — 6 legacy hits):
    - Outer: bg-[var(--bg)] min-h-screen.
    - Repo selector: bg-[var(--surface)] border border-[var(--border)] rounded-lg. Focus: border-[var(--accent)].
    - Question list: vertical rhythm via border-b border-[var(--border-subtle)]. Each row: 22px Clash Display question title, 14px DM Sans var(--muted) metadata, difficulty pill with semantic badge colors.
    - Keyword chips: bg-[var(--surface-muted)] text-[var(--muted)] rounded-full.
    - Fetch logic (src/lib/github-service.ts via /api/github) UNCHANGED.

  Step 4 (verify):
    - npx tsc --noEmit
    - npm run build
    - npm run test -- --run
    - Scoped grep gate (MUST be 0):
        grep -cE "nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\\bbtn-primary\\b|\\bbtn-accent\\b|gradient-shift|\\bfloat\\b|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse" src/app/dashboard/page.tsx src/app/question-banks/page.tsx

  Step 5 (atomic commit): `refactor(15-03): migrate /dashboard + /question-banks to DESIGN tokens`
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npm run test -- --run &amp;&amp; test "$(grep -cE 'nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\bbtn-primary\b|\bbtn-accent\b|gradient-shift|\bfloat\b|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse' src/app/dashboard/page.tsx src/app/question-banks/page.tsx | awk -F: '{s+=$2} END {print s}')" = "0"</automated>
  </verify>
  <done>/dashboard wizard + /question-banks migrated, zero legacy references, flow preserved, tests green, atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate /pdf and /history</name>
  <files>src/app/pdf/page.tsx, src/app/history/page.tsx</files>
  <action>
  Step 1 (baseline):
    grep -cE "nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\\bbtn-primary\\b|\\bbtn-accent\\b|gradient-shift|\\bfloat\\b|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse" src/app/pdf/page.tsx src/app/history/page.tsx

  Step 2 (/pdf — 3 legacy hits):
    - Outer: bg-[var(--bg)] min-h-screen.
    - Preview frame: bg-[var(--surface)] border border-[var(--border)] rounded-xl p-6 shadow-[0_1px_2px_rgba(0,0,0,0.04)].
    - Download CTA: .btn-accent-flat.
    - NOTE: The @react-pdf/renderer template styles inside src/components/PDFReport.tsx are NOT in scope — those use PDF-specific StyleSheet. Only page chrome around the preview migrates.

  Step 3 (/history — 8 legacy hits):
    - Outer: bg-[var(--bg)] min-h-screen.
    - History list: each row bg-[var(--surface)] border border-[var(--border)] rounded-lg p-4, hover bg-[var(--highlight)].
    - Associate slug / session id: font-mono var(--muted), text-sm.
    - Score pill: semantic badge colors per DESIGN.md.
    - Empty state: DM Sans var(--muted), Clash Display 28px heading var(--ink).
    - Delete buttons: .btn-secondary-flat with text-[var(--danger)].

  Step 4 (verify):
    - npx tsc --noEmit
    - npm run build
    - npm run test -- --run
    - Scoped grep gate (MUST be 0):
        grep -cE "nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\\bbtn-primary\\b|\\bbtn-accent\\b|gradient-shift|\\bfloat\\b|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse" src/app/pdf/page.tsx src/app/history/page.tsx

  Step 5 (atomic commit): `refactor(15-03): migrate /pdf + /history to DESIGN tokens`
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npm run test -- --run &amp;&amp; test "$(grep -cE 'nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\bbtn-primary\b|\bbtn-accent\b|gradient-shift|\bfloat\b|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse' src/app/pdf/page.tsx src/app/history/page.tsx | awk -F: '{s+=$2} END {print s}')" = "0"</automated>
  </verify>
  <done>/pdf + /history migrated, zero legacy references, tests green, atomic commit landed.</done>
</task>

</tasks>

<verification>
Plan-level gate across all four files:
```
grep -rE "nlm-bg|glass-card|gradient-text|glow-border|progress-gradient|recording-ring|--nlm-|\bbtn-primary\b|\bbtn-accent\b|gradient-shift|\bfloat\b|pulse-glow|shimmer|recording-pulse|progress-glow|border-glow-pulse" \
  src/app/dashboard/page.tsx src/app/pdf/page.tsx src/app/history/page.tsx src/app/question-banks/page.tsx
# MUST return nothing.
```
Full suite: typecheck + build + vitest green.
</verification>

<success_criteria>
- All four pages migrated.
- Zero legacy references in the four files.
- Dashboard wizard flow preserved (manual spot-check: step through phases, confirm adaptive weight pre-population + curriculum filter still populate correctly — but no blocking checkpoint in this plan; the wizard does not have mid-session state risk).
- Typecheck + build + vitest green.
- Two atomic commits landed.
</success_criteria>

<output>
After completion, create `.planning/phases/15-design-cohesion-sweep/15-03-SUMMARY.md`. Include baseline vs. post grep counts per file, commit SHAs, deviations.
</output>
