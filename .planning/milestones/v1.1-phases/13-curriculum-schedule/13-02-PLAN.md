---
phase: 13-curriculum-schedule
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/app/trainer/cohorts/[id]/curriculum/page.tsx
  - src/components/trainer/CurriculumTable.tsx
  - src/components/trainer/CurriculumWeekRow.tsx
  - src/components/trainer/AddCurriculumWeekForm.tsx
autonomous: false
requirements: [CURRIC-01]
must_haves:
  truths:
    - "Trainer can view all curriculum weeks for a cohort in a table"
    - "Trainer can add a new week via inline form"
    - "Trainer can edit a week inline and save changes"
    - "Trainer can delete a week with a confirm dialog"
    - "UI uses DESIGN.md tokens (warm parchment, Clash Display, DM Sans, burnt orange)"
  artifacts:
    - path: "src/app/trainer/cohorts/[id]/curriculum/page.tsx"
      provides: "Trainer curriculum management page per cohort"
    - path: "src/components/trainer/CurriculumTable.tsx"
      provides: "Sortable table of weeks with edit/delete actions"
      min_lines: 40
    - path: "src/components/trainer/AddCurriculumWeekForm.tsx"
      provides: "Inline add-week form"
      min_lines: 30
  key_links:
    - from: "src/components/trainer/CurriculumTable.tsx"
      to: "/api/cohorts/[id]/curriculum"
      via: "fetch GET/POST/PATCH/DELETE"
      pattern: "fetch.*api/cohorts.*curriculum"
---

<objective>
Build the trainer curriculum management UI: per-cohort table with add/edit/delete controls (CURRIC-01).

Purpose: Gives trainers the actual tool to define curriculum that Plan 03's wizard filter will consume. Without this, trainers can only CRUD via curl.
Output: Trainer page at `/trainer/cohorts/[id]/curriculum` plus three client components wired to the Plan 01 API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-curriculum-schedule/13-CONTEXT.md
@.planning/phases/13-curriculum-schedule/13-01-SUMMARY.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CurriculumTable and row components</name>
  <files>src/components/trainer/CurriculumTable.tsx, src/components/trainer/CurriculumWeekRow.tsx</files>
  <read_first>DESIGN.md, src/components/trainer/ (any existing cohort table pattern from Phase 11)</read_first>
  <action>Create `CurriculumTable` (client component, `'use client'`) that fetches `GET /api/cohorts/{cohortId}/curriculum` on mount (D-08), renders columns: Week #, Skill Name, Topic Tags, Start Date, Actions. Each row is a `CurriculumWeekRow` that supports two modes: display (default) and edit (click pencil icon → fields become inputs, Save calls PATCH, Cancel reverts). Delete button opens a confirm dialog (native `confirm()` acceptable for v1.1 per D-05) then calls DELETE. Tags shown as comma-separated text in display, comma-separated input in edit. Date formatted as `YYYY-MM-DD` for display and `<input type="date">` in edit. Use DESIGN.md tokens: warm parchment background, Clash Display for headers, DM Sans body, burnt orange for action buttons (D-07). Empty state: "No curriculum weeks yet. Add one below." Loading and error states required.</action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint -- src/components/trainer</automated>
  </verify>
  <acceptance_criteria>
    - CurriculumTable.tsx exports default client component, fetches on mount
    - CurriculumWeekRow supports display + edit modes
    - Delete triggers confirm prompt before calling DELETE
    - Components reference DESIGN.md color/font tokens (not raw hex/inline font names)
  </acceptance_criteria>
  <done>Type checks pass, lint clean, table renders from API response.</done>
</task>

<task type="auto">
  <name>Task 2: Build AddCurriculumWeekForm and trainer page</name>
  <files>src/components/trainer/AddCurriculumWeekForm.tsx, src/app/trainer/cohorts/[id]/curriculum/page.tsx</files>
  <read_first>src/components/trainer/CurriculumTable.tsx, src/middleware.ts</read_first>
  <action>Create `AddCurriculumWeekForm` (client component): inline form with inputs for weekNumber (number), skillName (text), topicTags (comma-separated → split to array on submit), startDate (date picker). Single "Add Week" button posts to `POST /api/cohorts/{cohortId}/curriculum` (D-04, D-10). On success, call `onWeekAdded` callback (parent refetches table) and reset form. Show inline validation errors. Styled per DESIGN.md (D-07). Then create the trainer page `src/app/trainer/cohorts/[id]/curriculum/page.tsx` — server-rendered shell that reads cohort metadata (name, dates) from Prisma for the page header, then mounts `<CurriculumTable cohortId={id} />` and `<AddCurriculumWeekForm cohortId={id} onWeekAdded={...} />`. Ensure `/trainer/*` middleware protection already covers this route (no middleware change needed — verify). Page title: `{cohort.name} — Curriculum`.</action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - AddCurriculumWeekForm posts JSON to /api/cohorts/[id]/curriculum
    - page.tsx renders both table and form, reads cohort from Prisma for header
    - npm run build succeeds (no route errors)
  </acceptance_criteria>
  <done>Build green, page renders at /trainer/cohorts/[id]/curriculum with working add/edit/delete roundtrip.</done>
</task>

<task type="auto">
  <name>Task 3: Start dev server for verification</name>
  <files></files>
  <action>Run `npm run dev` in background, wait for "ready". Log the URL.</action>
  <verify>
    <automated>curl -fsS http://localhost:3000 -o /dev/null -w "%{http_code}" | grep -q 200</automated>
  </verify>
  <done>Dev server responds on :3000.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Trainer curriculum UI — server at http://localhost:3000</what-built>
  <how-to-verify>
    1. Log in as trainer, navigate to an existing cohort (from Phase 11)
    2. Visit `/trainer/cohorts/[id]/curriculum`
    3. Verify: empty state shows, add-week form visible with all 4 fields
    4. Add a week: number=1, skill="react", tags="hooks,state", startDate=today. Confirm row appears.
    5. Edit the row inline — change skill name, save, verify persists after refresh.
    6. Delete the row — confirm dialog appears, row removed after OK.
    7. Visual: warm parchment background, Clash Display headers, DM Sans body, burnt orange buttons (DESIGN.md).
    8. Add a future-dated week (startDate in next week) and a past-dated week — both visible in table.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds
- [ ] Add/edit/delete round-trip works against Plan 01 API
- [ ] DESIGN.md tokens used (no raw hex in new components)
- [ ] Trainer auth middleware still protects the route
</verification>

<success_criteria>
- All tasks complete
- User approved visual + functional behavior
- CURRIC-01 trainer UI ships complete
</success_criteria>

<output>
After completion, create `.planning/phases/13-curriculum-schedule/13-02-SUMMARY.md`
</output>
