---
phase: 13-curriculum-schedule
plan: 01
type: execute
wave: 1
depends_on: ["11-01", "11-02"]
files_modified:
  - src/app/api/cohorts/[id]/curriculum/route.ts
  - src/app/api/cohorts/[id]/curriculum/[weekId]/route.ts
  - src/lib/curriculumService.ts
  - src/lib/curriculumService.test.ts
files_touched_external:
  - prisma/schema.prisma
autonomous: true
requirements: [CURRIC-01]
must_haves:
  truths:
    - "Trainer can create a curriculum week for a cohort via API with required canonical skillSlug"
    - "Trainer can list, update, and delete curriculum weeks for a cohort"
    - "Taught-weeks query returns only weeks where startDate <= today"
    - "Attempting to create/update a week violating @@unique([cohortId, weekNumber]) returns 409 (not 500) — per Codex finding #9"
    - "skillSlug is required, lowercase-kebab, and stored separately from skillName (display text)"
  artifacts:
    - path: "src/app/api/cohorts/[id]/curriculum/route.ts"
      provides: "GET list + POST create curriculum weeks, with ?taught=true filter"
      exports: ["GET", "POST"]
    - path: "src/app/api/cohorts/[id]/curriculum/[weekId]/route.ts"
      provides: "PATCH update + DELETE remove curriculum week"
      exports: ["PATCH", "DELETE"]
    - path: "src/lib/curriculumService.ts"
      provides: "Pure DB access functions for curriculum weeks including skillSlug"
      contains: "getTaughtWeeks"
  key_links:
    - from: "src/app/api/cohorts/[id]/curriculum/route.ts"
      to: "prisma.curriculumWeek"
      via: "Prisma query with cohortId scope and skillSlug validation"
      pattern: "prisma\\.curriculumWeek\\.(findMany|create)"
    - from: "src/lib/curriculumService.ts"
      to: "prisma.curriculumWeek"
      via: "taught-weeks query filters startDate <= now"
      pattern: "startDate.*lte.*new Date"
---

<objective>
Build the Curriculum CRUD API and service layer for per-cohort weekly schedules (CURRIC-01) with canonical `skillSlug` support and 409 handling for the DB-enforced `@@unique([cohortId, weekNumber])` constraint from Phase 8 (Codex finding #9).

Purpose: Provides the data foundation downstream plans consume. Matching downstream uses `skillSlug` (canonical), not `skillName` (display).
Output: Four REST endpoints, a pure-function service layer, Vitest coverage of the taught-weeks filter and slug validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-curriculum-schedule/13-CONTEXT.md
@prisma/schema.prisma
@src/lib/prisma.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create curriculumService with taught-weeks filter and skillSlug support</name>
  <files>src/lib/curriculumService.ts, src/lib/curriculumService.test.ts</files>
  <read_first>prisma/schema.prisma, src/lib/prisma.ts, src/lib/gapService.ts</read_first>
  <behavior>
    - listWeeks(cohortId): all weeks ordered by weekNumber asc
    - getTaughtWeeks(cohortId, now=new Date()): weeks where startDate <= now
    - createWeek(cohortId, input): validates (skillSlug required, lower-kebab regex) and inserts
    - updateWeek(weekId, input): updates fields, returns updated row
    - deleteWeek(weekId): deletes row
    - Test: getTaughtWeeks filters out future-dated weeks
    - Test: getTaughtWeeks includes weeks with startDate == now (boundary)
    - Test: createWeek with missing skillSlug throws validation error
    - Test: createWeek with non-kebab skillSlug (e.g. "React JS") throws validation error
    - Test: createWeek propagates Prisma P2002 unique-violation error untouched so route can map to 409
  </behavior>
  <action>
    Create pure async service functions wrapping `prisma.curriculumWeek`. Export TS types `CurriculumWeek`, `CurriculumWeekInput` — input includes `skillSlug: string` as a required field.

    Validation (in service or via Zod schema exported from this module):
    - `skillSlug`: required, matches `/^[a-z0-9][a-z0-9-]*$/`, length 1..50
    - `skillName`: required, length 1..80 (display only)
    - `weekNumber`: integer >= 1
    - `topicTags`: string[] default []
    - `startDate`: Date

    Do NOT catch Prisma's unique-constraint violation at the service layer — let it bubble so the route handler can translate P2002 into a 409.

    Write Vitest tests with mocked Prisma covering all behaviors above.
  </action>
  <verify>
    <automated>npm run test -- src/lib/curriculumService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - curriculumService.ts exports: listWeeks, getTaughtWeeks, createWeek, updateWeek, deleteWeek
    - skillSlug validation regex enforced
    - Tests pass including slug-validation and boundary cases
  </acceptance_criteria>
  <done>All service functions typed, tested, and green. skillSlug is a required, validated field.</done>
</task>

<task type="auto">
  <name>Task 2: Create curriculum collection API route (list + create + 409 on unique violation)</name>
  <files>src/app/api/cohorts/[id]/curriculum/route.ts</files>
  <read_first>src/lib/curriculumService.ts, src/app/api/settings/route.ts, src/lib/auth-server.ts</read_first>
  <action>
    Create `GET` and `POST` at `src/app/api/cohorts/[id]/curriculum/route.ts`.

    GET: read `?taught=true` query param; branches to `getTaughtWeeks` vs `listWeeks` (D-08, D-09).

    POST: Zod-validate body `{ weekNumber: number (int, >=1), skillName: string (1..80), skillSlug: string (/^[a-z0-9][a-z0-9-]*$/, 1..50), topicTags: string[] (default []), startDate: string (ISO date) }` then call `createWeek`. On Prisma `P2002` unique-violation (violating `@@unique([cohortId, weekNumber])` from Phase 8) → return 409 with `{ error: 'Week number already exists for this cohort' }` (D-24 per Codex #9). Other Prisma errors → 500.

    Both handlers trainer-auth guarded (D-13). Return 201 on POST success with created row. Return 404 if cohort doesn't exist. Return 400 on Zod failure with `{ error, issues }`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint -- src/app/api/cohorts</automated>
  </verify>
  <acceptance_criteria>
    - route.ts exports GET and POST
    - POST catches P2002 and returns 409
    - POST Zod schema requires skillSlug
    - Both handlers check trainer auth before DB access
  </acceptance_criteria>
  <done>Type check passes, lint clean, 409 mapping for unique violations, skillSlug required.</done>
</task>

<task type="auto">
  <name>Task 3: Create per-week API route (update + delete)</name>
  <files>src/app/api/cohorts/[id]/curriculum/[weekId]/route.ts</files>
  <read_first>src/app/api/cohorts/[id]/curriculum/route.ts, src/lib/curriculumService.ts</read_first>
  <action>
    Create `PATCH` and `DELETE` at `src/app/api/cohorts/[id]/curriculum/[weekId]/route.ts`.

    PATCH: partial Zod (same fields optional, skillSlug regex if present). Calls `updateWeek`. On P2002 → 409.

    DELETE: calls `deleteWeek`.

    Both verify `week.cohortId === params.id` before mutation (cross-cohort tampering guard). Trainer-auth guarded. PATCH returns 200 with updated row. DELETE returns 204.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - route.ts exports PATCH and DELETE
    - Both verify week.cohortId matches params.id
    - Unauthenticated requests return 401
    - PATCH returns 409 on unique-constraint violation
  </acceptance_criteria>
  <done>Type check passes, auth guard present, cross-cohort mismatch returns 404, unique violations return 409.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test -- curriculumService` passes including slug validation
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run lint` clean for modified files
- [ ] Manual smoke: POST a duplicate `(cohortId, weekNumber)` → 409 (not 500)
- [ ] Manual smoke: POST with missing `skillSlug` → 400
</verification>

<success_criteria>
- All 4 endpoints implemented and auth-guarded
- curriculumService has unit test coverage on taught-weeks filter AND slug validation
- P2002 unique-violation → 409 (Codex #9)
- CURRIC-01 API surface complete and consumable by Plan 02 (UI) and Plan 03 (wizard)
</success_criteria>

<output>
After completion, create `.planning/phases/13-curriculum-schedule/13-01-SUMMARY.md`
</output>
