---
phase: 11-cohort-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/cohorts/route.ts
  - src/app/api/cohorts/[id]/route.ts
  - src/lib/cohort-types.ts
  - src/app/api/cohorts/route.test.ts
  - src/app/api/cohorts/[id]/route.test.ts
autonomous: true
requirements: [COHORT-01]
must_haves:
  truths:
    - "Trainer can create a cohort via POST /api/cohorts and receive the created record"
    - "Trainer can list all cohorts via GET /api/cohorts ordered by startDate desc"
    - "Trainer can edit a cohort via PATCH /api/cohorts/[id]"
    - "Trainer can delete a cohort via DELETE /api/cohorts/[id] without cascading to associates"
    - "All routes reject unauthenticated requests with 401"
    - "All routes reject invalid payloads with 400 + zod issues"
  artifacts:
    - path: "src/app/api/cohorts/route.ts"
      provides: "GET list + POST create"
      exports: ["GET", "POST"]
    - path: "src/app/api/cohorts/[id]/route.ts"
      provides: "GET single + PATCH update + DELETE"
      exports: ["GET", "PATCH", "DELETE"]
    - path: "src/lib/cohort-types.ts"
      provides: "Shared TS types for Cohort DTOs used by UI plans"
  key_links:
    - from: "src/app/api/cohorts/[id]/route.ts DELETE"
      to: "prisma.associate.updateMany + prisma.cohort.delete"
      via: "prisma.$transaction"
      pattern: "\\$transaction"
    - from: "all route handlers"
      to: "isAuthenticatedSession"
      via: "auth guard"
      pattern: "isAuthenticatedSession"
---

<objective>
Build the `/api/cohorts` CRUD layer that powers Phase 11 UI work. Auth-guarded, zod-validated, non-cascading delete.

Purpose: Deliver COHORT-01's backend so UI plans (02, 03) can consume a stable API.
Output: Two route files, shared types, unit tests for each handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-cohort-management/11-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@prisma/schema.prisma
@src/app/api/trainer/route.ts
@src/app/api/trainer/[slug]/route.ts
@src/lib/auth-server.ts
@src/lib/prisma.ts

<interfaces>
Existing helpers to reuse verbatim:

```typescript
// src/lib/auth-server.ts
export async function isAuthenticatedSession(): Promise<boolean>;

// src/lib/prisma.ts
export const prisma: PrismaClient;
```

Expected Cohort model (from Phase 8 — verify before coding):
```prisma
model Cohort {
  id          Int       @id @default(autoincrement())
  name        String
  startDate   DateTime
  endDate     DateTime
  description String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  associates  Associate[]
}
```

New type exports (this plan creates):
```typescript
// src/lib/cohort-types.ts
export interface CohortDTO {
  id: number;
  name: string;
  startDate: string;  // ISO
  endDate: string;    // ISO
  description: string | null;
  associateCount?: number;
}

export interface CohortCreateInput {
  name: string;
  startDate: string;
  endDate: string;
  description?: string | null;
}

export type CohortUpdateInput = Partial<CohortCreateInput>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared types + /api/cohorts GET and POST</name>
  <files>src/lib/cohort-types.ts, src/app/api/cohorts/route.ts, src/app/api/cohorts/route.test.ts</files>
  <behavior>
    - Test: unauthenticated GET returns 401
    - Test: authenticated GET returns array of CohortDTO ordered by startDate desc, each with associateCount
    - Test: unauthenticated POST returns 401
    - Test: POST with valid body returns 201 and the created cohort as CohortDTO
    - Test: POST with empty name returns 400 with zod issues
    - Test: POST with endDate < startDate returns 400
    - Test: POST with description > 500 chars returns 400
  </behavior>
  <action>
    First verify `prisma/schema.prisma` has `Cohort` model and `Associate.cohortId` nullable FK from Phase 8. If missing, STOP and escalate — Phase 8 must complete first.

    Create `src/lib/cohort-types.ts` exporting the interfaces in the `<interfaces>` block above.

    Create `src/app/api/cohorts/route.ts` (per D-01, D-03, D-04, D-16, D-17):
    - Import `NextResponse`, `z` from 'zod', `isAuthenticatedSession`, `prisma`, `CohortDTO`.
    - Define `CreateCohortSchema = z.object({ name: z.string().min(1).max(100), startDate: z.coerce.date(), endDate: z.coerce.date(), description: z.string().max(500).nullable().optional() }).refine(d => d.endDate >= d.startDate, { message: 'endDate must be >= startDate', path: ['endDate'] })`.
    - `GET`: auth guard → `prisma.cohort.findMany({ include: { _count: { select: { associates: true } } }, orderBy: { startDate: 'desc' } })` → map to CohortDTO with ISO dates and `associateCount`.
    - `POST`: auth guard → parse JSON → `CreateCohortSchema.safeParse` → on failure return 400 with `{ error: 'Invalid input', issues: result.error.issues }` → on success `prisma.cohort.create` → return 201 with CohortDTO. Catch Prisma P2002 → 409.

    Write vitest tests in `route.test.ts` covering the `<behavior>` cases. Mock `@/lib/auth-server` and `@/lib/prisma`.
  </action>
  <verify>
    <automated>npm run test -- src/app/api/cohorts/route.test.ts</automated>
  </verify>
  <done>All behavior tests pass; npx tsc --noEmit clean; npm run lint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /api/cohorts/[id] GET, PATCH, DELETE with non-cascading delete</name>
  <files>src/app/api/cohorts/[id]/route.ts, src/app/api/cohorts/[id]/route.test.ts</files>
  <behavior>
    - Test: unauthenticated for any method returns 401
    - Test: GET with valid id returns CohortDTO with associateCount
    - Test: GET with non-existent id returns 404
    - Test: GET with non-numeric id returns 400
    - Test: PATCH with partial body updates only provided fields, returns 200
    - Test: PATCH with endDate < startDate returns 400
    - Test: PATCH with empty name returns 400
    - Test: DELETE nullifies associate.cohortId for all associates in the cohort, then deletes the cohort (verified via mock transaction calls)
    - Test: DELETE returns 204 on success; 404 if cohort missing
  </behavior>
  <action>
    Create `src/app/api/cohorts/[id]/route.ts` (per D-02, D-03, D-04, D-06, D-07, D-16):
    - Signature: `{ params: Promise<{ id: string }> }` — match Next.js 16 App Router pattern from `src/app/api/trainer/[slug]/route.ts`.
    - Parse id: `const idNum = Number(id); if (!Number.isInteger(idNum) || idNum <= 0) return 400`.
    - `UpdateCohortSchema = CreateCohortSchema.partial().refine(d => !d.startDate || !d.endDate || d.endDate >= d.startDate, ...)`. Import `CreateCohortSchema` or duplicate inline (prefer inline — avoids cross-file zod imports).
    - `GET`: auth → `prisma.cohort.findUnique({ where: { id: idNum }, include: { _count: { select: { associates: true } } } })` → 404 if null → return CohortDTO.
    - `PATCH`: auth → parse+validate → `prisma.cohort.update` → catch P2025 (not found) → 404 → return updated CohortDTO.
    - `DELETE` (non-cascading per D-06): auth → wrap in `prisma.$transaction(async (tx) => { await tx.associate.updateMany({ where: { cohortId: idNum }, data: { cohortId: null } }); await tx.cohort.delete({ where: { id: idNum } }); })` → catch P2025 → 404 → return `new NextResponse(null, { status: 204 })`.

    Also verify `prisma/schema.prisma` sets `onDelete: SetNull` on `Associate.cohortId` FK (D-07). If not, add a note to the commit and log a warning — the transaction is still the primary safety mechanism.

    Write vitest tests per `<behavior>`. Mock `$transaction` to capture the updateMany + delete calls and assert order.
  </action>
  <verify>
    <automated>npm run test -- src/app/api/cohorts/[id]/route.test.ts</automated>
  </verify>
  <done>All behavior tests pass; DELETE proven non-cascading via mock assertion; typecheck + lint clean.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/app/api/cohorts` — all tests pass
- `npx tsc --noEmit` clean
- `npm run lint` clean
- Manual smoke: start dev server, `curl` GET/POST/PATCH/DELETE with a valid trainer session cookie
</verification>

<success_criteria>
- GET /api/cohorts returns cohort list with associateCount
- POST /api/cohorts creates cohort and returns 201
- PATCH /api/cohorts/[id] updates cohort
- DELETE /api/cohorts/[id] unassigns associates (cohortId → null) then removes cohort — associates remain in DB
- All endpoints return 401 when unauthenticated
- Zod validation errors return 400 with issues
</success_criteria>

<output>
After completion, create `.planning/phases/11-cohort-management/11-01-SUMMARY.md`
</output>
