---
phase: 03-associate-profiles
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/app/associate/[slug]/page.tsx
autonomous: false
requirements: [PERSIST-02]

must_haves:
  truths:
    - "Navigating to /associate/jane-doe shows all sessions for that associate"
    - "Unauthenticated users are redirected to /login"
    - "Unknown slugs show a 'not found' state (not an error page)"
    - "Sessions are listed newest-first with date, score summary, and status"
    - "Associate with zero sessions shows an empty state message"
  artifacts:
    - path: "src/app/associate/[slug]/page.tsx"
      provides: "Server Component rendering associate profile with session list"
      min_lines: 40
  key_links:
    - from: "src/app/associate/[slug]/page.tsx"
      to: "src/lib/prisma.ts"
      via: "prisma.associate.findUnique with sessions include"
      pattern: "prisma\\.associate\\.findUnique"
    - from: "src/app/associate/[slug]/page.tsx"
      to: "src/lib/auth-server.ts"
      via: "isAuthenticatedSession() guard"
      pattern: "isAuthenticatedSession"
---

<objective>
Create the associate profile page at /associate/[slug] as a Server Component that shows all sessions for a given associate.

Purpose: Give trainers a way to see an associate's session history in one place, laying the foundation for gap charts and readiness signals in later phases.
Output: Working /associate/[slug] route with auth guard, session list, and empty/not-found states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-associate-profiles/03-CONTEXT.md
@.planning/phases/03-associate-profiles/03-RESEARCH.md
@.planning/phases/03-associate-profiles/03-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 and existing codebase. -->

From src/lib/prisma.ts (Phase 1):
```typescript
export const prisma: PrismaClient
```

From src/lib/auth-server.ts:
```typescript
export async function isAuthenticatedSession(): Promise<boolean>
// Reads 'nlm_session' HttpOnly cookie, returns true if 'authenticated'
```

From prisma/schema.prisma (after Plan 01 Task 1):
```prisma
model Associate {
  id          Int       @id @default(autoincrement())
  slug        String    @unique
  displayName String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  sessions    Session[]
}

model Session {
  // ... Phase 2 fields ...
  associateId  Int?
  associate    Associate? @relation(fields: [associateId], references: [id])
}
```

Session model fields available for the profile list (from Phase 2 schema — confirm exact field names by reading prisma/schema.prisma before implementing):
- id, date/createdAt, status, candidateName
- overallTechnicalScore, overallSoftSkillScore (or equivalent — check Phase 2 schema)
- assessments (JSON column)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /associate/[slug] Server Component profile page</name>
  <files>src/app/associate/[slug]/page.tsx</files>
  <read_first>
    - prisma/schema.prisma (confirm Session model field names — need exact column names for the SELECT query)
    - src/lib/auth-server.ts (confirm isAuthenticatedSession import path and usage)
    - src/lib/prisma.ts (confirm prisma singleton export)
    - DESIGN.md (styling direction — warm editorial, DM Sans body, Clash Display for display text, --surface cards, --border, --ink, --muted, --accent tokens)
    - src/app/dashboard/page.tsx (reference for existing page patterns — but note this is a Client Component while /associate/[slug] will be a Server Component)
  </read_first>
  <action>
1. **Create `src/app/associate/[slug]/page.tsx`** as a Server Component (per D-04):

   ```typescript
   import { prisma } from '@/lib/prisma';
   import { isAuthenticatedSession } from '@/lib/auth-server';
   import { redirect, notFound } from 'next/navigation';
   ```

2. **Auth guard**: Call `isAuthenticatedSession()` — if false, `redirect('/login')`. This matches the pattern used by all protected routes (per RESEARCH.md security domain).

3. **Params handling**: In Next.js 16 (based on 15+), route params are async. Use:
   ```typescript
   const { slug } = await params;
   ```
   If this errors during dev (unlikely but flagged in RESEARCH.md A2), fall back to `params.slug` directly.

4. **Data query**: Use Prisma to fetch associate with sessions:
   ```typescript
   const associate = await prisma.associate.findUnique({
     where: { slug },
     include: {
       sessions: {
         orderBy: { createdAt: 'desc' },
         select: {
           id: true,
           createdAt: true,
           status: true,
           candidateName: true,
           // Include score fields — check exact names from schema
         },
       },
     },
   });
   ```
   NOTE: Check the actual Session model field names from prisma/schema.prisma. The RESEARCH.md suggests `overallTechnicalScore` and `overallSoftSkillScore` but Phase 2 may use different names. Read the schema first.

5. **Not found state**: If `associate` is null, call `notFound()` to render Next.js 404. Alternatively, render an inline "Associate not found" message. Prefer `notFound()` for proper HTTP semantics.

6. **Empty sessions state**: If `associate.sessions.length === 0`, show: "No sessions recorded yet for [slug]. Sessions will appear here after completing a mock interview." Use `--muted` color text.

7. **Profile layout** (per D-04 — simple list view, no charts):
   - Page title: Associate display name (or slug if no displayName) in Clash Display 28px
   - Subtitle: slug in `--muted` color, 14px, DM Sans
   - Member since: `createdAt` formatted as readable date
   - Session count badge
   - Session list: Each session as a card (`--surface` bg, `--border` border, rounded-xl, p-4 padding):
     - Date (formatted, 14px, `--muted`)
     - Candidate name (if present)
     - Status badge: colored per status (completed = `--success`, in-progress = `--warning`, review = `--accent`)
     - Score summary: Technical and Soft Skill scores if available (Clash Display for numbers)
   - Use responsive layout: single column on mobile, comfortable width (max-w-3xl) centered

   IMPORTANT: Since this is a NEW page (not modifying an existing one), use DESIGN.md tokens from the start. The existing dashboard uses legacy glass-card styling, but new pages should adopt the design system. Use CSS custom properties or Tailwind classes that map to the DESIGN.md color tokens. If the DESIGN.md tokens are not yet implemented as CSS variables in the project, use the literal hex values from DESIGN.md with comments noting the token name.

8. **Back navigation**: Link back to /dashboard or /trainer (whichever exists).
  </action>
  <verify>
    <automated>test -f src/app/associate/\[slug\]/page.tsx && grep -q "isAuthenticatedSession" src/app/associate/\[slug\]/page.tsx && grep -q "prisma.associate.findUnique" src/app/associate/\[slug\]/page.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/associate/[slug]/page.tsx exists as a Server Component (no 'use client' directive)
    - Page imports and calls isAuthenticatedSession(), redirects to /login if false
    - Page uses `await params` to extract slug (Next.js 16 async params)
    - Page queries prisma.associate.findUnique with sessions include ordered by createdAt desc
    - Page calls notFound() or renders not-found state when associate is null
    - Page renders empty state message when associate has zero sessions
    - Page renders session list with date, status, and score summary for each session
    - Page uses DESIGN.md color tokens (--surface, --border, --ink, --muted, --accent or hex equivalents)
    - Page uses Clash Display for display text and DM Sans for body text (or appropriate font classes)
    - No 'use client' directive at top of file
  </acceptance_criteria>
  <done>Associate profile page at /associate/[slug] renders session history with auth guard, not-found handling, empty state, and DESIGN.md styling. Trainers can view any associate's session history by navigating to /associate/{slug}.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete associate identity flow: slug input in dashboard wizard, session linkage via API, and profile page at /associate/[slug]</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Navigate to http://localhost:3000/dashboard
    3. In setup wizard Phase 2 (Participant Details), verify "Associate ID" input exists below candidate/interviewer names
    4. Enter a slug like "test-associate" — verify no validation error
    5. Enter invalid slug like "Test Associate!" — verify inline error appears
    6. Complete a full mock interview with the slug set
    7. Navigate to http://localhost:3000/associate/test-associate
    8. Verify the session you just completed appears in the list
    9. Navigate to http://localhost:3000/associate/nonexistent — verify 404/not-found state
    10. Open an incognito window, navigate to /associate/test-associate — verify redirect to /login
    11. Complete another session with the SAME slug "test-associate" — verify both sessions appear on the profile page
    12. Complete a session WITHOUT a slug — verify it saves normally (backward compat), no associate record created
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> /associate/[slug] | Slug in URL is untrusted user input |
| Server Component -> Prisma | URL slug used in database query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | /associate/[slug] enumeration | mitigate | isAuthenticatedSession() guard — unauthenticated users redirected to /login before any data query |
| T-03-07 | Tampering | Slug in URL path | mitigate | Prisma parameterized queries — slug from URL params is never interpolated into raw SQL. findUnique uses parameterized WHERE clause. |
| T-03-08 | Information Disclosure | Session data exposure | accept | Profile page shows session scores and dates — this is intentional for authenticated trainers. No PII beyond candidate name (already visible in existing interview flow). |
</threat_model>

<verification>
1. `npm run build` — Next.js build succeeds with new route
2. `/associate/test-slug` renders profile page (after creating a session with that slug)
3. `/associate/nonexistent` shows not-found state
4. Unauthenticated access to `/associate/any-slug` redirects to /login
5. Profile page shows sessions newest-first with scores and status
</verification>

<success_criteria>
- /associate/[slug] route exists and renders as Server Component
- Auth guard prevents unauthenticated access
- Profile page shows all sessions for the associate, newest first
- Not-found state for unknown slugs
- Empty state for associates with zero sessions
- Visual design follows DESIGN.md tokens
</success_criteria>

<output>
After completion, create `.planning/phases/03-associate-profiles/03-02-SUMMARY.md`
</output>
