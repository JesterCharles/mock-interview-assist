---
phase: 41-gapscore-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 41-01
  - 41-02
files_modified:
  - .planning/PROJECT.md
  - DESIGN.md
autonomous: true
requirements:
  - CODING-SCORE-04
must_haves:
  truths:
    - "PROJECT.md contains a 'Readiness math with coding signals' subsection in v1.4 active section explaining the full pipeline: signalType weight (Phase 36) × difficulty multiplier (Phase 41) × 0.8 decay (gapService)"
    - "PROJECT.md worked example computes weightedScore end-to-end for one associate with mixed interview + coding history — numbers verifiable against Phase 36 D-16 table + Phase 41 D-02 multipliers"
    - "PROJECT.md contains an 'Open Items for v1.5' section with coding backfill as the first entry (per D-10)"
    - "DESIGN.md contains a 'Trainer dashboard: coding panel' section documenting visual hierarchy + chart-4 rationale + why coding lives adjacent to interview"
    - "Section references are discoverable via grep on canonical headings"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Readiness-math-with-coding-signals subsection + Open Items for v1.5 section"
      contains: "Readiness math with coding signals"
    - path: "DESIGN.md"
      provides: "Trainer dashboard: coding panel visual spec"
      contains: "Trainer dashboard: coding panel"
  key_links:
    - from: ".planning/PROJECT.md"
      to: "Phase 36 D-16 signalType weight table"
      via: "explicit reference + inline re-statement"
      pattern: "signalType.*weight"
    - from: ".planning/PROJECT.md"
      to: "Plan 41-01 DIFFICULTY_MULTIPLIERS"
      via: "explicit reference + inline re-statement"
      pattern: "easy.*0\\.7.*medium.*1\\.0.*hard.*1\\.3"
    - from: "DESIGN.md"
      to: "DESIGN §Chart Palette --chart-4"
      via: "internal section reference"
      pattern: "chart-4"
---

<objective>
Close CODING-SCORE-04 by updating both canonical docs to reflect the Phase 41 implementation: PROJECT.md gains a readiness-math subsection explaining coding signal contribution vs interview signal, and DESIGN.md gains a trainer dashboard coding panel visual spec. Also adds "Open Items for v1.5" to PROJECT.md per D-10 (coding backfill deferral).

Purpose: Satisfy CODING-SCORE-04 gate + D-08, D-09, D-10. Without these doc updates, the phase isn't auditable — future engineers wouldn't know whether the mixed-signal math was deliberate or accidental. The worked example is the load-bearing artifact: it makes the decay + difficulty + signal-type stack concrete.

Output: Two doc files updated. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@DESIGN.md
@.planning/phases/41-gapscore-integration/41-CONTEXT.md
@.planning/phases/41-gapscore-integration/41-01-SUMMARY.md
@.planning/phases/41-gapscore-integration/41-02-SUMMARY.md
@.planning/phases/36-data-model-schema/36-CONTEXT.md

<reference_values>
<!-- Pull-through of locked constants so doc writer doesn't have to hop files -->

From Phase 36 D-16 (codingSignalService weight table):
| signalType    | mappedScore | weight | Rationale |
|---------------|-------------|--------|-----------|
| pass          | 100         | 1.0    | Full credit |
| partial       | (fraction × 100) | 0.85 | Discount partial |
| fail          | 0           | 1.0    | Clear signal |
| compile_error | 10          | 0.6    | Weaker than wrong-answer — "tried but didn't run" |
| timeout       | 20          | 0.8    | Algorithm chosen but inefficient |

From Phase 41 D-02 (DIFFICULTY_MULTIPLIERS):
| difficulty | multiplier |
|------------|------------|
| easy       | 0.7        |
| medium     | 1.0        |
| hard       | 1.3        |

From Phase 4 (pre-existing, src/lib/gapService.ts): DECAY_FACTOR = 0.8

GapScore (final) = recencyWeightedAverage([...per-attempt-weighted-scores], decay=0.8)
where per-attempt weighted score = mappedScore × signalType.weight × difficulty.multiplier
</reference_values>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Readiness math with coding signals" subsection to PROJECT.md + Open Items section</name>
  <files>.planning/PROJECT.md</files>
  <action>
    1. Read current `.planning/PROJECT.md`.
    2. Locate the v1.4 active section (line 62 `## Active Milestone: v1.4`).
    3. Insert a new subsection BEFORE the `### v1.4 Architecture Headlines` heading (around line 86):
       ```markdown
       ### Readiness Math with Coding Signals

       v1.4 extends the existing 0.8-decay recency-weighted gap engine (unchanged since v1.0) to consume coding-attempt signals alongside interview signals. The pipeline stacks three locked coefficients:

       **Pipeline (per coding attempt):**
       ```
       perAttemptWeightedScore
         = mappedScore                         // Phase 36 D-16 (by signalType)
           × signalType.weight                 // Phase 36 D-16 (by signalType)
           × DIFFICULTY_MULTIPLIERS[difficulty]  // Phase 41 D-02
       ```

       Then the existing `gapService.recencyWeightedAverage()` applies 0.8^index decay across per-attempt rows keyed on `(associateId, skill, topic)` where `topic = "coding:<language>"`.

       **Signal-type weight table (Phase 36 D-16, unchanged by Phase 41):**

       | signalType    | mappedScore | weight | Rationale |
       |---------------|-------------|--------|-----------|
       | pass          | 100         | 1.0    | Full credit |
       | partial       | fraction × 100 | 0.85 | Discount partial vs clean pass |
       | fail          | 0           | 1.0    | Clear signal |
       | compile_error | 10          | 0.6    | Weaker than wrong-answer |
       | timeout       | 20          | 0.8    | Algorithm chosen but inefficient |

       **Difficulty multiplier (Phase 41 D-02, prevents easy-attempt farming):**

       | difficulty | multiplier |
       |------------|------------|
       | easy       | 0.7        |
       | medium     | 1.0        |
       | hard       | 1.3        |

       **Worked example — associate "ada" mixed history on `python-fundamentals`:**

       | # | Rep type   | Event                                   | Raw score | After signal weight | After difficulty multiplier | Decay factor (newest→oldest) |
       |---|-----------|-----------------------------------------|-----------|---------------------|------------------------------|------------------------------|
       | 1 | Interview | Session (mean of Python questions)      | 72        | 72                  | 72 (decay-only path)         | 0.8⁰ = 1.00                  |
       | 2 | Coding    | Hard challenge, pass                    | 100       | 100 × 1.0 = 100     | 100 × 1.3 = **130**          | 0.8¹ = 0.80                  |
       | 3 | Coding    | Medium challenge, compile_error         | 10        | 10 × 0.6 = 6        | 6 × 1.0 = **6**              | 0.8² = 0.64                  |
       | 4 | Interview | Session (Python avg)                    | 65        | 65                  | 65 (decay-only path)         | 0.8³ = 0.512                 |
       | 5 | Coding    | Easy challenge, pass                    | 100       | 100 × 1.0 = 100     | 100 × 0.7 = **70**           | 0.8⁴ = 0.4096                |

       *Note:* interview-signal rows skip the difficulty multiplier because interviews have no Judge0-authored difficulty. They feed gapService through the existing `saveGapScores()` path unchanged.

       `recencyWeightedAverage` over the five per-session-or-per-attempt rows (newest first):

       ```
       weightedSum  = 72×1.00 + 130×0.80 + 6×0.64 + 65×0.512 + 70×0.4096
                    = 72.00 + 104.00 + 3.84 + 33.28 + 28.672
                    = 241.79
       weightTotal  = 1.00 + 0.80 + 0.64 + 0.512 + 0.4096
                    = 3.3616
       GapScore     = 241.79 / 3.3616 ≈ 71.93
       ```

       Readiness classification (`readinessService.ts`, unchanged) then uses this 71.93 against the configured threshold (default 75) alongside trend + session-count gates.

       **Farming resistance:** Any number of easy passes cannot drag weightedScore as high as the same number of hard passes — easy contributes 70 per attempt, hard 130. A trainer auditing a suspicious ready-signal can check the attempt table's difficulty column in the new `/trainer/[slug]` coding panel (Plan 41-02) to confirm the source mix.

       **Phase invariant:** `src/lib/gapService.ts` is not modified by v1.4. The 0.8 decay, the recency math, and the `(associateId, skill, topic)` uniqueness key are all Phase 4 / v1.0 contracts. v1.4 Phase 41 only extends `gapPersistence.ts` with a new entrypoint and hooks it from the Phase 39 poll handler fire-and-forget.
       ```
    4. Append a new section at the END of the file (after the `*Last updated:*` footer, or absorb into it):
       ```markdown
       ---

       ## Open Items for v1.5

       1. **Coding-attempt backfill** (D-10, Phase 41) — No retroactive backfill of pre-v1.4 per-cohort coding data shipped in v1.4. Legacy data was manually administered without Judge0 and lacks a reliable verdict mapping. Decide in v1.5 whether to ship an opt-in importer with trainer manual sign-off, or leave pre-v1.4 history untracked.
       2. **Coding signal decay cap** — Consider clamping max movement per single attempt (e.g., single attempt can move GapScore by ≤ N points) to reduce noise from one-off bad days. Tuning decision — needs live data.
       3. **Per-language readiness breakdown on associate UI** — Currently associate-facing views aggregate across languages. Trainer view already shows per-language via `topic="coding:<lang>"` — mirror to associate dashboard if trainers request it.
       4. **Trainer alerts on coding gap threshold** — Push notification when an associate's coding-only weightedScore crosses a warning boundary. Out of v1.4 scope; revisit after first cohort runs with v1.4.
       ```
    5. Update the `*Last updated:*` footer line to today's date and append "— Phase 41 readiness-math documentation".
  </action>
  <verify>
    <automated>grep -q "Readiness Math with Coding Signals" .planning/PROJECT.md && grep -q "DIFFICULTY_MULTIPLIERS" .planning/PROJECT.md && grep -q "Open Items for v1.5" .planning/PROJECT.md && grep -q "Coding-attempt backfill" .planning/PROJECT.md</automated>
  </verify>
  <done>
    - New subsection present in v1.4 active section with pipeline formula, both coefficient tables, and worked 5-row example
    - Worked example arithmetic verified (241.79 / 3.3616 ≈ 71.93)
    - "Open Items for v1.5" section at end with coding backfill as first entry (D-10)
    - Last-updated footer bumped
    - No code changes (file is PROJECT.md only)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add "Trainer dashboard: coding panel" section to DESIGN.md</name>
  <files>DESIGN.md</files>
  <action>
    1. Read current `DESIGN.md`.
    2. Locate the existing Chart Palette section (around line 134) and the Trajectory visual note (line 227). The new section goes BETWEEN them or AFTER the Tooltip section — planner picks: insert AFTER the Tooltip / Shadow subsection (around line 197-200) BEFORE "Trajectory visual".
    3. Insert new section:
       ```markdown
       ### Trainer dashboard: coding panel

       **Context.** Phase 41 added coding-attempt telemetry to `/trainer/[slug]`. The coding panel lives **adjacent to**, not merged with, the existing interview dashboard. Rationale: trainers diagnosing a readiness gap need to triage whether the deficit is coming from coding practice or interview performance before assigning remediation. Merging the views would collapse that distinction.

       **Placement.** Below `<AssociateDashboardClient />`, same max-width (1120px), separated by 48px vertical margin. Same `var(--surface)` card treatment as other dashboard panels.

       **Visual hierarchy (top to bottom):**
       1. Section heading "Coding practice" — Clash Display 28px, 600 weight, `var(--ink)`
       2. Filter row (Language dropdown, Skill dropdown) — DM Sans 13px, matches existing trainer dropdown treatment
       3. Per-skill coding bar chart (`CodingSkillBars`)
       4. Latest 20 attempts table (`CodingAttemptsTable`)
       5. "Load more" pagination action if > 20 attempts exist

       **Chart color — distinct from interview.** Interview trend uses `--accent` (burnt orange). Coding bars use `--chart-4` (warm taupe `#8C7B6E` light / `#A89080` dark). Both are earth-tone, on-brand, and adjacent enough to not clash when stacked vertically — but different enough that a trainer scanning the page immediately separates the two signal sources. Per Chart Palette §, never use hardcoded hex; consume `var(--chart-4)` directly in recharts `fill`.

       **Verdict badges (coding attempt rows):**
       - `pass` → foreground `var(--success)`, background `var(--success-bg)`
       - `fail` / `timeout` / `mle` / `runtime_error` / `compile_error` → foreground `var(--danger)`, background `var(--danger-bg)`
       - `pending` → `var(--muted)`

       **Difficulty pill.** Neutral gray background (`var(--border)`), 11px DM Sans 500, uppercase tracking. No color coding on difficulty itself — the color signal is already in the verdict badge.

       **Empty state.** "No coding attempts yet." — DM Sans 14px, `var(--muted)`, centered in the panel. Shown when `attempts.length === 0`.

       **Dark mode.** All tokens dual-mapped in `globals.css` (see Chart Palette table). No extra work.

       **Why not a tab.** Tab navigation was evaluated (CONTEXT D-06 left the choice open). Trainer page is currently a vertical stack — adding tab chrome would fragment the scroll and require more page-level state. A stacked panel preserves the existing UX pattern and keeps interview + coding context visible simultaneously.
       ```
    4. Update the `*Last updated:*` footer or add a new entry to the changelog table (line 250-ish) for today's date: `| 2026-04-NN | Trainer coding panel section added | Phase 41: visual spec for coding practice panel on /trainer/[slug], chart-4 color rationale, verdict/difficulty badge treatments. |`
  </action>
  <verify>
    <automated>grep -q "Trainer dashboard: coding panel" DESIGN.md && grep -q "var(--chart-4)" DESIGN.md && grep -q "pending.*var(--muted)" DESIGN.md</automated>
  </verify>
  <done>
    - New "Trainer dashboard: coding panel" section inserted in correct location
    - chart-4 color rationale explicit (distinct from interview --accent)
    - Verdict + difficulty badge treatments documented
    - Changelog entry or footer updated
    - No hardcoded hex in the new section text (all references via `var(--*)`)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Doc-only plan — no runtime trust boundaries. Documentation itself is a control against future drift.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-09 | Repudiation | Code drifts from docs: future engineer changes difficulty multipliers or chart colors without updating PROJECT.md / DESIGN.md | mitigate | Plan requires explicit doc update as part of Phase 41 gate (CODING-SCORE-04). Worked example in PROJECT.md pins numeric values so review would catch drift. DESIGN.md changelog entry timestamps the decision. Future planner agents read CONTEXT → both docs referenced. |
| T-41-10 | Information Disclosure | Worked example contains real associate PII | accept | Example uses placeholder "ada" — no real identifiers. Verified in Task 1 action text. |
| T-41-11 | Tampering | Doc written with incorrect math (e.g., 241.79 / 3.3616 ≠ 71.93) | mitigate | Explicit arithmetic in Task 1 action; executor must validate the division result matches the stated ≈ 71.93. Automated verify greps for the constant to confirm the documented value is preserved on write. |
</threat_model>

<verification>
- `grep -q "Readiness Math with Coding Signals" .planning/PROJECT.md` passes
- `grep -q "Open Items for v1.5" .planning/PROJECT.md` passes
- `grep -q "Trainer dashboard: coding panel" DESIGN.md` passes
- Arithmetic sanity: sum numerator = 72 + 104 + 3.84 + 33.28 + 28.672 ≈ 241.79 ✓; denominator = 3.3616 ✓; quotient ≈ 71.93 ✓
- No code changes (`git diff --name-only` after this plan should list only `.planning/PROJECT.md` and `DESIGN.md`)
</verification>

<success_criteria>
- **CODING-SCORE-04:** PROJECT.md + DESIGN.md readiness-math section updated (D-08, D-09).
- **D-10:** Open Items for v1.5 section in PROJECT.md with coding backfill as entry #1.
- Docs are self-contained — future engineer can compute readiness from the worked example without cross-referencing source code.
</success_criteria>

<output>
After completion, create `.planning/phases/41-gapscore-integration/41-03-SUMMARY.md` documenting:
- PROJECT.md line ranges of the new subsection + Open Items section
- DESIGN.md line range of the new coding panel section
- Changelog entry dates
- Confirmation that no code files were modified
</output>
