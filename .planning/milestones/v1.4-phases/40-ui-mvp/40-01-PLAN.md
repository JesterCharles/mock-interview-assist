---
phase: 40-ui-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/app/coding/page.tsx
  - src/app/coding/[challengeId]/page.tsx
  - src/components/shell/sidebar-configs.ts
  - src/components/coding/MonacoEditor.tsx
  - src/components/coding/monaco-theme.ts
autonomous: false
requirements:
  - CODING-UI-05
tags:
  - ui
  - coding
  - monaco
  - nav
must_haves:
  truths:
    - "UI-SPEC.md exists at .planning/phases/40-ui-mvp/40-UI-SPEC.md before Wave 2/3 plans execute"
    - "Associate sees 'Coding' nav item pointing to /coding"
    - "Trainer sees 'Coding' nav item pointing to /coding"
    - "Visiting /coding renders a server component shell (placeholder ok — flesh-out in 40-02)"
    - "Visiting /coding/[challengeId] renders a server component shell (placeholder ok — flesh-out in 40-03)"
    - "@monaco-editor/react is installed and lazy-loaded via next/dynamic with ssr:false"
    - "MonacoEditor wrapper component file exists and exports a dynamic-loaded editor"
  artifacts:
    - path: ".planning/phases/40-ui-mvp/40-UI-SPEC.md"
      provides: "Per CONTEXT D-16: UI-SPEC gate prior to visual plans"
      contains: "generated by /gsd-ui-phase 40"
    - path: "package.json"
      provides: "@monaco-editor/react dependency"
      contains: "@monaco-editor/react"
    - path: "src/app/coding/page.tsx"
      provides: "Server component route shell for /coding"
      min_lines: 10
    - path: "src/app/coding/[challengeId]/page.tsx"
      provides: "Server component route shell for /coding/[challengeId]"
      min_lines: 10
    - path: "src/components/shell/sidebar-configs.ts"
      provides: "Nav config updated with Coding entry for trainer and associate"
      contains: "Coding"
    - path: "src/components/coding/MonacoEditor.tsx"
      provides: "Dynamic-import wrapper for @monaco-editor/react (ssr:false)"
      contains: "next/dynamic"
    - path: "src/components/coding/monaco-theme.ts"
      provides: "Light/dark theme pairing derived from DESIGN tokens"
      min_lines: 20
  key_links:
    - from: "src/components/shell/sidebar-configs.ts"
      to: "/coding"
      via: "dashboardSidebarGroups + associateSidebarGroups entries"
      pattern: "href:.*'/coding'"
    - from: "src/components/coding/MonacoEditor.tsx"
      to: "@monaco-editor/react"
      via: "next/dynamic(() => import('@monaco-editor/react'), { ssr: false })"
      pattern: "next/dynamic"
---

<objective>
Lay the phase-40 foundation: generate the UI spec (if not already), install Monaco, add the "Coding" nav entry for both associate and trainer roles, and scaffold the two App Router routes with server component shells.

Purpose: Downstream plans (40-02 list, 40-03 solve, 40-04 polling) need working routes, a dynamic-imported Monaco wrapper they can compose with, and a navigable entry point. This plan does the low-risk plumbing once so 40-02/03/04 can run in parallel where possible without duplicating infrastructure work.

Output: UI-SPEC.md for phase 40, Monaco installed + wrapped, sidebar-configs updated for both roles, two server component shells ready to receive the flesh-out work in later plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-ui-mvp/40-CONTEXT.md
@DESIGN.md
@CLAUDE.md
@src/components/shell/sidebar-configs.ts
@src/components/shell/AppShell.tsx
@src/components/shell/AssociateShell.tsx
@src/components/shell/SectionSidebar.tsx
@src/components/shell/types.ts

<interfaces>
<!-- Sidebar config contract (existing): -->

From src/components/shell/types.ts:
```typescript
export type IconComponent = React.ComponentType<any>;
export interface SidebarItem { href: string; label: string; icon: IconComponent; }
export interface SidebarGroup { label: string; items: SidebarItem[]; }
```

From src/components/shell/sidebar-configs.ts (current exports):
```typescript
export const dashboardSidebarGroups: SidebarGroup[];  // trainer groups
export function associateSidebarGroups(slug: string): SidebarGroup[];
```

Current trainer groups: Overview (Roster, Gap Analysis, Calibration) + Actions (New Mock, Reports, Batch Upload).
Current associate groups: single unlabeled group — Dashboard, Interviews, Curriculum.

Add "Coding" nav entry to BOTH. Lucide icon: `Code2` (already used elsewhere in lucide-react; picks cleanly with warm ink color).

From src/components/shell/AppShell.tsx — shell imports `dashboardSidebarGroups` directly.
From src/components/shell/AssociateShell.tsx — shell calls `associateSidebarGroups(slug)`.

No shell changes needed; only `sidebar-configs.ts` edit.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0 [PRE-EXECUTE GATE]: Generate UI-SPEC.md via /gsd-ui-phase 40</name>
  <files>.planning/phases/40-ui-mvp/40-UI-SPEC.md</files>
  <what-built>PRE-EXECUTE GATE per D-16. Before any UI-producing task in this plan (T2, T3) or any visual-heavy task in 40-02/03/04 runs, UI-SPEC.md must exist at `.planning/phases/40-ui-mvp/40-UI-SPEC.md`.</what-built>
  <action>Pause execution. If `.planning/phases/40-ui-mvp/40-UI-SPEC.md` does not yet exist, instruct the user to run `/gsd-ui-phase 40` in a separate session. Do NOT attempt to synthesize a UI-SPEC inline — that subverts the gate. Resume once the file is present. If the user explicitly says "skip", note it as Phase 40 follow-up debt in 40-01-SUMMARY.md and continue.</action>
  <how-to-verify>
    Check for file existence:
    ```bash
    test -f .planning/phases/40-ui-mvp/40-UI-SPEC.md && echo "UI-SPEC present" || echo "MISSING — run /gsd-ui-phase 40 before continuing"
    ```
    If missing, exit this plan and run `/gsd-ui-phase 40`, then resume.
    If present, type "approved" to continue.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/40-ui-mvp/40-UI-SPEC.md && echo OK || echo MISSING</automated>
  </verify>
  <done>UI-SPEC.md exists at `.planning/phases/40-ui-mvp/40-UI-SPEC.md` OR user has explicitly deferred with follow-up debt recorded.</done>
  <resume-signal>Type "approved" once UI-SPEC.md exists, or "skip" if user explicitly defers (adds follow-up debt)</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install @monaco-editor/react + create dynamic wrapper</name>
  <files>package.json, package-lock.json, src/components/coding/MonacoEditor.tsx, src/components/coding/monaco-theme.ts</files>
  <action>
    Install Monaco per D-07:
    1. `npm install @monaco-editor/react` (pins latest stable; React 19 compatible — verify peer). If peer warning surfaces about react@19, use `--legacy-peer-deps`.
    2. Create `src/components/coding/MonacoEditor.tsx`:
       - `'use client'` directive
       - `import dynamic from 'next/dynamic'`
       - `const MonacoEditor = dynamic(() => import('@monaco-editor/react').then(m => m.Editor), { ssr: false, loading: () => <div style={{height:'100%',display:'flex',alignItems:'center',justifyContent:'center',color:'var(--muted)'}}>Loading editor…</div> })`
       - Export a typed wrapper component `CodingEditor` that accepts `{ language, value, onChange, theme: 'light' | 'dark' }` and delegates to `<MonacoEditor>` with sensible options (minimap:false, fontSize:13, fontFamily:'var(--font-mono), JetBrains Mono, monospace', lineNumbers:'on', automaticLayout:true, scrollBeyondLastLine:false, tabSize:2, wordWrap:'on')
       - Map `theme` prop to Monaco built-in: `light` → `vs`, `dark` → `vs-dark` (detailed theme tuning happens in monaco-theme.ts)
       - Supported language ids per D-07/D-08: `python`, `javascript`, `typescript`, `java`, `sql`, `csharp`
    3. Create `src/components/coding/monaco-theme.ts`:
       - Export `resolveMonacoTheme(colorMode: 'light' | 'dark'): 'vs' | 'vs-dark'` — initial simple mapping; downstream plans may define a custom theme via `monaco.editor.defineTheme` if needed
       - Document in header comment that full palette tuning is Claude's discretion per CONTEXT (reuse DESIGN tokens where Monaco's theme schema permits)

    WHY dynamic + ssr:false: Monaco is ~2MB; initial page bundle must stay lean. `next/dynamic` with `ssr:false` prevents SSR execution (Monaco depends on `window`/`document`) AND code-splits the chunk.

    Do NOT ship a `<script>` bundling strategy; rely on the library's default worker loading. If worker errors surface in testing, document in SUMMARY as a Phase-40 debt ticket for v1.5.
  </action>
  <verify>
    <automated>test -f src/components/coding/MonacoEditor.tsx && test -f src/components/coding/monaco-theme.ts &amp;&amp; grep -q "@monaco-editor/react" package.json &amp;&amp; grep -q "next/dynamic" src/components/coding/MonacoEditor.tsx &amp;&amp; grep -q "ssr: false" src/components/coding/MonacoEditor.tsx &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - `@monaco-editor/react` listed in package.json dependencies
    - `MonacoEditor.tsx` exists, uses `next/dynamic` with `ssr:false`
    - `monaco-theme.ts` exports `resolveMonacoTheme`
    - `npx tsc --noEmit` passes (no new type errors introduced by this file)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add "Coding" nav entry for trainer + associate (per D-02)</name>
  <files>src/components/shell/sidebar-configs.ts</files>
  <action>
    Update `sidebar-configs.ts` per D-02 (role-aware nav):

    1. Import `Code2` from `lucide-react` (alongside existing icons).
    2. Add to `dashboardSidebarGroups` — append a Coding entry in the `Actions` group (after `New Mock`):
       ```ts
       { href: '/coding', label: 'Coding', icon: Code2 }
       ```
    3. Add to `associateSidebarGroups(slug)` — add a Coding entry after `Interviews`:
       ```ts
       { href: '/coding', label: 'Coding', icon: Code2 }
       ```

    WHY `/coding` (not `/associate/[slug]/coding`): associates and trainers share the route; identity + cohort scoping is server-side (Phase 39 CODING-API-06, reinforced in CONTEXT D-02 for the list API). Shared URL simplifies nav + bookmarking.

    Do NOT add an `/coding` entry inside any Settings accordion — Coding is a first-class practice rep surface, not a settings child.

    Do NOT modify `AppShell.tsx` or `AssociateShell.tsx`. Shells pull these configs directly; updating the source config cascades.
  </action>
  <verify>
    <automated>grep -c "href: '/coding'" src/components/shell/sidebar-configs.ts</automated>
  </verify>
  <done>
    - Trainer sidebar (`dashboardSidebarGroups`) contains one entry with `href: '/coding'`, label `'Coding'`, icon `Code2`
    - Associate sidebar (`associateSidebarGroups`) contains one entry with `href: '/coding'`, label `'Coding'`, icon `Code2`
    - `grep -c "href: '/coding'"` returns `2`
    - No unused-import warnings (`Code2` used)
  </done>
</task>

<task type="auto">
  <name>Task 3: Scaffold server-component route shells for /coding and /coding/[challengeId]</name>
  <files>src/app/coding/page.tsx, src/app/coding/[challengeId]/page.tsx</files>
  <action>
    Create the two App Router routes per D-01 (flat structure, no route grouping).

    File 1 — `src/app/coding/page.tsx` (server component shell, flesh-out in 40-02):
    - Default export `async function CodingListPage({ searchParams })` signature
    - Call `getCallerIdentity()` from `@/lib/identity`
    - If anonymous, redirect to `/signin`
    - Render a minimal placeholder: a `<main className="p-6 max-w-[1120px] mx-auto">` with a Clash Display 48px page title "Coding Challenges" and a muted subtitle "List rendering wired in 40-02"
    - Use DESIGN.md tokens only (var(--ink), var(--muted), var(--font-display), var(--font-dm-sans)). NO hardcoded hex.
    - Wrap content in either `AppShell` (trainer role) or `AssociateShell` (associate role) — branch on identity kind
    - For trainer: `<AppShell>{content}</AppShell>`
    - For associate: `<AssociateShell slug={caller.slug} cohortName={caller.cohortName}>{content}</AssociateShell>` (look up existing usage in `/associate/[slug]/page.tsx` for the cohortName lookup pattern)

    File 2 — `src/app/coding/[challengeId]/page.tsx` (server component shell, flesh-out in 40-03):
    - Signature: `async function CodingSolvePage({ params }: { params: Promise<{ challengeId: string }> })` (Next 16 async params)
    - Same auth gate as file 1
    - Same shell branching
    - Placeholder content: title "Challenge" + subtitle "Solve view wired in 40-03" + a small line showing `challengeId` echoed back (proves param extraction works)

    WHY server components: CONTEXT D-03 and D-06 both default to server-component shells that hydrate client children. Scaffolding them as server components now means 40-02/03 only add children + fetching, not conversion.

    Do NOT add the Monaco editor here. Do NOT fetch challenge data here. Those are 40-02/40-03 jobs. Keep this plan's surface small.
  </action>
  <verify>
    <automated>test -f src/app/coding/page.tsx &amp;&amp; test -f src/app/coding/[challengeId]/page.tsx &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -10 &amp;&amp; npm run lint 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - `src/app/coding/page.tsx` renders without throwing; identity gate redirects anonymous users to `/signin`
    - `src/app/coding/[challengeId]/page.tsx` renders; identity gate wired; `challengeId` extracted from async params
    - Both files wrap content in the correct shell per role
    - `npx tsc --noEmit` passes; `npm run lint` passes (no new errors)
    - No hardcoded hex values (grep: `grep -E "#[0-9a-fA-F]{3,6}" src/app/coding/` returns empty)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous → /coding routes | Untrusted callers must not access associate/trainer surfaces |
| bundle delivery to client | Monaco chunk (~2MB) must not bloat initial page load for non-coding routes |
| nav visibility → role | Associates must not see trainer-only nav items (Coding is intentionally shared, but pattern matters for future additions) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | D (Denial of Service / perf) | Monaco bundle in main entry chunk | mitigate | `next/dynamic({ ssr: false })` forces a separate lazy chunk; wrapper in `src/components/coding/MonacoEditor.tsx` is the only entry point (enforce via grep in 40-03 tests) |
| T-40-02 | I (Information Disclosure) | `/coding` accessible to anonymous users | mitigate | Both route shells call `getCallerIdentity()` + redirect to `/signin` when anonymous (Task 3) |
| T-40-05 | E (Elevation of Privilege / role confusion) | Sidebar showing wrong role's entries | mitigate | Associate nav comes from `associateSidebarGroups(slug)`, trainer from `dashboardSidebarGroups` — Coding entry added to both by design (D-02); no cross-role leakage because shells pull separate configs |
</threat_model>

<verification>
- UI-SPEC gate satisfied (Task 0)
- `npm run test` passes (no existing suite regressed)
- `npx tsc --noEmit` passes
- `npm run lint` passes
- Visiting `/coding` while signed in as trainer renders AppShell + placeholder title
- Visiting `/coding` while signed in as associate renders AssociateShell + placeholder title
- Visiting `/coding` while anonymous redirects to `/signin`
- Sidebar shows "Coding" entry in both roles
- Monaco bundle is NOT present in the main-page bundle (`next build` + inspect; optional — codex can validate later)
</verification>

<success_criteria>
- Phase 40 plans 40-02/03/04 can import `CodingEditor` from `@/components/coding/MonacoEditor` and use it without further infrastructure work
- Nav is role-aware; trainer + associate both see "Coding"
- Routes respond with placeholder shells; later plans replace placeholder content only
- Auth gate confirmed — anonymous visits redirect cleanly
- UI-SPEC.md exists for phase 40 (Task 0 satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/40-ui-mvp/40-01-SUMMARY.md` documenting:
- Whether UI-SPEC was generated here or deferred (if deferred, record as follow-up debt)
- Monaco version installed
- Any peer dependency warnings encountered
- Any deviations from the plan (theme tuning choices, etc.)
</output>
