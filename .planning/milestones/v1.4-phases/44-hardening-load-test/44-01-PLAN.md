---
phase: 44-hardening-load-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/load-test-coding.ts
  - scripts/load-test-fixtures/python-1.json
  - scripts/load-test-fixtures/python-2.json
  - scripts/load-test-fixtures/javascript-1.json
  - scripts/load-test-fixtures/javascript-2.json
  - scripts/load-test-fixtures/typescript-1.json
  - scripts/load-test-fixtures/typescript-2.json
  - scripts/load-test-fixtures/java-1.json
  - scripts/load-test-fixtures/java-2.json
  - scripts/load-test-fixtures/sql-1.json
  - scripts/load-test-fixtures/csharp-1.json
  - scripts/abuse-test-coding.ts
  - scripts/abuse-test-fixtures/fork-bomb.json
  - scripts/abuse-test-fixtures/infinite-loop.json
  - scripts/abuse-test-fixtures/network-egress.json
  - scripts/abuse-test-fixtures/stdout-flood.json
  - scripts/abuse-test-fixtures/memory-bomb.json
  - scripts/abuse-test-fixtures/fd-bomb.json
  - package.json
  - .planning/phases/44-hardening-load-test/44-LOAD-TEST-REPORT.md
  - .planning/phases/44-hardening-load-test/44-ABUSE-TEST-REPORT.md
autonomous: false
requirements:
  - HARD-01
  - HARD-02
user_setup:
  - service: deployed-stack
    why: "Load + abuse tests run against the Phase 43 deployed stack, not local Docker"
    env_vars:
      - name: LOAD_TEST_BASE_URL
        source: "Deployed app URL (from Phase 43 outputs.tf)"
      - name: LOAD_TEST_ASSOCIATE_EMAIL
        source: "Trainer-provisioned test associate (dedicated for this test)"
      - name: LOAD_TEST_ASSOCIATE_PASSWORD
        source: "Password for the test associate — rotated after test"
      - name: JUDGE0_VM_SSH_KEY_PATH
        source: "SSH key for Judge0 VM (from GH secrets) — required to run docker stats during abuse run"
    dashboard_config:
      - task: "Provision a dedicated test-associate Supabase account assigned to a test cohort with the 10 fixture challenges seeded"
        location: "Supabase dashboard + /trainer/cohorts UI"
      - task: "Confirm no real associate traffic is in flight during the test window (load test is destructive to queue state)"
        location: "Judge0 Logs Explorer"

must_haves:
  truths:
    - "Running `npm run load-test-coding` exits 0 against the deployed stack"
    - "Running `npm run abuse-test-coding` exits 0 against the deployed stack"
    - "44-LOAD-TEST-REPORT.md documents measured p50/p95/max per language and per-container CPU/RAM peaks"
    - "44-ABUSE-TEST-REPORT.md documents each payload class's expected vs observed containment verdict"
    - "All 50 concurrent submissions return verdicts (no hung pollers, no dropped submissions)"
    - "Per-submission p95 latency ≤ 10 sec across all 5 languages (D-03)"
    - "Judge0 queue depth never exceeds worker-count × 2 sustained for > 30 sec (D-03)"
    - "App VM CPU stays under 80% at p95; Judge0 VM CPU stays under 85% at p95 (D-03)"
    - "Each abuse payload class is contained: fork bomb → max_processes cap, infinite loop → timeout verdict, network egress → connection refused, stdout flood → runtime_error/fail, memory bomb → mle/runtime_error, fd bomb → Docker default limit (D-05)"
    - "`docker stats` sampling on Judge0 VM during abuse run shows no cgroup escape (host CPU/RAM never breaches its own limits)"
  artifacts:
    - path: "scripts/load-test-coding.ts"
      provides: "50-concurrent load test harness using p-limit (per D-01)"
      contains: "import pLimit"
    - path: "scripts/abuse-test-coding.ts"
      provides: "6-payload-class abuse test harness (per D-05, D-06)"
      contains: "const PAYLOAD_CLASSES"
    - path: "scripts/load-test-fixtures/"
      provides: "10 fixed challenges — 2 per language except 1 each for SQL + C# (per D-02)"
    - path: "scripts/abuse-test-fixtures/"
      provides: "Per-payload-class fixtures with expected containment verdicts"
    - path: ".planning/phases/44-hardening-load-test/44-LOAD-TEST-REPORT.md"
      provides: "Measured p50/p95, container peaks, pass/fail verdict against D-03 thresholds"
      min_lines: 60
    - path: ".planning/phases/44-hardening-load-test/44-ABUSE-TEST-REPORT.md"
      provides: "Per-payload-class containment evidence + docker stats sampling log"
      min_lines: 50
    - path: "package.json"
      provides: "npm run scripts: `load-test-coding` + `abuse-test-coding`"
      contains: "load-test-coding"
  key_links:
    - from: "scripts/load-test-coding.ts"
      to: "POST /api/coding/submit + GET /api/coding/attempts/[id] (deployed)"
      via: "Supabase-authenticated fetch using test-associate session cookie"
      pattern: "fetch.*\\$\\{process.env.LOAD_TEST_BASE_URL\\}/api/coding/submit"
    - from: "scripts/abuse-test-coding.ts"
      to: "POST /api/coding/submit (deployed)"
      via: "Same auth path; asserts expected containment verdict per payload class"
      pattern: "expect(verdict).toBe\\(.*timeout|mle|runtime_error.*\\)"
    - from: "scripts/load-test-coding.ts"
      to: "pass/fail assertions against D-03 thresholds"
      via: "`process.exit(1)` on threshold breach"
      pattern: "process\\.exit\\(1\\)"
---

<objective>
Build the 50-concurrent load test harness (HARD-01) and the 6-payload-class abuse test harness (HARD-02), run both against the Phase 43-deployed stack, and commit the resulting reports as the blocking gate for Phase 44 merge.

Purpose: Prove the coding stack survives production load (p95 ≤ 10 sec, no queue death, no app degradation) and that Phase 38's sandbox caps actually contain the 6 abuse payload classes on real hardware. These are the last empirical gates before the stack is declared production-ready.

Output: Two executable scripts + fixture payloads + two committed reports with measured numbers. Reports are the human-readable receipts; the scripts' exit codes are the machine-readable gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-hardening-load-test/44-CONTEXT.md
@.planning/phases/38-judge0-infrastructure/38-CONTEXT.md
@.planning/phases/43-msa-deployment/43-CONTEXT.md

<interfaces>
<!-- Existing endpoints the harness calls. Consumers use Supabase-authenticated session cookie, not service-role -->

From Phase 39 (live on deployed stack):
- `POST /api/coding/submit` — body: `{ challengeId: string, language: 'python'|'javascript'|'typescript'|'java'|'sql'|'csharp', code: string }` → returns `{ attemptId: string }`
- `GET /api/coding/attempts/[id]` — returns `{ verdict: 'pending'|'pass'|'fail'|'timeout'|'mle'|'runtime_error'|'compile_error', visibleTestResults: [...], hiddenTestsPassed: number, hiddenTestsTotal: number }`

From Phase 38 sandbox caps (expected containment):
- `max_processes=60`, `max_cpu_time_limit=10`, `max_memory_limit=256000` KB, `max_file_size=8192` KB, `enable_network=false`

Auth pattern (from src/lib/auth-server.ts):
- Supabase sign-in with email/password → session cookie → subsequent fetch calls include cookie
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Load test harness + fixtures (HARD-01)</name>
  <files>scripts/load-test-coding.ts, scripts/load-test-fixtures/*.json, package.json</files>
  <behavior>
    - Authenticates as the test associate via Supabase email/password (env vars), captures session cookie
    - Loads 10 fixture challenges from `scripts/load-test-fixtures/` (2 python, 2 JS, 2 TS, 2 Java, 1 SQL, 1 C#) per D-02
    - Uses `p-limit(50)` to fire 50 submissions concurrently (5 attempts per challenge, cycling)
    - For each submission: POST /api/coding/submit → poll GET /api/coding/attempts/[id] (100ms interval, 20-sec cap) → record per-submission latency in ms
    - In parallel, samples `gcloud logging read judge0-metrics --limit 1 --freshness 5s` every 1 sec to capture queue depth
    - After all submissions complete, computes per-language p50/p95/max + overall wall clock + max queue depth window
    - Asserts D-03 thresholds: all 50 returned verdicts (none `pending`), per-lang p95 ≤ 10 sec, queue depth ≤ worker-count × 2 for any 30-sec sustained window, app VM CPU p95 < 80%, Judge0 VM CPU p95 < 85%
    - On any assertion fail: `process.exit(1)` with failing threshold printed
    - On pass: writes report to `.planning/phases/44-hardening-load-test/44-LOAD-TEST-REPORT.md` (markdown with measured numbers table, fixture list, environment snapshot, pass/fail lines) and exits 0
    - Adds `npm run load-test-coding` and `npm run abuse-test-coding` (next task) script entries to package.json
  </behavior>
  <action>
Create the load test harness per D-01..D-04 against the DEPLOYED stack (not local docker compose).

Implementation (per D-01, Claude's discretion to use `p-limit` + zero new deps if `p-limit` already installed; check first — if missing, `npm i -D p-limit`):

1. `scripts/load-test-coding.ts`:
   - `import pLimit from 'p-limit'` (or implement manual Promise-batching fallback)
   - Read env: `LOAD_TEST_BASE_URL`, `LOAD_TEST_ASSOCIATE_EMAIL`, `LOAD_TEST_ASSOCIATE_PASSWORD`. Throw if any missing.
   - Sign in via Supabase (`@supabase/supabase-js` client — already a dep); capture session tokens; set cookie jar for fetch calls.
   - Load 10 fixture files from `scripts/load-test-fixtures/*.json`. Each fixture shape: `{ challengeId: string, language: string, code: string, expectedVerdict: 'pass' }` — a known-passing submission so we're measuring pipeline latency not solve difficulty.
   - Run 50 concurrent submissions via `pLimit(50)` — 5 attempts per challenge, cycling fixtures.
   - For each, POST /api/coding/submit → poll /api/coding/attempts/[id] @ 100ms until verdict ≠ 'pending' or 20-sec cap.
   - Spawn background loop: every 1 sec, `execFileSync('gcloud', ['logging','read','jsonPayload.queueDepth>0','--limit','1','--freshness=5s','--format=json'])` — parse queueDepth. If gcloud fails, log warning + continue (report will note `queueDepth: UNAVAILABLE`).
   - After all promises settle: compute stats per language (quantile helper — simple sort + index).
   - Assertion block: check all 6 D-03 thresholds; collect failures into array; if non-empty, print red "FAIL:" lines and `process.exit(1)`.
   - On pass: write `44-LOAD-TEST-REPORT.md` using a template string. Print green "PASS" line. Exit 0.

2. `scripts/load-test-fixtures/*.json`:
   - Create 10 fixture files. Each references a real seed-data challenge slug provided by the trainer-provisioned test associate. For each language, use an off-the-shelf starter-solution payload that passes (e.g., `two-sum-py-1.json` → known passing Python for `two-sum` challenge).
   - Fixture shape: `{ "challengeId": "<seeded-challenge-id>", "language": "python", "code": "def solve():\n    ...", "expectedVerdict": "pass" }`.
   - **IMPORTANT**: The trainer provisions these challenges in their test cohort; the fixture files use the slugs/IDs from the test-cohort seed. The task's user_setup (above) calls this out explicitly. If the seed doesn't exist yet, the test associate Supabase account must be created with 10 matching challenges in the cohort BEFORE running the harness. The fixture JSON files document expected challengeId slugs (e.g., `"challengeId": "test-load-two-sum-py"`) so the trainer knows exactly what to seed.

3. `package.json`: add `"load-test-coding": "tsx scripts/load-test-coding.ts"` and `"abuse-test-coding": "tsx scripts/abuse-test-coding.ts"` under `scripts`.

4. Test harness uses `tsx` (already a dep) — do NOT add `ts-node`.

Avoid: real associate accounts (use dedicated test associate only), `wait=true` Judge0 calls (Phase 39 already forbids; harness must use async submit/poll), hardcoded base URLs (env-driven).

NOTE: Because this task's `<verify>` automation requires the deployed stack to be live, the `<automated>` command below is a DRY-RUN (validates the script compiles + fixtures are well-formed). The actual load test runs in the Wave-1 GATE task below under human supervision.
  </action>
  <verify>
    <automated>npx tsc --noEmit scripts/load-test-coding.ts && node -e "const fs=require('fs');const files=fs.readdirSync('scripts/load-test-fixtures');if(files.length!==10)process.exit(1);files.forEach(f=>{const j=JSON.parse(fs.readFileSync('scripts/load-test-fixtures/'+f));if(!j.challengeId||!j.language||!j.code)process.exit(2)})"</automated>
  </verify>
  <done>
    - scripts/load-test-coding.ts compiles under strict TS with no errors
    - 10 fixture files exist in scripts/load-test-fixtures/ with required keys (challengeId, language, code)
    - Each language coverage: 2 python, 2 javascript, 2 typescript, 2 java, 1 sql, 1 csharp (per D-02)
    - `npm run load-test-coding` script entry added to package.json
    - Harness asserts all 6 D-03 thresholds and exits 1 on any breach
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Abuse test harness + fixtures (HARD-02)</name>
  <files>scripts/abuse-test-coding.ts, scripts/abuse-test-fixtures/*.json</files>
  <behavior>
    - Loads 6 payload-class fixtures (fork-bomb, infinite-loop, network-egress, stdout-flood, memory-bomb, fd-bomb) per D-05
    - For each payload class, submits it in every applicable language (fork bomb: bash via Python os.system + C if Judge0 supports; infinite loop: all 5; network egress: all 5; stdout flood: all 5; memory bomb: all 5; fd bomb: all 5)
    - Uses `expectedContainment` field per fixture to assert the returned verdict matches (e.g., `timeout` for infinite loop; `mle` or `runtime_error` for memory bomb)
    - Before run: starts `ssh judge0-vm docker stats --no-stream --format json` sampling loop at 2 Hz (writes to temp log)
    - After run: parses docker stats log and asserts no container exceeded its declared cgroup memory/CPU cap (no host-level escape)
    - On any mismatch: fails loudly with payload class + actual vs expected verdict; `process.exit(1)`
    - On pass: writes `44-ABUSE-TEST-REPORT.md` with per-payload class table (payload, expected, actual, evidence) + `docker stats` summary + SAFE verdict
  </behavior>
  <action>
Create abuse test harness per D-05, D-06 (single file with payload switch, per Claude's discretion in CONTEXT).

1. `scripts/abuse-test-coding.ts`:
   - Single file. `const PAYLOAD_CLASSES = [{name:'fork-bomb',fixtures:[...],expectedContainment:['timeout','runtime_error']}, ...]` (6 entries total).
   - For each payload class × applicable language: POST /api/coding/submit → poll /api/coding/attempts/[id] → assert verdict ∈ expectedContainment.
   - Before the main loop, spawn `ssh -i $JUDGE0_VM_SSH_KEY_PATH judge0-vm 'docker stats --no-stream --format "{{json .}}" judge0-server judge0-workers'` in a setInterval(500ms) pushing JSON lines to `/tmp/abuse-docker-stats.jsonl`.
   - After main loop: `ssh judge0-vm 'cat /tmp/abuse-docker-stats.jsonl'` back to local, parse, assert no container's MemPerc > 95% or CPUPerc sustained > 200% for > 5 sec.
   - Collect failures; if any, exit 1 with detailed output.
   - On pass, write `44-ABUSE-TEST-REPORT.md`:
     ```
     | Payload class | Language | Expected | Actual | Containment evidence |
     | fork bomb | python | max_processes cap → runtime_error OR timeout | runtime_error | max_processes=60 caps recursion |
     ...
     ```
     Plus a "Docker stats summary" section with peak MemPerc/CPUPerc per container during run.

2. `scripts/abuse-test-fixtures/*.json` (6 files):
   - `fork-bomb.json`: `{ "name": "fork-bomb", "languages": { "python": "import os\nwhile True: os.fork()", "javascript": "while(true){require('child_process').spawn('node',['-e','while(1){}'])}", "java": "while(true){new ProcessBuilder(\"java\",\"-e\",\"\").start();}" }, "expectedContainment": ["timeout","runtime_error"] }`
   - `infinite-loop.json`: covers all 5 langs; `expectedContainment: ["timeout"]`
   - `network-egress.json`: HTTP request attempts per lang; `expectedContainment: ["runtime_error","fail"]`
   - `stdout-flood.json`: print-loop per lang; `expectedContainment: ["runtime_error","fail","timeout"]` (max_file_size=8192 KB truncates)
   - `memory-bomb.json`: large allocation per lang; `expectedContainment: ["mle","runtime_error"]` (max_memory_limit=256000 KB kills)
   - `fd-bomb.json`: open-10000-files per lang; `expectedContainment: ["runtime_error","fail"]` (Docker default ulimit contains)

Avoid: running against production-real-associate account (test associate only); running without `docker stats` sampler (must prove no escape); assuming a single universal expected verdict (containment paths differ per payload per Phase 38 caps — expectedContainment is an array).
  </action>
  <verify>
    <automated>npx tsc --noEmit scripts/abuse-test-coding.ts && node -e "const fs=require('fs');const payloads=['fork-bomb','infinite-loop','network-egress','stdout-flood','memory-bomb','fd-bomb'];payloads.forEach(p=>{const j=JSON.parse(fs.readFileSync('scripts/abuse-test-fixtures/'+p+'.json'));if(!j.languages||!j.expectedContainment||j.expectedContainment.length===0)throw new Error(p+' malformed')})"</automated>
  </verify>
  <done>
    - scripts/abuse-test-coding.ts compiles under strict TS
    - All 6 payload-class fixtures exist with `languages` map + non-empty `expectedContainment` array
    - Each payload class's expectedContainment matches Phase 38 sandbox cap behavior (D-05 mapping)
    - Harness includes docker stats sampler + cgroup-escape assertion
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 [BLOCKING GATE]: Run both harnesses against deployed stack and commit reports</name>
  <files>.planning/phases/44-hardening-load-test/44-LOAD-TEST-REPORT.md, .planning/phases/44-hardening-load-test/44-ABUSE-TEST-REPORT.md</files>
  <action>
Operator (human) runs both Wave-1 harnesses against the Phase 43-deployed stack in a coordinated maintenance window, then commits the two report files to the repo. This is the empirical gate for HARD-01 and HARD-02 — the dry-run automation in Tasks 1 and 2 only proves the scripts compile + fixtures are well-formed; this task is where real measurements happen on the deployed stack.
  </action>
  <what-built>Load test harness + abuse test harness + 6+10 fixtures. Both scripts exit 0 only when every D-03 threshold (load) and every expectedContainment verdict (abuse) passes on the real deployed stack.</what-built>
  <how-to-verify>
    1. Confirm Phase 43 deploy workflow has completed successfully and the deployed stack is healthy: `curl $LOAD_TEST_BASE_URL/api/health | jq .checks.judge0` returns `"ok"`.
    2. Confirm test associate + 10 test-cohort challenges are provisioned in the deployed Supabase (see user_setup).
    3. Export env: `LOAD_TEST_BASE_URL`, `LOAD_TEST_ASSOCIATE_EMAIL`, `LOAD_TEST_ASSOCIATE_PASSWORD`, `JUDGE0_VM_SSH_KEY_PATH`.
    4. Run `npm run load-test-coding`. Expect: exit 0, `.planning/phases/44-hardening-load-test/44-LOAD-TEST-REPORT.md` written with measured p50/p95/max per language + queue/CPU/RAM peaks.
    5. Run `npm run abuse-test-coding`. Expect: exit 0, `.planning/phases/44-hardening-load-test/44-ABUSE-TEST-REPORT.md` written with per-payload-class containment evidence + docker stats summary.
    6. Inspect both reports manually: confirm every language has a p95 ≤ 10 sec row (load) and every payload class has a containment verdict in `expectedContainment` (abuse).
    7. TEARDOWN: Rotate `LOAD_TEST_ASSOCIATE_PASSWORD`; do NOT leave test associate in production cohort with reuseable creds.
    8. Commit both reports.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/44-hardening-load-test/44-LOAD-TEST-REPORT.md && test -f .planning/phases/44-hardening-load-test/44-ABUSE-TEST-REPORT.md && grep -q "PASS" .planning/phases/44-hardening-load-test/44-LOAD-TEST-REPORT.md && grep -q "SAFE\|PASS" .planning/phases/44-hardening-load-test/44-ABUSE-TEST-REPORT.md</automated>
  </verify>
  <done>
    - 44-LOAD-TEST-REPORT.md committed with PASS verdict and per-language measurements
    - 44-ABUSE-TEST-REPORT.md committed with containment evidence for all 6 payload classes
    - Test associate password rotated; no leftover production-cohort access
  </done>
  <resume-signal>Type "approved" after both scripts exit 0 AND both reports are committed. If either script exits non-zero, investigate root cause (Judge0 sizing, app perf, sandbox config regression) BEFORE re-running — do not re-run blindly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-harness → deployed app | Authenticated session cookie; untrusted fixtures cross into submission endpoint |
| abuse payload → Judge0 sandbox | Actively hostile input designed to escape — relies on Phase 38 caps |
| gcloud/ssh from dev machine → GCE VMs | Dev-machine creds reach prod infra (test harness only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01-S | Spoofing | load-test auth | mitigate | Dedicated test-associate Supabase account (user_setup); rotated after test; never reused across runs |
| T-44-01-T | Tampering | test associate persists mid-test | mitigate | Test associate assigned to isolated cohort with 10 test challenges only; no cross-contamination with real readiness data |
| T-44-01-D | DoS | load test floods production | mitigate | Capped at 50 concurrent (HARD-01 ceiling); runs in a trainer-coordinated maintenance window (user_setup step 2); harness exits on first threshold breach |
| T-44-01-E | Elevation | abuse payload escapes sandbox | transfer | Phase 38 D-04..D-07 sandbox caps are the real mitigation; this plan VERIFIES containment, does not ADD it. Harness asserts no cgroup escape via docker stats |
| T-44-01-I | Info Disclosure | abuse payload reads hidden tests | mitigate | Phase 39 CODING-API-02 already blocks hidden-test leakage; harness submits non-hidden-scoped payloads |
| T-44-01-R | Repudiation | load test leaves untraceable artifacts | mitigate | All submissions tagged with test-associate ID; cleanup script documented in teardown (user_setup) |
</threat_model>

<verification>
- Both scripts compile under `npx tsc --noEmit` in Wave 1 dry-run
- Phase 43 deployed stack is healthy before GATE task runs
- GATE task exits 0 for both `load-test-coding` and `abuse-test-coding`
- `44-LOAD-TEST-REPORT.md` contains per-language p95 numbers and a "PASS" line referencing D-03 thresholds
- `44-ABUSE-TEST-REPORT.md` contains all 6 payload classes with containment evidence
- Test associate password rotated post-test
</verification>

<success_criteria>
HARD-01 satisfied when 44-LOAD-TEST-REPORT.md documents p95 ≤ 10 sec across all 5 languages with queue depth under worker-count × 2 and VM CPUs under 80%/85%. HARD-02 satisfied when 44-ABUSE-TEST-REPORT.md documents all 6 payload classes contained with docker stats evidence of no cgroup escape. Both reports committed to `.planning/phases/44-hardening-load-test/`.
</success_criteria>

<output>
After completion, create `.planning/phases/44-hardening-load-test/44-01-SUMMARY.md` summarizing harness architecture, measured numbers, any threshold margins, and teardown evidence.
</output>
