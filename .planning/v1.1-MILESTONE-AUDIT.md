# v1.1 Cohort Readiness System — Milestone Audit

**Auditor:** integration-check subagent
**Audited:** 2026-04-15
**PR:** `4238e36` (merged to `main`)
**Deploy:** DEFERRED to v1.2 (user choice — code on main, prod not yet promoted)
**Scope:** Phases 8–15, 22 plans, 14 requirements (AUTH-01..04, PIPE-01..02, COHORT-01..04, CURRIC-01..02, DESIGN-01..03)

---

## Overall Verdict: **COMPLETE (with tracked tech debt)**

All 14 v1.1 requirements have evidence in phase SUMMARYs and verifications. All cross-phase integration seams are wired in source. Codex P1/P2 findings were addressed pre-merge (`21187f9`). Non-automated items (browser visual QA, scheduled readiness sweep, PIN auth flag flip) are explicitly parked — no silent gaps.

**Blockers to closing the milestone: NONE.** All tech debt is either deferred by decision (v1.2) or requires human browser QA that cannot run headless.

---

## 1. Requirements Coverage Matrix (14 × phases)

| REQ-ID | Phase | Plan Evidence | Source Evidence | Status |
|--------|-------|---------------|-----------------|--------|
| **AUTH-01** Trainer generates 6-digit PIN | 9 | 09-01, 09-03 SUMMARY | `src/app/api/associate/pin/generate/route.ts`; `src/lib/pinService.ts`; `src/app/trainer/components/GeneratePinButton.tsx:104` | COVERED (flag-gated) |
| **AUTH-02** PIN → HttpOnly session cookie | 9 | 09-01, 09-02 SUMMARY | `src/app/api/associate/pin/verify/route.ts`; `src/lib/associateSession.ts` (HMAC w/ `ASSOCIATE_SESSION_SECRET`, ver = `pinGeneratedAt`) | COVERED |
| **AUTH-03** Trainer/associate cookies isolated | 9 | 09-02 SUMMARY | `src/middleware.ts:29-58` — identity enum from cookie only; trainer gate rejects associate (line 43 redirect `/login`) | COVERED |
| **AUTH-04** `/associate/[slug]` guarded; 403 on mismatch | 9 | 09-03 SUMMARY | `src/app/associate/[slug]/page.tsx` uses `getAssociateSession` + JSX 403 shell | COVERED |
| **PIPE-01** Automated session links to authenticated associate | 10 | 10-01 SUMMARY | `src/app/api/associate/interview/complete/route.ts:64-75` — cookie-only identity, overrides client `associateSlug` | COVERED |
| **PIPE-02** Automated sessions feed gap + readiness pipeline | 10 | 10-01, 10-03 SUMMARY | `runReadinessPipeline(assocSession.associateId, session.id)` at line 82; sweep at `src/lib/readinessSweep.ts`; endpoint `src/app/api/admin/readiness-sweep/route.ts` | COVERED |
| **COHORT-01** Trainer CRUD cohorts | 11 | 11-01, 11-02 SUMMARY + VALIDATION | `src/app/api/cohorts/route.ts`; `src/app/api/cohorts/[id]/route.ts`; `src/app/trainer/cohorts/CohortsClient.tsx` | COVERED |
| **COHORT-02** Nullable FK; unassigned functional | 11 | 11-03 SUMMARY | `schema.prisma:27-28` (`cohortId Int?` + SetNull); `PATCH /api/trainer/[slug]`; `AssociateCohortSelect.tsx` | COVERED |
| **COHORT-03** Roster cohort filter | 12 | 12-01, 12-02 SUMMARY | `src/app/api/trainer/route.ts:26-41` (`?cohortId=`); `src/components/cohort/CohortFilter.tsx` | COVERED |
| **COHORT-04** Aggregate readiness summary | 12 | 12-01, 12-02 SUMMARY | `src/app/api/trainer/route.ts:72` (`includeSummary && cohortId` wrap); `src/components/cohort/ReadinessSummaryBar.tsx` | COVERED |
| **CURRIC-01** Trainer defines curriculum weeks (skillSlug + unique) | 13 | 13-01, 13-02 SUMMARY | `src/app/api/cohorts/[id]/curriculum/route.ts`; `src/app/trainer/cohorts/[id]/curriculum/CurriculumManager.tsx`; schema `@@unique([cohortId, weekNumber])` | COVERED |
| **CURRIC-02** Wizard filters to taught slugs; gap composes on top | 13 | 13-03 SUMMARY + VERIFICATION | `src/lib/curriculumFilter.ts` (exact first-segment match); `src/app/interview/new/page.tsx:17,72-76,157,294` (allTechs/availableTechs separation) | COVERED |
| **DESIGN-01** Public/associate/auth on DESIGN.md tokens | 14 | 14-01 SUMMARY + VERIFICATION | `src/components/layout/PublicShell.tsx`; signin/associate/PIN routes import PublicShell | COVERED |
| **DESIGN-02** Cohort + curriculum UIs on tokens from day one | 14 | 14-02 SUMMARY | `src/components/cohort/*`, `src/components/trainer/Curriculum*`, tokens only | COVERED |
| **DESIGN-03** All routes unified; legacy `--nlm-*` deleted | 15 | 15-04 SUMMARY + VERIFICATION | `src/app/globals.css` (179 lines, zero `--nlm-*`); 24/24 Playwright green; `06987c7` deletion commit | COVERED |

**Coverage: 14/14 requirements satisfied.** No uncovered requirements.

Requirements with no cross-phase wiring (self-contained, not a concern): DESIGN-02, DESIGN-03 — purely CSS token unification within their own phases.

---

## 2. Cross-Phase Integration Checklist

| Seam | Expected | Evidence | Verdict |
|------|----------|----------|---------|
| **Phase 8 schema → Phase 9** uses `pinHash`/`pinGeneratedAt` | Associate model carries PIN fields; Phase 9 services read them | `prisma/schema.prisma:25-26`; 17 files in `src/` read `pinHash`/`pinGeneratedAt` (pinService, associateSession, associateService, auth-server, middleware, routes) | **PASS** |
| **Phase 9 auth → Phase 10** authenticated interview uses `assocSession` cookie | `/api/associate/interview/complete` derives identity from cookie only | `src/app/api/associate/interview/complete/route.ts:64-75` — `getAssociateSession()`, 401 on miss, overrides client slug | **PASS** |
| **Phase 10 marker → Phase 8 schema column + sweep endpoint** | `Session.readinessRecomputeStatus` column exists; pipeline writes pending→done/failed; sweep repairs | `schema.prisma:56` column + `@@index`; `src/lib/readinessPipeline.ts:41` transitions status and returns boolean; `src/lib/readinessSweep.ts:81` respects `ok` before closing siblings; `src/app/api/admin/readiness-sweep/route.ts` trainer-authed | **PASS** |
| **Phase 11 Cohort → Phase 12** filter uses `cohortId` | `/api/trainer?cohortId=` filters roster | `src/app/api/trainer/route.ts:26-41` parses + validates `cohortId`, `where: { cohortId }` | **PASS** |
| **Phase 12 → Phase 13** CurriculumWeek FK to Cohort | `CurriculumWeek.cohortId` cascades with Cohort | `schema.prisma:101,109` FK + onDelete: Cascade | **PASS** |
| **Phase 13 skillSlug filter → Phase 14/15** setup wizard consumes CurriculumFilterBadge | Wizard imports filter + badge component | `src/app/interview/new/page.tsx:17-18` imports both; `CurriculumFilterBadge` rendered at line 568 | **PASS** |
| **Phase 13 separation allTechs/availableTechs** (Codex P2) | Filter never compounds | `src/app/interview/new/page.tsx:76-77` separate state; filter always reads from `allTechs` (line 157 source param, line 281/316 pass `allTechs`) | **PASS** |
| **Phase 14 DESIGN tokens + PublicShell → Phase 15** legacy deletion | PublicShell used in new routes; legacy tokens removed | 5 files use `PublicShell`; `globals.css` 179 lines, 0 `--nlm-*` refs (Phase 15 VERIFICATION Truth #1-5) | **PASS** |
| **Phase 15 single /signin tabs** replaces `/login` + `/associate/login` | Legacy paths 301-redirect | `src/app/login/page.tsx` and `src/app/associate/login/page.tsx` are `redirect(...)` shells (Phase 15 VERIFICATION Truth #8) | **PASS** |

**9/9 seams PASS.** No broken integrations.

---

## 3. E2E Flow Verdicts

### Flow A: Trainer full loop (sign-in → cohort → curriculum → filtered wizard → interview → review → PDF)
- Sign-in: `src/app/signin/page.tsx:23-47` handles trainer tab + redirect to `/trainer`
- Trainer roster: `src/app/trainer/page.tsx` consumes `/api/trainer` (supports `?cohortId=`, `?includeSummary=`)
- Cohort CRUD: `/trainer/cohorts` → `CohortsClient.tsx` → `/api/cohorts` CRUD (trainer-auth guard in each route)
- Curriculum: `/trainer/cohorts/[id]/curriculum/CurriculumManager.tsx` → `/api/cohorts/[id]/curriculum` + `[weekId]` CRUD
- Associate assignment: `AssociateCohortSelect.tsx` → `PATCH /api/trainer/[slug]`
- Setup wizard: `/interview/new` → parallel fetch curriculum + gap scores → `filterTechsByCurriculum` (exact match) → `applyGapScores` composes on top
- Interview → review → PDF flow preserved from v1.0 (Phase 15 migrated CSS only, preserved functionality per Plan 15-02 HIGH RISK trainer smoke)
- **Verdict: WIRED end-to-end.** (Human browser QA residual per Phase 12/13/15 `human_needed` status.)

### Flow B: Anonymous automated interview (PIN feature-flagged off)
- `/api/public/interview/start`, `/api/public/interview/agent`, `/api/public/interview/complete` all exist
- `src/app/api/public/interview/complete/route.ts:56-60` unconditionally nulls `associateSlug` (Codex #3 closure) — no forged linkage possible
- Persists with `mode: 'automated'` (Codex P2 fix)
- **Verdict: WIRED.**

### Flow C: Admin readiness sweep
- `POST /api/admin/readiness-sweep` at `src/app/api/admin/readiness-sweep/route.ts`
- Line 36-39: `isAuthenticatedSession()` trainer-auth gate
- Line 31-33: zod-validated `batchSize` (1..200)
- Line 54-57: delegates to `runReadinessSweep`
- **Verdict: WIRED.**

### Flow D: Authenticated associate flow (flag-gated)
- `/signin?as=associate` tab (visible only when `isAssociateAuthEnabled()`)
- `/associate/login` 301 → `/signin?as=associate`
- PIN verify → cookie → `/associate/[slug]` or `/associate/[slug]/interview`
- Completion endpoint returns 404 when flag off (`src/app/api/associate/interview/complete/route.ts:25-27`)
- **Verdict: WIRED behind flag.** Code path exercised by vitest (flag=true); production default=false.

---

## 4. Feature-Flagged Surface Confirmation

Verified all PIN surfaces hidden when `ENABLE_ASSOCIATE_AUTH=false`:

| Surface | Gating Evidence |
|---------|-----------------|
| `/signin` Associate tab | `src/app/signin/page.tsx:26,29-30` — `associateEnabled` gates initial tab; SignInTabs tabs conditionally rendered |
| `/` "Sign in to track progress" CTA | `src/app/page.tsx:84-99,752` — `/api/associate/status` probe + `{associateAuthEnabled && ...}` wrap |
| `/associate/[slug]` Start CTA | `src/app/associate/[slug]/page.tsx:308` — `{isAssociateAuthEnabled() && ...}` wraps Start mock interview link |
| `/trainer/[slug]` Generate PIN button | `src/app/trainer/[slug]/page.tsx:27,32-41,196` — status probe + `{associateAuthEnabled && <GeneratePinButton .../>}` |
| `/api/associate/pin/generate` | `route.ts:14-16` → 404 |
| `/api/associate/pin/verify` | `route.ts:40-…` → 404 |
| `/api/associate/interview/complete` | `route.ts:25-27` → 404 |
| `/api/associate/me` | `route.ts:12-…` → 404 |
| `/api/associate/status` | Always returns `{ enabled: bool }` (intentional — UI probe) |

**All routes/CTAs correctly gated.** No surface exposes PIN UX when the flag is off.

---

## 5. Tech Debt (Consolidated)

### P1 — Carry to v1.2 planning
1. **PIN auth flag flip** — `ENABLE_ASSOCIATE_AUTH=false` in prod. Harden rate limiter further (IP reputation, exponential backoff), validate in staging, then flip. [source: PIPELINE-SHIP.md, MILESTONE_SUMMARY §6]
2. **Production deploy of v1.1** — merged to main but not yet deployed (user choice). [source: user prompt]

### P2 — Scheduled / ops
3. **Readiness sweep has no cron** — endpoint exists and is trainer-authed; no scheduled trigger wired. Trainers invoke manually. [source: MILESTONE_SUMMARY §6]
4. **GitHub question-bank cache deferred** — recursive discovery is the real wizard perf bottleneck; `<400ms` remains advisory (Codex #7). [source: 13-CONTEXT.md, MILESTONE_SUMMARY]
5. **Dark mode not fully QA'd** — tokens defined and toggle wired, but not every interactive state (error banners, disabled buttons, readiness pill variants) visually verified. [source: MILESTONE_SUMMARY §6]

### P3 — Process / debt hygiene
6. **Nyquist validation gap** — only Phase 11 has full `VALIDATION.md`; Phases 12/13/14/15 ship `VERIFICATION.md` with `human_needed` residuals. Same gap pattern as v1.0. [source: MILESTONE_SUMMARY §6, phase files]
7. **Legacy path redirects permanent** — `/dashboard`, `/login`, `/associate/login` 301 forever; no removal milestone scheduled. [source: MILESTONE_SUMMARY §6]
8. **File-storage write path** — v1.1 declared Postgres canonical (PROJECT.md Constraints); v1.0 file-write path still present. Full deletion deferred to a later milestone. [source: PROJECT.md]

### Deferred (explicitly out of v1.1)
- Magic link / Supabase Auth OTP (replace PIN) — AUTH-FUTURE-01
- Curriculum cloning between cohorts
- Curriculum-scoped gap computation
- Cohort snapshots + per-cohort trend charts
- Readiness-change email notifications (Resend)
- Self-service PIN reset

---

## 6. Blockers to Closing the Milestone

**NONE.** Code is merged, all 14 requirements covered, all integration seams verified in source, Codex P1/P2 findings resolved in `21187f9`, health stack green (typecheck clean, 239/239 vitest pass, 24/24 Playwright).

Deployment being deferred to v1.2 is an explicit user choice, not an audit blocker — the milestone boundary is "code on main + verified," which this audit confirms.

**Recommended next step:** `/gsd-complete-milestone` → archive `.planning/phases/08..15` to `milestones/v1.1-*`, tag `v1.1` on the merged commit, begin v1.2 planning (PIN limiter hardening + flag flip + cached manifest + notifications).

---

*Audit generated by integration-check subagent per `/gsd-audit-milestone` contract. Evidence paths are absolute-relative to repo root; line numbers current as of audit date.*
