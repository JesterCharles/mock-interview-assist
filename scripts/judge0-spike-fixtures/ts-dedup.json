{
  "name": "ts-dedup",
  "language": "typescript",
  "sourceCode": "import * as fs from 'fs';\nconst lines: string[] = fs.readFileSync(0, 'utf-8').split('\\n').filter(l => l.length > 0);\nconst seen = new Set<string>();\nconst out: string[] = [];\nfor (const l of lines) { if (!seen.has(l)) { seen.add(l); out.push(l); } }\nconsole.log(out.join('\\n'));\n",
  "stdin": "a\nb\na\nc\nb\n",
  "expectedStdout": "a\nb\nc\n",
  "maxWallTimeSec": 15,
  "notes": "TS generics Set<T>"
}
